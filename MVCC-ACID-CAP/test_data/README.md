# 测试数据说明

> **文档编号**: TEST-DATA-README-001
> **主题**: View核心文档测试数据说明
> **版本**: v1.0

---

## 📋 概述

本目录包含用于view核心文档的测试数据和数据生成脚本。

## 📁 目录结构

```text
test_data/
├── README.md                    # 本文件
├── generate_test_data.py        # 数据生成脚本
└── data/                        # 生成的测试数据（可选）
```

## 🚀 使用方法

### 1. 安装依赖

```bash
pip install psycopg2-binary
```

### 2. 生成测试数据

```bash
# 生成默认测试数据
python generate_test_data.py

# 自定义数据量
python generate_test_data.py --users 200 --products 100 --orders 500

# 清理现有数据并重新生成
python generate_test_data.py --cleanup
```

### 3. 连接字符串

默认连接字符串：`dbname=testdb user=postgres password=postgres host=localhost`

可以通过 `--connection` 参数自定义：

```bash
python generate_test_data.py --connection "dbname=mydb user=myuser password=mypass host=localhost port=5432"
```

## 📊 测试数据说明

### 基础测试数据

1. **test_users** - 用户表
   - id: 主键
   - name: 用户名
   - email: 邮箱（唯一）
   - created_at: 创建时间
   - updated_at: 更新时间

2. **test_products** - 产品表
   - id: 主键
   - name: 产品名称
   - description: 产品描述
   - price: 价格
   - stock: 库存
   - created_at: 创建时间

3. **test_orders** - 订单表
   - id: 主键
   - user_id: 用户ID（外键）
   - product_name: 产品名称
   - quantity: 数量
   - price: 总价
   - status: 订单状态
   - created_at: 创建时间

### MVCC测试数据

1. **mvcc_test** - MVCC测试表
   - id: 主键
   - data: 数据内容
   - version: 版本号

   用于测试版本链和可见性判断。

## 🔧 数据生成参数

- `--users`: 用户数量（默认：100）
- `--products`: 产品数量（默认：50）
- `--orders`: 订单数量（默认：200）
- `--cleanup`: 清理现有数据
- `--connection`: 数据库连接字符串

## 📝 注意事项

1. **数据库准备**：确保PostgreSQL数据库已创建并运行
2. **权限要求**：需要CREATE TABLE和INSERT权限
3. **数据清理**：使用 `--cleanup` 参数会删除所有测试表
4. **数据量**：根据实际需求调整数据量，避免生成过多数据

## 🔗 相关文档

- [文档质量提升总结](../文档质量提升总结.md)
- [View核心文档](../)

---

**最后更新**: 2024年
**维护状态**: ✅ 已完成
