# PostgreSQL 向量搜索性能问题排查

> **更新时间**: 2025 年 11 月 1 日
> **技术版本**: PostgreSQL 14+, pgvector 0.7.0+
> **文档编号**: 09-03-02

## 📑 目录

- [PostgreSQL 向量搜索性能问题排查](#postgresql-向量搜索性能问题排查)
  - [📑 目录](#-目录)
  - [1. 概述](#1-概述)
    - [1.1 性能问题类型](#11-性能问题类型)
    - [1.2 排查流程](#12-排查流程)
  - [2. 慢查询诊断](#2-慢查询诊断)
    - [2.1 启用慢查询日志](#21-启用慢查询日志)
    - [2.2 分析查询计划](#22-分析查询计划)
    - [2.3 查询统计信息](#23-查询统计信息)
  - [3. 索引问题排查](#3-索引问题排查)
    - [3.1 检查索引使用情况](#31-检查索引使用情况)
    - [3.2 检查索引大小](#32-检查索引大小)
    - [3.3 重建索引](#33-重建索引)
  - [4. 资源瓶颈排查](#4-资源瓶颈排查)
    - [4.1 CPU 使用率](#41-cpu-使用率)
    - [4.2 内存使用](#42-内存使用)
    - [4.3 磁盘 I/O](#43-磁盘-io)
  - [5. 常见性能问题](#5-常见性能问题)
    - [5.1 问题：查询不使用索引](#51-问题查询不使用索引)
    - [5.2 问题：ef\_search 参数不当](#52-问题ef_search-参数不当)
    - [5.3 问题：连接池不足](#53-问题连接池不足)
  - [6. 性能优化建议](#6-性能优化建议)
    - [6.1 索引优化](#61-索引优化)
    - [6.2 查询优化](#62-查询优化)
    - [6.3 配置优化](#63-配置优化)
  - [7. 参考资料](#7-参考资料)

---

## 1. 概述

### 1.1 性能问题类型

- **查询慢**: 向量搜索查询延迟高
- **吞吐量低**: QPS 不达标
- **资源占用高**: CPU/内存使用率高
- **索引构建慢**: 索引创建时间长

### 1.2 排查流程

```text
1. 识别问题（慢查询、高资源占用）
   ↓
2. 收集诊断信息（查询计划、系统指标）
   ↓
3. 分析根本原因（索引、配置、数据分布）
   ↓
4. 实施优化方案
   ↓
5. 验证优化效果
```

## 2. 慢查询诊断

### 2.1 启用慢查询日志

```sql
-- 启用慢查询日志
ALTER SYSTEM SET log_min_duration_statement = 1000;  -- 记录 > 1秒的查询
ALTER SYSTEM SET log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h ';
SELECT pg_reload_conf();

-- 查看慢查询日志
SELECT * FROM pg_stat_statements
WHERE mean_exec_time > 1000
ORDER BY mean_exec_time DESC
LIMIT 10;
```

### 2.2 分析查询计划

```sql
-- 分析向量搜索查询计划
EXPLAIN (ANALYZE, BUFFERS, VERBOSE)
SELECT id, metadata, embedding <=> query_vector AS distance
FROM vectors
ORDER BY embedding <=> query_vector
LIMIT 10;

-- 检查是否使用索引
-- 应该看到: Index Scan using vectors_embedding_idx
-- 如果看到: Seq Scan，说明没有使用索引
```

### 2.3 查询统计信息

```sql
-- 使用 pg_stat_statements 分析查询
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;

SELECT
    query,
    calls,
    total_exec_time,
    mean_exec_time,
    max_exec_time,
    stddev_exec_time
FROM pg_stat_statements
WHERE query LIKE '%embedding%'
ORDER BY mean_exec_time DESC
LIMIT 10;
```

## 3. 索引问题排查

### 3.1 检查索引使用情况

```sql
-- 检查索引是否被使用
SELECT
    schemaname,
    tablename,
    indexname,
    idx_scan,
    idx_tup_read,
    idx_tup_fetch
FROM pg_stat_user_indexes
WHERE tablename = 'vectors'
ORDER BY idx_scan;

-- 如果 idx_scan = 0，说明索引未被使用
```

### 3.2 检查索引大小

```sql
-- 检查索引大小
SELECT
    schemaname,
    tablename,
    indexname,
    pg_size_pretty(pg_relation_size(indexrelid)) AS index_size
FROM pg_stat_user_indexes
WHERE tablename = 'vectors';

-- 如果索引过大，考虑优化参数
```

### 3.3 重建索引

```sql
-- 重建索引（如果索引损坏或性能下降）
REINDEX INDEX vectors_embedding_idx;

-- 或者重建表的所有索引
REINDEX TABLE vectors;
```

## 4. 资源瓶颈排查

### 4.1 CPU 使用率

```sql
-- 查看当前活动查询
SELECT
    pid,
    usename,
    application_name,
    state,
    query_start,
    now() - query_start AS duration,
    query
FROM pg_stat_activity
WHERE state = 'active'
ORDER BY query_start;

-- 查看 CPU 密集型查询
SELECT
    pid,
    query,
    now() - query_start AS duration
FROM pg_stat_activity
WHERE state = 'active'
AND query LIKE '%embedding%'
ORDER BY duration DESC;
```

### 4.2 内存使用

```sql
-- 查看共享缓冲区使用
SELECT
    name,
    setting,
    unit
FROM pg_settings
WHERE name IN ('shared_buffers', 'work_mem', 'maintenance_work_mem');

-- 查看缓存命中率
SELECT
    sum(heap_blks_read) AS heap_read,
    sum(heap_blks_hit) AS heap_hit,
    sum(heap_blks_hit) / (sum(heap_blks_hit) + sum(heap_blks_read)) AS cache_hit_ratio
FROM pg_statio_user_tables
WHERE schemaname = 'public';
```

### 4.3 磁盘 I/O

```sql
-- 查看表大小和 I/O 统计
SELECT
    schemaname,
    tablename,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS total_size,
    seq_scan,
    seq_tup_read,
    idx_scan,
    idx_tup_fetch
FROM pg_stat_user_tables
WHERE tablename = 'vectors';
```

## 5. 常见性能问题

### 5.1 问题：查询不使用索引

**症状**:

- EXPLAIN 显示 Seq Scan
- 查询速度慢

**原因**:

- 索引未创建
- 查询条件不匹配索引
- 索引参数不当

**解决方案**:

```sql
-- 1. 检查索引是否存在
SELECT indexname FROM pg_indexes WHERE tablename = 'vectors';

-- 2. 创建 HNSW 索引
CREATE INDEX ON vectors USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);

-- 3. 确保查询使用索引
SET hnsw.ef_search = 40;
SELECT * FROM vectors
ORDER BY embedding <=> query_vector
LIMIT 10;
```

### 5.2 问题：ef_search 参数不当

**症状**:

- 查询速度慢
- 召回率低

**解决方案**:

```sql
-- 根据需求调整 ef_search
-- 快速查询（召回率可能降低）
SET hnsw.ef_search = 20;

-- 高召回率查询（速度可能较慢）
SET hnsw.ef_search = 200;

-- 查询
SELECT * FROM vectors
ORDER BY embedding <=> query_vector
LIMIT 10;
```

### 5.3 问题：连接池不足

**症状**:

- 连接超时
- 查询排队

**解决方案**:

```python
# 增加连接池大小
from asyncpg import create_pool

pool = await create_pool(
    database_url,
    min_size=20,  # 增加最小连接数
    max_size=100,  # 增加最大连接数
    max_queries=50000
)
```

## 6. 性能优化建议

### 6.1 索引优化

```sql
-- 1. 选择合适的索引类型
-- HNSW: 高精度、快速查询
CREATE INDEX ON vectors USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);

-- IVFFlat: 大规模数据、高更新频率
CREATE INDEX ON vectors USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);

-- 2. 优化索引参数
-- 根据数据量和查询模式调整
```

### 6.2 查询优化

```sql
-- 1. 使用 LIMIT 限制结果
SELECT * FROM vectors
ORDER BY embedding <=> query_vector
LIMIT 10;  -- 不要查询所有结果

-- 2. 只选择需要的列
SELECT id, metadata FROM vectors
ORDER BY embedding <=> query_vector
LIMIT 10;

-- 3. 使用预编译语句（应用层）
```

### 6.3 配置优化

```sql
-- PostgreSQL 配置优化
-- shared_buffers: 25% of RAM
ALTER SYSTEM SET shared_buffers = '2GB';

-- work_mem: 用于排序和哈希操作
ALTER SYSTEM SET work_mem = '64MB';

-- effective_cache_size: 操作系统和 PostgreSQL 缓存总和
ALTER SYSTEM SET effective_cache_size = '6GB';

-- 重启 PostgreSQL 使配置生效
```

## 7. 参考资料

- [常见问题诊断](./常见问题诊断.md)
- [HNSW 性能优化](../../01-向量与混合搜索/性能优化/HNSW性能优化.md)
- [查询性能优化](../../01-向量与混合搜索/性能优化/查询性能优化.md)

---

**最后更新**: 2025 年 11 月 1 日
**维护者**: PostgreSQL Modern Team
**文档编号**: 09-03-02
