# 📘 AI应用性能优化完整手册

> **更新日期**: 2025年12月4日
> **适用场景**: RAG应用、向量搜索、AI推理优化
> **文档类型**: P3持续实践手册

---

## 📑 目录

- [一、向量搜索优化](#一向量搜索优化)
- [二、RAG应用优化](#二rag应用优化)
- [三、模型推理优化](#三模型推理优化)
- [四、批处理优化](#四批处理优化)
- [五、缓存策略优化](#五缓存策略优化)
- [六、并发优化](#六并发优化)
- [七、成本优化](#七成本优化)
- [八、综合优化案例](#八综合优化案例)

---

## 一、向量搜索优化

### 1.1 索引选择与调优

#### 小数据量（<10万）

**最佳实践**：不使用索引，暴力搜索

```sql
-- 无需索引，直接查询
SELECT *, 1 - (embedding <=> query_vector) AS similarity
FROM documents
ORDER BY embedding <=> query_vector
LIMIT 10;
```

**优势**：

- ✅ 100%召回率
- ✅ 无索引维护成本
- ✅ 适合实时更新

#### 中等数据量（10万-100万）

**最佳实践**：HNSW索引

```sql
-- 创建HNSW索引
CREATE INDEX ON documents USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);

-- 查询时调整ef_search
SET hnsw.ef_search = 100;  -- 默认40
```

**参数调优表**：

| 场景 | m | ef_construction | ef_search | 召回率 | 延迟 |
|------|---|----------------|-----------|--------|------|
| 平衡 | 16 | 64 | 40 | 95% | 中 |
| 高精度 | 32 | 200 | 100 | 98% | 高 |
| 高性能 | 8 | 32 | 20 | 90% | 低 |

#### 大数据量（>100万）

**最佳实践**：IVFFlat索引

```sql
-- 创建IVFFlat索引
CREATE INDEX ON documents USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 1000);  -- lists = sqrt(总行数)

-- 查询时调整probes
SET ivfflat.probes = 10;  -- 默认1
```

**参数调优表**：

| 数据量 | lists | probes | 召回率 | QPS |
|--------|-------|--------|--------|-----|
| 100万 | 1000 | 1 | 85% | 500 |
| 100万 | 1000 | 10 | 92% | 250 |
| 1000万 | 3000 | 10 | 90% | 200 |

### 1.2 混合检索优化

**策略1：先过滤再向量搜索**:

```sql
-- ✅ 好：先过滤，减少向量搜索范围
SELECT *, 1 - (embedding <=> $1) AS similarity
FROM documents
WHERE category = 'tech'  -- 先过滤80%数据
  AND created_at > NOW() - INTERVAL '30 days'
ORDER BY embedding <=> $1
LIMIT 10;
```

**策略2：加权融合向量和关键词**:

```sql
-- 向量（70%） + 全文搜索（30%）
SELECT *,
       (1 - (embedding <=> $1)) * 0.7 AS vec_score,
       ts_rank(to_tsvector('english', content), $2) * 0.3 AS text_score,
       ((1 - (embedding <=> $1)) * 0.7 +
        ts_rank(to_tsvector('english', content), $2) * 0.3) AS final_score
FROM documents
WHERE to_tsvector('english', content) @@ $2
ORDER BY final_score DESC
LIMIT 10;
```

**性能对比**：

| 方法 | 延迟 | 召回率 | 相关性 |
|------|------|--------|--------|
| 纯向量 | 50ms | 95% | 85% |
| 纯关键词 | 20ms | 70% | 75% |
| 混合（先过滤） | 30ms | 93% | 90% |
| 混合（加权融合） | 60ms | 96% | 92% |

### 1.3 批量查询优化

```python
# ❌ 慢：逐个查询
for query in queries:
    results = db.search(query)
    process(results)

# ✅ 快：批量查询
batch_size = 100
for i in range(0, len(queries), batch_size):
    batch = queries[i:i+batch_size]
    results = db.batch_search(batch)
    process_batch(results)
```

**性能提升**：

- 吞吐量：+300%（100 → 400 QPS）
- 延迟：-50%（每个查询）

---

## 二、RAG应用优化

### 2.1 文档分块优化

**策略对比**：

| 策略 | chunk_size | overlap | 优势 | 劣势 |
|------|-----------|---------|------|------|
| 小块 | 200-500 | 50 | 精确召回 | 上下文不足 |
| 中块 | 500-1000 | 100-200 | 平衡 | - |
| 大块 | 1000-2000 | 200-500 | 上下文丰富 | 噪音多 |

**最佳实践**：

```python
from langchain.text_splitter import RecursiveCharacterTextSplitter

splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,  # 推荐：1000（中文约500字）
    chunk_overlap=200,  # 20% overlap
    separators=["\n\n", "\n", "。", "！", "？", " ", ""]
)

chunks = splitter.split_text(text)
```

### 2.2 检索策略优化

**策略1：MultiQuery Retriever**:

```python
from langchain.retrievers import MultiQueryRetriever

# 生成多个查询变体
retriever = MultiQueryRetriever.from_llm(
    retriever=base_retriever,
    llm=llm
)

# 原查询："什么是向量数据库"
# 生成变体：
# - "向量数据库的定义"
# - "如何使用向量数据库"
# - "向量数据库的优势"
```

**提升**：召回率+15%（85% → 98%）

**策略2：Self-Query Retriever**:

```python
from langchain.retrievers import SelfQueryRetriever

# 自动生成过滤条件
retriever = SelfQueryRetriever.from_llm(
    llm=llm,
    vectorstore=vectorstore,
    document_contents="技术文档",
    metadata_field_info=[
        {"name": "category", "type": "string"},
        {"name": "date", "type": "date"}
    ]
)

# 查询："2024年的PostgreSQL文档"
# 自动生成：WHERE category='PostgreSQL' AND date >= '2024-01-01'
```

**提升**：相关性+20%

**策略3：Parent Document Retriever**:

```python
from langchain.retrievers import ParentDocumentRetriever

# 小块检索，大块返回
retriever = ParentDocumentRetriever(
    vectorstore=vectorstore,
    docstore=docstore,
    child_splitter=small_chunk_splitter,  # 小块用于检索
    parent_splitter=large_chunk_splitter   # 大块用于返回
)
```

**提升**：上下文完整性+30%

### 2.3 LLM调用优化

**优化1：使用更快的模型**:

```python
# ❌ 慢：GPT-4（30秒）
llm = ChatOpenAI(model="gpt-4", temperature=0)

# ✅ 快：GPT-3.5-turbo（3秒）
llm = ChatOpenAI(model="gpt-3.5-turbo", temperature=0)

# ✅ 更快：本地模型（0.5秒）
from langchain.llms import HuggingFacePipeline
llm = HuggingFacePipeline.from_model_id(
    model_id="BAAI/bge-large-zh",
    task="text-generation",
    device=0  # GPU
)
```

**优化2：流式输出**:

```python
# 用户体验更好
for chunk in llm.stream("问题"):
    print(chunk.content, end="", flush=True)
```

**优化3：缓存重复查询**:

```python
from langchain.cache import RedisCache
from langchain.globals import set_llm_cache

set_llm_cache(RedisCache(redis_client))

# 相同查询直接返回缓存
# 延迟：30s → 10ms
```

---

## 三、模型推理优化

### 3.1 模型选择

| 模型 | 参数量 | 延迟（CPU） | 延迟（GPU） | 质量 | 适用场景 |
|------|--------|-----------|-----------|------|---------|
| **Embedding模型** ||||||
| all-MiniLM-L6-v2 | 22M | 10ms | 2ms | 中 | 实时应用 |
| bge-base-zh | 110M | 50ms | 5ms | 高 | 中文应用 |
| bge-large-zh | 326M | 150ms | 15ms | 极高 | 离线处理 |
| **LLM模型** ||||||
| Llama-2-7B | 7B | 2s | 200ms | 中 | 边缘设备 |
| Llama-2-13B | 13B | 5s | 500ms | 高 | 服务器 |
| Llama-2-70B | 70B | 30s | 2s | 极高 | 云端 |

### 3.2 量化优化

**INT8量化**：

```python
from transformers import AutoModelForCausalLM, BitsAndBytesConfig

quantization_config = BitsAndBytesConfig(
    load_in_8bit=True,
    llm_int8_threshold=6.0
)

model = AutoModelForCausalLM.from_pretrained(
    "meta-llama/Llama-2-7b-hf",
    quantization_config=quantization_config,
    device_map="auto"
)
```

**效果**：

- 内存：-50%（14GB → 7GB）
- 推理速度：+20%
- 质量损失：<2%

**INT4量化（GPTQ）**：

```python
from auto_gptq import AutoGPTQForCausalLM

model = AutoGPTQForCausalLM.from_quantized(
    "TheBloke/Llama-2-7B-GPTQ",
    device="cuda:0",
    use_triton=True
)
```

**效果**：

- 内存：-75%（14GB → 3.5GB）
- 推理速度：+50%
- 质量损失：<5%

### 3.3 批处理推理

```python
# ❌ 慢：逐个推理
for text in texts:
    embedding = model.encode(text)
    store(embedding)

# ✅ 快：批处理
embeddings = model.encode(texts, batch_size=32)
batch_store(embeddings)
```

**性能对比**：

| batch_size | 吞吐量 | 延迟（per item） |
|-----------|--------|----------------|
| 1 | 10 items/s | 100ms |
| 8 | 60 items/s | 17ms |
| 32 | 200 items/s | 5ms |
| 128 | 250 items/s | 4ms |

---

## 四、批处理优化

### 4.1 数据库批量操作

```python
# ❌ 慢：逐条插入
for doc in documents:
    embedding = model.encode(doc['content'])
    cur.execute("INSERT INTO documents (content, embedding) VALUES (%s, %s)",
                (doc['content'], embedding.tolist()))
    conn.commit()

# ✅ 快：批量插入
embeddings = model.encode([doc['content'] for doc in documents], batch_size=32)
cur.executemany("INSERT INTO documents (content, embedding) VALUES (%s, %s)",
                [(doc['content'], emb.tolist()) for doc, emb in zip(documents, embeddings)])
conn.commit()

# ✅ 更快：COPY
from io import StringIO
import csv

f = StringIO()
writer = csv.writer(f)
for doc, emb in zip(documents, embeddings):
    writer.writerow([doc['content'], emb.tolist()])
f.seek(0)
cur.copy_from(f, 'documents', columns=['content', 'embedding'], sep=',')
conn.commit()
```

**性能对比**：

- 逐条INSERT：100 items/s
- executemany：1,000 items/s（**+10x**）
- COPY：10,000 items/s（**+100x**）

---

## 五、缓存策略优化

### 5.1 多级缓存

```python
from functools import lru_cache
import redis

# L1：内存缓存（最快）
@lru_cache(maxsize=1000)
def get_embedding_l1(text):
    return get_embedding_l2(text)

# L2：Redis缓存（快）
redis_client = redis.Redis()

def get_embedding_l2(text):
    cached = redis_client.get(f"emb:{text}")
    if cached:
        return pickle.loads(cached)

    embedding = get_embedding_l3(text)
    redis_client.setex(f"emb:{text}", 3600, pickle.dumps(embedding))
    return embedding

# L3：数据库/模型（慢）
def get_embedding_l3(text):
    # 从数据库查询或模型生成
    pass
```

**性能对比**：

- L1缓存命中：<1ms
- L2缓存命中：5ms
- L3计算：50ms

**命中率优化**：

- 热点数据预加载
- TTL合理设置
- 缓存预热

---

## 六、并发优化

### 6.1 异步处理

```python
import asyncio
import asyncpg

async def search_async(query):
    conn = await asyncpg.connect(DATABASE_URL)
    results = await conn.fetch(
        "SELECT * FROM documents ORDER BY embedding <=> $1 LIMIT 10",
        query_embedding
    )
    await conn.close()
    return results

# 并发执行多个查询
queries = [...]
results = await asyncio.gather(*[search_async(q) for q in queries])
```

**性能提升**：

- 吞吐量：+5x（单线程 → 异步）
- 延迟：相同（单个查询）

### 6.2 连接池

```python
from psycopg2 import pool

# 创建连接池
connection_pool = pool.ThreadedConnectionPool(
    minconn=10,
    maxconn=100,
    host="localhost",
    database="mydb"
)

def search(query):
    conn = connection_pool.getconn()
    try:
        cur = conn.cursor()
        cur.execute("SELECT ...")
        return cur.fetchall()
    finally:
        connection_pool.putconn(conn)
```

---

## 七、成本优化

### 7.1 API vs 本地模型

**成本对比**（100万次embedding）：

| 方案 | 初始投入 | 月运行成本 | 年总成本 |
|------|---------|-----------|---------|
| OpenAI API | $0 | $20,000 | **$240,000** |
| 本地GPU（RTX 4090） | $2,000 | $50 | **$2,600** |
| 云GPU（A100） | $0 | $1,500 | **$18,000** |

**结论**：本地部署节省**99%成本**！

### 7.2 模型优化

**策略1：使用更小的模型**:

```python
# 如果精度要求不高
# bge-large-zh (326M) → bge-base-zh (110M)
# 速度：+3x，质量：-3%
```

**策略2：按需加载**:

```python
# 不是所有请求都需要LLM
if needs_llm(query):
    result = llm.invoke(query)
else:
    result = simple_rule(query)
```

---

## 八、综合优化案例

### 案例：智能客服RAG系统

**初始状态**：

- 延迟：2秒
- QPS：10
- 成本：$5,000/月

**优化步骤**：

**Step 1：向量索引优化**:

```sql
-- 添加HNSW索引
CREATE INDEX ON knowledge_base USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);
```

- 延迟：2s → 500ms（**-75%**）

**Step 2：检索策略优化**:

```python
# 使用混合检索
hybrid_results = hybrid_search(query, filters={'category': 'FAQ'})
```

- 相关性：75% → 90%（**+15%**）

**Step 3：模型优化**:

```python
# OpenAI API → 本地bge-large-zh
model = SentenceTransformer('BAAI/bge-large-zh', device='cuda')
```

- 成本：$5,000/月 → $50/月（**-99%**）

**Step 4：缓存优化**:

```python
# Redis缓存热点查询
cache_hit_rate = 60%
```

- 延迟：500ms → 200ms（**-60%** for cached）

**Step 5：批处理优化**:

```python
# 批量生成embedding
embeddings = model.encode(texts, batch_size=32)
```

- 吞吐量：10 QPS → 100 QPS（**+10x**）

**最终状态**：

- 延迟：200ms（**-90%**）
- QPS：100（**+10x**）
- 成本：$50/月（**-99%**）
- 相关性：90%（**+15%**）

---

**🎯 综合运用这些优化技巧，打造高性能AI应用！** 🚀

---

**最后更新**: 2025年12月4日
**维护者**: PostgreSQL Modern Team
**文档编号**: P3-2-AI-OPTIMIZATION-2025-12
