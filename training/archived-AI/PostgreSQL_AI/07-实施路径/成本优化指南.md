# 成本优化指南

> **文档编号**: AI-07-05
> **最后更新**: 2025年1月
> **主题**: 07-实施路径
> **子主题**: 05-成本优化指南

## 📑 目录

- [成本优化指南](#成本优化指南)
  - [📑 目录](#-目录)
  - [一、概述](#一概述)
  - [二、成本构成分析](#二成本构成分析)
    - [2.1 基础设施成本](#21-基础设施成本)
    - [2.2 开发成本](#22-开发成本)
    - [2.3 运维成本](#23-运维成本)
    - [2.4 API调用成本](#24-api调用成本)
  - [三、基础设施优化](#三基础设施优化)
    - [3.1 Serverless方案](#31-serverless方案)
    - [3.2 资源调度优化](#32-资源调度优化)
    - [3.3 存储优化](#33-存储优化)
  - [四、开发成本优化](#四开发成本优化)
    - [4.1 自动化工具使用](#41-自动化工具使用)
    - [4.2 代码复用](#42-代码复用)
    - [4.3 开发效率提升](#43-开发效率提升)
  - [五、运维成本优化](#五运维成本优化)
    - [5.1 自动化运维](#51-自动化运维)
    - [5.2 监控告警](#52-监控告警)
    - [5.3 故障自愈](#53-故障自愈)
  - [六、API成本优化](#六api成本优化)
    - [6.1 缓存策略](#61-缓存策略)
    - [6.2 批量处理](#62-批量处理)
    - [6.3 模型选择](#63-模型选择)
  - [七、成本监控](#七成本监控)
    - [7.1 成本追踪](#71-成本追踪)
    - [7.2 成本告警](#72-成本告警)
    - [7.3 成本分析](#73-成本分析)
  - [八、最佳实践](#八最佳实践)
  - [九、关联主题](#九关联主题)
  - [十、对标资源](#十对标资源)
    - [技术文档](#技术文档)
    - [工具](#工具)

## 一、概述

PostgreSQL AI应用的成本优化指南，从基础设施、开发、运维、API调用等多个维度提供成本优化策略，帮助降低总体拥有成本（TCO）。

## 二、成本构成分析

### 2.1 基础设施成本

**成本构成**:

- 计算资源（CPU、内存）
- 存储资源（SSD、对象存储）
- 网络资源（带宽、流量）
- 云服务费用

**优化策略**:

```sql
-- 使用Serverless按需付费
-- Neon/Supabase Serverless
-- 闲置时成本为0
-- 按实际使用付费

-- 使用分区表优化存储
CREATE TABLE documents_2024 PARTITION OF documents
FOR VALUES FROM ('2024-01-01') TO ('2025-01-01');

-- 旧数据归档到对象存储
-- 使用FDW访问归档数据
CREATE EXTENSION postgres_fdw;
CREATE SERVER s3_server FOREIGN DATA WRAPPER postgres_fdw
    OPTIONS (host 's3-bridge', dbname 'archive');
```

### 2.2 开发成本

**成本构成**:

- 人力成本（开发、测试）
- 工具成本（IDE、许可证）
- 培训成本

**优化策略**:

```sql
-- 使用pgai Vectorizer减少开发工作量
SELECT ai.create_vectorizer(
    'news_articles'::regclass,
    destination => 'news_embeddings',
    embedding => ai.embedding_openai('text-embedding-3-small', 'content')
);

-- 开发周期缩短85%
-- 人力成本降低75%
```

### 2.3 运维成本

**成本构成**:

- DBA人力成本
- 监控工具成本
- 备份存储成本

**优化策略**:

```sql
-- 使用PilotScope AI自治运维
-- DBA人力成本降低70%

-- 自动化运维任务
SELECT cron.schedule(
    'auto-vacuum',
    '0 2 * * *',
    'VACUUM ANALYZE documents'
);

-- 自动化备份
SELECT cron.schedule(
    'auto-backup',
    '0 2 * * *',
    'pg_dump -h localhost -U postgres -d ai_app -F c -f /backup/daily_$(date +\%Y\%m\%d).dump'
);
```

### 2.4 API调用成本

**成本构成**:

- OpenAI API调用费用
- Anthropic API调用费用
- 其他AI服务费用

**优化策略**:

```sql
-- 使用缓存减少API调用
CREATE TABLE embedding_cache (
    text_hash TEXT PRIMARY KEY,
    embedding vector(1536),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '30 days'
);

-- 使用缓存的Embedding生成
CREATE OR REPLACE FUNCTION cached_embedding(
    p_text TEXT
) RETURNS vector(1536) AS $$
DECLARE
    v_hash TEXT;
    v_embedding vector(1536);
BEGIN
    v_hash := md5(p_text);

    SELECT embedding INTO v_embedding
    FROM embedding_cache
    WHERE text_hash = v_hash
      AND expires_at > NOW();

    IF v_embedding IS NOT NULL THEN
        RETURN v_embedding;
    END IF;

    -- 生成新Embedding
    v_embedding := ai.embedding('text-embedding-3-small', p_text);

    -- 缓存
    INSERT INTO embedding_cache (text_hash, embedding)
    VALUES (v_hash, v_embedding)
    ON CONFLICT (text_hash) DO UPDATE
    SET embedding = v_embedding,
        expires_at = NOW() + INTERVAL '30 days';

    RETURN v_embedding;
END;
$$ LANGUAGE plpgsql;
```

## 三、基础设施优化

### 3.1 Serverless方案

```bash
# Neon Serverless
# 按需付费，闲置时成本为0
neon projects create my-app

# 成本优势：
# - 无闲置资源成本
# - 按实际使用付费
# - 自动扩展
```

### 3.2 资源调度优化

```sql
-- 使用连接池减少资源消耗
-- PgBouncer配置
pool_mode = transaction
max_client_conn = 10000
default_pool_size = 25

-- 资源使用监控
SELECT
    COUNT(*) AS active_connections,
    SUM(pg_database_size(datname)) AS total_size
FROM pg_stat_activity
WHERE state = 'active';
```

### 3.3 存储优化

```sql
-- 使用分区表优化存储
CREATE TABLE documents_2024 PARTITION OF documents
FOR VALUES FROM ('2024-01-01') TO ('2025-01-01');

-- 旧数据归档
CREATE TABLE documents_archive (
    LIKE documents INCLUDING ALL
);

-- 归档旧数据
INSERT INTO documents_archive
SELECT * FROM documents
WHERE created_at < NOW() - INTERVAL '2 years';

DELETE FROM documents
WHERE created_at < NOW() - INTERVAL '2 years';
```

## 四、开发成本优化

### 4.1 自动化工具使用

```sql
-- 使用pgai Vectorizer自动化向量化
SELECT ai.create_vectorizer(
    'news_articles'::regclass,
    destination => 'news_embeddings',
    embedding => ai.embedding_openai('text-embedding-3-small', 'content')
);

-- 开发工作量减少85%
-- 代码量减少70%
```

### 4.2 代码复用

```sql
-- 创建通用函数库
CREATE SCHEMA ai_utils;

-- 通用向量检索函数
CREATE OR REPLACE FUNCTION ai_utils.vector_search(
    p_table_name TEXT,
    p_query_vector vector(1536),
    p_limit INTEGER DEFAULT 10
) RETURNS TABLE (
    id INTEGER,
    similarity DECIMAL
) AS $$
BEGIN
    RETURN QUERY
    EXECUTE format('
        SELECT id, 1 - (embedding <=> $1) AS similarity
        FROM %I
        WHERE embedding <=> $1 < 0.8
        ORDER BY embedding <=> $1
        LIMIT $2
    ', p_table_name) USING p_query_vector, p_limit;
END;
$$ LANGUAGE plpgsql;
```

### 4.3 开发效率提升

```sql
-- 使用模板快速创建表
CREATE OR REPLACE FUNCTION create_vector_table(
    p_table_name TEXT,
    p_vector_dim INTEGER DEFAULT 1536
) RETURNS void AS $$
BEGIN
    EXECUTE format('
        CREATE TABLE %I (
            id SERIAL PRIMARY KEY,
            content TEXT,
            embedding vector(%s),
            created_at TIMESTAMPTZ DEFAULT NOW()
        );

        CREATE INDEX ON %I
        USING hnsw (embedding vector_cosine_ops);
    ', p_table_name, p_vector_dim, p_table_name);
END;
$$ LANGUAGE plpgsql;
```

## 五、运维成本优化

### 5.1 自动化运维

```sql
-- 自动化VACUUM
SELECT cron.schedule(
    'auto-vacuum',
    '0 2 * * *',
    'VACUUM ANALYZE documents'
);

-- 自动化索引维护
SELECT cron.schedule(
    'auto-reindex',
    '0 3 * * 0',
    'REINDEX TABLE CONCURRENTLY documents'
);

-- 自动化统计信息更新
SELECT cron.schedule(
    'auto-analyze',
    '0 4 * * *',
    'ANALYZE documents'
);
```

### 5.2 监控告警

```sql
-- 成本监控
CREATE VIEW cost_monitoring AS
SELECT
    'api_calls' AS cost_type,
    COUNT(*) AS daily_count,
    COUNT(*) * 0.01 AS estimated_cost
FROM ai_api_log
WHERE created_at > NOW() - INTERVAL '1 day';

-- 成本告警
CREATE OR REPLACE FUNCTION check_daily_cost()
RETURNS void AS $$
DECLARE
    v_cost DECIMAL;
BEGIN
    SELECT estimated_cost INTO v_cost
    FROM cost_monitoring
    WHERE cost_type = 'api_calls';

    IF v_cost > 100 THEN
        PERFORM pg_notify('cost_alert', format('Daily cost: $%s', v_cost));
    END IF;
END;
$$ LANGUAGE plpgsql;
```

### 5.3 故障自愈

```sql
-- 自动故障恢复
CREATE OR REPLACE FUNCTION auto_recovery()
RETURNS void AS $$
BEGIN
    -- 检查索引健康
    IF (SELECT COUNT(*) FROM pg_stat_user_indexes WHERE idx_scan = 0) > 10 THEN
        -- 重建未使用索引
        PERFORM pg_reindex('documents');
    END IF;

    -- 检查连接数
    IF (SELECT COUNT(*) FROM pg_stat_activity) > 150 THEN
        -- 清理空闲连接
        PERFORM pg_terminate_backend(pid)
        FROM pg_stat_activity
        WHERE state = 'idle'
          AND state_change < NOW() - INTERVAL '1 hour';
    END IF;
END;
$$ LANGUAGE plpgsql;
```

## 六、API成本优化

### 6.1 缓存策略

```sql
-- Embedding缓存
CREATE TABLE embedding_cache (
    text_hash TEXT PRIMARY KEY,
    embedding vector(1536),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '30 days'
);

-- LLM响应缓存
CREATE TABLE llm_cache (
    prompt_hash TEXT PRIMARY KEY,
    response JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '7 days'
);
```

### 6.2 批量处理

```sql
-- 批量生成Embedding
CREATE OR REPLACE FUNCTION batch_embedding(
    p_texts TEXT[]
) RETURNS vector(1536)[] AS $$
DECLARE
    v_embeddings vector(1536)[];
BEGIN
    -- 使用批量API（更便宜）
    SELECT ai.embedding_batch('text-embedding-3-small', p_texts)
    INTO v_embeddings;

    RETURN v_embeddings;
END;
$$ LANGUAGE plpgsql;
```

### 6.3 模型选择

```sql
-- 使用更便宜的模型
-- text-embedding-3-small vs text-embedding-3-large
-- 成本差异：10x

-- 根据场景选择模型
CREATE OR REPLACE FUNCTION smart_embedding(
    p_text TEXT,
    p_quality TEXT DEFAULT 'standard'  -- 'high', 'standard', 'low'
) RETURNS vector(1536) AS $$
BEGIN
    CASE p_quality
        WHEN 'high' THEN
            RETURN ai.embedding('text-embedding-3-large', p_text);
        WHEN 'standard' THEN
            RETURN ai.embedding('text-embedding-3-small', p_text);
        WHEN 'low' THEN
            -- 使用本地模型
            RETURN local_embedding(p_text);
    END CASE;
END;
$$ LANGUAGE plpgsql;
```

## 七、成本监控

### 7.1 成本追踪

```sql
-- 成本追踪表
CREATE TABLE cost_tracking (
    id SERIAL PRIMARY KEY,
    cost_type TEXT,  -- 'infrastructure', 'api', 'development', 'operations'
    amount DECIMAL(10,2),
    period_start TIMESTAMPTZ,
    period_end TIMESTAMPTZ,
    metadata JSONB
);

-- 记录成本
INSERT INTO cost_tracking (cost_type, amount, period_start, period_end)
VALUES (
    'api',
    (SELECT COUNT(*) * 0.01 FROM ai_api_log WHERE created_at > NOW() - INTERVAL '1 day'),
    NOW() - INTERVAL '1 day',
    NOW()
);
```

### 7.2 成本告警

```sql
-- 成本告警函数
CREATE OR REPLACE FUNCTION check_cost_alerts()
RETURNS void AS $$
DECLARE
    v_daily_cost DECIMAL;
    v_monthly_cost DECIMAL;
BEGIN
    -- 检查日成本
    SELECT SUM(amount) INTO v_daily_cost
    FROM cost_tracking
    WHERE cost_type = 'api'
      AND period_start > NOW() - INTERVAL '1 day';

    IF v_daily_cost > 100 THEN
        PERFORM pg_notify('cost_alert',
            format('Daily API cost exceeds threshold: $%s', v_daily_cost));
    END IF;

    -- 检查月成本
    SELECT SUM(amount) INTO v_monthly_cost
    FROM cost_tracking
    WHERE cost_type IN ('infrastructure', 'api', 'operations')
      AND period_start > NOW() - INTERVAL '1 month';

    IF v_monthly_cost > 5000 THEN
        PERFORM pg_notify('cost_alert',
            format('Monthly cost exceeds threshold: $%s', v_monthly_cost));
    END IF;
END;
$$ LANGUAGE plpgsql;
```

### 7.3 成本分析

```sql
-- 成本分析视图
CREATE VIEW cost_analysis AS
SELECT
    cost_type,
    DATE_TRUNC('month', period_start) AS month,
    SUM(amount) AS total_cost,
    AVG(amount) AS avg_daily_cost,
    COUNT(*) AS record_count
FROM cost_tracking
GROUP BY cost_type, DATE_TRUNC('month', period_start)
ORDER BY month DESC, cost_type;

-- 成本趋势分析
SELECT
    month,
    SUM(total_cost) AS monthly_total,
    LAG(SUM(total_cost)) OVER (ORDER BY month) AS prev_month,
    SUM(total_cost) - LAG(SUM(total_cost)) OVER (ORDER BY month) AS change
FROM cost_analysis
GROUP BY month
ORDER BY month DESC;
```

## 八、最佳实践

1. **基础设施优化**:
   - 使用Serverless按需付费
   - 合理使用分区表
   - 归档旧数据

2. **开发成本优化**:
   - 使用自动化工具
   - 代码复用
   - 提高开发效率

3. **运维成本优化**:
   - 自动化运维任务
   - 监控告警
   - 故障自愈

4. **API成本优化**:
   - 使用缓存
   - 批量处理
   - 选择合适的模型

5. **成本监控**:
   - 追踪所有成本
   - 设置告警阈值
   - 定期分析趋势

## 九、关联主题

- [TCO总拥有成本分析](../06-对比分析/TCO总拥有成本分析.md) - 成本分析
- [性能调优黄金法则](./性能调优黄金法则.md) - 性能优化
- [风险应对措施](./风险应对措施.md) - 成本风险

## 十、对标资源

### 技术文档

- [Neon成本计算器](https://neon.tech/pricing)
- [Supabase定价](https://supabase.com/pricing)
- [OpenAI定价](https://openai.com/pricing)

### 工具

- 成本监控工具
- 预算管理工具

---

**最后更新**: 2025年1月
**维护者**: PostgreSQL Modern Team
**文档编号**: AI-07-05
