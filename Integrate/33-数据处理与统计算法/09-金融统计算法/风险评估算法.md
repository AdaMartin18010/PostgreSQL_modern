# PostgreSQL 风险评估算法完整指南

> **创建日期**: 2025年1月
> **技术栈**: PostgreSQL 17+/18+ | 风险评估 | 风险度量
> **难度级别**: ⭐⭐⭐⭐ (高级)

---

## 📋 目录

- [PostgreSQL 风险评估算法完整指南](#postgresql-风险评估算法完整指南)
  - [📋 目录](#-目录)
  - [风险评估概述](#风险评估概述)
    - [理论基础](#理论基础)
      - [风险评估问题定义](#风险评估问题定义)
      - [风险度量方法分类](#风险度量方法分类)
      - [风险度量选择策略](#风险度量选择策略)
    - [核心风险指标](#核心风险指标)
  - [1. 波动率计算](#1-波动率计算)
    - [1.1 波动率计算原理](#11-波动率计算原理)
      - [波动率定义](#波动率定义)
      - [波动率计算方法](#波动率计算方法)
    - [1.2 历史波动率实现](#12-历史波动率实现)
    - [1.3 滚动波动率](#13-滚动波动率)
    - [1.4 条件波动率](#14-条件波动率)
  - [2. VaR计算](#2-var计算)
    - [2.1 VaR计算原理](#21-var计算原理)
      - [VaR定义](#var定义)
      - [VaR计算方法](#var计算方法)
    - [2.2 历史模拟法VaR实现](#22-历史模拟法var实现)
    - [2.3 参数法VaR](#23-参数法var)
    - [2.4 蒙特卡洛VaR](#24-蒙特卡洛var)
  - [3. 风险指标](#3-风险指标)
    - [3.1 最大回撤原理](#31-最大回撤原理)
      - [最大回撤定义](#最大回撤定义)
      - [回撤计算方法](#回撤计算方法)
    - [3.2 最大回撤实现](#32-最大回撤实现)
    - [3.3 夏普比率原理](#33-夏普比率原理)
      - [夏普比率定义](#夏普比率定义)
      - [夏普比率公式](#夏普比率公式)
    - [3.4 夏普比率实现](#34-夏普比率实现)
    - [3.5 其他风险指标](#35-其他风险指标)
  - [4. 风险评估优化策略](#4-风险评估优化策略)
    - [4.1 索引优化](#41-索引优化)
    - [4.2 窗口函数优化](#42-窗口函数优化)
    - [4.3 批量计算优化](#43-批量计算优化)
  - [5. 实际应用案例](#5-实际应用案例)
    - [5.1 投资组合风险评估](#51-投资组合风险评估)
    - [5.2 风险限额监控](#52-风险限额监控)
    - [5.3 风险归因分析](#53-风险归因分析)
    - [5.4 风险预警系统](#54-风险预警系统)
  - [6. 算法性能对比与优化](#6-算法性能对比与优化)
    - [6.1 风险度量方法对比](#61-风险度量方法对比)
    - [6.2 性能优化建议](#62-性能优化建议)
    - [6.3 常见问题与解决方案](#63-常见问题与解决方案)
  - [7. 最佳实践](#7-最佳实践)
    - [7.1 风险度量选择](#71-风险度量选择)
    - [7.2 参数设置](#72-参数设置)
    - [7.3 数据质量](#73-数据质量)
    - [7.4 SQL实现注意事项](#74-sql实现注意事项)
  - [📚 参考资源](#-参考资源)
    - [学术文献](#学术文献)
    - [PostgreSQL官方文档](#postgresql官方文档)
    - [在线资源](#在线资源)
    - [相关算法](#相关算法)

---

## 风险评估概述

**风险评估（Risk Assessment）**用于量化投资风险，是金融风险管理和投资决策的核心工具。

### 理论基础

#### 风险评估问题定义

**风险评估问题**：给定收益率序列$R = \{r_1, r_2, \ldots, r_T\}$，计算风险度量$\rho(R)$：
$$\rho(R) = f(R)$$

其中$f$是风险度量函数。

#### 风险度量方法分类

风险度量方法分类：

1. **波动率（Volatility）**：
   - 历史波动率：$\sigma = \sqrt{\text{Var}(r)}$
   - 滚动波动率：使用滚动窗口
   - 时间复杂度：$O(n)$

2. **VaR（Value at Risk）**：
   - 历史模拟法：基于历史分位数
   - 参数法：基于分布假设
   - 时间复杂度：$O(n \log n)$

3. **最大回撤（Maximum Drawdown）**：
   - 定义：$\text{MDD} = \max_{t} \frac{\text{Peak}_t - \text{Price}_t}{\text{Peak}_t}$
   - 时间复杂度：$O(n)$

4. **风险调整收益**：
   - 夏普比率：$\text{Sharpe} = \frac{R - R_f}{\sigma}$
   - 时间复杂度：$O(n)$

#### 风险度量选择策略

选择风险度量的考虑因素：

1. **风险类型**：市场风险用波动率，极端风险用VaR
2. **时间范围**：短期用波动率，长期用最大回撤
3. **数据要求**：历史数据用历史模拟，分布已知用参数法

### 核心风险指标

| 指标 | 数学定义 | 公式 | 用途 | 时间复杂度 | 适用场景 |
|------|---------|------|------|-----------|---------|
| **波动率** | $\sigma = \sqrt{\text{Var}(r)}$ | $\sqrt{\text{Var}(r)}$ | 衡量价格波动程度 | $O(n)$ | 市场风险 |
| **VaR** | $P(\text{Loss} > \text{VaR}) = \alpha$ | 分位数 | 在险价值 | $O(n \log n)$ | 极端风险 |
| **最大回撤** | $\max_{t} \frac{\text{Peak}_t - \text{Price}_t}{\text{Peak}_t}$ | $\max(\text{Peak} - \text{Trough})$ | 最大损失幅度 | $O(n)$ | 下行风险 |
| **夏普比率** | $\frac{R - R_f}{\sigma}$ | $(R - R_f) / \sigma$ | 风险调整收益 | $O(n)$ | 收益风险比 |

---

## 1. 波动率计算

### 1.1 波动率计算原理

**波动率（Volatility）**是衡量资产价格波动程度的指标。

#### 波动率定义

**波动率**：收益率的标准差：
$$\sigma = \sqrt{\text{Var}(r)} = \sqrt{\frac{1}{n-1}\sum_{i=1}^{n}(r_i - \bar{r})^2}$$

**年化波动率**：将日波动率年化：
$$\sigma_{annual} = \sigma_{daily} \times \sqrt{252}$$

#### 波动率计算方法

**计算方法**：

1. **历史波动率**：基于历史收益率计算
2. **滚动波动率**：使用滚动窗口计算
3. **条件波动率**：考虑条件异方差（需要GARCH模型）

### 1.2 历史波动率实现

**历史波动率**的实现和使用。

```sql
-- 创建收益率数据表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 已存在，先删除';
            DROP TABLE returns_data CASCADE;
        END IF;

        CREATE TABLE returns_data (
            date DATE NOT NULL,
            symbol VARCHAR(10) NOT NULL,
            return_rate NUMERIC(10, 6) NOT NULL,
            PRIMARY KEY (date, symbol)
        );

        -- 插入示例数据
        INSERT INTO returns_data (date, symbol, return_rate) VALUES
            ('2024-01-01', 'AAPL', 0.02), ('2024-01-02', 'AAPL', -0.01),
            ('2024-01-03', 'AAPL', 0.015), ('2024-01-04', 'AAPL', -0.005),
            ('2024-01-05', 'AAPL', 0.01), ('2024-01-01', 'GOOGL', 0.01),
            ('2024-01-02', 'GOOGL', 0.02), ('2024-01-03', 'GOOGL', -0.015),
            ('2024-01-04', 'GOOGL', 0.005), ('2024-01-05', 'GOOGL', 0.01);

        RAISE NOTICE '表 returns_data 创建成功，已插入10条数据';
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE WARNING '表 returns_data 已存在';
        WHEN OTHERS THEN
            RAISE EXCEPTION '创建表失败: %', SQLERRM;
    END;
END $$;

-- 计算历史波动率（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法计算波动率';
            RETURN;
        END IF;
        RAISE NOTICE '开始计算历史波动率';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '波动率计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 计算日波动率和年化波动率
WITH volatility_stats AS (
    SELECT
        symbol,
        COUNT(*) AS trading_days,
        AVG(return_rate) AS mean_return,
        STDDEV(return_rate) AS std_return,
        252.0 AS trading_days_per_year
    FROM returns_data
    GROUP BY symbol
)
SELECT
    symbol,
    trading_days,
    ROUND(mean_return::numeric, 6) AS mean_return,
    ROUND(std_return::numeric, 6) AS daily_volatility,
    ROUND((std_return * SQRT(trading_days_per_year))::numeric, 6) AS annualized_volatility,
    ROUND((std_return * SQRT(trading_days_per_year) * 100)::numeric, 2) AS annualized_volatility_pct
FROM volatility_stats
ORDER BY symbol;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    symbol,
    STDDEV(return_rate) AS daily_volatility
FROM returns_data
GROUP BY symbol;
```

---

### 1.3 滚动波动率

**滚动波动率**：使用滚动窗口计算波动率。

```sql
-- 滚动波动率（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法计算滚动波动率';
            RETURN;
        END IF;
        RAISE NOTICE '开始计算滚动波动率';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '滚动波动率计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 滚动波动率：使用30日滚动窗口
WITH rolling_volatility AS (
    SELECT
        date,
        symbol,
        return_rate,
        STDDEV(return_rate) OVER (
            PARTITION BY symbol
            ORDER BY date
            ROWS BETWEEN 29 PRECEDING AND CURRENT ROW
        ) AS rolling_volatility_30d,
        STDDEV(return_rate) OVER (
            PARTITION BY symbol
            ORDER BY date
            ROWS BETWEEN 59 PRECEDING AND CURRENT ROW
        ) AS rolling_volatility_60d
    FROM returns_data
)
SELECT
    date,
    symbol,
    return_rate,
    ROUND(rolling_volatility_30d::numeric, 6) AS vol_30d,
    ROUND((rolling_volatility_30d * SQRT(252))::numeric, 6) AS annualized_vol_30d,
    ROUND(rolling_volatility_60d::numeric, 6) AS vol_60d,
    ROUND((rolling_volatility_60d * SQRT(252))::numeric, 6) AS annualized_vol_60d
FROM rolling_volatility
WHERE rolling_volatility_30d IS NOT NULL
ORDER BY symbol, date;
```

### 1.4 条件波动率

**条件波动率**：考虑条件异方差的波动率（简化版）。

```sql
-- 条件波动率（简化版，带错误处理）
DO $$
BEGIN
    BEGIN
        RAISE NOTICE '条件波动率需要GARCH模型，PostgreSQL中实现复杂';
        -- 实际实现需要外部工具或自定义函数
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '条件波动率准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

---

## 2. VaR计算

### 2.1 VaR计算原理

**VaR（Value at Risk，在险价值）**衡量在给定置信水平下的最大可能损失。

#### VaR定义

**VaR**：在置信水平$\alpha$下，未来$T$期的最大可能损失：
$$P(\text{Loss} > \text{VaR}) = 1 - \alpha$$

**VaR公式**：
$$\text{VaR}_\alpha = -F^{-1}(1 - \alpha)$$

其中$F^{-1}$是损失分布的反函数。

#### VaR计算方法

**计算方法**：

1. **历史模拟法**：基于历史分位数
2. **参数法**：基于分布假设（如正态分布）
3. **蒙特卡洛法**：基于随机模拟

### 2.2 历史模拟法VaR实现

**历史模拟法VaR**的实现和使用。

```sql
-- VaR计算（带错误处理和性能测试）
DO $$
DECLARE
    confidence_level NUMERIC := 0.95;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法计算VaR';
            RETURN;
        END IF;
        RAISE NOTICE '开始计算VaR，置信水平: %', confidence_level;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING 'VaR计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 历史模拟法VaR
WITH ranked_returns AS (
    SELECT
        symbol,
        return_rate,
        PERCENT_RANK() OVER (PARTITION BY symbol ORDER BY return_rate) AS percentile_rank
    FROM returns_data
),
var_calculation AS (
    SELECT
        symbol,
        return_rate AS var_value,
        percentile_rank
    FROM ranked_returns
    WHERE percentile_rank <= 0.05  -- 95%置信水平，取5%分位数
    ORDER BY symbol, return_rate
    LIMIT 1000
)
SELECT
    symbol,
    MIN(var_value) AS var_95,
    ROUND((MIN(var_value) * 100)::numeric, 4) AS var_95_pct
FROM var_calculation
GROUP BY symbol
ORDER BY symbol;

-- 使用PERCENTILE_CONT计算VaR
WITH var_percentiles AS (
    SELECT
        symbol,
        PERCENTILE_CONT(0.05) WITHIN GROUP (ORDER BY return_rate) AS var_95,
        PERCENTILE_CONT(0.01) WITHIN GROUP (ORDER BY return_rate) AS var_99
    FROM returns_data
    GROUP BY symbol
)
SELECT
    symbol,
    ROUND(var_95::numeric, 6) AS var_95,
    ROUND((var_95 * 100)::numeric, 4) AS var_95_pct,
    ROUND(var_99::numeric, 6) AS var_99,
    ROUND((var_99 * 100)::numeric, 4) AS var_99_pct
FROM var_percentiles
ORDER BY symbol;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    symbol,
    PERCENTILE_CONT(0.05) WITHIN GROUP (ORDER BY return_rate) AS var_95
FROM returns_data
GROUP BY symbol;
```

---

### 2.3 参数法VaR

**参数法VaR**：基于正态分布假设的VaR。

```sql
-- 参数法VaR（带错误处理和性能测试）
DO $$
DECLARE
    confidence_level NUMERIC := 0.95;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法计算参数法VaR';
            RETURN;
        END IF;
        RAISE NOTICE '开始计算参数法VaR，置信水平: %', confidence_level;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '参数法VaR计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 参数法VaR：假设收益率服从正态分布
WITH return_stats AS (
    SELECT
        symbol,
        AVG(return_rate) AS mean_return,
        STDDEV(return_rate) AS std_return,
        COUNT(*) AS n_obs
    FROM returns_data
    GROUP BY symbol
),
var_calculation AS (
    SELECT
        symbol,
        mean_return,
        std_return,
        -- VaR = -mean - z_score * std（正态分布）
        -(mean_return + ABS(NORMAL_INVERSE(confidence_level)) * std_return) AS var_95,
        -(mean_return + ABS(NORMAL_INVERSE(0.99)) * std_return) AS var_99
    FROM return_stats
    WHERE std_return IS NOT NULL
)
SELECT
    symbol,
    ROUND(mean_return::numeric, 6) AS mean_return,
    ROUND(std_return::numeric, 6) AS std_return,
    ROUND(var_95::numeric, 6) AS var_95,
    ROUND((var_95 * 100)::numeric, 4) AS var_95_pct,
    ROUND(var_99::numeric, 6) AS var_99,
    ROUND((var_99 * 100)::numeric, 4) AS var_99_pct
FROM var_calculation
ORDER BY symbol;
```

### 2.4 蒙特卡洛VaR

**蒙特卡洛VaR**：基于随机模拟的VaR（概念演示）。

```sql
-- 蒙特卡洛VaR（概念演示，带错误处理）
DO $$
BEGIN
    BEGIN
        RAISE NOTICE '蒙特卡洛VaR需要大量随机模拟，PostgreSQL中实现复杂';
        -- 实际实现需要外部工具或自定义函数
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '蒙特卡洛VaR准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

---

## 3. 风险指标

### 3.1 最大回撤原理

**最大回撤（Maximum Drawdown）**衡量从峰值到谷底的最大跌幅。

#### 最大回撤定义

**最大回撤**：从历史最高点到最低点的最大跌幅：
$$\text{MDD} = \max_{t} \frac{\text{Peak}_t - \text{Price}_t}{\text{Peak}_t}$$

其中$\text{Peak}_t$是到时刻$t$为止的最高价格。

#### 回撤计算方法

**计算方法**：

1. **滚动峰值**：计算滚动最高价
2. **回撤计算**：计算当前价格相对于峰值的回撤
3. **最大回撤**：取所有回撤的最大值

### 3.2 最大回撤实现

**最大回撤**的实现和使用。

```sql
-- 最大回撤计算（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'stock_prices') THEN
            RAISE WARNING '表 stock_prices 不存在，创建示例表';

            CREATE TABLE stock_prices (
                date DATE NOT NULL,
                symbol VARCHAR(10) NOT NULL,
                close_price NUMERIC(10, 2) NOT NULL,
                PRIMARY KEY (date, symbol)
            );

            INSERT INTO stock_prices (date, symbol, close_price) VALUES
                ('2024-01-01', 'AAPL', 150.00), ('2024-01-02', 'AAPL', 155.00),
                ('2024-01-03', 'AAPL', 152.00), ('2024-01-04', 'AAPL', 148.00),
                ('2024-01-05', 'AAPL', 151.00);

            RAISE NOTICE '表 stock_prices 创建成功';
        END IF;
        RAISE NOTICE '开始计算最大回撤';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '最大回撤计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 计算最大回撤
WITH price_series AS (
    SELECT
        date,
        symbol,
        close_price,
        MAX(close_price) OVER (PARTITION BY symbol ORDER BY date ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS running_max,
        MIN(close_price) OVER (PARTITION BY symbol ORDER BY date ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING) AS running_min_ahead
    FROM stock_prices
),
drawdowns AS (
    SELECT
        date,
        symbol,
        close_price,
        running_max,
        (close_price - running_max) / NULLIF(running_max, 0) AS drawdown
    FROM price_series
)
SELECT
    symbol,
    MIN(drawdown) AS max_drawdown,
    ROUND((MIN(drawdown) * 100)::numeric, 2) AS max_drawdown_pct,
    MIN(date) FILTER (WHERE drawdown = (SELECT MIN(drawdown) FROM drawdowns d2 WHERE d2.symbol = drawdowns.symbol)) AS drawdown_start_date
FROM drawdowns
GROUP BY symbol
ORDER BY symbol;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    symbol,
    MAX(close_price) OVER (PARTITION BY symbol ORDER BY date) AS running_max
FROM stock_prices
ORDER BY symbol, date;
```

### 3.3 夏普比率原理

**夏普比率（Sharpe Ratio）**衡量风险调整后的收益。

#### 夏普比率定义

**夏普比率**：超额收益与波动率的比值：
$$\text{Sharpe} = \frac{R - R_f}{\sigma}$$

其中：

- $R$：资产收益率
- $R_f$：无风险收益率
- $\sigma$：收益率标准差

#### 夏普比率公式

**年化夏普比率**：
$$\text{Sharpe}_{annual} = \frac{R_{annual} - R_{f,annual}}{\sigma_{annual}} = \frac{(R_{daily} - R_{f,daily}) \times 252}{\sigma_{daily} \times \sqrt{252}}$$

### 3.4 夏普比率实现

**夏普比率**的实现和使用。

```sql
-- 夏普比率计算（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法计算夏普比率';
            RETURN;
        END IF;
        RAISE NOTICE '开始计算夏普比率';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '夏普比率计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 计算夏普比率
WITH return_stats AS (
    SELECT
        symbol,
        COUNT(*) AS trading_days,
        AVG(return_rate) AS mean_return,
        STDDEV(return_rate) AS std_return,
        0.03 / 252.0 AS risk_free_rate_daily  -- 假设年化无风险利率3%
    FROM returns_data
    GROUP BY symbol
)
SELECT
    symbol,
    trading_days,
    ROUND(mean_return::numeric, 6) AS mean_daily_return,
    ROUND(std_return::numeric, 6) AS daily_volatility,
    ROUND(((mean_return - risk_free_rate_daily) / NULLIF(std_return, 0))::numeric, 4) AS daily_sharpe_ratio,
    ROUND(((mean_return - risk_free_rate_daily) / NULLIF(std_return, 0) * SQRT(252.0))::numeric, 4) AS annualized_sharpe_ratio
FROM return_stats
ORDER BY annualized_sharpe_ratio DESC;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    symbol,
    AVG(return_rate) AS mean_return,
    STDDEV(return_rate) AS std_return
FROM returns_data
GROUP BY symbol;
```

---

### 3.5 其他风险指标

**其他风险指标**：索提诺比率、卡玛比率等。

```sql
-- 索提诺比率（Sortino Ratio）：只考虑下行风险（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法计算索提诺比率';
            RETURN;
        END IF;
        RAISE NOTICE '开始计算索提诺比率';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '索提诺比率计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 索提诺比率：只考虑下行波动率
WITH downside_returns AS (
    SELECT
        symbol,
        return_rate,
        CASE WHEN return_rate < 0 THEN return_rate ELSE 0 END AS downside_return
    FROM returns_data
),
return_stats AS (
    SELECT
        symbol,
        AVG(return_rate) AS mean_return,
        STDDEV(downside_return) AS downside_std,
        0.03 / 252.0 AS risk_free_rate_daily
    FROM downside_returns
    GROUP BY symbol
)
SELECT
    symbol,
    ROUND(mean_return::numeric, 6) AS mean_return,
    ROUND(downside_std::numeric, 6) AS downside_volatility,
    CASE
        WHEN downside_std = 0 THEN NULL
        ELSE ROUND(((mean_return - risk_free_rate_daily) / downside_std * SQRT(252.0))::numeric, 4)
    END AS sortino_ratio
FROM return_stats
ORDER BY sortino_ratio DESC NULLS LAST;
```

---

## 4. 风险评估优化策略

### 4.1 索引优化

**索引优化**：为风险评估查询创建合适的索引。

```sql
-- 索引优化（带错误处理）
DO $$
BEGIN
    BEGIN
        -- 收益率表索引
        CREATE INDEX IF NOT EXISTS idx_returns_data_date_symbol
        ON returns_data(date, symbol);

        CREATE INDEX IF NOT EXISTS idx_returns_data_symbol_date
        ON returns_data(symbol, date);

        RAISE NOTICE '风险评估索引创建成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '索引创建失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 4.2 窗口函数优化

**窗口函数优化**：使用窗口函数优化滚动计算。

```sql
-- 窗口函数优化：使用物化视图缓存风险指标
CREATE MATERIALIZED VIEW IF NOT EXISTS risk_metrics_cache AS
SELECT
    date,
    symbol,
    return_rate,
    STDDEV(return_rate) OVER (
        PARTITION BY symbol
        ORDER BY date
        ROWS BETWEEN 29 PRECEDING AND CURRENT ROW
    ) AS rolling_volatility_30d
FROM returns_data;

CREATE INDEX IF NOT EXISTS idx_risk_metrics_cache_date_symbol
ON risk_metrics_cache(date, symbol);

-- 刷新物化视图
REFRESH MATERIALIZED VIEW CONCURRENTLY risk_metrics_cache;
```

### 4.3 批量计算优化

**批量计算优化**：使用批量操作提高性能。

```sql
-- 批量计算优化（带错误处理）
DO $$
BEGIN
    BEGIN
        SET work_mem = '512MB';
        SET max_parallel_workers_per_gather = 4;
        RAISE NOTICE '批量计算优化配置已设置';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '批量计算优化失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

---

## 5. 实际应用案例

### 5.1 投资组合风险评估

**投资组合风险评估**综合评估多资产组合的风险。

```sql
-- 投资组合风险评估示例（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'portfolio_weights') THEN
            RAISE WARNING '表 portfolio_weights 不存在，创建示例表';

            CREATE TABLE portfolio_weights (
                symbol VARCHAR(10) PRIMARY KEY,
                weight NUMERIC(5, 4) NOT NULL CHECK (weight >= 0 AND weight <= 1)
            );

            INSERT INTO portfolio_weights (symbol, weight) VALUES
                ('AAPL', 0.4),
                ('GOOGL', 0.6);

            RAISE NOTICE '表 portfolio_weights 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，请先创建';
            RETURN;
        END IF;

        RAISE NOTICE '开始执行投资组合风险评估';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '投资组合风险评估准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 计算投资组合风险指标
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH portfolio_returns AS (
    SELECT
        rd.date,
        SUM(rd.return_rate * pw.weight) AS portfolio_return
    FROM returns_data rd
    JOIN portfolio_weights pw ON rd.symbol = pw.symbol
    GROUP BY rd.date
),
portfolio_stats AS (
    SELECT
        COUNT(*) AS trading_days,
        AVG(portfolio_return) AS mean_return,
        STDDEV(portfolio_return) AS std_return,
        PERCENTILE_CONT(0.05) WITHIN GROUP (ORDER BY portfolio_return) AS var_95,
        PERCENTILE_CONT(0.01) WITHIN GROUP (ORDER BY portfolio_return) AS var_99,
        0.03 / 252.0 AS risk_free_rate_daily
    FROM portfolio_returns
)
SELECT
    trading_days,
    ROUND(mean_return::numeric, 6) AS portfolio_mean_return,
    ROUND(std_return::numeric, 6) AS portfolio_volatility,
    ROUND((std_return * SQRT(252.0))::numeric, 6) AS annualized_volatility,
    ROUND(var_95::numeric, 6) AS var_95,
    ROUND(var_99::numeric, 6) AS var_99,
    ROUND(((mean_return - risk_free_rate_daily) / NULLIF(std_return, 0) * SQRT(252.0))::numeric, 4) AS sharpe_ratio
FROM portfolio_stats;
```

### 5.2 风险限额监控

**风险限额监控**：监控投资组合是否超过风险限额。

```sql
-- 风险限额监控（带错误处理和性能测试）
DO $$
DECLARE
    max_var_limit NUMERIC := -0.05;  -- 最大VaR限制：-5%
    max_volatility_limit NUMERIC := 0.20;  -- 最大波动率限制：20%
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法执行风险限额监控';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行风险限额监控，VaR限制: %, 波动率限制: %', max_var_limit, max_volatility_limit;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '风险限额监控准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 风险限额监控：检查是否超过风险限额
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH portfolio_returns AS (
    SELECT
        rd.date,
        SUM(rd.return_rate * pw.weight) AS portfolio_return
    FROM returns_data rd
    JOIN portfolio_weights pw ON rd.symbol = pw.symbol
    GROUP BY rd.date
),
risk_metrics AS (
    SELECT
        STDDEV(portfolio_return) * SQRT(252.0) AS annualized_volatility,
        PERCENTILE_CONT(0.05) WITHIN GROUP (ORDER BY portfolio_return) AS var_95
    FROM portfolio_returns
)
SELECT
    annualized_volatility,
    var_95,
    CASE
        WHEN var_95 < max_var_limit THEN 'VaR Limit Exceeded'
        WHEN annualized_volatility > max_volatility_limit THEN 'Volatility Limit Exceeded'
        ELSE 'Within Limits'
    END AS risk_status,
    CASE
        WHEN var_95 < max_var_limit THEN ABS(var_95 - max_var_limit)
        WHEN annualized_volatility > max_volatility_limit THEN annualized_volatility - max_volatility_limit
        ELSE 0
    END AS excess_risk
FROM risk_metrics;
```

### 5.3 风险归因分析

**风险归因分析**：分析投资组合风险的来源。

```sql
-- 风险归因分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法执行风险归因分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行风险归因分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '风险归因分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 风险归因分析：分析各资产对组合风险的贡献
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH asset_stats AS (
    SELECT
        symbol,
        AVG(return_rate) AS mean_return,
        STDDEV(return_rate) AS std_return,
        COUNT(*) AS n_obs
    FROM returns_data
    GROUP BY symbol
),
covariance_matrix AS (
    SELECT
        a1.symbol AS asset1,
        a2.symbol AS asset2,
        AVG((a1.return_rate - s1.mean_return) * (a2.return_rate - s2.mean_return)) AS covariance
    FROM returns_data a1
    JOIN returns_data a2 ON a1.date = a2.date
    JOIN asset_stats s1 ON a1.symbol = s1.symbol
    JOIN asset_stats s2 ON a2.symbol = s2.symbol
    GROUP BY a1.symbol, a2.symbol
),
portfolio_risk_contribution AS (
    SELECT
        pw.symbol,
        pw.weight,
        as_stats.std_return,
        -- 风险贡献（简化版）
        pw.weight * as_stats.std_return AS risk_contribution,
        pw.weight * as_stats.std_return / SUM(pw.weight * as_stats.std_return) OVER () AS risk_contribution_pct
    FROM portfolio_weights pw
    JOIN asset_stats as_stats ON pw.symbol = as_stats.symbol
)
SELECT
    symbol,
    weight,
    ROUND(std_return::numeric, 6) AS asset_volatility,
    ROUND(risk_contribution::numeric, 6) AS risk_contribution,
    ROUND((risk_contribution_pct * 100)::numeric, 2) AS risk_contribution_pct
FROM portfolio_risk_contribution
ORDER BY risk_contribution DESC;
```

### 5.4 风险预警系统

**风险预警系统**：实时监控风险指标并发出预警。

```sql
-- 风险预警系统（带错误处理和性能测试）
DO $$
DECLARE
    var_warning_threshold NUMERIC := -0.03;  -- VaR预警阈值：-3%
    volatility_warning_threshold NUMERIC := 0.15;  -- 波动率预警阈值：15%
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'returns_data') THEN
            RAISE WARNING '表 returns_data 不存在，无法执行风险预警';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行风险预警，VaR预警阈值: %, 波动率预警阈值: %', var_warning_threshold, volatility_warning_threshold;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '风险预警准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 风险预警系统：实时监控风险指标
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH recent_returns AS (
    SELECT
        date,
        symbol,
        return_rate
    FROM returns_data
    WHERE date >= CURRENT_DATE - INTERVAL '30 days'
),
portfolio_returns AS (
    SELECT
        rr.date,
        SUM(rr.return_rate * pw.weight) AS portfolio_return
    FROM recent_returns rr
    JOIN portfolio_weights pw ON rr.symbol = pw.symbol
    GROUP BY rr.date
),
current_risk_metrics AS (
    SELECT
        MAX(date) AS latest_date,
        STDDEV(portfolio_return) * SQRT(252.0) AS current_volatility,
        PERCENTILE_CONT(0.05) WITHIN GROUP (ORDER BY portfolio_return) AS current_var_95
    FROM portfolio_returns
)
SELECT
    latest_date,
    current_volatility,
    current_var_95,
    CASE
        WHEN current_var_95 < var_warning_threshold THEN 'HIGH RISK: VaR Exceeded'
        WHEN current_volatility > volatility_warning_threshold THEN 'WARNING: High Volatility'
        ELSE 'NORMAL'
    END AS risk_alert,
    CASE
        WHEN current_var_95 < var_warning_threshold THEN ABS(current_var_95 - var_warning_threshold)
        WHEN current_volatility > volatility_warning_threshold THEN current_volatility - volatility_warning_threshold
        ELSE 0
    END AS risk_excess
FROM current_risk_metrics;
```

---

## 6. 算法性能对比与优化

### 6.1 风险度量方法对比

| 方法 | 数学定义 | 时间复杂度 | 空间复杂度 | 适用场景 | 优点 | 缺点 |
|------|---------|-----------|-----------|---------|------|------|
| **波动率** | $\sigma = \sqrt{\text{Var}(r)}$ | $O(n)$ | $O(1)$ | 市场风险 | 简单、直观 | 假设正态分布 |
| **VaR（历史模拟）** | 分位数 | $O(n \log n)$ | $O(n)$ | 极端风险 | 无分布假设 | 需要大量历史数据 |
| **VaR（参数法）** | 基于分布 | $O(n)$ | $O(1)$ | 快速计算 | 快速 | 需要分布假设 |
| **最大回撤** | $\max(\text{Peak} - \text{Trough})$ | $O(n)$ | $O(1)$ | 下行风险 | 直观 | 不预测未来 |
| **夏普比率** | $\frac{R - R_f}{\sigma}$ | $O(n)$ | $O(1)$ | 收益风险比 | 综合指标 | 假设正态分布 |

### 6.2 性能优化建议

1. **索引优化**：
   - 在(date, symbol)上创建复合索引
   - 为常用查询创建索引

2. **窗口函数优化**：
   - 使用窗口函数优化滚动计算
   - 使用物化视图缓存结果

3. **批量处理**：
   - 批量计算多个资产的风险指标
   - 使用并行查询

### 6.3 常见问题与解决方案

**问题1**：VaR计算性能慢

- **解决方案**：使用参数法、创建索引、并行查询

**问题2**：波动率估计不准确

- **解决方案**：选择合适的窗口、处理异常值、使用滚动窗口

**问题3**：风险指标不一致

- **解决方案**：统一计算方法、标准化参数、定期校准

**问题4**：实时监控性能差

- **解决方案**：使用物化视图、增量更新、优化查询

---

## 7. 最佳实践

### 7.1 风险度量选择

1. **市场风险**：使用波动率
2. **极端风险**：使用VaR
3. **下行风险**：使用最大回撤
4. **收益风险比**：使用夏普比率

### 7.2 参数设置

1. **时间窗口**：根据业务需求选择（30日、60日、252日）
2. **置信水平**：常用95%或99%
3. **年化倍数**：252个交易日

### 7.3 数据质量

1. **数据完整性**：确保数据完整
2. **异常值处理**：识别和处理异常值
3. **数据更新**：定期更新数据

### 7.4 SQL实现注意事项

1. **精度控制**：使用NUMERIC类型
2. **NULL处理**：正确处理NULL值
3. **性能优化**：使用索引和物化视图
4. **错误处理**：添加错误处理逻辑

---

## 📚 参考资源

### 学术文献

- 《风险管理与金融机构》（Risk Management and Financial Institutions）- 风险管理理论
- 《金融风险管理》（Financial Risk Management）- VaR计算方法

### PostgreSQL官方文档

- [窗口函数](https://www.postgresql.org/docs/current/tutorial-window.html)
- [聚合函数](https://www.postgresql.org/docs/current/functions-aggregate.html)
- [统计函数](https://www.postgresql.org/docs/current/functions-aggregate.html#FUNCTIONS-AGGREGATE-STATISTICS-TABLE)

### 在线资源

- PostgreSQL金融风险计算指南
- VaR计算方法详解
- 风险管理最佳实践

### 相关算法

- [收益率计算](./收益率计算.md) - 收益率计算
- [投资组合优化](./投资组合优化.md) - 投资组合优化
- [金融时间序列分析](./金融时间序列分析.md) - 时间序列分析

---

**最后更新**: 2025年1月
**文档状态**: ✅ 已完成
