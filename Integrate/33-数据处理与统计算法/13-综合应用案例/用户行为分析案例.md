# PostgreSQL 用户行为分析综合案例

> **创建日期**: 2025年1月
> **技术栈**: PostgreSQL 17+/18+ | 用户行为 | 数据分析
> **难度级别**: ⭐⭐⭐⭐ (高级)

---

## 📋 目录

- [PostgreSQL 用户行为分析综合案例](#postgresql-用户行为分析综合案例)
  - [📋 目录](#-目录)
  - [案例概述](#案例概述)
  - [理论基础](#理论基础)
    - [用户行为分析问题定义](#用户行为分析问题定义)
    - [核心分析维度](#核心分析维度)
    - [分析方法分类](#分析方法分类)
    - [技术栈](#技术栈)
  - [1. 数据准备](#1-数据准备)
  - [2. 用户活跃度分析](#2-用户活跃度分析)
  - [3. 用户路径分析](#3-用户路径分析)
  - [4. 用户留存分析](#4-用户留存分析)
  - [5. 用户分群分析](#5-用户分群分析)
  - [6. 转化漏斗分析](#6-转化漏斗分析)
  - [7. 用户生命周期分析](#7-用户生命周期分析)
  - [8. 用户价值分析](#8-用户价值分析)
  - [📊 PostgreSQL 18并行用户行为分析](#-postgresql-18并行用户行为分析)
    - [并行用户行为分析概述](#并行用户行为分析概述)
      - [并行用户行为分析配置](#并行用户行为分析配置)
      - [并行用户行为分析实现](#并行用户行为分析实现)
  - [📊 性能优化建议](#-性能优化建议)
    - [索引优化](#索引优化)
    - [分区表](#分区表)
    - [物化视图](#物化视图)
  - [🎯 最佳实践](#-最佳实践)
    - [数据质量](#数据质量)
    - [分析策略](#分析策略)
    - [隐私保护](#隐私保护)
    - [性能优化](#性能优化)
    - [SQL实现注意事项](#sql实现注意事项)
  - [📈 分析方法对比](#-分析方法对比)
  - [🔍 常见问题与解决方案](#-常见问题与解决方案)
    - [问题1：用户行为数据量大，查询慢](#问题1用户行为数据量大查询慢)
    - [问题2：留存率计算不准确](#问题2留存率计算不准确)
    - [问题3：用户分群效果不好](#问题3用户分群效果不好)

---

## 案例概述

本案例综合运用窗口函数、聚合算法、时间序列分析等技术，对用户行为数据进行深入分析。

## 理论基础

### 用户行为分析问题定义

用户行为分析是通过收集、处理和分析用户在应用系统中的行为数据，以了解用户行为模式、优化用户体验、提升转化率和增加用户价值的过程。

### 核心分析维度

1. **用户活跃度分析**：
   - 日活跃用户（DAU）
   - 月活跃用户（MAU）
   - 用户活跃度趋势
   - 用户活跃度分布

2. **用户路径分析**：
   - 用户访问路径
   - 页面流转分析
   - 转化漏斗分析
   - 关键路径识别

3. **用户留存分析**：
   - 留存率计算
   - 留存率趋势
   - 同期群分析
   - 流失用户分析

4. **用户分群分析**：
   - RFM模型分群
   - 行为特征分群
   - 价值分群
   - 生命周期分群

### 分析方法分类

1. **描述性分析**：统计汇总、趋势分析、分布分析
2. **诊断性分析**：路径分析、转化分析、流失分析
3. **预测性分析**：留存预测、流失预测、价值预测
4. **规范性分析**：个性化推荐、精准营销、体验优化

### 技术栈

- **窗口函数**：ROW_NUMBER、RANK、LAG、LEAD、FIRST_VALUE、LAST_VALUE
- **聚合算法**：COUNT、SUM、AVG、GROUP BY、FILTER
- **时间序列分析**：DATE_TRUNC、滑动窗口、移动平均
- **关联分析**：JOIN、CTE、子查询
- **统计分析**：百分位数、标准差、相关性

---

## 1. 数据准备

```sql
-- 创建用户行为数据表结构（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 已存在，先删除';
            DROP TABLE user_events CASCADE;
        END IF;

        CREATE TABLE user_events (
            event_id SERIAL PRIMARY KEY,
            user_id INTEGER NOT NULL,
            event_type VARCHAR(50) NOT NULL,
            event_timestamp TIMESTAMP NOT NULL,
            page_url VARCHAR(200),
            session_id VARCHAR(100),
            device_type VARCHAR(20),
            duration_seconds INTEGER
        );

        -- 插入示例用户行为数据
        INSERT INTO user_events (user_id, event_type, event_timestamp, page_url, session_id, device_type, duration_seconds) VALUES
            (1, 'page_view', '2024-01-01 10:00:00', '/home', 'session_001', 'desktop', 30),
            (1, 'page_view', '2024-01-01 10:00:30', '/products', 'session_001', 'desktop', 45),
            (1, 'click', '2024-01-01 10:01:15', '/products/item1', 'session_001', 'desktop', NULL),
            (1, 'page_view', '2024-01-01 10:01:20', '/products/item1', 'session_001', 'desktop', 120),
            (1, 'purchase', '2024-01-01 10:03:20', '/checkout', 'session_001', 'desktop', NULL),
            (2, 'page_view', '2024-01-01 11:00:00', '/home', 'session_002', 'mobile', 20),
            (2, 'page_view', '2024-01-01 11:00:20', '/products', 'session_002', 'mobile', 30),
            (2, 'page_view', '2024-01-02 10:00:00', '/home', 'session_003', 'mobile', 25),
            (3, 'page_view', '2024-01-01 12:00:00', '/home', 'session_004', 'desktop', 40),
            (3, 'page_view', '2024-01-01 12:00:40', '/products', 'session_004', 'desktop', 60),
            (3, 'click', '2024-01-01 12:01:40', '/products/item2', 'session_004', 'desktop', NULL),
            (4, 'page_view', '2024-01-01 13:00:00', '/home', 'session_005', 'mobile', 15),
            (4, 'page_view', '2024-01-01 13:00:15', '/products', 'session_005', 'mobile', 20),
            (5, 'page_view', '2024-01-01 14:00:00', '/home', 'session_006', 'desktop', 50),
            (5, 'purchase', '2024-01-01 14:00:50', '/checkout', 'session_006', 'desktop', NULL);

        RAISE NOTICE '用户行为数据表创建成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE EXCEPTION '创建表失败: %', SQLERRM;
    END;
END $$;
```

---

## 2. 用户活跃度分析

```sql
-- 用户活跃度分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法进行用户活跃度分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始进行用户活跃度分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '用户活跃度分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 用户日活跃度统计
SELECT
    DATE_TRUNC('day', event_timestamp) AS date,
    COUNT(DISTINCT user_id) AS daily_active_users,
    COUNT(DISTINCT session_id) AS daily_sessions,
    COUNT(*) AS total_events,
    AVG(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS avg_session_duration
FROM user_events
GROUP BY DATE_TRUNC('day', event_timestamp)
ORDER BY date;

-- 用户活跃度排行
SELECT
    user_id,
    COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS active_days,
    COUNT(DISTINCT session_id) AS total_sessions,
    COUNT(*) AS total_events,
    SUM(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS total_duration,
    MAX(event_timestamp) AS last_active_time,
    CURRENT_TIMESTAMP - MAX(event_timestamp) AS days_since_last_active
FROM user_events
GROUP BY user_id
ORDER BY active_days DESC, total_events DESC;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    DATE_TRUNC('day', event_timestamp) AS date,
    COUNT(DISTINCT user_id) AS daily_active_users
FROM user_events
GROUP BY DATE_TRUNC('day', event_timestamp);
```

---

## 3. 用户路径分析

```sql
-- 用户路径分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法进行用户路径分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始进行用户路径分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '用户路径分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 用户会话路径分析
WITH session_paths AS (
    SELECT
        session_id,
        user_id,
        page_url,
        event_type,
        event_timestamp,
        ROW_NUMBER() OVER (PARTITION BY session_id ORDER BY event_timestamp) AS step_number,
        LAG(page_url) OVER (PARTITION BY session_id ORDER BY event_timestamp) AS previous_page
    FROM user_events
    WHERE event_type = 'page_view'
)
SELECT
    session_id,
    user_id,
    STRING_AGG(page_url, ' -> ' ORDER BY step_number) AS user_path,
    COUNT(*) AS path_length,
    MAX(CASE WHEN page_url = '/checkout' THEN 1 ELSE 0 END) AS reached_checkout
FROM session_paths
GROUP BY session_id, user_id
ORDER BY path_length DESC;

-- 页面流转分析
WITH page_transitions AS (
    SELECT
        session_id,
        page_url AS from_page,
        LEAD(page_url) OVER (PARTITION BY session_id ORDER BY event_timestamp) AS to_page
    FROM user_events
    WHERE event_type = 'page_view'
)
SELECT
    from_page,
    to_page,
    COUNT(*) AS transition_count,
    ROUND((COUNT(*)::numeric / SUM(COUNT(*)) OVER (PARTITION BY from_page) * 100)::numeric, 2) AS transition_percentage
FROM page_transitions
WHERE to_page IS NOT NULL
GROUP BY from_page, to_page
ORDER BY transition_count DESC;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    session_id,
    STRING_AGG(page_url, ' -> ' ORDER BY event_timestamp) AS user_path
FROM user_events
WHERE event_type = 'page_view'
GROUP BY session_id;
```

---

## 4. 用户留存分析

```sql
-- 用户留存分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法进行用户留存分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始进行用户留存分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '用户留存分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 用户首次活跃日期
WITH user_first_active AS (
    SELECT
        user_id,
        MIN(DATE_TRUNC('day', event_timestamp)) AS first_active_date
    FROM user_events
    GROUP BY user_id
),
daily_active_users AS (
    SELECT
        DATE_TRUNC('day', event_timestamp) AS active_date,
        user_id
    FROM user_events
    GROUP BY DATE_TRUNC('day', event_timestamp), user_id
),
retention_analysis AS (
    SELECT
        ufa.first_active_date AS cohort_date,
        dau.active_date,
        dau.active_date - ufa.first_active_date AS days_since_first,
        COUNT(DISTINCT dau.user_id) AS retained_users
    FROM user_first_active ufa
    JOIN daily_active_users dau ON ufa.user_id = dau.user_id
    GROUP BY ufa.first_active_date, dau.active_date
)
SELECT
    cohort_date,
    active_date,
    days_since_first,
    retained_users,
    (SELECT COUNT(DISTINCT user_id) FROM user_first_active WHERE first_active_date = cohort_date) AS cohort_size,
    ROUND((retained_users::numeric / NULLIF((SELECT COUNT(DISTINCT user_id) FROM user_first_active WHERE first_active_date = cohort_date), 0) * 100)::numeric, 2) AS retention_rate
FROM retention_analysis
ORDER BY cohort_date, days_since_first;

-- 简化版留存率计算
WITH user_first_active AS (
    SELECT
        user_id,
        MIN(DATE_TRUNC('day', event_timestamp)) AS first_active_date
    FROM user_events
    GROUP BY user_id
),
retention_by_day AS (
    SELECT
        ufa.first_active_date,
        COUNT(DISTINCT CASE WHEN DATE_TRUNC('day', ue.event_timestamp) = ufa.first_active_date THEN ue.user_id END) AS day_0_users,
        COUNT(DISTINCT CASE WHEN DATE_TRUNC('day', ue.event_timestamp) = ufa.first_active_date + INTERVAL '1 day' THEN ue.user_id END) AS day_1_users,
        COUNT(DISTINCT CASE WHEN DATE_TRUNC('day', ue.event_timestamp) = ufa.first_active_date + INTERVAL '7 days' THEN ue.user_id END) AS day_7_users,
        COUNT(DISTINCT CASE WHEN DATE_TRUNC('day', ue.event_timestamp) = ufa.first_active_date + INTERVAL '30 days' THEN ue.user_id END) AS day_30_users
    FROM user_first_active ufa
    LEFT JOIN user_events ue ON ufa.user_id = ue.user_id
    GROUP BY ufa.first_active_date
)
SELECT
    first_active_date AS cohort_date,
    day_0_users,
    day_1_users,
    ROUND((day_1_users::numeric / NULLIF(day_0_users, 0) * 100)::numeric, 2) AS day_1_retention_rate,
    day_7_users,
    ROUND((day_7_users::numeric / NULLIF(day_0_users, 0) * 100)::numeric, 2) AS day_7_retention_rate,
    day_30_users,
    ROUND((day_30_users::numeric / NULLIF(day_0_users, 0) * 100)::numeric, 2) AS day_30_retention_rate
FROM retention_by_day
ORDER BY first_active_date;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    user_id,
    MIN(DATE_TRUNC('day', event_timestamp)) AS first_active_date
FROM user_events
GROUP BY user_id;
```

---

## 5. 用户分群分析

```sql
-- 用户分群分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法进行用户分群分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始进行用户分群分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '用户分群分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 基于行为的用户分群（RFM模型简化版）
WITH user_metrics AS (
    SELECT
        user_id,
        COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS frequency,
        MAX(event_timestamp) AS last_active_time,
        CURRENT_TIMESTAMP - MAX(event_timestamp) AS recency_days,
        COUNT(DISTINCT session_id) AS session_count,
        SUM(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS total_duration,
        COUNT(*) FILTER (WHERE event_type = 'purchase') AS purchase_count
    FROM user_events
    GROUP BY user_id
),
user_segments AS (
    SELECT
        user_id,
        frequency,
        recency_days,
        session_count,
        total_duration,
        purchase_count,
        CASE
            WHEN recency_days <= INTERVAL '7 days' AND frequency >= 5 THEN 'Champion'
            WHEN recency_days <= INTERVAL '30 days' AND frequency >= 3 THEN 'Loyal'
            WHEN recency_days <= INTERVAL '7 days' AND frequency < 3 THEN 'New'
            WHEN recency_days > INTERVAL '30 days' AND frequency >= 3 THEN 'At Risk'
            ELSE 'Inactive'
        END AS user_segment
    FROM user_metrics
)
SELECT
    user_segment,
    COUNT(*) AS user_count,
    ROUND(AVG(frequency)::numeric, 2) AS avg_frequency,
    ROUND(AVG(EXTRACT(EPOCH FROM recency_days))::numeric, 2) AS avg_recency_days,
    ROUND(AVG(session_count)::numeric, 2) AS avg_sessions,
    ROUND(AVG(total_duration)::numeric, 2) AS avg_duration,
    SUM(purchase_count) AS total_purchases
FROM user_segments
GROUP BY user_segment
ORDER BY user_count DESC;

-- 设备类型用户分群
SELECT
    device_type,
    COUNT(DISTINCT user_id) AS user_count,
    COUNT(DISTINCT session_id) AS session_count,
    AVG(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS avg_session_duration,
    COUNT(*) FILTER (WHERE event_type = 'purchase') AS purchase_count,
    ROUND((COUNT(*) FILTER (WHERE event_type = 'purchase')::numeric / COUNT(*) * 100)::numeric, 2) AS conversion_rate
FROM user_events
WHERE device_type IS NOT NULL
GROUP BY device_type
ORDER BY user_count DESC;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    user_id,
    COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS frequency,
    MAX(event_timestamp) AS last_active_time
FROM user_events
GROUP BY user_id;
```

---

## 6. 转化漏斗分析

```sql
-- 转化漏斗分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法进行转化漏斗分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始进行转化漏斗分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '转化漏斗分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 转化漏斗统计
WITH funnel_steps AS (
    SELECT
        session_id,
        MAX(CASE WHEN page_url = '/home' THEN 1 ELSE 0 END) AS step1_home,
        MAX(CASE WHEN page_url = '/products' THEN 1 ELSE 0 END) AS step2_products,
        MAX(CASE WHEN page_url LIKE '/products/%' THEN 1 ELSE 0 END) AS step3_product_detail,
        MAX(CASE WHEN event_type = 'click' AND page_url LIKE '/products/%' THEN 1 ELSE 0 END) AS step4_click,
        MAX(CASE WHEN page_url = '/checkout' THEN 1 ELSE 0 END) AS step5_checkout,
        MAX(CASE WHEN event_type = 'purchase' THEN 1 ELSE 0 END) AS step6_purchase
    FROM user_events
    GROUP BY session_id
)
SELECT
    'Step 1: Home' AS funnel_step,
    COUNT(*) FILTER (WHERE step1_home = 1) AS user_count,
    100.0 AS conversion_rate
FROM funnel_steps
UNION ALL
SELECT
    'Step 2: Products',
    COUNT(*) FILTER (WHERE step2_products = 1),
    ROUND((COUNT(*) FILTER (WHERE step2_products = 1)::numeric / NULLIF(COUNT(*) FILTER (WHERE step1_home = 1), 0) * 100)::numeric, 2)
FROM funnel_steps
UNION ALL
SELECT
    'Step 3: Product Detail',
    COUNT(*) FILTER (WHERE step3_product_detail = 1),
    ROUND((COUNT(*) FILTER (WHERE step3_product_detail = 1)::numeric / NULLIF(COUNT(*) FILTER (WHERE step1_home = 1), 0) * 100)::numeric, 2)
FROM funnel_steps
UNION ALL
SELECT
    'Step 4: Click',
    COUNT(*) FILTER (WHERE step4_click = 1),
    ROUND((COUNT(*) FILTER (WHERE step4_click = 1)::numeric / NULLIF(COUNT(*) FILTER (WHERE step1_home = 1), 0) * 100)::numeric, 2)
FROM funnel_steps
UNION ALL
SELECT
    'Step 5: Checkout',
    COUNT(*) FILTER (WHERE step5_checkout = 1),
    ROUND((COUNT(*) FILTER (WHERE step5_checkout = 1)::numeric / NULLIF(COUNT(*) FILTER (WHERE step1_home = 1), 0) * 100)::numeric, 2)
FROM funnel_steps
UNION ALL
SELECT
    'Step 6: Purchase',
    COUNT(*) FILTER (WHERE step6_purchase = 1),
    ROUND((COUNT(*) FILTER (WHERE step6_purchase = 1)::numeric / NULLIF(COUNT(*) FILTER (WHERE step1_home = 1), 0) * 100)::numeric, 2)
FROM funnel_steps
ORDER BY funnel_step;
```

---

## 7. 用户生命周期分析

```sql
-- 用户生命周期分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法进行用户生命周期分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始进行用户生命周期分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '用户生命周期分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 用户生命周期阶段识别
WITH user_lifecycle AS (
    SELECT
        user_id,
        MIN(event_timestamp) AS first_event_date,
        MAX(event_timestamp) AS last_event_date,
        COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS active_days,
        COUNT(DISTINCT session_id) AS total_sessions,
        COUNT(*) FILTER (WHERE event_type = 'purchase') AS purchase_count,
        CURRENT_DATE - MAX(DATE_TRUNC('day', event_timestamp))::date AS days_since_last_active
    FROM user_events
    GROUP BY user_id
)
SELECT
    user_id,
    first_event_date,
    last_event_date,
    active_days,
    total_sessions,
    purchase_count,
    days_since_last_active,
    CASE
        WHEN days_since_last_active <= 7 AND active_days >= 5 THEN 'Active'
        WHEN days_since_last_active <= 30 AND active_days >= 3 THEN 'Engaged'
        WHEN days_since_last_active <= 7 AND active_days < 3 THEN 'New'
        WHEN days_since_last_active <= 30 AND active_days < 3 THEN 'At Risk'
        WHEN days_since_last_active > 30 AND active_days >= 3 THEN 'Churned'
        ELSE 'Inactive'
    END AS lifecycle_stage
FROM user_lifecycle
ORDER BY days_since_last_active DESC;

-- 生命周期阶段分布
WITH user_lifecycle AS (
    SELECT
        user_id,
        MIN(event_timestamp) AS first_event_date,
        MAX(event_timestamp) AS last_event_date,
        COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS active_days,
        CURRENT_DATE - MAX(DATE_TRUNC('day', event_timestamp))::date AS days_since_last_active
    FROM user_events
    GROUP BY user_id
)
SELECT
    CASE
        WHEN days_since_last_active <= 7 AND active_days >= 5 THEN 'Active'
        WHEN days_since_last_active <= 30 AND active_days >= 3 THEN 'Engaged'
        WHEN days_since_last_active <= 7 AND active_days < 3 THEN 'New'
        WHEN days_since_last_active <= 30 AND active_days < 3 THEN 'At Risk'
        WHEN days_since_last_active > 30 AND active_days >= 3 THEN 'Churned'
        ELSE 'Inactive'
    END AS lifecycle_stage,
    COUNT(*) AS user_count,
    ROUND(AVG(active_days)::numeric, 2) AS avg_active_days,
    ROUND(AVG(days_since_last_active)::numeric, 2) AS avg_days_since_last_active
FROM user_lifecycle
GROUP BY lifecycle_stage
ORDER BY user_count DESC;
```

---

## 8. 用户价值分析

```sql
-- 用户价值分析（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法进行用户价值分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始进行用户价值分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '用户价值分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 用户价值评分（基于行为）
WITH user_value_metrics AS (
    SELECT
        user_id,
        COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS active_days,
        COUNT(DISTINCT session_id) AS session_count,
        SUM(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS total_duration,
        COUNT(*) FILTER (WHERE event_type = 'purchase') AS purchase_count,
        COUNT(*) FILTER (WHERE event_type = 'click') AS click_count,
        MAX(event_timestamp) AS last_active_time,
        CURRENT_TIMESTAMP - MAX(event_timestamp) AS recency
    FROM user_events
    GROUP BY user_id
),
user_value_scores AS (
    SELECT
        user_id,
        active_days,
        session_count,
        total_duration,
        purchase_count,
        click_count,
        EXTRACT(EPOCH FROM recency) / 86400 AS recency_days,
        -- 活跃度得分 (0-30分)
        LEAST(active_days * 2, 30) AS activity_score,
        -- 参与度得分 (0-30分)
        LEAST(session_count * 2, 30) AS engagement_score,
        -- 转化得分 (0-30分)
        LEAST(purchase_count * 10, 30) AS conversion_score,
        -- 最近性得分 (0-10分，最近越活跃得分越高)
        CASE
            WHEN recency_days <= 1 THEN 10
            WHEN recency_days <= 7 THEN 7
            WHEN recency_days <= 30 THEN 4
            ELSE 1
        END AS recency_score
    FROM user_value_metrics
)
SELECT
    user_id,
    active_days,
    session_count,
    purchase_count,
    ROUND(recency_days::numeric, 2) AS recency_days,
    activity_score,
    engagement_score,
    conversion_score,
    recency_score,
    activity_score + engagement_score + conversion_score + recency_score AS total_value_score,
    CASE
        WHEN activity_score + engagement_score + conversion_score + recency_score >= 80 THEN 'High Value'
        WHEN activity_score + engagement_score + conversion_score + recency_score >= 50 THEN 'Medium Value'
        ELSE 'Low Value'
    END AS value_segment
FROM user_value_scores
ORDER BY total_value_score DESC;
```

---

## 📊 PostgreSQL 18并行用户行为分析

### 并行用户行为分析概述

**PostgreSQL 18并行用户行为分析**：利用PostgreSQL 18的并行查询能力，显著提升大规模用户行为分析的性能。

#### 并行用户行为分析配置

```sql
-- 配置并行用户行为分析参数（PostgreSQL 18+）
DO $$
BEGIN
    BEGIN
        -- 设置并行工作进程数
        SET max_parallel_workers_per_gather = 4;

        -- 设置并行表扫描阈值
        SET min_parallel_table_scan_size = '8MB';

        -- 设置并行聚合成本阈值
        SET parallel_setup_cost = 0;
        SET parallel_tuple_cost = 0.0001;

        RAISE NOTICE '并行用户行为分析参数已配置';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '并行用户行为分析配置失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

#### 并行用户行为分析实现

```sql
-- 并行用户活跃度分析（PostgreSQL 18+）
DO $$
BEGIN
    BEGIN
        -- 启用并行查询
        SET max_parallel_workers_per_gather = 4;
        SET parallel_setup_cost = 0;
        SET parallel_tuple_cost = 0.0001;

        RAISE NOTICE '开始执行并行用户行为分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '并行用户行为分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 并行日活跃用户（DAU）分析
EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    DATE_TRUNC('day', event_time) AS date,
    COUNT(DISTINCT user_id) AS dau,
    COUNT(*) AS total_events,
    COUNT(DISTINCT page_id) AS unique_pages
FROM user_events
WHERE event_time >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY DATE_TRUNC('day', event_time)
ORDER BY date DESC;
```

---

## 📊 性能优化建议

### 索引优化

```sql
-- 创建关键索引
CREATE INDEX IF NOT EXISTS idx_events_user ON user_events(user_id);
CREATE INDEX IF NOT EXISTS idx_events_timestamp ON user_events(event_timestamp);
CREATE INDEX IF NOT EXISTS idx_events_session ON user_events(session_id);
CREATE INDEX IF NOT EXISTS idx_events_type ON user_events(event_type);
CREATE INDEX IF NOT EXISTS idx_events_url ON user_events(page_url);
```

### 分区表

```sql
-- 按时间分区事件表（PostgreSQL 10+）
CREATE TABLE user_events_partitioned (
    LIKE user_events INCLUDING ALL
) PARTITION BY RANGE (event_timestamp);

CREATE TABLE events_2024_01 PARTITION OF user_events_partitioned
    FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');
CREATE TABLE events_2024_02 PARTITION OF user_events_partitioned
    FOR VALUES FROM ('2024-02-01') TO ('2024-03-01');
```

### 物化视图

```sql
-- 创建常用统计的物化视图
CREATE MATERIALIZED VIEW IF NOT EXISTS mv_daily_active_users AS
SELECT
    DATE_TRUNC('day', event_timestamp) AS date,
    COUNT(DISTINCT user_id) AS daily_active_users,
    COUNT(DISTINCT session_id) AS daily_sessions,
    COUNT(*) AS total_events
FROM user_events
GROUP BY DATE_TRUNC('day', event_timestamp);

CREATE UNIQUE INDEX ON mv_daily_active_users(date);

-- 定期刷新物化视图
REFRESH MATERIALIZED VIEW CONCURRENTLY mv_daily_active_users;
```

---

## 🎯 最佳实践

### 数据质量

1. **数据完整性**：确保事件数据完整准确
2. **数据一致性**：确保跨表数据一致性
3. **数据时效性**：及时收集和处理事件数据
4. **数据清洗**：定期清洗异常数据

### 分析策略

1. **分层分析**：从宏观到微观逐步深入
2. **对比分析**：与历史数据、行业标准对比
3. **多维度分析**：从时间、用户、行为等多维度分析
4. **异常检测**：识别异常行为和异常模式

### 隐私保护

1. **数据脱敏**：对敏感数据进行脱敏处理
2. **访问控制**：控制数据访问权限
3. **合规性**：遵守数据保护法规
4. **用户同意**：获得用户同意后收集数据

### 性能优化

1. **索引设计**：根据查询模式设计索引
2. **分区策略**：对大数据量表进行分区
3. **物化视图**：缓存常用查询结果
4. **查询优化**：优化SQL查询语句

### SQL实现注意事项

1. **错误处理**：使用DO块进行错误处理
2. **NULL值处理**：使用COALESCE和NULLIF处理NULL值
3. **性能测试**：使用EXPLAIN (ANALYZE, BUFFERS, TIMING)测试性能
4. **数据类型转换**：注意数据类型转换和精度

### PostgreSQL 18 新特性应用

**PostgreSQL 18**引入了多项增强功能，可以显著提升用户行为分析的性能：

1. **Skip Scan优化**：
   - 对于包含用户ID的索引，Skip Scan可以跳过不必要的索引扫描
   - 特别适用于Top-N用户查询和分页查询

2. **异步I/O增强**：
   - 对于大规模用户行为分析，异步I/O可以显著提升性能
   - 适用于批量用户分析和实时监控

3. **并行查询增强**：
   - 用户行为分析支持更好的并行执行
   - 适用于大规模用户聚合和多维度分析

**示例：使用Skip Scan优化活跃用户查询**

```sql
-- 为用户活跃度创建Skip Scan优化索引
CREATE INDEX IF NOT EXISTS idx_user_activity_skip_scan
ON user_events(user_id, event_timestamp DESC)
WHERE event_timestamp >= CURRENT_DATE - INTERVAL '30 days';

-- Skip Scan优化查询：查找最近30天最活跃的前10个用户
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    user_id,
    COUNT(*) AS event_count,
    COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS active_days,
    MAX(event_timestamp) AS last_active_time
FROM user_events
WHERE event_timestamp >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY user_id
ORDER BY event_count DESC
LIMIT 10;
```

### 9. 高级优化技巧

**1. 使用物化视图缓存用户行为分析结果**

对于频繁使用的分析查询，使用物化视图缓存结果：

```sql
-- 创建物化视图缓存用户行为分析结果
CREATE MATERIALIZED VIEW IF NOT EXISTS user_behavior_analytics_cache AS
SELECT
    user_id,
    DATE_TRUNC('day', event_timestamp) AS activity_date,
    COUNT(*) AS daily_events,
    COUNT(DISTINCT session_id) AS daily_sessions,
    COUNT(DISTINCT event_type) AS unique_event_types,
    COUNT(DISTINCT page_url) AS unique_pages_visited,
    SUM(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS total_duration,
    MAX(event_timestamp) AS last_event_time
FROM user_events
WHERE event_timestamp >= CURRENT_DATE - INTERVAL '90 days'
GROUP BY user_id, DATE_TRUNC('day', event_timestamp);

-- 创建索引加速物化视图查询
CREATE INDEX idx_user_behavior_cache_user_date ON user_behavior_analytics_cache(user_id, activity_date DESC);
CREATE INDEX idx_user_behavior_cache_daily_events ON user_behavior_analytics_cache(daily_events DESC);

-- 定期刷新物化视图（每天刷新一次）
REFRESH MATERIALIZED VIEW CONCURRENTLY user_behavior_analytics_cache;
```

**2. 用户画像构建：多维度特征提取**

**用户画像构建**：基于用户行为数据，构建多维度用户画像。

```sql
-- 用户画像构建：多维度特征提取（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法构建用户画像';
            RETURN;
        END IF;
        RAISE NOTICE '开始构建用户画像';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '用户画像构建准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 用户画像：多维度特征提取
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH user_behavior_features AS (
    SELECT
        user_id,
        -- 活跃度特征
        COUNT(*) AS total_events,
        COUNT(DISTINCT DATE_TRUNC('day', event_timestamp)) AS active_days,
        COUNT(DISTINCT session_id) AS total_sessions,
        -- 行为多样性特征
        COUNT(DISTINCT event_type) AS unique_event_types,
        COUNT(DISTINCT page_url) AS unique_pages_visited,
        -- 时间特征
        AVG(EXTRACT(HOUR FROM event_timestamp)) AS avg_active_hour,
        -- 设备特征
        MODE() WITHIN GROUP (ORDER BY device_type) AS preferred_device,
        -- 参与度特征
        AVG(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS avg_session_duration,
        SUM(duration_seconds) FILTER (WHERE duration_seconds IS NOT NULL) AS total_duration,
        -- 活跃度趋势特征
        COUNT(*) FILTER (WHERE event_timestamp >= CURRENT_DATE - INTERVAL '7 days') AS events_last_7d,
        COUNT(*) FILTER (WHERE event_timestamp >= CURRENT_DATE - INTERVAL '30 days') AS events_last_30d,
        -- 留存特征
        MIN(event_timestamp) AS first_active_date,
        MAX(event_timestamp) AS last_active_date,
        CURRENT_DATE - DATE(MAX(event_timestamp)) AS days_since_last_active
    FROM user_events
    GROUP BY user_id
),
user_segments AS (
    SELECT
        user_id,
        total_events,
        active_days,
        total_sessions,
        unique_event_types,
        unique_pages_visited,
        ROUND(avg_active_hour::numeric, 2) AS avg_active_hour,
        preferred_device,
        ROUND(avg_session_duration::numeric, 2) AS avg_session_duration_minutes,
        ROUND(total_duration::numeric, 2) AS total_duration_minutes,
        events_last_7d,
        events_last_30d,
        first_active_date,
        last_active_date,
        days_since_last_active,
        -- 用户分群：活跃度分群
        CASE
            WHEN active_days >= 20 AND events_last_7d >= 10 THEN 'Highly Active'
            WHEN active_days >= 10 AND events_last_7d >= 5 THEN 'Active'
            WHEN active_days >= 5 AND events_last_30d >= 10 THEN 'Moderately Active'
            WHEN active_days >= 1 THEN 'Low Activity'
            ELSE 'Inactive'
        END AS activity_segment,
        -- 用户分群：参与度分群
        CASE
            WHEN avg_session_duration > 600 THEN 'High Engagement'  -- 大于10分钟
            WHEN avg_session_duration > 300 THEN 'Medium Engagement'  -- 大于5分钟
            WHEN avg_session_duration > 60 THEN 'Low Engagement'  -- 大于1分钟
            ELSE 'Very Low Engagement'
        END AS engagement_segment,
        -- 用户分群：流失风险分群
        CASE
            WHEN days_since_last_active <= 1 THEN 'Current User'
            WHEN days_since_last_active <= 7 THEN 'Recent User'
            WHEN days_since_last_active <= 30 THEN 'Churned Risk'
            ELSE 'Churned'
        END AS churn_risk_segment
    FROM user_behavior_features
)
SELECT
    user_id,
    total_events,
    active_days,
    total_sessions,
    unique_event_types,
    unique_pages_visited,
    avg_active_hour,
    preferred_device,
    avg_session_duration_minutes,
    total_duration_minutes,
    events_last_7d,
    events_last_30d,
    first_active_date,
    last_active_date,
    days_since_last_active,
    activity_segment,
    engagement_segment,
    churn_risk_segment,
    -- 综合用户价值评分（0-100）
    LEAST(100,
        (active_days * 2) +  -- 活跃天数得分（最多40分）
        (LEAST(events_last_7d, 20) * 1) +  -- 最近7天事件得分（最多20分）
        (LEAST(avg_session_duration_minutes / 10, 20)) +  -- 参与度得分（最多20分）
        (LEAST(days_since_last_active, 30) / 30 * 20)  -- 留存得分（最多20分）
    ) AS user_value_score
FROM user_segments
ORDER BY user_value_score DESC, total_events DESC
LIMIT 100;
```

**3. 转化漏斗分析：多阶段转化路径优化**

**转化漏斗分析**：分析用户从访问到购买的多阶段转化路径。

```sql
-- 转化漏斗分析：多阶段转化路径优化（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'user_events') THEN
            RAISE WARNING '表 user_events 不存在，无法执行转化漏斗分析';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行转化漏斗分析';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '转化漏斗分析准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 转化漏斗：访问 -> 浏览 -> 点击 -> 加购 -> 购买
WITH conversion_funnel AS (
    SELECT
        session_id,
        user_id,
        -- 各个阶段的事件数量
        COUNT(*) FILTER (WHERE event_type = 'page_view') AS visits,
        COUNT(*) FILTER (WHERE event_type = 'page_view' AND page_url LIKE '/products%') AS product_views,
        COUNT(*) FILTER (WHERE event_type = 'click' AND page_url LIKE '/products/item%') AS product_clicks,
        COUNT(*) FILTER (WHERE event_type = 'add_to_cart') AS add_to_carts,
        COUNT(*) FILTER (WHERE event_type = 'purchase') AS purchases,
        -- 各阶段的时间戳（用于计算转化时间）
        MIN(event_timestamp) FILTER (WHERE event_type = 'page_view') AS first_visit_time,
        MIN(event_timestamp) FILTER (WHERE event_type = 'purchase') AS first_purchase_time
    FROM user_events
    GROUP BY session_id, user_id
),
funnel_analysis AS (
    SELECT
        '访问' AS funnel_stage,
        COUNT(*) AS user_count,
        COUNT(*) FILTER (WHERE visits > 0) AS converted_count,
        ROUND((COUNT(*) FILTER (WHERE visits > 0)::numeric / COUNT(*) * 100)::numeric, 2) AS conversion_rate
    FROM conversion_funnel

    UNION ALL

    SELECT
        '产品浏览' AS funnel_stage,
        COUNT(*) FILTER (WHERE visits > 0) AS user_count,
        COUNT(*) FILTER (WHERE visits > 0 AND product_views > 0) AS converted_count,
        ROUND((COUNT(*) FILTER (WHERE visits > 0 AND product_views > 0)::numeric /
               NULLIF(COUNT(*) FILTER (WHERE visits > 0), 0) * 100)::numeric, 2) AS conversion_rate
    FROM conversion_funnel

    UNION ALL

    SELECT
        '产品点击' AS funnel_stage,
        COUNT(*) FILTER (WHERE product_views > 0) AS user_count,
        COUNT(*) FILTER (WHERE product_views > 0 AND product_clicks > 0) AS converted_count,
        ROUND((COUNT(*) FILTER (WHERE product_views > 0 AND product_clicks > 0)::numeric /
               NULLIF(COUNT(*) FILTER (WHERE product_views > 0), 0) * 100)::numeric, 2) AS conversion_rate
    FROM conversion_funnel

    UNION ALL

    SELECT
        '加入购物车' AS funnel_stage,
        COUNT(*) FILTER (WHERE product_clicks > 0) AS user_count,
        COUNT(*) FILTER (WHERE product_clicks > 0 AND add_to_carts > 0) AS converted_count,
        ROUND((COUNT(*) FILTER (WHERE product_clicks > 0 AND add_to_carts > 0)::numeric /
               NULLIF(COUNT(*) FILTER (WHERE product_clicks > 0), 0) * 100)::numeric, 2) AS conversion_rate
    FROM conversion_funnel

    UNION ALL

    SELECT
        '购买' AS funnel_stage,
        COUNT(*) FILTER (WHERE add_to_carts > 0) AS user_count,
        COUNT(*) FILTER (WHERE add_to_carts > 0 AND purchases > 0) AS converted_count,
        ROUND((COUNT(*) FILTER (WHERE add_to_carts > 0 AND purchases > 0)::numeric /
               NULLIF(COUNT(*) FILTER (WHERE add_to_carts > 0), 0) * 100)::numeric, 2) AS conversion_rate
    FROM conversion_funnel
),
funnel_with_dropoff AS (
    SELECT
        funnel_stage,
        user_count,
        converted_count,
        conversion_rate,
        user_count - converted_count AS dropped_count,
        ROUND(((user_count - converted_count)::numeric / NULLIF(user_count, 0) * 100)::numeric, 2) AS dropoff_rate,
        LAG(user_count) OVER (ORDER BY
            CASE funnel_stage
                WHEN '访问' THEN 1
                WHEN '产品浏览' THEN 2
                WHEN '产品点击' THEN 3
                WHEN '加入购物车' THEN 4
                WHEN '购买' THEN 5
            END
        ) AS previous_stage_count
    FROM funnel_analysis
)
SELECT
    funnel_stage,
    user_count AS users_at_stage,
    converted_count AS users_converted,
    dropped_count AS users_dropped,
    conversion_rate AS stage_conversion_rate_pct,
    dropoff_rate AS stage_dropoff_rate_pct,
    CASE
        WHEN previous_stage_count IS NOT NULL THEN
            ROUND((converted_count::numeric / previous_stage_count * 100)::numeric, 2)
        ELSE NULL
    END AS conversion_from_previous_pct,
    -- 总体转化率：从访问到当前阶段的转化率
    ROUND((converted_count::numeric / (SELECT user_count FROM funnel_analysis WHERE funnel_stage = '访问') * 100)::numeric, 2) AS overall_conversion_rate_pct
FROM funnel_with_dropoff
ORDER BY
    CASE funnel_stage
        WHEN '访问' THEN 1
        WHEN '产品浏览' THEN 2
        WHEN '产品点击' THEN 3
        WHEN '加入购物车' THEN 4
        WHEN '购买' THEN 5
    END;
```

---

## 📈 分析方法对比

| 分析方法 | 适用场景 | 优势 | 局限性 |
|---------|---------|------|--------|
| 描述性分析 | 现状了解、趋势分析 | 简单直观、易于理解 | 无法预测未来 |
| 诊断性分析 | 问题定位、原因分析 | 深入挖掘、发现规律 | 需要大量数据 |
| 预测性分析 | 趋势预测、流失预测 | 提前预警、支持决策 | 准确性有限 |
| 规范性分析 | 个性化推荐、精准营销 | 提供行动方案 | 需要领域知识 |

---

## 🔍 常见问题与解决方案

### 问题1：用户行为数据量大，查询慢

**原因**：

- 数据量过大
- 缺少索引
- 复杂查询

**解决方案**：

- 使用分区表
- 创建合适的索引
- 使用物化视图缓存结果
- 优化查询语句

### 问题2：留存率计算不准确

**原因**：

- 数据缺失
- 计算逻辑错误
- 时间窗口选择不当

**解决方案**：

- 提高数据质量
- 仔细检查计算公式
- 选择合适的时间窗口
- 与业务人员确认

### 问题3：用户分群效果不好

**原因**：

- 特征选择不当
- 分群标准不合理
- 数据质量差

**解决方案**：

- 选择合适的特征
- 调整分群标准
- 提高数据质量
- 使用多种分群方法

---

**最后更新**: 2025年1月
**文档状态**: ✅ 已完成
