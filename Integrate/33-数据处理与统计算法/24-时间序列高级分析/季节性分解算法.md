# PostgreSQL 季节性分解算法完整指南

> **创建日期**: 2025年1月
> **技术栈**: PostgreSQL 17+/18+ | 时间序列 | 季节性分解 | STL分解
> **难度级别**: ⭐⭐⭐⭐ (高级)
> **参考标准**: Seasonal Decomposition, STL Decomposition, Time Series Analysis

---

## 📋 目录

- [PostgreSQL 季节性分解算法完整指南](#postgresql-季节性分解算法完整指南)
  - [📋 目录](#-目录)
  - [季节性分解概述](#季节性分解概述)
    - [分解模型](#分解模型)
  - [1. STL分解](#1-stl分解)
    - [1.1 STL算法原理](#11-stl算法原理)
    - [1.2 STL分解实现](#12-stl分解实现)
    - [1.3 季节性强度评估](#13-季节性强度评估)
  - [2. 移动平均分解](#2-移动平均分解)
    - [2.1 经典分解方法](#21-经典分解方法)
    - [2.2 移动平均分解实现](#22-移动平均分解实现)
    - [2.3 乘法模型分解](#23-乘法模型分解)
  - [3. 分解质量评估](#3-分解质量评估)
    - [3.1 残差分析](#31-残差分析)
  - [📚 参考资源](#-参考资源)
  - [📊 性能优化建议](#-性能优化建议)
  - [🎯 最佳实践](#-最佳实践)

---

## 季节性分解概述

**季节性分解**将时间序列分解为趋势、季节性和残差成分，用于理解时间序列的结构。

### 分解模型

**加法模型**：$Y_t = T_t + S_t + R_t$

**乘法模型**：$Y_t = T_t \times S_t \times R_t$

其中：

- $T_t$：趋势成分
- $S_t$：季节性成分
- $R_t$：残差成分

---

## 1. STL分解

### 1.1 STL算法原理

**STL（Seasonal and Trend decomposition using Loess）**使用局部回归进行分解。

**算法步骤**：

1. **去趋势**：去除趋势成分
2. **季节性平滑**：提取季节性成分
3. **去季节性**：去除季节性成分
4. **趋势平滑**：提取趋势成分
5. **迭代**：重复步骤1-4直到收敛

### 1.2 STL分解实现

```sql
-- 季节性分解数据准备（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'seasonal_data') THEN
            DROP TABLE seasonal_data CASCADE;
        END IF;

        CREATE TABLE seasonal_data (
            date DATE PRIMARY KEY,
            value NUMERIC NOT NULL
        );

        -- 插入示例数据（带趋势和季节性）
        INSERT INTO seasonal_data (date, value)
        SELECT
            '2023-01-01'::DATE + (i || ' days')::INTERVAL,
            -- 趋势：线性增长
            100 + i * 0.1 +
            -- 季节性：12个月周期
            10 * SIN(2 * PI() * EXTRACT(MONTH FROM ('2023-01-01'::DATE + (i || ' days')::INTERVAL)) / 12) +
            -- 噪声
            (RANDOM() - 0.5) * 2
        FROM generate_series(0, 730) i;  -- 2年数据

        CREATE INDEX idx_seasonal_date ON seasonal_data(date);

        RAISE NOTICE '季节性数据表创建成功，共 % 条记录', (SELECT COUNT(*) FROM seasonal_data);
    EXCEPTION
        WHEN OTHERS THEN
            RAISE EXCEPTION '创建表失败: %', SQLERRM;
    END;
END $$;

-- STL分解：步骤1 - 趋势提取（移动平均）
WITH trend_component AS (
    SELECT
        date,
        value,
        -- 移动平均提取趋势（窗口大小=12，对应月度数据）
        AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        ) AS trend
    FROM seasonal_data
),
detrended AS (
    SELECT
        date,
        value,
        trend,
        value - trend AS detrended_value
    FROM trend_component
),
-- STL分解：步骤2 - 季节性提取
seasonal_component AS (
    SELECT
        date,
        value,
        trend,
        detrended_value,
        -- 季节性：按月份平均去趋势值
        AVG(detrended_value) OVER (
            PARTITION BY EXTRACT(MONTH FROM date)
        ) AS seasonal,
        -- 季节性调整：确保季节性均值为0
        AVG(detrended_value) OVER (
            PARTITION BY EXTRACT(MONTH FROM date)
        ) - AVG(detrended_value) OVER () AS seasonal_adjusted
    FROM detrended
),
-- STL分解：步骤3 - 残差计算
residual_component AS (
    SELECT
        date,
        value,
        trend,
        seasonal_adjusted AS seasonal,
        value - trend - seasonal_adjusted AS residual
    FROM seasonal_component
)
SELECT
    date,
    EXTRACT(MONTH FROM date) AS month,
    ROUND(value::numeric, 2) AS original,
    ROUND(trend::numeric, 2) AS trend,
    ROUND(seasonal::numeric, 2) AS seasonal,
    ROUND(residual::numeric, 2) AS residual,
    -- 验证：原始值 = 趋势 + 季节性 + 残差
    ROUND(ABS(value - (trend + seasonal + residual))::numeric, 4) AS decomposition_error
FROM residual_component
ORDER BY date
LIMIT 50;
```

### 1.3 季节性强度评估

```sql
-- 季节性强度：评估季节性的重要性
WITH decomposition AS (
    SELECT
        date,
        value,
        AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        ) AS trend,
        AVG(value - AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        )) OVER (
            PARTITION BY EXTRACT(MONTH FROM date)
        ) - AVG(value - AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        )) OVER () AS seasonal
    FROM seasonal_data
),
seasonal_strength AS (
    SELECT
        VAR_POP(seasonal) AS seasonal_variance,
        VAR_POP(value - trend - seasonal) AS residual_variance,
        VAR_POP(value) AS total_variance
    FROM decomposition
)
SELECT
    ROUND(seasonal_variance::numeric, 4) AS seasonal_variance,
    ROUND(residual_variance::numeric, 4) AS residual_variance,
    ROUND(total_variance::numeric, 4) AS total_variance,
    ROUND((seasonal_variance / NULLIF(total_variance, 0))::numeric, 4) AS seasonal_strength,
    CASE
        WHEN (seasonal_variance / NULLIF(total_variance, 0)) > 0.5 THEN 'Strong seasonality'
        WHEN (seasonal_variance / NULLIF(total_variance, 0)) > 0.2 THEN 'Moderate seasonality'
        ELSE 'Weak seasonality'
    END AS seasonality_assessment
FROM seasonal_strength;
```

---

## 2. 移动平均分解

### 2.1 经典分解方法

**经典分解**使用移动平均提取趋势和季节性。

**步骤**：

1. 计算移动平均提取趋势
2. 从原始序列减去趋势得到去趋势序列
3. 按季节平均去趋势序列得到季节性
4. 残差 = 原始 - 趋势 - 季节性

### 2.2 移动平均分解实现

```sql
-- 移动平均分解：经典方法
WITH ma_trend AS (
    SELECT
        date,
        value,
        -- 中心移动平均（窗口大小=12）
        AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        ) AS ma_trend
    FROM seasonal_data
),
classical_seasonal AS (
    SELECT
        date,
        value,
        ma_trend,
        value - ma_trend AS detrended,
        -- 季节性：按月份平均
        AVG(value - ma_trend) OVER (
            PARTITION BY EXTRACT(MONTH FROM date)
        ) AS seasonal_raw
    FROM ma_trend
),
seasonal_adjusted AS (
    SELECT
        date,
        value,
        ma_trend AS trend,
        seasonal_raw - AVG(seasonal_raw) OVER () AS seasonal,
        value - ma_trend - (seasonal_raw - AVG(seasonal_raw) OVER ()) AS residual
    FROM classical_seasonal
)
SELECT
    date,
    EXTRACT(MONTH FROM date) AS month,
    ROUND(value::numeric, 2) AS original,
    ROUND(trend::numeric, 2) AS trend,
    ROUND(seasonal::numeric, 2) AS seasonal,
    ROUND(residual::numeric, 2) AS residual
FROM seasonal_adjusted
ORDER BY date
LIMIT 50;
```

### 2.3 乘法模型分解

```sql
-- 乘法模型分解：Y = T × S × R
WITH multiplicative_decomposition AS (
    SELECT
        date,
        value,
        -- 趋势（使用移动平均）
        AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        ) AS trend,
        -- 去趋势序列
        value / NULLIF(AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        ), 0) AS detrended_ratio
    FROM seasonal_data
    WHERE value > 0  -- 确保正值
),
multiplicative_seasonal AS (
    SELECT
        date,
        value,
        trend,
        -- 季节性比率
        AVG(detrended_ratio) OVER (
            PARTITION BY EXTRACT(MONTH FROM date)
        ) AS seasonal_ratio,
        -- 残差比率
        detrended_ratio / NULLIF(
            AVG(detrended_ratio) OVER (
                PARTITION BY EXTRACT(MONTH FROM date)
            ),
            0
        ) AS residual_ratio
    FROM multiplicative_decomposition
)
SELECT
    date,
    ROUND(value::numeric, 2) AS original,
    ROUND(trend::numeric, 2) AS trend,
    ROUND(seasonal_ratio::numeric, 4) AS seasonal_ratio,
    ROUND(residual_ratio::numeric, 4) AS residual_ratio,
    -- 验证：原始值 ≈ 趋势 × 季节性比率 × 残差比率
    ROUND(ABS(value - (trend * seasonal_ratio * residual_ratio))::numeric, 4) AS decomposition_error
FROM multiplicative_seasonal
ORDER BY date
LIMIT 50;
```

---

## 3. PostgreSQL 18 并行季节性分解增强

**PostgreSQL 18** 显著增强了并行季节性分解计算能力，支持并行执行STL分解、移动平均分解和分解质量评估，大幅提升大规模时间序列季节性分解的性能。

### 3.1 并行季节性分解原理

PostgreSQL 18 的并行季节性分解通过以下方式实现：

1. **并行扫描**：多个工作进程并行扫描时间序列数据
2. **并行趋势提取**：每个工作进程独立提取趋势成分
3. **并行季节性提取**：并行执行季节性成分提取
4. **结果合并**：主进程合并所有工作进程的分解结果

### 3.2 并行STL分解

```sql
-- PostgreSQL 18 并行STL分解（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'seasonal_data') THEN
            RAISE WARNING '表 seasonal_data 不存在，无法执行并行STL分解';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行PostgreSQL 18并行STL分解';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '并行STL分解准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

SET max_parallel_workers_per_gather = 4;
SET parallel_setup_cost = 0;

-- 并行趋势提取：移动平均
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH trend_extraction AS (
    SELECT
        date,
        value,
        AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        ) AS trend_component
    FROM seasonal_data
),
seasonal_extraction AS (
    SELECT
        date,
        value,
        trend_component,
        value - trend_component AS detrended,
        AVG(value - trend_component) OVER (
            PARTITION BY EXTRACT(MONTH FROM date)
        ) AS seasonal_component
    FROM trend_extraction
)
SELECT
    date,
    ROUND(value::numeric, 4) AS original,
    ROUND(trend_component::numeric, 4) AS trend,
    ROUND(seasonal_component::numeric, 4) AS seasonal,
    ROUND((value - trend_component - seasonal_component)::numeric, 4) AS residual
FROM seasonal_extraction
ORDER BY date;
```

### 3.3 并行分解质量评估

```sql
-- PostgreSQL 18 并行分解质量评估（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'seasonal_data') THEN
            RAISE WARNING '表 seasonal_data 不存在，无法执行并行分解质量评估';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行PostgreSQL 18并行分解质量评估';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '并行分解质量评估准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

SET max_parallel_workers_per_gather = 4;
SET parallel_setup_cost = 0;

-- 并行分解质量：方差分解
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH decomposition_variance AS (
    SELECT
        VAR_POP(value) AS total_variance,
        VAR_POP(trend_component) AS trend_variance,
        VAR_POP(seasonal_component) AS seasonal_variance,
        VAR_POP(residual) AS residual_variance
    FROM stl_decomposition
)
SELECT
    ROUND(total_variance::numeric, 4) AS total_var,
    ROUND(trend_variance::numeric, 4) AS trend_var,
    ROUND(seasonal_variance::numeric, 4) AS seasonal_var,
    ROUND(residual_variance::numeric, 4) AS residual_var,
    ROUND((trend_variance / NULLIF(total_variance, 0) * 100)::numeric, 2) AS trend_percentage,
    ROUND((seasonal_variance / NULLIF(total_variance, 0) * 100)::numeric, 2) AS seasonal_percentage
FROM decomposition_variance;
```

---

## 4. 分解质量评估

### 3.1 残差分析

```sql
-- 残差分析：检查分解质量
WITH decomposition AS (
    SELECT
        date,
        value,
        AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        ) AS trend,
        AVG(value - AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        )) OVER (
            PARTITION BY EXTRACT(MONTH FROM date)
        ) - AVG(value - AVG(value) OVER (
            ORDER BY date
            ROWS BETWEEN 5 PRECEDING AND 6 FOLLOWING
        )) OVER () AS seasonal
    FROM seasonal_data
),
residuals AS (
    SELECT
        value - trend - seasonal AS residual
    FROM decomposition
)
SELECT
    COUNT(*) AS sample_count,
    ROUND(AVG(residual)::numeric, 6) AS mean_residual,
    ROUND(STDDEV(residual)::numeric, 4) AS std_residual,
    ROUND(MIN(residual)::numeric, 2) AS min_residual,
    ROUND(MAX(residual)::numeric, 2) AS max_residual,
    CASE
        WHEN ABS(AVG(residual)) < 0.01 AND STDDEV(residual) < STDDEV(value) * 0.1
        THEN 'Good decomposition'
        ELSE 'Poor decomposition'
    END AS quality_assessment
FROM residuals
CROSS JOIN (SELECT STDDEV(value) AS value_std FROM seasonal_data) stats;
```

---

## 📚 参考资源

1. **Cleveland, R.B., et al. (1990)**: "STL: A Seasonal-Trend Decomposition Procedure Based on Loess"
2. **Hyndman, R.J., Athanasopoulos, G. (2021)**: "Forecasting: Principles and Practice", 3rd Edition

---

## 📊 性能优化建议

1. **窗口大小**：根据数据频率选择合适窗口大小
2. **迭代次数**：STL分解需要多次迭代
3. **并行计算**：并行处理多个时间序列
4. **缓存结果**：缓存分解结果避免重复计算

---

## 🎯 最佳实践

1. **模型选择**：根据数据特性选择加法或乘法模型
2. **季节性周期**：正确识别季节性周期
3. **趋势处理**：处理非线性趋势
4. **残差检查**：检查残差是否随机

---

**最后更新**: 2025年1月
**文档状态**: ✅ 已完成
