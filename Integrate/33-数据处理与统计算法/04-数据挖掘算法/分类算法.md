# PostgreSQL 分类算法完整指南

> **创建日期**: 2025年1月
> **技术栈**: PostgreSQL 17+/18+ | 分类算法 | 预测分析
> **难度级别**: ⭐⭐⭐⭐ (高级)

---

## 📋 目录

- [PostgreSQL 分类算法完整指南](#postgresql-分类算法完整指南)
  - [📋 目录](#-目录)
  - [分类算法概述](#分类算法概述)
    - [理论基础](#理论基础)
      - [分类问题定义](#分类问题定义)
      - [分类算法分类](#分类算法分类)
      - [分类评估指标](#分类评估指标)
    - [核心分类算法](#核心分类算法)
  - [1. 朴素贝叶斯分类](#1-朴素贝叶斯分类)
    - [1.1 朴素贝叶斯原理](#11-朴素贝叶斯原理)
      - [贝叶斯定理](#贝叶斯定理)
      - [朴素贝叶斯假设](#朴素贝叶斯假设)
      - [分类决策](#分类决策)
      - [概率估计](#概率估计)
    - [1.2 概率计算实现](#12-概率计算实现)
  - [2. 决策树分类](#2-决策树分类)
    - [2.1 决策树原理](#21-决策树原理)
      - [决策树构建](#决策树构建)
      - [信息熵（Entropy）](#信息熵entropy)
      - [信息增益（Information Gain）](#信息增益information-gain)
      - [基尼系数（Gini Index）](#基尼系数gini-index)
    - [2.2 信息增益计算实现](#22-信息增益计算实现)
  - [3. KNN分类](#3-knn分类)
    - [3.1 KNN原理](#31-knn原理)
      - [算法步骤](#算法步骤)
      - [距离度量](#距离度量)
      - [K值选择](#k值选择)
    - [3.2 KNN实现](#32-knn实现)
  - [4. 分类评估](#4-分类评估)
    - [4.1 混淆矩阵](#41-混淆矩阵)
    - [4.2 准确率和召回率实现](#42-准确率和召回率实现)
  - [5. 实际应用案例](#5-实际应用案例)
    - [5.1 客户分类](#51-客户分类)
    - [5.2 邮件 spam 分类](#52-邮件-spam-分类)
    - [5.3 产品分类](#53-产品分类)
    - [5.4 用户行为分类](#54-用户行为分类)
    - [5.5 分类模型评估](#55-分类模型评估)
  - [6. 算法性能对比与优化](#6-算法性能对比与优化)
    - [6.1 分类算法对比](#61-分类算法对比)
    - [6.2 性能优化建议](#62-性能优化建议)
    - [6.3 常见问题与解决方案](#63-常见问题与解决方案)
  - [7. 最佳实践](#7-最佳实践)
    - [7.1 数据准备](#71-数据准备)
    - [7.2 模型训练](#72-模型训练)
    - [7.3 模型部署](#73-模型部署)
    - [7.4 SQL实现注意事项](#74-sql实现注意事项)
  - [📚 参考资源](#-参考资源)
    - [学术文献](#学术文献)
    - [PostgreSQL官方文档](#postgresql官方文档)
    - [在线资源](#在线资源)
    - [相关算法](#相关算法)

---

## 分类算法概述

**分类（Classification）**是监督学习中的核心任务，用于预测数据点的类别标签。PostgreSQL可以通过SQL实现多种分类算法，虽然性能不如专门的机器学习库，但在某些场景下可以提供便捷的分类功能。

### 理论基础

#### 分类问题定义

**分类问题**：给定训练数据集$D = \{(x_1, y_1), (x_2, y_2), ..., (x_n, y_n)\}$，其中$x_i$是特征向量，$y_i \in \{c_1, c_2, ..., c_k\}$是类别标签，学习一个分类函数$f: X \rightarrow Y$，使得对于新的输入$x$，能够预测其类别$y = f(x)$。

**分类目标**：最小化分类错误率：
$$Error = \frac{1}{n}\sum_{i=1}^{n} I(f(x_i) \neq y_i)$$

其中$I(\cdot)$是指示函数。

#### 分类算法分类

1. **概率分类**：基于概率模型（朴素贝叶斯）
2. **规则分类**：基于决策规则（决策树）
3. **距离分类**：基于距离度量（KNN）
4. **线性分类**：基于线性模型（逻辑回归）

#### 分类评估指标

**混淆矩阵（Confusion Matrix）**：

- **TP（True Positive）**：真正例
- **TN（True Negative）**：真负例
- **FP（False Positive）**：假正例
- **FN（False Negative）**：假负例

**准确率（Accuracy）**：
$$\text{Accuracy} = \frac{TP + TN}{TP + TN + FP + FN}$$

**精确率（Precision）**：
$$\text{Precision} = \frac{TP}{TP + FP}$$

**召回率（Recall）**：
$$\text{Recall} = \frac{TP}{TP + FN}$$

**F1分数（F1-Score）**：
$$\text{F1} = \frac{2 \times \text{Precision} \times \text{Recall}}{\text{Precision} + \text{Recall}}$$

### 核心分类算法

| 算法 | 原理 | 时间复杂度 | 空间复杂度 | 优势 | 劣势 |
|------|------|-----------|-----------|------|------|
| **朴素贝叶斯** | 贝叶斯定理 | $O(n)$ | $O(n)$ | 简单快速、适合高维数据 | 特征独立性假设 |
| **决策树** | 信息增益/基尼系数 | $O(n \log n)$ | $O(n)$ | 可解释性强、处理非线性 | 容易过拟合 |
| **KNN** | 距离度量 | $O(n^2)$ | $O(n)$ | 简单直观、无需训练 | 计算量大、对噪声敏感 |
| **逻辑回归** | 逻辑函数 | $O(n)$ | $O(1)$ | 概率输出、可解释 | 线性边界限制 |

---

## 1. 朴素贝叶斯分类

### 1.1 朴素贝叶斯原理

**朴素贝叶斯（Naive Bayes）**是基于贝叶斯定理和特征独立性假设的分类算法。

#### 贝叶斯定理

**贝叶斯定理**：
$$P(Y|X) = \frac{P(X|Y) \times P(Y)}{P(X)}$$

其中：

- $P(Y|X)$：后验概率（给定特征$X$，类别$Y$的概率）
- $P(X|Y)$：似然概率（给定类别$Y$，特征$X$的概率）
- $P(Y)$：先验概率（类别$Y$的概率）
- $P(X)$：证据（特征$X$的概率）

#### 朴素贝叶斯假设

**特征独立性假设**：给定类别，特征之间相互独立：
$$P(X|Y) = P(x_1, x_2, ..., x_n|Y) = \prod_{i=1}^{n} P(x_i|Y)$$

#### 分类决策

**最大后验概率（MAP）决策**：
$$\hat{y} = \arg\max_{y} P(Y=y|X) = \arg\max_{y} P(Y=y) \prod_{i=1}^{n} P(x_i|Y=y)$$

**对数形式**（避免数值下溢）：
$$\hat{y} = \arg\max_{y} \left[\log P(Y=y) + \sum_{i=1}^{n} \log P(x_i|Y=y)\right]$$

#### 概率估计

**先验概率**：
$$P(Y=c_k) = \frac{|\{i: y_i = c_k\}|}{n}$$

**条件概率**（离散特征）：
$$P(x_i = v|Y=c_k) = \frac{|\{j: x_{j,i} = v \text{ and } y_j = c_k\}|}{|\{j: y_j = c_k\}|}$$

**条件概率**（连续特征，使用高斯分布）：
$$P(x_i|Y=c_k) = \frac{1}{\sqrt{2\pi\sigma_{k,i}^2}} \exp\left(-\frac{(x_i - \mu_{k,i})^2}{2\sigma_{k,i}^2}\right)$$

### 1.2 概率计算实现

```sql
-- 朴素贝叶斯分类（带错误处理和性能测试）
DO $$
DECLARE
    total_count BIGINT;
    class_a_count BIGINT;
    class_b_count BIGINT;
    class_a_prob NUMERIC;
    class_b_prob NUMERIC;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'training_data') THEN
            RAISE WARNING '表 training_data 不存在，无法执行朴素贝叶斯分类';
            RETURN;
        END IF;

        SELECT COUNT(*) INTO total_count FROM training_data;
        SELECT COUNT(*) INTO class_a_count FROM training_data WHERE class = 'A';
        SELECT COUNT(*) INTO class_b_count FROM training_data WHERE class = 'B';

        class_a_prob := class_a_count::NUMERIC / NULLIF(total_count, 0);
        class_b_prob := class_b_count::NUMERIC / NULLIF(total_count, 0);

        RAISE NOTICE '先验概率:';
        RAISE NOTICE '  P(A) = %', class_a_prob;
        RAISE NOTICE '  P(B) = %', class_b_prob;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '朴素贝叶斯分类失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 计算条件概率
WITH class_probs AS (
    SELECT
        class,
        COUNT(*)::NUMERIC / SUM(COUNT(*)) OVER () AS prior_prob
    FROM training_data
    GROUP BY class
),
feature_probs AS (
    SELECT
        class,
        feature_value,
        COUNT(*)::NUMERIC / SUM(COUNT(*)) OVER (PARTITION BY class) AS conditional_prob
    FROM training_data
    GROUP BY class, feature_value
)
SELECT
    cp.class,
    cp.prior_prob,
    fp.feature_value,
    fp.conditional_prob,
    cp.prior_prob * fp.conditional_prob AS posterior_prob
FROM class_probs cp
JOIN feature_probs fp ON cp.class = fp.class
ORDER BY cp.class, fp.feature_value;
```

---

## 2. 决策树分类

### 2.1 决策树原理

**决策树（Decision Tree）**通过一系列规则对数据进行分类，每个内部节点表示一个特征测试，每个分支表示测试结果，每个叶子节点表示一个类别。

#### 决策树构建

**算法步骤**：

1. **选择最优特征**：使用信息增益或基尼系数
2. **分裂节点**：根据特征值将数据分成子集
3. **递归构建**：对每个子集递归构建子树
4. **停止条件**：达到最大深度、样本数过少、纯度足够高

#### 信息熵（Entropy）

**信息熵**衡量数据的不确定性：
$$H(D) = -\sum_{k=1}^{K} p_k \log_2 p_k$$

其中$p_k$是类别$k$在数据集$D$中的比例。

#### 信息增益（Information Gain）

**信息增益**衡量特征对分类的贡献：
$$\text{IG}(D, A) = H(D) - \sum_{v=1}^{V} \frac{|D_v|}{|D|} H(D_v)$$

其中：

- $A$：特征
- $V$：特征$A$的取值数
- $D_v$：特征$A$取值为$v$的子集

**选择特征**：选择信息增益最大的特征。

#### 基尼系数（Gini Index）

**基尼系数**衡量数据的不纯度：
$$\text{Gini}(D) = 1 - \sum_{k=1}^{K} p_k^2$$

**基尼增益**：
$$\text{Gini\_Gain}(D, A) = \text{Gini}(D) - \sum_{v=1}^{V} \frac{|D_v|}{|D|} \text{Gini}(D_v)$$

### 2.2 信息增益计算实现

```sql
-- 信息增益计算（带错误处理和性能测试）
DO $$
DECLARE
    entropy_before NUMERIC;
    entropy_after NUMERIC;
    information_gain NUMERIC;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'training_data') THEN
            RAISE WARNING '表 training_data 不存在，无法计算信息增益';
            RETURN;
        END IF;

        -- 计算分裂前的熵
        WITH class_counts AS (
            SELECT
                class,
                COUNT(*)::NUMERIC / SUM(COUNT(*)) OVER () AS prob
            FROM training_data
            GROUP BY class
        )
        SELECT -SUM(prob * LN(NULLIF(prob, 0))) INTO entropy_before
        FROM class_counts;

        -- 计算分裂后的加权熵（简化版）
        WITH split_entropy AS (
            SELECT
                feature_value,
                -SUM((class_count::NUMERIC / NULLIF(total_count, 0)) * LN(NULLIF(class_count::NUMERIC / NULLIF(total_count, 0), 0))) AS entropy
            FROM (
                SELECT
                    feature_value,
                    class,
                    COUNT(*) AS class_count,
                    SUM(COUNT(*)) OVER (PARTITION BY feature_value) AS total_count
                FROM training_data
                GROUP BY feature_value, class
            ) AS split_counts
            GROUP BY feature_value
        )
        SELECT AVG(entropy) INTO entropy_after FROM split_entropy;

        information_gain := entropy_before - entropy_after;

        RAISE NOTICE '信息增益计算:';
        RAISE NOTICE '  分裂前熵: %', entropy_before;
        RAISE NOTICE '  分裂后熵: %', entropy_after;
        RAISE NOTICE '  信息增益: %', information_gain;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '信息增益计算失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

---

## 3. KNN分类

### 3.1 KNN原理

**K最近邻（K-Nearest Neighbors, KNN）**是一种基于实例的学习算法，通过计算待分类样本与训练样本的距离，选择最近的$K$个样本，根据这$K$个样本的类别进行投票决定分类结果。

#### 算法步骤

1. **计算距离**：计算待分类样本与所有训练样本的距离
2. **选择K个最近邻**：选择距离最小的$K$个样本
3. **投票决策**：根据$K$个最近邻的类别进行投票
4. **分类结果**：选择得票最多的类别

#### 距离度量

**欧氏距离（Euclidean Distance）**：
$$d(x, y) = \sqrt{\sum_{i=1}^{n} (x_i - y_i)^2}$$

**曼哈顿距离（Manhattan Distance）**：
$$d(x, y) = \sum_{i=1}^{n} |x_i - y_i|$$

**闵可夫斯基距离（Minkowski Distance）**：
$$d(x, y) = \left(\sum_{i=1}^{n} |x_i - y_i|^p\right)^{1/p}$$

#### K值选择

- **K值小**：模型复杂，容易过拟合，对噪声敏感
- **K值大**：模型简单，可能欠拟合，计算量大
- **经验值**：通常选择$\sqrt{n}$（$n$是训练样本数）

### 3.2 KNN实现

```sql
-- KNN分类：计算距离并分类（简化实现）
WITH test_sample AS (
    SELECT 5.0 AS feature1, 3.0 AS feature2  -- 待分类样本
),
distances AS (
    SELECT
        ts.feature1,
        ts.feature2,
        td.id,
        td.class,
        SQRT(POWER(ts.feature1 - td.feature1, 2) +
             POWER(ts.feature2 - td.feature2, 2)) AS distance
    FROM test_sample ts
    CROSS JOIN training_data td
),
k_nearest AS (
    SELECT
        class,
        distance
    FROM distances
    ORDER BY distance
    LIMIT 5  -- K=5
),
class_votes AS (
    SELECT
        class,
        COUNT(*) AS vote_count
    FROM k_nearest
    GROUP BY class
)
SELECT
    class AS predicted_class,
    vote_count,
    vote_count::numeric / SUM(vote_count) OVER () AS confidence
FROM class_votes
ORDER BY vote_count DESC
LIMIT 1;
```

## 4. 分类评估

### 4.1 混淆矩阵

**混淆矩阵（Confusion Matrix）**是评估分类性能的基础工具，展示分类结果的详细情况。

```sql
-- 混淆矩阵（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'predictions') THEN
            RAISE WARNING '表 predictions 不存在，无法计算混淆矩阵';
            RETURN;
        END IF;
        RAISE NOTICE '开始计算混淆矩阵';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '混淆矩阵计算准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
SELECT
    actual_class,
    predicted_class,
    COUNT(*) AS count
FROM predictions
GROUP BY actual_class, predicted_class
ORDER BY actual_class, predicted_class;
```

### 4.2 准确率和召回率实现

**准确率和召回率**是评估分类性能的重要指标。

```sql
-- 准确率和召回率（带错误处理和性能测试）
DO $$
DECLARE
    true_positive BIGINT;
    false_positive BIGINT;
    false_negative BIGINT;
    precision_val NUMERIC;
    recall_val NUMERIC;
    f1_score NUMERIC;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'predictions') THEN
            RAISE WARNING '表 predictions 不存在，无法计算准确率和召回率';
            RETURN;
        END IF;

        SELECT COUNT(*) INTO true_positive
        FROM predictions
        WHERE actual_class = 'positive' AND predicted_class = 'positive';

        SELECT COUNT(*) INTO false_positive
        FROM predictions
        WHERE actual_class = 'negative' AND predicted_class = 'positive';

        SELECT COUNT(*) INTO false_negative
        FROM predictions
        WHERE actual_class = 'positive' AND predicted_class = 'negative';

        precision_val := true_positive::NUMERIC / NULLIF(true_positive + false_positive, 0);
        recall_val := true_positive::NUMERIC / NULLIF(true_positive + false_negative, 0);
        f1_score := 2 * precision_val * recall_val / NULLIF(precision_val + recall_val, 0);

        RAISE NOTICE '分类性能指标:';
        RAISE NOTICE '  精确率 (Precision): %', precision_val;
        RAISE NOTICE '  召回率 (Recall): %', recall_val;
        RAISE NOTICE '  F1分数: %', f1_score;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '准确率和召回率计算失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

---

## 5. 实际应用案例

### 5.1 客户分类

**客户分类**根据客户特征进行分类，识别不同的客户群体。

```sql
-- 客户分类（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'customers') THEN
            RAISE WARNING '表 customers 不存在，无法执行客户分类';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行客户分类';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '客户分类准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

WITH customer_features AS (
    SELECT
        customer_id,
        total_orders,
        total_amount,
        avg_order_amount,
        last_order_days,
        CASE
            WHEN total_orders >= 10 AND total_amount >= 10000 THEN 'VIP'
            WHEN total_orders >= 5 AND total_amount >= 5000 THEN 'Regular'
            WHEN total_orders >= 1 THEN 'Occasional'
            ELSE 'New'
        END AS customer_segment
    FROM customers
)
SELECT
    customer_segment,
    COUNT(*) AS customer_count,
    AVG(total_orders) AS avg_orders,
    AVG(total_amount) AS avg_amount
FROM customer_features
GROUP BY customer_segment
ORDER BY avg_amount DESC;
```

### 5.2 邮件 spam 分类

**场景**：使用朴素贝叶斯分类邮件是否为spam。

```sql
-- 邮件spam分类：朴素贝叶斯实现
WITH email_features AS (
    SELECT
        email_id,
        CASE WHEN email_text LIKE '%free%' THEN 1 ELSE 0 END AS has_free,
        CASE WHEN email_text LIKE '%money%' THEN 1 ELSE 0 END AS has_money,
        CASE WHEN email_text LIKE '%click%' THEN 1 ELSE 0 END AS has_click,
        CASE WHEN email_text LIKE '%urgent%' THEN 1 ELSE 0 END AS has_urgent,
        is_spam
    FROM emails
),
class_probs AS (
    SELECT
        is_spam,
        COUNT(*)::numeric / SUM(COUNT(*)) OVER () AS prior_prob
    FROM email_features
    GROUP BY is_spam
),
feature_probs AS (
    SELECT
        is_spam,
        'has_free' AS feature,
        has_free AS feature_value,
        COUNT(*)::numeric / SUM(COUNT(*)) OVER (PARTITION BY is_spam) AS conditional_prob
    FROM email_features
    GROUP BY is_spam, has_free
    UNION ALL
    SELECT
        is_spam,
        'has_money' AS feature,
        has_money AS feature_value,
        COUNT(*)::numeric / SUM(COUNT(*)) OVER (PARTITION BY is_spam) AS conditional_prob
    FROM email_features
    GROUP BY is_spam, has_money
),
prediction AS (
    SELECT
        ef.email_id,
        cp.is_spam,
        cp.prior_prob,
        EXP(SUM(LN(NULLIF(fp.conditional_prob, 0)))) AS likelihood,
        cp.prior_prob * EXP(SUM(LN(NULLIF(fp.conditional_prob, 0)))) AS posterior_prob
    FROM email_features ef
    CROSS JOIN class_probs cp
    LEFT JOIN feature_probs fp ON cp.is_spam = fp.is_spam
        AND fp.feature_value = CASE
            WHEN fp.feature = 'has_free' THEN ef.has_free
            WHEN fp.feature = 'has_money' THEN ef.has_money
            ELSE 0
        END
    GROUP BY ef.email_id, cp.is_spam, cp.prior_prob
)
SELECT
    email_id,
    is_spam AS predicted_class,
    posterior_prob,
    ROW_NUMBER() OVER (PARTITION BY email_id ORDER BY posterior_prob DESC) AS rank
FROM prediction
WHERE rank = 1;
```

### 5.3 产品分类

**场景**：使用决策树规则对产品进行分类。

```sql
-- 产品分类：决策树规则实现
WITH product_features AS (
    SELECT
        product_id,
        price,
        weight,
        category,
        CASE
            WHEN price < 50 AND weight < 1 THEN 'Low-end Light'
            WHEN price < 50 AND weight >= 1 THEN 'Low-end Heavy'
            WHEN price >= 50 AND price < 200 AND weight < 1 THEN 'Mid-range Light'
            WHEN price >= 50 AND price < 200 AND weight >= 1 THEN 'Mid-range Heavy'
            WHEN price >= 200 AND weight < 1 THEN 'High-end Light'
            ELSE 'High-end Heavy'
        END AS predicted_category
    FROM products
)
SELECT
    predicted_category,
    COUNT(*) AS product_count,
    AVG(price) AS avg_price,
    AVG(weight) AS avg_weight
FROM product_features
GROUP BY predicted_category
ORDER BY avg_price DESC;
```

### 5.4 用户行为分类

**场景**：使用KNN分类用户行为模式。

```sql
-- 用户行为分类：KNN实现
WITH user_features AS (
    SELECT
        user_id,
        login_frequency,
        page_views,
        purchase_count,
        user_segment
    FROM user_behavior
),
test_user AS (
    SELECT 10 AS login_frequency, 50 AS page_views, 2 AS purchase_count
),
distances AS (
    SELECT
        uf.user_id,
        uf.user_segment,
        SQRT(
            POWER(tu.login_frequency - uf.login_frequency, 2) +
            POWER(tu.page_views - uf.page_views, 2) +
            POWER(tu.purchase_count - uf.purchase_count, 2)
        ) AS distance
    FROM test_user tu
    CROSS JOIN user_features uf
),
k_nearest AS (
    SELECT
        user_segment,
        distance
    FROM distances
    ORDER BY distance
    LIMIT 5  -- K=5
),
segment_votes AS (
    SELECT
        user_segment,
        COUNT(*) AS vote_count,
        AVG(distance) AS avg_distance
    FROM k_nearest
    GROUP BY user_segment
)
SELECT
    user_segment AS predicted_segment,
    vote_count,
    ROUND(avg_distance::numeric, 2) AS avg_distance,
    vote_count::numeric / SUM(vote_count) OVER () AS confidence
FROM segment_votes
ORDER BY vote_count DESC
LIMIT 1;
```

### 5.5 分类模型评估

**场景**：全面评估分类模型的性能。

```sql
-- 分类模型评估：综合评估指标
WITH classification_results AS (
    SELECT
        actual_class,
        predicted_class,
        COUNT(*) AS count
    FROM predictions
    GROUP BY actual_class, predicted_class
),
class_metrics AS (
    SELECT
        cr.actual_class,
        cr.predicted_class,
        cr.count,
        SUM(cr.count) OVER (PARTITION BY cr.actual_class) AS actual_total,
        SUM(cr.count) OVER (PARTITION BY cr.predicted_class) AS predicted_total,
        SUM(cr.count) OVER () AS grand_total
    FROM classification_results cr
),
metrics AS (
    SELECT
        actual_class,
        predicted_class,
        count,
        actual_total,
        predicted_total,
        grand_total,
        CASE WHEN actual_class = predicted_class THEN count ELSE 0 END AS correct_count,
        count::numeric / NULLIF(actual_total, 0) AS recall,
        count::numeric / NULLIF(predicted_total, 0) AS precision
    FROM class_metrics
)
SELECT
    actual_class,
    predicted_class,
    count,
    ROUND(recall::numeric, 4) AS recall,
    ROUND(precision::numeric, 4) AS precision,
    CASE
        WHEN actual_class = predicted_class THEN 'Correct'
        ELSE 'Incorrect'
    END AS classification_status
FROM metrics
ORDER BY actual_class, predicted_class;
```

---

## 6. 算法性能对比与优化

### 6.1 分类算法对比

| 算法 | 训练时间 | 预测时间 | 内存需求 | 可解释性 | 适用场景 |
|------|---------|---------|---------|---------|---------|
| **朴素贝叶斯** | 快 | 快 | 低 | 中 | 文本分类、高维数据 |
| **决策树** | 中 | 快 | 中 | 高 | 规则提取、特征选择 |
| **KNN** | 无 | 慢 | 高 | 低 | 小数据集、非线性数据 |
| **逻辑回归** | 快 | 快 | 低 | 中 | 线性可分数据、概率输出 |

### 6.2 性能优化建议

1. **特征选择**：
   - 选择相关特征
   - 去除冗余特征
   - 特征标准化

2. **数据预处理**：
   - 处理缺失值
   - 处理异常值
   - 数据平衡（处理类别不平衡）

3. **算法选择**：
   - 小数据集：KNN
   - 高维数据：朴素贝叶斯
   - 需要解释：决策树
   - 线性数据：逻辑回归

4. **参数调优**：
   - KNN：选择合适的K值
   - 决策树：控制树深度
   - 朴素贝叶斯：平滑参数

### 6.3 常见问题与解决方案

**问题1**：分类准确率低

- **解决方案**：特征工程、数据清洗、算法选择、参数调优

**问题2**：过拟合

- **解决方案**：增加训练数据、简化模型、正则化、交叉验证

**问题3**：类别不平衡

- **解决方案**：重采样、代价敏感学习、调整阈值

**问题4**：计算时间过长

- **解决方案**：特征选择、数据采样、算法优化、并行处理

---

## 7. 最佳实践

### 7.1 数据准备

1. **特征工程**：
   - 选择相关特征
   - 特征标准化
   - 处理缺失值

2. **数据分割**：
   - 训练集：70-80%
   - 验证集：10-15%
   - 测试集：10-15%

3. **数据质量**：
   - 数据清洗
   - 异常值处理
   - 数据平衡

### 7.2 模型训练

1. **算法选择**：
   - 根据数据特征选择算法
   - 考虑业务需求
   - 平衡准确率和可解释性

2. **参数调优**：
   - 使用交叉验证
   - 网格搜索
   - 早停策略

3. **模型评估**：
   - 使用多个指标
   - 混淆矩阵分析
   - 特征重要性分析

### 7.3 模型部署

1. **性能监控**：
   - 监控准确率
   - 监控预测延迟
   - 监控数据漂移

2. **模型更新**：
   - 定期重新训练
   - 增量学习
   - A/B测试

3. **可解释性**：
   - 提供预测原因
   - 特征重要性
   - 决策路径

### 7.4 SQL实现注意事项

1. **性能优化**：
   - 创建适当索引
   - 使用物化视图
   - 批量处理

2. **数值稳定性**：
   - 使用对数避免下溢
   - 处理除零错误
   - 数值精度控制

3. **代码可读性**：
   - 清晰的注释
   - 模块化设计
   - 错误处理

---

## 📚 参考资源

### 学术文献

1. **Mitchell, T. M. (1997)**: "Machine Learning", *McGraw-Hill* - 第3章 决策树学习

2. **Bishop, C. M. (2006)**: "Pattern Recognition and Machine Learning", *Springer* - 第4章 线性分类模型

3. **《机器学习》**（周志华, 2016）- 第4章 决策树、第7章 贝叶斯分类器

4. **《统计学习方法》**（李航, 2012）- 第5章 决策树、第4章 朴素贝叶斯

### PostgreSQL官方文档

- **窗口函数**: <https://www.postgresql.org/docs/current/tutorial-window.html>
- **聚合函数**: <https://www.postgresql.org/docs/current/functions-aggregate.html>
- **数组操作**: <https://www.postgresql.org/docs/current/functions-array.html>

### 在线资源

- **分类算法**: <https://scikit-learn.org/stable/supervised_learning.html>
- **朴素贝叶斯**: <https://en.wikipedia.org/wiki/Naive_Bayes_classifier>
- **决策树**: <https://en.wikipedia.org/wiki/Decision_tree>
- **KNN**: <https://en.wikipedia.org/wiki/K-nearest_neighbors_algorithm>

### 相关算法

- **聚类分析**：无监督学习
- **关联规则挖掘**：发现关联关系
- **逻辑回归**：线性分类
- **支持向量机**：非线性分类

---

**最后更新**: 2025年1月
**维护者**: PostgreSQL Modern Team
**文档状态**: ✅ 已完成
