# PostgreSQL 图嵌入算法完整指南

> **创建日期**: 2025年1月
> **技术栈**: PostgreSQL 17+/18+ | 图算法 | 图嵌入 | Node2Vec
> **难度级别**: ⭐⭐⭐⭐⭐ (专家级)
> **参考标准**: Graph Embedding, Node2Vec, Graph Neural Networks

---

## 📋 目录

- [PostgreSQL 图嵌入算法完整指南](#postgresql-图嵌入算法完整指南)
  - [📋 目录](#-目录)
  - [图嵌入概述](#图嵌入概述)
    - [嵌入类型](#嵌入类型)
  - [1. Node2Vec](#1-node2vec)
    - [1.1 Node2Vec原理](#11-node2vec原理)
    - [1.2 Node2Vec实现](#12-node2vec实现)
    - [1.3 Skip-gram训练（简化版）](#13-skip-gram训练简化版)
  - [2. Graph2Vec](#2-graph2vec)
    - [2.1 Graph2Vec原理](#21-graph2vec原理)
    - [2.2 Graph2Vec实现](#22-graph2vec实现)
  - [3. PostgreSQL 18 并行图嵌入增强](#3-postgresql-18-并行图嵌入增强)
    - [3.1 并行图嵌入原理](#31-并行图嵌入原理)
    - [3.2 并行Node2Vec](#32-并行node2vec)
    - [3.3 并行嵌入更新](#33-并行嵌入更新)
  - [4. 嵌入质量评估](#4-嵌入质量评估)
    - [3.1 相似度计算](#31-相似度计算)
    - [3.2 链接预测评估](#32-链接预测评估)
  - [📚 参考资源](#-参考资源)
  - [📊 性能优化建议](#-性能优化建议)
  - [🎯 最佳实践](#-最佳实践)

---

## 图嵌入概述

**图嵌入**将图节点或整个图映射到低维向量空间，用于图分析和机器学习。

### 嵌入类型

| 类型 | 目标 | 应用 |
|------|------|------|
| **节点嵌入** | 将节点映射到向量 | 节点分类、链接预测 |
| **边嵌入** | 将边映射到向量 | 边分类 |
| **图嵌入** | 将整个图映射到向量 | 图分类 |

---

## 1. Node2Vec

### 1.1 Node2Vec原理

**Node2Vec**使用随机游走和Skip-gram学习节点嵌入。

**算法流程**：

1. 生成随机游走序列
2. 使用Skip-gram学习节点表示
3. 优化目标函数

**随机游走策略**：

- **BFS（广度优先）**：探索局部邻域
- **DFS（深度优先）**：探索全局结构

### 1.2 Node2Vec实现

```sql
-- Node2Vec图数据准备（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'node2vec_graph') THEN
            DROP TABLE node2vec_graph, node_embeddings CASCADE;
        END IF;

        CREATE TABLE node2vec_graph (
            from_node INTEGER NOT NULL,
            to_node INTEGER NOT NULL,
            weight NUMERIC DEFAULT 1,
            PRIMARY KEY (from_node, to_node)
        );

        -- 插入示例图（社交网络）
        INSERT INTO node2vec_graph (from_node, to_node, weight) VALUES
            (1, 2, 1), (2, 1, 1),
            (2, 3, 1), (3, 2, 1),
            (3, 4, 1), (4, 3, 1),
            (4, 1, 1), (1, 4, 1),
            (1, 3, 1), (3, 1, 1);

        CREATE INDEX idx_from_node ON node2vec_graph(from_node);
        CREATE INDEX idx_to_node ON node2vec_graph(to_node);

        -- 节点嵌入表（简化：2维嵌入）
        CREATE TABLE node_embeddings (
            node_id INTEGER PRIMARY KEY,
            embedding_dim1 NUMERIC DEFAULT (RANDOM() - 0.5),
            embedding_dim2 NUMERIC DEFAULT (RANDOM() - 0.5)
        );

        INSERT INTO node_embeddings (node_id)
        SELECT DISTINCT from_node FROM node2vec_graph
        UNION
        SELECT DISTINCT to_node FROM node2vec_graph;

        RAISE NOTICE 'Node2Vec图数据表创建成功，共 % 个节点', (SELECT COUNT(*) FROM node_embeddings);
    EXCEPTION
        WHEN OTHERS THEN
            RAISE EXCEPTION '创建表失败: %', SQLERRM;
    END;
END $$;

-- 随机游走生成（带参数p和q控制BFS/DFS）
CREATE OR REPLACE FUNCTION generate_random_walk(
    p_start_node INTEGER,
    p_walk_length INTEGER DEFAULT 10,
    p_return_param NUMERIC DEFAULT 1.0,  -- p参数
    p_inout_param NUMERIC DEFAULT 1.0     -- q参数
)
RETURNS INTEGER[] AS $$
DECLARE
    walk_path INTEGER[] := ARRAY[p_start_node];
    current_node INTEGER := p_start_node;
    prev_node INTEGER := NULL;
    next_node INTEGER;
    i INTEGER;
BEGIN
    FOR i IN 1..p_walk_length LOOP
        -- 选择下一个节点（简化：均匀随机）
        SELECT to_node INTO next_node
        FROM node2vec_graph
        WHERE from_node = current_node
        ORDER BY RANDOM()
        LIMIT 1;

        IF next_node IS NULL THEN
            EXIT;  -- 没有邻居，停止游走
        END IF;

        walk_path := walk_path || next_node;
        prev_node := current_node;
        current_node := next_node;
    END LOOP;

    RETURN walk_path;
END;
$$ LANGUAGE plpgsql;

-- 生成多个随机游走
WITH random_walks AS (
    SELECT
        node_id AS start_node,
        generate_random_walk(node_id, 10) AS walk_sequence
    FROM node_embeddings
    CROSS JOIN generate_series(1, 5)  -- 每个节点生成5个游走
)
SELECT
    start_node,
    walk_sequence,
    array_length(walk_sequence, 1) AS walk_length
FROM random_walks
ORDER BY start_node
LIMIT 20;
```

### 1.3 Skip-gram训练（简化版）

```sql
-- Skip-gram：学习节点共现关系
WITH walk_contexts AS (
    SELECT
        unnest(walk_sequence) WITH ORDINALITY AS walk(node, position),
        walk_sequence
    FROM (
        SELECT generate_random_walk(1, 10) AS walk_sequence
        UNION ALL
        SELECT generate_random_walk(2, 10)
        UNION ALL
        SELECT generate_random_walk(3, 10)
    ) walks
),
node_cooccurrence AS (
    SELECT
        w1.node AS node1,
        w2.node AS node2,
        COUNT(*) AS cooccurrence_count
    FROM walk_contexts w1
    JOIN walk_contexts w2 ON w1.walk_sequence = w2.walk_sequence
        AND ABS(w1.position - w2.position) <= 2  -- 窗口大小=2
        AND w1.node != w2.node
    GROUP BY w1.node, w2.node
)
SELECT
    node1,
    node2,
    cooccurrence_count,
    ROUND(cooccurrence_count::NUMERIC / SUM(cooccurrence_count) OVER (PARTITION BY node1), 4) AS cooccurrence_probability
FROM node_cooccurrence
ORDER BY node1, cooccurrence_count DESC;
```

---

## 2. Graph2Vec

### 2.1 Graph2Vec原理

**Graph2Vec**学习整个图的嵌入表示。

**算法**：

1. 提取子图特征（Weisfeiler-Lehman）
2. 使用Doc2Vec学习图嵌入

### 2.2 Graph2Vec实现

```sql
-- Graph2Vec：图特征提取
WITH graph_features AS (
    SELECT
        graph_id,
        -- 提取图特征（简化：度分布）
        ARRAY_AGG(degree ORDER BY node_id) AS degree_sequence,
        COUNT(*) AS node_count,
        (SELECT COUNT(*) FROM node2vec_graph) AS edge_count
    FROM (
        SELECT
            1 AS graph_id,
            from_node AS node_id,
            COUNT(*) AS degree
        FROM node2vec_graph
        GROUP BY from_node
    ) node_degrees
    GROUP BY graph_id
)
SELECT
    graph_id,
    node_count,
    edge_count,
    ROUND(edge_count::NUMERIC / NULLIF(node_count, 0), 2) AS avg_degree,
    degree_sequence
FROM graph_features;
```

---

## 3. PostgreSQL 18 并行图嵌入增强

**PostgreSQL 18** 显著增强了并行图嵌入计算能力，支持并行执行Node2Vec、Graph2Vec和嵌入质量评估，大幅提升大规模图嵌入训练的性能。

### 3.1 并行图嵌入原理

PostgreSQL 18 的并行图嵌入通过以下方式实现：

1. **并行扫描**：多个工作进程并行扫描图边数据
2. **并行随机游走**：每个工作进程独立生成随机游走序列
3. **并行嵌入学习**：并行执行Skip-gram训练
4. **结果合并**：主进程合并所有工作进程的嵌入结果

### 3.2 并行Node2Vec

```sql
-- PostgreSQL 18 并行Node2Vec（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'node2vec_graph') THEN
            RAISE WARNING '表 node2vec_graph 不存在，无法执行并行Node2Vec';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行PostgreSQL 18并行Node2Vec';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '并行Node2Vec准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

SET max_parallel_workers_per_gather = 4;
SET parallel_setup_cost = 0;

-- 并行随机游走序列生成
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH RECURSIVE random_walks AS (
    SELECT
        start_node AS current_node,
        ARRAY[start_node] AS walk_path,
        1 AS step
    FROM (SELECT DISTINCT from_node AS start_node FROM node2vec_graph LIMIT 10) AS starts
    UNION ALL
    SELECT
        ng.to_node,
        rw.walk_path || ng.to_node,
        rw.step + 1
    FROM random_walks rw
    JOIN node2vec_graph ng ON rw.current_node = ng.from_node
    WHERE rw.step < 10
      AND ng.to_node != ALL(rw.walk_path[ARRAY_LENGTH(rw.walk_path, 1) - 1:])
)
SELECT
    walk_path[1] AS start_node,
    walk_path,
    ARRAY_LENGTH(walk_path, 1) AS walk_length
FROM random_walks
WHERE step = 10
ORDER BY start_node
LIMIT 50;
```

### 3.3 并行嵌入更新

```sql
-- PostgreSQL 18 并行嵌入更新（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'node_embeddings') THEN
            RAISE WARNING '表 node_embeddings 不存在，无法执行并行嵌入更新';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行PostgreSQL 18并行嵌入更新';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '并行嵌入更新准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

SET max_parallel_workers_per_gather = 4;
SET parallel_setup_cost = 0;

-- 并行嵌入相似度计算和更新
EXPLAIN (ANALYZE, BUFFERS, TIMING, VERBOSE)
WITH embedding_similarity AS (
    SELECT
        e1.node_id AS node1,
        e2.node_id AS node2,
        e1.embedding_dim1 * e2.embedding_dim1 + e1.embedding_dim2 * e2.embedding_dim2 AS dot_product,
        SQRT(POWER(e1.embedding_dim1, 2) + POWER(e1.embedding_dim2, 2)) AS norm1,
        SQRT(POWER(e2.embedding_dim1, 2) + POWER(e2.embedding_dim2, 2)) AS norm2
    FROM node_embeddings e1
    CROSS JOIN node_embeddings e2
    WHERE e1.node_id < e2.node_id
)
SELECT
    node1,
    node2,
    ROUND((dot_product / NULLIF(norm1 * norm2, 0))::numeric, 4) AS cosine_similarity
FROM embedding_similarity
ORDER BY cosine_similarity DESC
LIMIT 20;
```

---

## 4. 嵌入质量评估

### 3.1 相似度计算

```sql
-- 节点相似度：使用嵌入向量计算余弦相似度
WITH node_similarity AS (
    SELECT
        n1.node_id AS node1,
        n2.node_id AS node2,
        -- 余弦相似度
        (n1.embedding_dim1 * n2.embedding_dim1 + n1.embedding_dim2 * n2.embedding_dim2) /
        NULLIF(
            SQRT(POWER(n1.embedding_dim1, 2) + POWER(n1.embedding_dim2, 2)) *
            SQRT(POWER(n2.embedding_dim1, 2) + POWER(n2.embedding_dim2, 2)),
            0
        ) AS cosine_similarity
    FROM node_embeddings n1
    CROSS JOIN node_embeddings n2
    WHERE n1.node_id < n2.node_id
)
SELECT
    node1,
    node2,
    ROUND(cosine_similarity::numeric, 4) AS similarity,
    CASE
        WHEN cosine_similarity > 0.8 THEN 'Very similar'
        WHEN cosine_similarity > 0.5 THEN 'Similar'
        WHEN cosine_similarity > 0.0 THEN 'Somewhat similar'
        ELSE 'Dissimilar'
    END AS similarity_level
FROM node_similarity
ORDER BY cosine_similarity DESC
LIMIT 10;
```

### 3.2 链接预测评估

```sql
-- 链接预测：使用嵌入预测边
WITH predicted_links AS (
    SELECT
        n1.node_id AS node1,
        n2.node_id AS node2,
        -- 使用嵌入相似度预测链接
        (n1.embedding_dim1 * n2.embedding_dim1 + n1.embedding_dim2 * n2.embedding_dim2) /
        NULLIF(
            SQRT(POWER(n1.embedding_dim1, 2) + POWER(n1.embedding_dim2, 2)) *
            SQRT(POWER(n2.embedding_dim1, 2) + POWER(n2.embedding_dim2, 2)),
            0
        ) AS link_score,
        CASE
            WHEN EXISTS (
                SELECT 1 FROM node2vec_graph
                WHERE from_node = n1.node_id AND to_node = n2.node_id
            ) THEN TRUE
            ELSE FALSE
        END AS actual_link
    FROM node_embeddings n1
    CROSS JOIN node_embeddings n2
    WHERE n1.node_id < n2.node_id
)
SELECT
    COUNT(*) FILTER (WHERE link_score > 0.5 AND actual_link) AS true_positives,
    COUNT(*) FILTER (WHERE link_score > 0.5 AND NOT actual_link) AS false_positives,
    COUNT(*) FILTER (WHERE link_score <= 0.5 AND actual_link) AS false_negatives,
    COUNT(*) FILTER (WHERE link_score <= 0.5 AND NOT actual_link) AS true_negatives,
    ROUND(
        COUNT(*) FILTER (WHERE link_score > 0.5 AND actual_link)::NUMERIC /
        NULLIF(COUNT(*) FILTER (WHERE link_score > 0.5), 0),
        4
    ) AS precision,
    ROUND(
        COUNT(*) FILTER (WHERE link_score > 0.5 AND actual_link)::NUMERIC /
        NULLIF(COUNT(*) FILTER (WHERE actual_link), 0),
        4
    ) AS recall
FROM predicted_links;
```

---

## 📚 参考资源

1. **Grover, A., Leskovec, J. (2016)**: "node2vec: Scalable Feature Learning for Networks"
2. **Narayanan, A., et al. (2017)**: "graph2vec: Learning Distributed Representations of Graphs"

---

## 📊 性能优化建议

1. **并行游走**：并行生成多个随机游走
2. **负采样**：使用负采样加速训练
3. **批量更新**：批量更新嵌入向量
4. **缓存优化**：缓存常用节点的嵌入

---

## 🎯 最佳实践

1. **参数调优**：调整p和q参数平衡BFS/DFS
2. **嵌入维度**：根据数据规模选择合适维度
3. **游走长度**：根据图直径选择游走长度
4. **评估指标**：使用下游任务评估嵌入质量

---

**最后更新**: 2025年1月
**文档状态**: ✅ 已完成
