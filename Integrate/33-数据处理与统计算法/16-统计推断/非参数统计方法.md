# PostgreSQL 非参数统计方法完整指南

> **创建日期**: 2025年1月
> **技术栈**: PostgreSQL 17+/18+ | 统计推断 | 非参数统计 | 秩检验
> **难度级别**: ⭐⭐⭐⭐ (高级)
> **参考标准**: Nonparametric Statistical Methods (Hollander et al.), Statistical Inference

---

## 📋 目录

- [PostgreSQL 非参数统计方法完整指南](#postgresql-非参数统计方法完整指南)
  - [📋 目录](#-目录)
  - [非参数统计概述](#非参数统计概述)
    - [理论基础](#理论基础)
    - [优势特点](#优势特点)
    - [应用场景](#应用场景)
  - [1. 秩检验](#1-秩检验)
    - [1.1 Wilcoxon符号秩检验](#11-wilcoxon符号秩检验)
  - [📚 参考资源](#-参考资源)

---

## 非参数统计概述

**非参数统计**不依赖于总体分布假设，适用于分布未知或非正态的数据。

### 理论基础

非参数方法基于**秩**、**符号**或**分布函数**，而非参数值本身。

### 优势特点

| 优势 | 说明 |
|------|------|
| **无分布假设** | 不要求正态分布 |
| **稳健性** | 对异常值不敏感 |
| **适用性广** | 适用于各种数据类型 |

### 应用场景

| 应用领域 | 具体应用 |
|---------|---------|
| **医学** | 临床试验、疗效评估 |
| **社会科学** | 调查数据分析 |
| **质量控制** | 非正态过程控制 |

---

## 1. 秩检验

### 1.1 Wilcoxon符号秩检验

**Wilcoxon符号秩检验**用于配对样本的非参数检验。

```sql
-- 非参数统计数据准备（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'nonparametric_data') THEN
            RAISE WARNING '表 nonparametric_data 已存在，先删除';
            DROP TABLE nonparametric_data CASCADE;
        END IF;

        CREATE TABLE nonparametric_data (
            id SERIAL PRIMARY KEY,
            before_value NUMERIC NOT NULL,
            after_value NUMERIC NOT NULL
        );

        INSERT INTO nonparametric_data (before_value, after_value) VALUES
            (10, 12), (15, 18), (20, 19), (25, 28), (30, 32);

        RAISE NOTICE '表 nonparametric_data 创建成功';
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE WARNING '表 nonparametric_data 已存在';
        WHEN OTHERS THEN
            RAISE EXCEPTION '创建表失败: %', SQLERRM;
    END;
END $$;

-- Wilcoxon符号秩检验
WITH differences AS (
    SELECT
        id,
        after_value - before_value AS difference,
        ABS(after_value - before_value) AS abs_difference
    FROM nonparametric_data
),
ranked_differences AS (
    SELECT
        id,
        difference,
        abs_difference,
        ROW_NUMBER() OVER (ORDER BY abs_difference) AS rank
    FROM differences
    WHERE difference != 0
),
signed_ranks AS (
    SELECT
        SUM(CASE WHEN difference > 0 THEN rank ELSE 0 END) AS positive_rank_sum,
        SUM(CASE WHEN difference < 0 THEN rank ELSE 0 END) AS negative_rank_sum,
        COUNT(*) AS n
    FROM ranked_differences
),
wilcoxon_statistic AS (
    SELECT
        LEAST(positive_rank_sum, negative_rank_sum) AS w_statistic,
        n
    FROM signed_ranks
)
SELECT
    w_statistic,
    n,
    CASE
        WHEN w_statistic <= 2 THEN 'Reject H0 (p < 0.05, n=5)'
        ELSE 'Fail to reject H0'
    END AS conclusion
FROM wilcoxon_statistic;
```

---

## 📚 参考资源

1. **Hollander, M., et al. (2013)**: "Nonparametric Statistical Methods"

---

**最后更新**: 2025年1月
**文档状态**: ✅ 已完成
