# PostgreSQL 时间序列预测完整指南

> **创建日期**: 2025年1月
> **技术栈**: PostgreSQL 17+/18+ | 时间序列预测 | 趋势预测
> **难度级别**: ⭐⭐⭐⭐ (高级)

---

## 📋 目录

- [PostgreSQL 时间序列预测完整指南](#postgresql-时间序列预测完整指南)
  - [📋 目录](#-目录)
  - [时间序列预测概述](#时间序列预测概述)
    - [理论基础](#理论基础)
      - [时间序列的组成](#时间序列的组成)
      - [预测方法分类](#预测方法分类)
      - [预测精度评估](#预测精度评估)
    - [核心预测方法](#核心预测方法)
  - [1. 线性趋势预测](#1-线性趋势预测)
    - [1.1 线性回归预测](#11-线性回归预测)
  - [2. 指数平滑预测](#2-指数平滑预测)
    - [2.1 单指数平滑原理](#21-单指数平滑原理)
      - [数学定义](#数学定义)
      - [参数选择](#参数选择)
      - [初始值选择](#初始值选择)
    - [2.2 单指数平滑实现](#22-单指数平滑实现)
  - [3. 移动平均预测](#3-移动平均预测)
    - [3.1 移动平均原理](#31-移动平均原理)
      - [数学定义](#数学定义-1)
      - [加权移动平均（Weighted Moving Average, WMA）](#加权移动平均weighted-moving-average-wma)
      - [指数加权移动平均（Exponentially Weighted Moving Average, EWMA）](#指数加权移动平均exponentially-weighted-moving-average-ewma)
      - [窗口大小选择](#窗口大小选择)
    - [3.2 移动平均预测实现](#32-移动平均预测实现)
  - [4. 高级预测方法](#4-高级预测方法)
    - [4.1 Holt-Winters方法](#41-holt-winters方法)
      - [加法模型](#加法模型)
    - [4.2 ARIMA模型概念](#42-arima模型概念)
  - [5. 实际应用案例](#5-实际应用案例)
    - [5.1 销售预测](#51-销售预测)
    - [5.2 需求预测](#52-需求预测)
    - [5.3 库存优化](#53-库存优化)
    - [5.4 预测精度评估](#54-预测精度评估)
    - [5.5 季节性预测](#55-季节性预测)
  - [6. 算法性能对比与优化](#6-算法性能对比与优化)
    - [6.1 预测方法对比](#61-预测方法对比)
    - [6.2 性能优化建议](#62-性能优化建议)
    - [6.3 常见问题与解决方案](#63-常见问题与解决方案)
  - [7. 最佳实践](#7-最佳实践)
    - [7.1 数据准备](#71-数据准备)
    - [7.2 模型选择](#72-模型选择)
    - [7.3 预测监控](#73-预测监控)
    - [7.4 SQL实现注意事项](#74-sql实现注意事项)
  - [📚 参考资源](#-参考资源)
    - [学术文献](#学术文献)
    - [在线资源](#在线资源)
    - [相关算法](#相关算法)

---

## 时间序列预测概述

**时间序列预测（Time Series Forecasting）**是基于历史数据预测未来值的重要技术，广泛应用于销售预测、需求规划、库存管理、金融分析等领域。PostgreSQL可以通过SQL实现多种预测算法。

### 理论基础

#### 时间序列的组成

时间序列通常可以分解为以下组成部分：

$$Y_t = T_t + S_t + C_t + I_t$$

其中：

- **$T_t$（Trend）**：长期趋势，数据的长期变化方向
- **$S_t$（Seasonality）**：季节性，周期性的重复模式
- **$C_t$（Cyclical）**：周期性，非固定周期的波动
- **$I_t$（Irregular）**：不规则波动，随机噪声

#### 预测方法分类

1. **定性方法**：基于专家判断、市场调研
2. **定量方法**：
   - **时间序列方法**：基于历史数据模式（ARIMA、指数平滑等）
   - **因果方法**：考虑外部因素（回归分析、机器学习）

#### 预测精度评估

**常用评估指标**：

- **MAE（Mean Absolute Error）**：$\frac{1}{n}\sum_{i=1}^{n}|y_i - \hat{y}_i|$
- **RMSE（Root Mean Squared Error）**：$\sqrt{\frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2}$
- **MAPE（Mean Absolute Percentage Error）**：$\frac{100}{n}\sum_{i=1}^{n}\left|\frac{y_i - \hat{y}_i}{y_i}\right|$

### 核心预测方法

| 方法 | 用途 | 复杂度 | 优点 | 缺点 |
|------|------|--------|------|------|
| **线性回归** | 趋势预测 | $O(n)$ | 简单、可解释 | 假设线性关系 |
| **指数平滑** | 短期预测 | $O(n)$ | 自适应、平滑 | 不处理趋势和季节性 |
| **移动平均** | 平滑预测 | $O(n \cdot w)$ | 简单、稳健 | 滞后、不预测趋势 |
| **ARIMA** | 复杂时间序列 | $O(n^2)$ | 处理趋势和季节性 | 参数调优复杂 |
| **Holt-Winters** | 趋势+季节性 | $O(n)$ | 处理趋势和季节性 | 参数敏感 |

---

## 1. 线性趋势预测

### 1.1 线性回归预测

**线性趋势预测**使用线性回归预测未来值。

```sql
-- 线性趋势预测（带错误处理和性能测试）
DO $$
DECLARE
    slope NUMERIC;
    intercept NUMERIC;
    n_val BIGINT;
    avg_x NUMERIC;
    avg_y NUMERIC;
    sum_xy NUMERIC;
    sum_x2 NUMERIC;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'time_series') THEN
            RAISE WARNING '表 time_series 不存在，无法执行线性趋势预测';
            RETURN;
        END IF;

        SELECT
            COUNT(*),
            AVG(EXTRACT(EPOCH FROM date)),
            AVG(value),
            SUM(EXTRACT(EPOCH FROM date) * value),
            SUM(EXTRACT(EPOCH FROM date) * EXTRACT(EPOCH FROM date))
        INTO n_val, avg_x, avg_y, sum_xy, sum_x2
        FROM time_series
        WHERE value IS NOT NULL;

        slope := (sum_xy - n_val * avg_x * avg_y) / NULLIF(sum_x2 - n_val * avg_x * avg_x, 0);
        intercept := avg_y - slope * avg_x;

        RAISE NOTICE '线性回归参数:';
        RAISE NOTICE '  斜率: %', slope;
        RAISE NOTICE '  截距: %', intercept;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '线性趋势预测失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 预测未来值
WITH regression AS (
    SELECT
        (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) AS slope,
        avg_y - (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) * avg_x AS intercept
    FROM (
        SELECT
            COUNT(*) AS n,
            AVG(EXTRACT(EPOCH FROM date)) AS avg_x,
            AVG(value) AS avg_y,
            SUM(EXTRACT(EPOCH FROM date) * value) AS sum_xy,
            SUM(EXTRACT(EPOCH FROM date) * EXTRACT(EPOCH FROM date)) AS sum_x2
        FROM time_series
        WHERE value IS NOT NULL
    ) AS stats
),
future_dates AS (
    SELECT generate_series(
        (SELECT MAX(date) FROM time_series) + INTERVAL '1 day',
        (SELECT MAX(date) FROM time_series) + INTERVAL '30 days',
        '1 day'::interval
    )::date AS date
)
SELECT
    fd.date,
    r.slope * EXTRACT(EPOCH FROM fd.date) + r.intercept AS predicted_value
FROM future_dates fd
CROSS JOIN regression r
ORDER BY fd.date;
```

---

## 2. 指数平滑预测

### 2.1 单指数平滑原理

**单指数平滑（Simple Exponential Smoothing, SES）**适用于没有明显趋势和季节性的时间序列。

#### 数学定义

**平滑公式**：
$$S_t = \alpha Y_t + (1-\alpha) S_{t-1}$$

其中：

- $S_t$ 是时刻 $t$ 的平滑值
- $Y_t$ 是时刻 $t$ 的观测值
- $\alpha \in [0,1]$ 是平滑参数（平滑系数）

**预测公式**：
$$\hat{Y}_{t+h} = S_t$$

其中 $h$ 是预测步长。

#### 参数选择

- **$\alpha$ 接近1**：对近期数据权重高，响应快，但波动大
- **$\alpha$ 接近0**：对历史数据权重高，平滑效果好，但响应慢
- **$\alpha$ 的选择**：通常通过交叉验证或最小化预测误差确定

#### 初始值选择

- **$S_1 = Y_1$**：使用第一个观测值
- **$S_1 = \bar{Y}$**：使用前几个观测值的均值

### 2.2 单指数平滑实现

```sql
-- 单指数平滑预测（带错误处理和性能测试）
DO $$
DECLARE
    alpha NUMERIC := 0.3;
    last_value NUMERIC;
    last_smoothed NUMERIC;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'time_series') THEN
            RAISE WARNING '表 time_series 不存在，无法执行指数平滑预测';
            RETURN;
        END IF;

        SELECT value INTO last_value
        FROM time_series
        ORDER BY date DESC
        LIMIT 1;

        RAISE NOTICE '指数平滑预测，平滑系数: %, 最后值: %', alpha, last_value;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '指数平滑预测失败: %', SQLERRM;
            RAISE;
    END;
END $$;

WITH smoothed_values AS (
    SELECT
        date,
        value,
        value AS smoothed,
        0.3 AS alpha
    FROM time_series
    WHERE date = (SELECT MIN(date) FROM time_series)
    UNION ALL
    SELECT
        t.date,
        t.value,
        s.smoothed * (1 - s.alpha) + t.value * s.alpha AS smoothed,
        s.alpha
    FROM time_series t
    JOIN smoothed_values s ON t.date = s.date + INTERVAL '1 day'
)
SELECT * FROM smoothed_values ORDER BY date;
```

---

## 3. 移动平均预测

### 3.1 移动平均原理

**移动平均（Moving Average, MA）**通过计算最近 $w$ 个观测值的平均值来预测未来值。

#### 数学定义

**简单移动平均（Simple Moving Average, SMA）**：
$$MA_t = \frac{1}{w}\sum_{i=t-w+1}^{t} Y_i$$

**预测公式**：
$$\hat{Y}_{t+h} = MA_t$$

#### 加权移动平均（Weighted Moving Average, WMA）

**加权移动平均**：
$$WMA_t = \frac{\sum_{i=t-w+1}^{t} w_i Y_i}{\sum_{i=t-w+1}^{t} w_i}$$

其中 $w_i$ 是权重，通常近期数据权重更大。

#### 指数加权移动平均（Exponentially Weighted Moving Average, EWMA）

**指数加权移动平均**：
$$EWMA_t = \alpha Y_t + (1-\alpha) EWMA_{t-1}$$

这与单指数平滑相同。

#### 窗口大小选择

- **小窗口（$w=3-5$）**：响应快，但波动大
- **大窗口（$w=10-20$）**：平滑效果好，但滞后明显
- **选择原则**：平衡响应速度和平滑效果

### 3.2 移动平均预测实现

```sql
-- 移动平均预测（带错误处理和性能测试）
DO $$
DECLARE
    window_size INTEGER := 7;
    last_avg NUMERIC;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'time_series') THEN
            RAISE WARNING '表 time_series 不存在，无法执行移动平均预测';
            RETURN;
        END IF;

        SELECT AVG(value) INTO last_avg
        FROM (
            SELECT value
            FROM time_series
            ORDER BY date DESC
            LIMIT window_size
        ) AS recent_values;

        RAISE NOTICE '移动平均预测，窗口大小: %, 预测值: %', window_size, last_avg;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '移动平均预测失败: %', SQLERRM;
            RAISE;
    END;
END $$;

WITH recent_avg AS (
    SELECT AVG(value) AS avg_value
    FROM (
        SELECT value
        FROM time_series
        ORDER BY date DESC
        LIMIT 7
    ) AS recent
),
future_dates AS (
    SELECT generate_series(
        (SELECT MAX(date) FROM time_series) + INTERVAL '1 day',
        (SELECT MAX(date) FROM time_series) + INTERVAL '7 days',
        '1 day'::interval
    )::date AS date
)
SELECT
    fd.date,
    ra.avg_value AS predicted_value
FROM future_dates fd
CROSS JOIN recent_avg ra
ORDER BY fd.date;
```

---

## 4. 高级预测方法

### 4.1 Holt-Winters方法

**Holt-Winters方法**扩展指数平滑，处理趋势和季节性。

#### 加法模型

**水平（Level）**：
$$L_t = \alpha \frac{Y_t}{S_{t-s}} + (1-\alpha)(L_{t-1} + T_{t-1})$$

**趋势（Trend）**：
$$T_t = \beta(L_t - L_{t-1}) + (1-\beta)T_{t-1}$$

**季节性（Seasonal）**：
$$S_t = \gamma \frac{Y_t}{L_t} + (1-\gamma)S_{t-s}$$

**预测**：
$$\hat{Y}_{t+h} = (L_t + h \cdot T_t) \cdot S_{t-s+h}$$

其中 $s$ 是季节周期长度。

### 4.2 ARIMA模型概念

**ARIMA（AutoRegressive Integrated Moving Average）**是经典的时间序列预测模型。

**ARIMA(p,d,q)模型**：

- **AR(p)**：自回归项，使用前 $p$ 个值
- **I(d)**：差分项，使序列平稳
- **MA(q)**：移动平均项，使用前 $q$ 个误差

**注意**：完整的ARIMA实现需要复杂的参数估计，PostgreSQL中可以使用简化版本或调用外部工具。

## 5. 实际应用案例

### 5.1 销售预测

**销售预测**预测未来销售趋势，支持库存管理和业务规划。

```sql
-- 销售预测（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'sales') THEN
            RAISE WARNING '表 sales 不存在，无法执行销售预测';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行销售预测';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '销售预测准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

WITH daily_sales AS (
    SELECT
        DATE_TRUNC('day', sale_date) AS date,
        SUM(amount) AS daily_amount
    FROM sales
    GROUP BY DATE_TRUNC('day', sale_date)
),
regression AS (
    SELECT
        (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) AS slope,
        avg_y - (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) * avg_x AS intercept
    FROM (
        SELECT
            COUNT(*) AS n,
            AVG(EXTRACT(EPOCH FROM date)) AS avg_x,
            AVG(daily_amount) AS avg_y,
            SUM(EXTRACT(EPOCH FROM date) * daily_amount) AS sum_xy,
            SUM(EXTRACT(EPOCH FROM date) * EXTRACT(EPOCH FROM date)) AS sum_x2
        FROM daily_sales
    ) AS stats
),
future_dates AS (
    SELECT generate_series(
        (SELECT MAX(date) FROM daily_sales) + INTERVAL '1 day',
        (SELECT MAX(date) FROM daily_sales) + INTERVAL '30 days',
        '1 day'::interval
    )::date AS date
)
SELECT
    fd.date,
    GREATEST(0, r.slope * EXTRACT(EPOCH FROM fd.date) + r.intercept) AS predicted_sales
FROM future_dates fd
CROSS JOIN regression r
ORDER BY fd.date;
```

### 5.2 需求预测

**场景**：预测产品需求量，优化库存管理。

```sql
-- 需求预测：使用多种方法预测产品需求
WITH demand_history AS (
    SELECT
        product_id,
        date,
        demand_quantity
    FROM product_demand
    WHERE date >= CURRENT_DATE - INTERVAL '90 days'
    ORDER BY product_id, date
),
-- 方法1：线性趋势预测
linear_forecast AS (
    SELECT
        product_id,
        (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) AS slope,
        avg_y - (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) * avg_x AS intercept,
        MAX(date) AS last_date
    FROM (
        SELECT
            product_id,
            COUNT(*) AS n,
            AVG(EXTRACT(EPOCH FROM date)) AS avg_x,
            AVG(demand_quantity) AS avg_y,
            SUM(EXTRACT(EPOCH FROM date) * demand_quantity) AS sum_xy,
            SUM(EXTRACT(EPOCH FROM date) * EXTRACT(EPOCH FROM date)) AS sum_x2
        FROM demand_history
        GROUP BY product_id
    ) AS stats
),
-- 方法2：指数平滑预测
exponential_smoothing AS (
    SELECT DISTINCT ON (product_id)
        product_id,
        demand_quantity AS last_smoothed,
        date AS last_date
    FROM (
        SELECT
            product_id,
            date,
            demand_quantity,
            0.3 AS alpha,
            LAG(demand_quantity * 0.3 + LAG(demand_quantity, 1) OVER (PARTITION BY product_id ORDER BY date) * 0.7, 1)
            OVER (PARTITION BY product_id ORDER BY date) AS smoothed
        FROM demand_history
    ) AS smoothed_data
    ORDER BY product_id, date DESC
),
-- 方法3：移动平均预测
moving_average AS (
    SELECT
        product_id,
        AVG(demand_quantity) AS ma_value,
        MAX(date) AS last_date
    FROM (
        SELECT
            product_id,
            date,
            demand_quantity,
            ROW_NUMBER() OVER (PARTITION BY product_id ORDER BY date DESC) AS rn
        FROM demand_history
    ) AS ranked
    WHERE rn <= 7  -- 7天移动平均
    GROUP BY product_id
),
-- 生成未来7天的预测
future_dates AS (
    SELECT
        product_id,
        generate_series(
            last_date + INTERVAL '1 day',
            last_date + INTERVAL '7 days',
            '1 day'::interval
        )::date AS forecast_date
    FROM (
        SELECT DISTINCT product_id, MAX(date) AS last_date
        FROM demand_history
        GROUP BY product_id
    ) AS max_dates
),
-- 综合预测结果
forecast_results AS (
    SELECT
        fd.product_id,
        fd.forecast_date,
        -- 线性预测
        GREATEST(0, lf.slope * EXTRACT(EPOCH FROM fd.forecast_date) + lf.intercept) AS linear_forecast,
        -- 指数平滑预测
        es.last_smoothed AS exponential_forecast,
        -- 移动平均预测
        ma.ma_value AS moving_avg_forecast,
        -- 综合预测（简单平均）
        (
            GREATEST(0, lf.slope * EXTRACT(EPOCH FROM fd.forecast_date) + lf.intercept) +
            es.last_smoothed +
            ma.ma_value
        ) / 3.0 AS ensemble_forecast
    FROM future_dates fd
    LEFT JOIN linear_forecast lf ON fd.product_id = lf.product_id
    LEFT JOIN exponential_smoothing es ON fd.product_id = es.product_id
    LEFT JOIN moving_average ma ON fd.product_id = ma.product_id
)
SELECT
    product_id,
    forecast_date,
    ROUND(linear_forecast::numeric, 2) AS linear_forecast,
    ROUND(exponential_forecast::numeric, 2) AS exponential_forecast,
    ROUND(moving_avg_forecast::numeric, 2) AS moving_avg_forecast,
    ROUND(ensemble_forecast::numeric, 2) AS ensemble_forecast
FROM forecast_results
ORDER BY product_id, forecast_date;
```

### 5.3 库存优化

**场景**：基于需求预测优化库存水平。

```sql
-- 库存优化：基于预测需求计算安全库存
WITH demand_forecast AS (
    -- 使用移动平均预测未来30天需求
    SELECT
        product_id,
        AVG(demand_quantity) AS forecasted_daily_demand,
        STDDEV(demand_quantity) AS demand_stddev,
        MAX(date) AS last_date
    FROM (
        SELECT
            product_id,
            date,
            demand_quantity,
            ROW_NUMBER() OVER (PARTITION BY product_id ORDER BY date DESC) AS rn
        FROM product_demand
        WHERE date >= CURRENT_DATE - INTERVAL '30 days'
    ) AS recent_demand
    WHERE rn <= 30
    GROUP BY product_id
),
lead_time_stats AS (
    SELECT
        product_id,
        AVG(lead_time_days) AS avg_lead_time,
        STDDEV(lead_time_days) AS lead_time_stddev
    FROM supplier_orders
    WHERE order_date >= CURRENT_DATE - INTERVAL '90 days'
    GROUP BY product_id
),
safety_stock AS (
    SELECT
        df.product_id,
        df.forecasted_daily_demand,
        df.demand_stddev,
        lt.avg_lead_time,
        lt.lead_time_stddev,
        -- 安全库存 = Z * sqrt(lead_time * demand_variance + demand_mean^2 * lead_time_variance)
        -- Z = 1.65 (95%服务水平)
        1.65 * SQRT(
            lt.avg_lead_time * POWER(df.demand_stddev, 2) +
            POWER(df.forecasted_daily_demand, 2) * POWER(lt.lead_time_stddev, 2)
        ) AS safety_stock_quantity,
        -- 再订货点 = lead_time_demand + safety_stock
        lt.avg_lead_time * df.forecasted_daily_demand +
        1.65 * SQRT(
            lt.avg_lead_time * POWER(df.demand_stddev, 2) +
            POWER(df.forecasted_daily_demand, 2) * POWER(lt.lead_time_stddev, 2)
        ) AS reorder_point
    FROM demand_forecast df
    LEFT JOIN lead_time_stats lt ON df.product_id = lt.product_id
)
SELECT
    product_id,
    ROUND(forecasted_daily_demand::numeric, 2) AS forecasted_daily_demand,
    ROUND(avg_lead_time::numeric, 1) AS avg_lead_time_days,
    ROUND(safety_stock_quantity::numeric, 0) AS safety_stock_quantity,
    ROUND(reorder_point::numeric, 0) AS reorder_point,
    CASE
        WHEN reorder_point > 0 THEN '需要补货'
        ELSE '库存充足'
    END AS inventory_status
FROM safety_stock
ORDER BY safety_stock_quantity DESC;
```

### 5.4 预测精度评估

**场景**：评估不同预测方法的准确性。

```sql
-- 预测精度评估：比较不同方法的预测误差
WITH historical_data AS (
    SELECT
        date,
        value
    FROM time_series
    WHERE date >= CURRENT_DATE - INTERVAL '60 days'
    ORDER BY date
),
-- 训练集（前45天）
training_data AS (
    SELECT *
    FROM historical_data
    WHERE date <= (SELECT MAX(date) - INTERVAL '15 days' FROM historical_data)
),
-- 测试集（后15天）
test_data AS (
    SELECT *
    FROM historical_data
    WHERE date > (SELECT MAX(date) - INTERVAL '15 days' FROM historical_data)
),
-- 线性回归模型
linear_model AS (
    SELECT
        (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) AS slope,
        avg_y - (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) * avg_x AS intercept
    FROM (
        SELECT
            COUNT(*) AS n,
            AVG(EXTRACT(EPOCH FROM date)) AS avg_x,
            AVG(value) AS avg_y,
            SUM(EXTRACT(EPOCH FROM date) * value) AS sum_xy,
            SUM(EXTRACT(EPOCH FROM date) * EXTRACT(EPOCH FROM date)) AS sum_x2
        FROM training_data
    ) AS stats
),
-- 移动平均模型（使用训练集最后7天）
ma_model AS (
    SELECT AVG(value) AS ma_value
    FROM (
        SELECT value
        FROM training_data
        ORDER BY date DESC
        LIMIT 7
    ) AS recent
),
-- 预测和评估
predictions AS (
    SELECT
        td.date,
        td.value AS actual_value,
        -- 线性预测
        lm.slope * EXTRACT(EPOCH FROM td.date) + lm.intercept AS linear_pred,
        -- 移动平均预测
        mam.ma_value AS ma_pred
    FROM test_data td
    CROSS JOIN linear_model lm
    CROSS JOIN ma_model mam
),
evaluation AS (
    SELECT
        COUNT(*) AS n,
        -- 线性回归误差
        AVG(ABS(actual_value - linear_pred)) AS linear_mae,
        SQRT(AVG(POWER(actual_value - linear_pred, 2))) AS linear_rmse,
        AVG(ABS((actual_value - linear_pred) / NULLIF(actual_value, 0))) * 100 AS linear_mape,
        -- 移动平均误差
        AVG(ABS(actual_value - ma_pred)) AS ma_mae,
        SQRT(AVG(POWER(actual_value - ma_pred, 2))) AS ma_rmse,
        AVG(ABS((actual_value - ma_pred) / NULLIF(actual_value, 0))) * 100 AS ma_mape
    FROM predictions
)
SELECT
    n AS test_samples,
    ROUND(linear_mae::numeric, 4) AS linear_mae,
    ROUND(linear_rmse::numeric, 4) AS linear_rmse,
    ROUND(linear_mape::numeric, 2) AS linear_mape_pct,
    ROUND(ma_mae::numeric, 4) AS ma_mae,
    ROUND(ma_rmse::numeric, 4) AS ma_rmse,
    ROUND(ma_mape::numeric, 2) AS ma_mape_pct,
    CASE
        WHEN linear_mae < ma_mae THEN '线性回归更优'
        ELSE '移动平均更优'
    END AS best_method
FROM evaluation;
```

### 5.5 季节性预测

**场景**：考虑季节性的销售预测。

```sql
-- 季节性预测：考虑周期性模式
WITH sales_data AS (
    SELECT
        DATE_TRUNC('month', sale_date) AS month,
        SUM(amount) AS monthly_sales
    FROM sales
    WHERE sale_date >= CURRENT_DATE - INTERVAL '24 months'
    GROUP BY DATE_TRUNC('month', sale_date)
    ORDER BY month
),
seasonal_indices AS (
    SELECT
        EXTRACT(MONTH FROM month) AS month_num,
        AVG(monthly_sales) AS avg_monthly_sales,
        (AVG(monthly_sales) / (SELECT AVG(monthly_sales) FROM sales_data)) AS seasonal_index
    FROM sales_data
    GROUP BY EXTRACT(MONTH FROM month)
),
trend_model AS (
    SELECT
        (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) AS slope,
        avg_y - (sum_xy - n * avg_x * avg_y) / NULLIF(sum_x2 - n * avg_x * avg_x, 0) * avg_x AS intercept
    FROM (
        SELECT
            COUNT(*) AS n,
            AVG(EXTRACT(EPOCH FROM month)) AS avg_x,
            AVG(monthly_sales) AS avg_y,
            SUM(EXTRACT(EPOCH FROM month) * monthly_sales) AS sum_xy,
            SUM(EXTRACT(EPOCH FROM month) * EXTRACT(EPOCH FROM month)) AS sum_x2
        FROM sales_data
    ) AS stats
),
future_months AS (
    SELECT generate_series(
        (SELECT MAX(month) FROM sales_data) + INTERVAL '1 month',
        (SELECT MAX(month) FROM sales_data) + INTERVAL '12 months',
        '1 month'::interval
    )::date AS forecast_month
),
seasonal_forecast AS (
    SELECT
        fm.forecast_month,
        EXTRACT(MONTH FROM fm.forecast_month) AS month_num,
        tm.slope * EXTRACT(EPOCH FROM fm.forecast_month) + tm.intercept AS trend_value,
        si.seasonal_index,
        (tm.slope * EXTRACT(EPOCH FROM fm.forecast_month) + tm.intercept) * si.seasonal_index AS seasonal_forecast
    FROM future_months fm
    CROSS JOIN trend_model tm
    JOIN seasonal_indices si ON EXTRACT(MONTH FROM fm.forecast_month) = si.month_num
)
SELECT
    forecast_month,
    ROUND(trend_value::numeric, 2) AS trend_forecast,
    ROUND(seasonal_index::numeric, 4) AS seasonal_index,
    ROUND(seasonal_forecast::numeric, 2) AS seasonal_adjusted_forecast
FROM seasonal_forecast
ORDER BY forecast_month;
```

---

## 6. 算法性能对比与优化

### 6.1 预测方法对比

| 方法 | 适用场景 | 优点 | 缺点 | 预测精度 |
|------|---------|------|------|---------|
| **线性回归** | 有明显趋势 | 简单、可解释 | 假设线性关系 | 中等 |
| **单指数平滑** | 无趋势无季节性 | 自适应、平滑 | 不处理趋势 | 中等 |
| **移动平均** | 短期预测 | 简单、稳健 | 滞后明显 | 较低 |
| **Holt-Winters** | 有趋势和季节性 | 处理复杂模式 | 参数敏感 | 较高 |
| **ARIMA** | 复杂时间序列 | 理论基础强 | 参数调优复杂 | 高 |

### 6.2 性能优化建议

1. **数据预处理**：处理缺失值、异常值、平稳化
2. **特征工程**：创建滞后特征、移动平均特征
3. **模型组合**：使用集成方法提高预测精度
4. **参数调优**：使用交叉验证选择最优参数
5. **实时更新**：定期重新训练模型

### 6.3 常见问题与解决方案

**问题1**：预测值滞后

- **解决方案**：使用更短的窗口、增加趋势项、使用ARIMA

**问题2**：季节性模式不明显

- **解决方案**：增加数据量、使用Holt-Winters、检查周期长度

**问题3**：预测误差大

- **解决方案**：检查数据质量、尝试不同方法、使用集成预测

**问题4**：数据不平稳

- **解决方案**：差分处理、对数变换、分段建模

---

## 7. 最佳实践

### 7.1 数据准备

1. **数据质量**：
   - 处理缺失值：插值、删除或填充
   - 处理异常值：识别和处理极端值
   - 数据清洗：去除噪声和错误数据

2. **数据探索**：
   - 可视化时间序列图
   - 检查趋势、季节性、周期性
   - 分析自相关和偏自相关

3. **数据分割**：
   - 训练集：70-80%
   - 验证集：10-15%
   - 测试集：10-15%

### 7.2 模型选择

1. **数据特征**：
   - 有趋势：使用线性回归或Holt-Winters
   - 有季节性：使用Holt-Winters或ARIMA
   - 平稳序列：使用ARIMA或指数平滑

2. **预测步长**：
   - 短期（1-7天）：移动平均、指数平滑
   - 中期（1-3个月）：线性回归、Holt-Winters
   - 长期（3个月以上）：趋势外推、因果模型

3. **模型评估**：
   - 使用多个评估指标（MAE、RMSE、MAPE）
   - 交叉验证评估泛化能力
   - 比较不同方法的预测精度

### 7.3 预测监控

1. **预测更新**：
   - 定期重新训练模型
   - 监控预测误差
   - 调整模型参数

2. **异常检测**：
   - 监控预测值与实际值的偏差
   - 设置预警阈值
   - 及时调整预测策略

### 7.4 SQL实现注意事项

1. **时间处理**：
   - 使用 `DATE_TRUNC()` 统一时间粒度
   - 处理时区和夏令时
   - 确保时间序列连续

2. **性能优化**：
   - 在时间列上创建索引
   - 使用窗口函数优化计算
   - 考虑使用物化视图缓存结果

3. **数值稳定性**：
   - 使用NUMERIC类型保持精度
   - 注意除零错误
   - 处理溢出问题

---

## 📚 参考资源

### 学术文献

1. **Box, G.E.P., Jenkins, G.M. (1976)**: "Time Series Analysis: Forecasting and Control", 3rd Edition, Prentice Hall.

2. **Holt, C.C. (2004)**: "Forecasting seasonals and trends by exponentially weighted moving averages", *International Journal of Forecasting*.

3. **Hyndman, R.J., Athanasopoulos, G. (2021)**: "Forecasting: principles and practice", 3rd Edition, OTexts.

4. **《时间序列分析》**（王燕，2013）- 应用时间序列分析

### 在线资源

- **PostgreSQL时间序列函数**: <https://www.postgresql.org/docs/current/functions-datetime.html>
- **Python statsmodels**: <https://www.statsmodels.org/stable/tsa.html>
- **R forecast包**: <https://pkg.robjhyndman.com/forecast/>

### 相关算法

- **趋势分析**：识别时间序列趋势
- **周期性分析**：检测周期性模式
- **滑动窗口计算**：移动统计量
- **ARIMA模型**：自回归积分移动平均模型
- **状态空间模型**：动态线性模型
