# 建模决策框架

> **创建日期**: 2025年1月
> **来源**: 数据建模最佳实践
> **状态**: ✅ 已完成
> **文档编号**: 03-02

---

## 📑 目录

- [建模决策框架](#建模决策框架)
  - [📑 目录](#-目录)
  - [1. 概述](#1-概述)
  - [1.1 理论基础](#11-理论基础)
    - [1.1.1 建模决策理论](#111-建模决策理论)
    - [1.1.2 需求分析理论](#112-需求分析理论)
    - [1.1.3 架构选择理论](#113-架构选择理论)
    - [1.1.4 技术选型理论](#114-技术选型理论)
    - [1.1.5 决策评估理论](#115-决策评估理论)
    - [1.1.6 复杂度分析](#116-复杂度分析)
  - [2. 需求分析框架](#2-需求分析框架)
    - [2.1 业务需求分析](#21-业务需求分析)
    - [2.2 功能需求分析](#22-功能需求分析)
    - [2.3 数据需求分析](#23-数据需求分析)
  - [3. 架构选择框架](#3-架构选择框架)
    - [3.1 OLTP vs OLAP决策](#31-oltp-vs-olap决策)
    - [3.2 数据模型选择](#32-数据模型选择)
  - [4. 技术选型框架](#4-技术选型框架)
    - [4.1 数据类型选择决策](#41-数据类型选择决策)
    - [4.2 索引策略选择决策](#42-索引策略选择决策)
    - [4.3 分区策略选择决策](#43-分区策略选择决策)
  - [5. 实施决策框架](#5-实施决策框架)
    - [5.1 实施优先级决策](#51-实施优先级决策)
    - [5.2 迁移策略决策](#52-迁移策略决策)
  - [6. PostgreSQL实现](#6-postgresql实现)
    - [6.1 综合决策支持系统](#61-综合决策支持系统)
  - [7. 实际应用案例 / Practical Application Examples](#7-实际应用案例--practical-application-examples)
    - [7.1 案例1: 电商系统建模决策](#71-案例1-电商系统建模决策)
    - [7.2 案例2: IoT系统建模决策](#72-案例2-iot系统建模决策)
    - [7.3 案例3: 金融系统建模决策](#73-案例3-金融系统建模决策)
  - [8. 常见问题解答 / FAQ](#8-常见问题解答--faq)
    - [Q1: 如何确定应该使用OLTP还是OLAP？](#q1-如何确定应该使用oltp还是olap)
    - [Q2: 如何选择合适的数据模型？](#q2-如何选择合适的数据模型)
    - [Q3: 如何平衡性能和成本？](#q3-如何平衡性能和成本)
    - [Q4: 建模决策如何记录和追踪？](#q4-建模决策如何记录和追踪)
    - [Q5: 如何评估建模决策的效果？](#q5-如何评估建模决策的效果)
  - [7. 相关资源](#7-相关资源)

---

## 1. 概述

建模决策框架提供系统化的方法，帮助数据建模师在复杂场景下做出正确的设计决策。
该框架涵盖从需求分析到技术实施的完整决策流程。

**核心原则**:

- **需求驱动**：以业务需求为核心
- **数据驱动**：基于数据特征做决策
- **成本效益**：平衡性能、成本和复杂度
- **可扩展性**：考虑未来变化和扩展

---

## 1.1 理论基础

### 1.1.1 建模决策理论

**建模决策**:

- **定义**: 在多个建模方案中选择最优方案的过程
- **目标**: 在性能、成本、复杂度间取得平衡
- **方法**: 系统化的决策框架

**决策原则**:

- **需求驱动**: 以业务需求为核心
- **数据驱动**: 基于数据特征做决策
- **成本效益**: 平衡性能、成本和复杂度

### 1.1.2 需求分析理论

**需求分析**:

- **业务需求**: 业务目标和业务流程
- **功能需求**: 系统功能和用户需求
- **数据需求**: 数据特征和数据使用场景

**需求分析方法**:

- **业务分析**: 分析业务目标和流程
- **用户分析**: 分析用户角色和需求
- **数据分析**: 分析数据特征和使用场景

### 1.1.3 架构选择理论

**架构选择**:

- **OLTP vs OLAP**: 根据业务需求选择架构
- **数据模型**: 根据数据特征选择模型
- **技术选型**: 根据技术需求选择技术

**选择原则**:

- **业务匹配**: 架构匹配业务需求
- **性能要求**: 满足性能要求
- **成本控制**: 控制实施成本

### 1.1.4 技术选型理论

**技术选型**:

- **数据类型**: 根据数据特征选择类型
- **索引策略**: 根据查询模式选择索引
- **分区策略**: 根据数据分布选择分区

**选型原则**:

- **性能优先**: 优先考虑性能
- **成本控制**: 控制实施成本
- **可维护性**: 考虑可维护性

### 1.1.5 决策评估理论

**决策评估**:

- **评估指标**: 性能、成本、复杂度、可维护性
- **评估方法**: 量化评估、定性评估、综合评估
- **评估结果**: 决策建议、风险评估

**评估原则**:

- **多维度**: 多维度评估
- **量化**: 尽可能量化评估
- **综合**: 综合评估结果

### 1.1.6 复杂度分析

**决策复杂度**:

- **需求分析**: $O(R)$ where R is requirement complexity
- **方案设计**: $O(S)$ where S is solution complexity
- **决策评估**: $O(E)$ where E is evaluation complexity

---

## 2. 需求分析框架

### 2.1 业务需求分析

**关键问题**:

1. 业务目标是什么？
2. 核心业务流程是什么？
3. 数据使用场景有哪些？
4. 性能要求是什么？
5. 数据量级是多少？

**分析维度**:

| 维度 | 问题 | 影响 |
|------|------|------|
| 业务目标 | 系统要解决什么问题？ | 确定建模重点 |
| 用户角色 | 谁使用数据？ | 影响查询模式 |
| 数据来源 | 数据从哪里来？ | 影响ETL设计 |
| 使用频率 | 数据访问频率？ | 影响索引策略 |
| 数据量 | 数据规模多大？ | 影响分区策略 |

### 2.2 功能需求分析

```sql
-- 需求分析记录表
CREATE TABLE requirement_analysis (
    req_id SERIAL PRIMARY KEY,
    project_name VARCHAR(200) NOT NULL,
    requirement_type VARCHAR(50) NOT NULL, -- 'functional', 'non-functional'
    requirement_text TEXT NOT NULL,
    priority INT DEFAULT 5, -- 1-10
    impact_on_modeling TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 示例：电商系统需求分析
INSERT INTO requirement_analysis (project_name, requirement_type, requirement_text, priority, impact_on_modeling) VALUES
    ('电商系统', 'functional', '支持用户注册、登录、个人信息管理', 9, '需要用户表、认证表'),
    ('电商系统', 'functional', '支持商品浏览、搜索、分类', 10, '需要商品表、分类表、搜索索引'),
    ('电商系统', 'functional', '支持订单创建、支付、配送', 10, '需要订单表、支付表、配送表'),
    ('电商系统', 'non-functional', '支持1000并发用户', 8, '需要连接池、查询优化'),
    ('电商系统', 'non-functional', '数据保留5年', 7, '需要分区策略、归档方案');
```

### 2.3 数据需求分析

**数据特征分析**:

```sql
-- 数据特征分析表
CREATE TABLE data_characteristics (
    char_id SERIAL PRIMARY KEY,
    entity_name VARCHAR(100) NOT NULL,
    -- 数据量特征
    estimated_rows BIGINT,
    growth_rate NUMERIC(5,2), -- 年增长率%
    -- 访问特征
    read_frequency VARCHAR(50), -- 'high', 'medium', 'low'
    write_frequency VARCHAR(50),
    -- 查询特征
    typical_query_pattern TEXT,
    -- 数据特征
    data_volatility VARCHAR(50), -- 'static', 'slow', 'fast'
    data_retention_years INT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 示例：订单数据特征
INSERT INTO data_characteristics (
    entity_name, estimated_rows, growth_rate,
    read_frequency, write_frequency, data_volatility, data_retention_years
) VALUES (
    '订单表', 10000000, 50.0,
    'high', 'high', 'fast', 5
);
```

---

## 3. 架构选择框架

### 3.1 OLTP vs OLAP决策

**决策矩阵**:

| 特征 | OLTP | OLAP |
|------|------|------|
| 主要用途 | 事务处理 | 数据分析 |
| 数据模型 | 规范化（3NF） | 维度建模 |
| 查询类型 | 简单查询，少量数据 | 复杂查询，大量数据 |
| 写入模式 | 频繁写入 | 批量加载 |
| 一致性要求 | 强一致性 | 最终一致性 |
| 响应时间 | 毫秒级 | 秒到分钟级 |

**决策流程**:

```sql
-- 架构选择决策表
CREATE TABLE architecture_decision (
    decision_id SERIAL PRIMARY KEY,
    scenario_description TEXT NOT NULL,
    -- 评估维度
    transaction_volume INT, -- 事务量/秒
    query_complexity VARCHAR(50), -- 'simple', 'complex'
    data_volume BIGINT,
    consistency_requirement VARCHAR(50), -- 'strong', 'eventual'
    -- 决策结果
    recommended_architecture VARCHAR(50), -- 'OLTP', 'OLAP', 'HTAP'
    reasoning TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 决策函数
CREATE OR REPLACE FUNCTION recommend_architecture(
    p_transaction_volume INT,
    p_query_complexity VARCHAR,
    p_data_volume BIGINT,
    p_consistency_requirement VARCHAR
)
RETURNS VARCHAR AS $$
BEGIN
    -- OLTP场景：高事务量、简单查询、强一致性
    IF p_transaction_volume > 1000
       AND p_query_complexity = 'simple'
       AND p_consistency_requirement = 'strong' THEN
        RETURN 'OLTP';
    END IF;

    -- OLAP场景：低事务量、复杂查询、大数据量
    IF p_transaction_volume < 100
       AND p_query_complexity = 'complex'
       AND p_data_volume > 1000000000 THEN
        RETURN 'OLAP';
    END IF;

    -- HTAP场景：混合需求
    RETURN 'HTAP';
END;
$$ LANGUAGE plpgsql;
```

### 3.2 数据模型选择

**规范化 vs 维度建模**:

```sql
-- 数据模型选择决策表
CREATE TABLE model_decision (
    decision_id SERIAL PRIMARY KEY,
    use_case VARCHAR(200) NOT NULL,
    -- 评估因素
    is_transactional BOOLEAN,
    requires_history BOOLEAN,
    query_pattern VARCHAR(50), -- 'point_lookup', 'aggregation', 'scan'
    -- 决策结果
    recommended_model VARCHAR(50), -- 'normalized', 'dimensional', 'hybrid'
    normalization_level INT, -- 1NF-5NF
    reasoning TEXT
);
```

---

## 4. 技术选型框架

### 4.1 数据类型选择决策

**决策树**:

```sql
-- 数据类型选择决策函数
CREATE OR REPLACE FUNCTION select_data_type(
    p_data_kind VARCHAR, -- 'number', 'text', 'date', 'boolean', 'structured'
    p_range_min BIGINT DEFAULT NULL,
    p_range_max BIGINT DEFAULT NULL,
    p_precision INT DEFAULT NULL,
    p_scale INT DEFAULT NULL,
    p_max_length INT DEFAULT NULL,
    p_need_timezone BOOLEAN DEFAULT FALSE
)
RETURNS VARCHAR AS $$
BEGIN
    -- 数值类型
    IF p_data_kind = 'number' THEN
        IF p_precision IS NOT NULL THEN
            RETURN format('NUMERIC(%s, %s)', p_precision, COALESCE(p_scale, 0));
        ELSIF p_range_max <= 32767 THEN
            RETURN 'SMALLINT';
        ELSIF p_range_max <= 2147483647 THEN
            RETURN 'INTEGER';
        ELSE
            RETURN 'BIGINT';
        END IF;
    END IF;

    -- 文本类型
    IF p_data_kind = 'text' THEN
        IF p_max_length IS NOT NULL AND p_max_length <= 255 THEN
            RETURN format('VARCHAR(%s)', p_max_length);
        ELSE
            RETURN 'TEXT';
        END IF;
    END IF;

    -- 日期时间类型
    IF p_data_kind = 'date' THEN
        IF p_need_timezone THEN
            RETURN 'TIMESTAMPTZ';
        ELSE
            RETURN 'TIMESTAMP';
        END IF;
    END IF;

    -- 布尔类型
    IF p_data_kind = 'boolean' THEN
        RETURN 'BOOLEAN';
    END IF;

    -- 结构化数据
    IF p_data_kind = 'structured' THEN
        RETURN 'JSONB';
    END IF;

    RETURN 'TEXT'; -- 默认
END;
$$ LANGUAGE plpgsql;
```

### 4.2 索引策略选择决策

```sql
-- 索引策略选择函数
CREATE OR REPLACE FUNCTION select_index_strategy(
    p_query_pattern VARCHAR, -- 'equality', 'range', 'prefix', 'full_text', 'spatial'
    p_data_type VARCHAR,
    p_cardinality BIGINT,
    p_update_frequency VARCHAR -- 'low', 'medium', 'high'
)
RETURNS VARCHAR AS $$
BEGIN
    -- 等值查询：B-Tree或Hash
    IF p_query_pattern = 'equality' THEN
        IF p_update_frequency = 'low' THEN
            RETURN 'HASH';
        ELSE
            RETURN 'BTREE';
        END IF;
    END IF;

    -- 范围查询：B-Tree或BRIN
    IF p_query_pattern = 'range' THEN
        IF p_cardinality > 1000000 AND p_update_frequency = 'low' THEN
            RETURN 'BRIN';
        ELSE
            RETURN 'BTREE';
        END IF;
    END IF;

    -- 全文搜索：GIN
    IF p_query_pattern = 'full_text' THEN
        RETURN 'GIN';
    END IF;

    -- 空间查询：GIST
    IF p_query_pattern = 'spatial' THEN
        RETURN 'GIST';
    END IF;

    RETURN 'BTREE'; -- 默认
END;
$$ LANGUAGE plpgsql;
```

### 4.3 分区策略选择决策

```sql
-- 分区策略选择函数
CREATE OR REPLACE FUNCTION select_partition_strategy(
    p_table_size BIGINT,
    p_partition_key_type VARCHAR, -- 'date', 'id', 'region'
    p_query_pattern VARCHAR, -- 'time_range', 'id_range', 'list'
    p_data_distribution VARCHAR -- 'uniform', 'skewed'
)
RETURNS VARCHAR AS $$
BEGIN
    -- 大表才需要分区
    IF p_table_size < 10000000 THEN
        RETURN 'NO_PARTITION';
    END IF;

    -- 时间分区：RANGE
    IF p_partition_key_type = 'date' AND p_query_pattern = 'time_range' THEN
        RETURN 'RANGE';
    END IF;

    -- ID范围分区：RANGE或HASH
    IF p_partition_key_type = 'id' THEN
        IF p_data_distribution = 'uniform' THEN
            RETURN 'HASH';
        ELSE
            RETURN 'RANGE';
        END IF;
    END IF;

    -- 列表分区：LIST
    IF p_query_pattern = 'list' THEN
        RETURN 'LIST';
    END IF;

    RETURN 'RANGE'; -- 默认
END;
$$ LANGUAGE plpgsql;
```

---

## 5. 实施决策框架

### 5.1 实施优先级决策

**优先级评估矩阵**:

```sql
-- 实施优先级评估表
CREATE TABLE implementation_priority (
    task_id SERIAL PRIMARY KEY,
    task_name VARCHAR(200) NOT NULL,
    -- 评估维度（1-10分）
    business_value INT CHECK (business_value BETWEEN 1 AND 10),
    technical_complexity INT CHECK (technical_complexity BETWEEN 1 AND 10),
    risk_level INT CHECK (risk_level BETWEEN 1 AND 10),
    dependencies TEXT,
    -- 计算优先级
    priority_score NUMERIC GENERATED ALWAYS AS (
        business_value * 0.4 - technical_complexity * 0.3 - risk_level * 0.3
    ) STORED,
    recommended_order INT
);

-- 示例：电商系统实施优先级
INSERT INTO implementation_priority (task_name, business_value, technical_complexity, risk_level) VALUES
    ('用户认证系统', 9, 3, 2),
    ('商品目录系统', 10, 5, 3),
    ('订单管理系统', 10, 7, 4),
    ('支付集成', 9, 8, 6),
    ('数据分析报表', 7, 6, 3);

-- 按优先级排序
SELECT
    task_name,
    business_value,
    technical_complexity,
    risk_level,
    priority_score,
    ROW_NUMBER() OVER (ORDER BY priority_score DESC) AS recommended_order
FROM implementation_priority
ORDER BY priority_score DESC;
```

### 5.2 迁移策略决策

**迁移策略选择**:

```sql
-- 迁移策略决策表
CREATE TABLE migration_strategy (
    strategy_id SERIAL PRIMARY KEY,
    scenario VARCHAR(200) NOT NULL,
    -- 评估因素
    downtime_tolerance VARCHAR(50), -- 'zero', 'low', 'high'
    data_volume BIGINT,
    complexity_level VARCHAR(50), -- 'low', 'medium', 'high'
    -- 推荐策略
    recommended_strategy VARCHAR(100), -- 'big_bang', 'parallel', 'gradual', 'blue_green'
    reasoning TEXT
);

-- 迁移策略决策函数
CREATE OR REPLACE FUNCTION select_migration_strategy(
    p_downtime_tolerance VARCHAR,
    p_data_volume BIGINT,
    p_complexity_level VARCHAR
)
RETURNS VARCHAR AS $$
BEGIN
    -- 零停机：蓝绿部署
    IF p_downtime_tolerance = 'zero' THEN
        RETURN 'blue_green';
    END IF;

    -- 大数据量+低复杂度：并行运行
    IF p_data_volume > 1000000000 AND p_complexity_level = 'low' THEN
        RETURN 'parallel';
    END IF;

    -- 低复杂度：大爆炸式
    IF p_complexity_level = 'low' THEN
        RETURN 'big_bang';
    END IF;

    -- 默认：渐进式
    RETURN 'gradual';
END;
$$ LANGUAGE plpgsql;
```

---

## 6. PostgreSQL实现

### 6.1 综合决策支持系统

```sql
-- 决策记录表
CREATE TABLE modeling_decisions (
    decision_id SERIAL PRIMARY KEY,
    project_name VARCHAR(200) NOT NULL,
    decision_type VARCHAR(50) NOT NULL, -- 'architecture', 'data_type', 'index', 'partition'
    decision_context JSONB NOT NULL,
    decision_result VARCHAR(200) NOT NULL,
    reasoning TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    created_by VARCHAR(100)
);

-- 决策查询视图
CREATE VIEW decision_summary AS
SELECT
    project_name,
    decision_type,
    COUNT(*) AS decision_count,
    jsonb_object_agg(decision_result, COUNT(*)) AS decisions
FROM modeling_decisions
GROUP BY project_name, decision_type;
```

---

## 7. 实际应用案例 / Practical Application Examples

### 7.1 案例1: 电商系统建模决策

**决策场景**: 设计电商系统的数据模型

**决策过程**:

```sql
-- 1. 需求分析
INSERT INTO modeling_decisions (
    project_name, decision_type, decision_context, decision_result, decision_reason
) VALUES (
    'ecommerce_system',
    'architecture',
    '{"query_type": "OLTP", "data_volume": "large", "write_frequency": "high"}',
    'OLTP',
    '需要支持高频交易，实时查询，选择OLTP架构'
);

-- 2. 模型选择决策
INSERT INTO modeling_decisions (
    project_name, decision_type, decision_context, decision_result, decision_reason
) VALUES (
    'ecommerce_system',
    'data_model',
    '{"entities": ["customer", "product", "order"], "relationships": "complex"}',
    'Party+Order+Product模式',
    '需要统一管理客户和商家，使用Party模式；需要订单管理，使用Order模式'
);

-- 3. 技术选型决策
INSERT INTO modeling_decisions (
    project_name, decision_type, decision_context, decision_result, decision_reason
) VALUES (
    'ecommerce_system',
    'partitioning',
    '{"table_size": "100GB", "query_pattern": "time_range", "retention": "2_years"}',
    'RANGE分区（按月）',
    '订单表按时间范围查询频繁，按月分区平衡查询性能和维护成本'
);
```

### 7.2 案例2: IoT系统建模决策

**决策场景**: 设计IoT监控系统的数据模型

**决策过程**:

```sql
-- 1. 架构决策
INSERT INTO modeling_decisions (
    project_name, decision_type, decision_context, decision_result, decision_reason
) VALUES (
    'iot_monitoring',
    'architecture',
    '{"query_type": "OLAP", "data_volume": "very_large", "write_frequency": "very_high"}',
    '时序数据库（TimescaleDB）',
    '时序数据特征明显，写入频率极高，查询主要是时间范围聚合，选择TimescaleDB'
);

-- 2. 分区决策
INSERT INTO modeling_decisions (
    project_name, decision_type, decision_context, decision_result, decision_reason
) VALUES (
    'iot_monitoring',
    'partitioning',
    '{"chunk_size": "1GB", "retention": "90_days", "compression": "required"}',
    'TimescaleDB Hypertable（按小时chunk）',
    '使用TimescaleDB自动分区，按小时chunk平衡查询性能和压缩效果'
);
```

### 7.3 案例3: 金融系统建模决策

**决策场景**: 设计银行核心系统的数据模型

**决策过程**:

```sql
-- 1. 模型选择决策
INSERT INTO modeling_decisions (
    project_name, decision_type, decision_context, decision_result, decision_reason
) VALUES (
    'banking_system',
    'data_model',
    '{"entities": ["customer", "account", "transaction"], "consistency": "critical"}',
    'Party+Account模式',
    '需要统一管理客户，使用Party模式；需要账户和余额管理，使用Account模式'
);

-- 2. 约束决策
INSERT INTO modeling_decisions (
    project_name, decision_type, decision_context, decision_result, decision_reason
) VALUES (
    'banking_system',
    'constraints',
    '{"data_integrity": "critical", "audit": "required"}',
    '完整约束+触发器审计',
    '金融数据完整性要求高，使用完整约束；需要审计追踪，使用触发器记录变更'
);
```

---

## 8. 常见问题解答 / FAQ

### Q1: 如何确定应该使用OLTP还是OLAP？

**A**: 决策原则：

| 特征 | OLTP | OLAP |
|------|------|------|
| 查询类型 | 单行查询、简单聚合 | 复杂聚合、分析查询 |
| 写入频率 | 高（每秒数千次） | 低（批量导入） |
| 数据量 | 中等（GB-TB） | 大（TB-PB） |
| 响应时间 | 毫秒级 | 秒到分钟级 |
| 一致性要求 | 强一致性 | 最终一致性 |

**决策树**:

- 需要实时交易处理 → OLTP
- 需要数据分析报表 → OLAP
- 两者都需要 → HTAP（混合架构）

### Q2: 如何选择合适的数据模型？

**A**: 选择原则：

1. **业务特征**: 分析业务实体和关系
2. **查询模式**: 分析常用查询模式
3. **扩展需求**: 考虑未来业务变化
4. **标准模式**: 优先使用标准模式（Party、Order、Account等）

**示例**:

- CRM系统 → Party模式
- 电商系统 → Party+Order+Product模式
- 银行系统 → Party+Account模式

### Q3: 如何平衡性能和成本？

**A**: 平衡策略：

```sql
-- 成本效益分析
SELECT
    option_name,
    development_cost,
    maintenance_cost,
    performance_score,
    (performance_score / (development_cost + maintenance_cost)) AS cost_benefit_ratio
FROM modeling_options
ORDER BY cost_benefit_ratio DESC;
```

**原则**:

- P0需求（性能关键）→ 优先性能
- P1需求（成本敏感）→ 平衡性能和成本
- P2需求（一般）→ 优先成本

### Q4: 建模决策如何记录和追踪？

**A**: 记录方法：

```sql
-- 创建决策记录表
CREATE TABLE modeling_decision_log (
    decision_id BIGSERIAL PRIMARY KEY,
    project_name VARCHAR(100) NOT NULL,
    decision_type VARCHAR(50) NOT NULL,
    decision_context JSONB NOT NULL,
    decision_result TEXT NOT NULL,
    decision_reason TEXT,
    decision_maker VARCHAR(100),
    decision_date TIMESTAMPTZ DEFAULT NOW(),
    review_date TIMESTAMPTZ,
    review_result TEXT
);

-- 定期审查决策
CREATE OR REPLACE FUNCTION review_decisions()
RETURNS TABLE(decision_id BIGINT, needs_review BOOLEAN) AS $$
BEGIN
    RETURN QUERY
    SELECT
        mdl.decision_id,
        (mdl.decision_date < NOW() - INTERVAL '6 months') AS needs_review
    FROM modeling_decision_log mdl
    WHERE mdl.review_date IS NULL;
END;
$$ LANGUAGE plpgsql;
```

### Q5: 如何评估建模决策的效果？

**A**: 评估方法：

```sql
-- 决策效果评估
CREATE VIEW decision_effectiveness AS
SELECT
    mdl.project_name,
    mdl.decision_type,
    mdl.decision_result,
    COUNT(*) AS implementation_count,
    AVG(pm.performance_score) AS avg_performance,
    AVG(cm.cost_score) AS avg_cost_score
FROM modeling_decision_log mdl
LEFT JOIN performance_metrics pm ON mdl.decision_id = pm.decision_id
LEFT JOIN cost_metrics cm ON mdl.decision_id = cm.decision_id
GROUP BY mdl.project_name, mdl.decision_type, mdl.decision_result;
```

---

## 7. 相关资源

- [思维表征方法](./思维表征方法.md) - 思维工具和方法
- [成本效益分析](./成本效益分析.md) - 成本效益评估
- [数据类型选择](../08-PostgreSQL建模实践/数据类型选择.md) - 数据类型选择指南
- [索引策略](../08-PostgreSQL建模实践/索引策略.md) - 索引策略指南
- [分区策略](../08-PostgreSQL建模实践/分区策略.md) - 分区策略指南

---

**最后更新**: 2025年1月
**维护者**: PostgreSQL Modern Team
