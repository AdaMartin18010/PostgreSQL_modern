# 产品管理模型完整指南

> **创建日期**: 2025年1月
> **来源**: Silverston《数据模型资源手册》卷1 Chapter 3 - Products + 实践总结
> **状态**: 基于权威资源深化扩展
> **文档编号**: 04-04

---

## 📑 目录

- [产品管理模型完整指南](#产品管理模型完整指南)
  - [📑 目录](#-目录)
  - [1. 概述](#1-概述)
    - [1.1 业务背景](#11-业务背景)
    - [1.2 核心概念](#12-核心概念)
    - [1.3 理论基础](#13-理论基础)
      - [1.3.1 产品模型基本概念](#131-产品模型基本概念)
      - [1.3.2 产品分类理论](#132-产品分类理论)
      - [1.3.3 产品特征理论](#133-产品特征理论)
      - [1.3.4 产品定价理论](#134-产品定价理论)
      - [1.3.5 产品成本理论](#135-产品成本理论)
      - [1.3.6 复杂度分析](#136-复杂度分析)
    - [1.4 与Volume 1的对应关系](#14-与volume-1的对应关系)
  - [2. 产品定义与分类](#2-产品定义与分类)
    - [2.1 Product（产品）实体](#21-product产品实体)
    - [2.2 Goods（商品）和Services（服务）](#22-goods商品和services服务)
    - [2.3 Product Category（产品分类）](#23-product-category产品分类)
    - [2.4 Product Identification（产品标识）](#24-product-identification产品标识)
  - [3. 产品特征管理](#3-产品特征管理)
    - [3.1 Product Feature（产品特征）](#31-product-feature产品特征)
    - [3.2 产品特征子类型](#32-产品特征子类型)
    - [3.3 产品特征交互](#33-产品特征交互)
    - [3.4 Unit of Measure（计量单位）](#34-unit-of-measure计量单位)
  - [4. 产品定价模型](#4-产品定价模型)
    - [4.1 Price Component（价格组件）](#41-price-component价格组件)
    - [4.2 定价子类型](#42-定价子类型)
    - [4.3 定价因子](#43-定价因子)
    - [4.4 国际定价](#44-国际定价)
  - [5. 产品成本模型](#5-产品成本模型)
    - [5.1 Estimated Product Cost（预估产品成本）](#51-estimated-product-cost预估产品成本)
    - [5.2 Cost Component Type（成本组件类型）](#52-cost-component-type成本组件类型)
  - [6. 产品关联关系](#6-产品关联关系)
    - [6.1 Product Component（产品组件）](#61-product-component产品组件)
    - [6.2 Product Substitute（产品替代）](#62-product-substitute产品替代)
    - [6.3 Product Obsolescence（产品淘汰）](#63-product-obsolescence产品淘汰)
    - [6.4 Product Complement（产品互补）](#64-product-complement产品互补)
    - [6.5 Product Incompatibility（产品不兼容）](#65-product-incompatibility产品不兼容)
  - [7. 供应商与制造商](#7-供应商与制造商)
    - [7.1 Supplier Product（供应商产品）](#71-supplier-product供应商产品)
    - [7.2 Manufacturer（制造商）](#72-manufacturer制造商)
    - [7.3 Reorder Guideline（补货指南）](#73-reorder-guideline补货指南)
  - [8. 库存管理](#8-库存管理)
    - [8.1 Inventory Item（库存项）](#81-inventory-item库存项)
    - [8.2 Lot（批次）管理](#82-lot批次管理)
    - [8.3 Inventory Item Variance（库存差异）](#83-inventory-item-variance库存差异)
  - [9. PostgreSQL完整实现](#9-postgresql完整实现)
    - [9.1 完整DDL脚本](#91-完整ddl脚本)
    - [9.2 索引设计](#92-索引设计)
    - [9.3 约束设计](#93-约束设计)
    - [9.4 PostgreSQL 18优化](#94-postgresql-18优化)
  - [10. 查询示例](#10-查询示例)
    - [查询1: 获取产品的完整信息](#查询1-获取产品的完整信息)
    - [查询2: 计算产品当前价格（考虑所有定价因子）](#查询2-计算产品当前价格考虑所有定价因子)
    - [查询3: 获取产品BOM（物料清单）](#查询3-获取产品bom物料清单)
  - [11. 性能优化](#11-性能优化)
    - [11.1 索引优化策略](#111-索引优化策略)
    - [11.2 分区策略](#112-分区策略)
    - [11.3 查询优化建议](#113-查询优化建议)
  - [12. 实际应用案例](#12-实际应用案例)
    - [案例1: 电商产品管理](#案例1-电商产品管理)
    - [案例2: B2B产品目录管理](#案例2-b2b产品目录管理)
    - [案例3: 制造业产品管理](#案例3-制造业产品管理)
  - [13. 常见问题解答 / FAQ](#13-常见问题解答--faq)
    - [Q1: 产品模型相比传统设计有什么优势？](#q1-产品模型相比传统设计有什么优势)
    - [Q2: 如何处理产品的大量变体（SKU）？](#q2-如何处理产品的大量变体sku)
    - [Q3: 如何实现复杂的产品定价规则？](#q3-如何实现复杂的产品定价规则)
    - [Q4: 产品模型如何处理产品版本和淘汰？](#q4-产品模型如何处理产品版本和淘汰)
    - [Q5: 如何优化产品模型的查询性能？](#q5-如何优化产品模型的查询性能)
  - [完整实现脚本补充](#完整实现脚本补充)
    - [完整DDL脚本（带错误处理）](#完整ddl脚本带错误处理)
    - [完整查询函数库](#完整查询函数库)
  - [14. 相关资源](#14-相关资源)
    - [14.1 核心相关文档](#141-核心相关文档)
    - [14.2 归档资源](#142-归档资源)
    - [14.3 参考资源](#143-参考资源)

---

## 1. 概述

### 1.1 业务背景

产品管理模型是Silverston《数据模型资源手册》卷1（Volume 1 Chapter 3: Products）中的核心模型，用于统一管理企业的产品、服务、供应商产品、竞争对手产品等完整产品信息。

**核心业务问题**:

- 如何统一管理企业自有产品、供应商产品和竞争对手产品？
- 如何支持产品的多种分类方式（产品线、型号、等级、行业细分等）？
- 如何管理产品的各种特征（质量、颜色、尺寸、品牌等）？
- 如何实现灵活的多维度定价（地理区域、客户类型、数量折扣等）？
- 如何跟踪产品成本以确保盈利性？
- 如何管理产品之间的关联关系（组件、替代、互补、不兼容等）？
- 如何管理供应商和制造商信息？
- 如何管理库存和批次信息？

### 1.2 核心概念

**Product（产品）**: 统一表示商品（Goods）和服务（Services）的高层抽象实体

**Product Category（产品分类）**: 产品的分类体系，支持层次结构和多分类

**Product Feature（产品特征）**: 产品的特征、选项、变体或修饰符

**Price Component（价格组件）**: 灵活的多维度定价模型

**Estimated Product Cost（预估产品成本）**: 产品成本估算模型

**Product Association（产品关联）**: 产品之间的关系（组件、替代、互补、不兼容等）

### 1.3 理论基础

#### 1.3.1 产品模型基本概念

**Product（产品）**是一个抽象概念，统一表示商品和服务：

- **统一抽象**: $Product = Goods \cup Services$
- **多态性**: 产品可以是Goods（商品）或Services（服务）
- **独立性**: 产品信息可能独立于供应商，也可能依赖于供应商

**产品模型优势**:

- **统一管理**: 统一存储企业自有产品、供应商产品和竞争对手产品
- **灵活分类**: 支持多维度产品分类
- **特征管理**: 灵活的产品特征和变体管理
- **定价灵活**: 支持复杂的多维度定价策略

#### 1.3.2 产品分类理论

**产品分类层次结构**:

- **分类树**: 产品分类可以形成层次结构（递归关系）
- **多分类**: 一个产品可以属于多个分类
- **主分类**: 每个产品有一个主分类，用于避免重复统计

**分类复杂度**:

- **分类查询**: $O(\log C)$ with index where C is number of categories
- **多分类统计**: 需要避免重复计算（使用主分类标志）

#### 1.3.3 产品特征理论

**产品特征模式**:

- **特征类型**: Required Feature（必需特征）、Standard Feature（标准特征）、Selectable Feature（可选特征）、Optional Feature（可选组件）
- **特征交互**: 特征之间可能存在依赖关系或不兼容关系
- **特征定价**: 特征可以独立定价或与产品一起定价

**特征复杂度**:

- **特征组合**: 如果有n个可选特征，可能的组合数为 $2^n$
- **特征查询**: 使用GIN索引优化JSONB特征查询

#### 1.3.4 产品定价理论

**定价组件模型**:

- **定价类型**: Base Price（基础价格）、Discount Component（折扣组件）、Surcharge Component（附加费组件）、Manufacturer's Suggested Price（建议零售价）
- **定价频率**: One Time Charge（一次性费用）、Recurring Charge（周期性费用）、Utilization Charge（使用量费用）
- **定价因子**: Geographic Boundary（地理边界）、Party Type（客户类型）、Product Category（产品分类）、Quantity Break（数量分段）、Order Value（订单价值）、Sale Type（销售类型）

**定价复杂度**:

- **定价规则**: 多个定价规则可能同时适用，需要业务规则确定优先级
- **定价计算**: $Price = BasePrice \times (1 - Discount) + Surcharge$

#### 1.3.5 产品成本理论

**成本组件模型**:

- **成本类型**: Estimated Materials Cost（预估材料成本）、Estimated Labor Cost（预估人工成本）、Estimated Other Cost（预估其他成本）
- **成本因子**: Geographic Boundary（地理边界）、Organization（组织）
- **成本估算**: 使用预估成本而非实际成本，以便预测未来趋势

**成本复杂度**:

- **成本计算**: $TotalCost = \sum CostComponents$
- **成本分析**: 需要按地理区域、组织等维度分析成本

#### 1.3.6 复杂度分析

**存储复杂度**:

- **Product表**: $O(P)$ where P is number of products
- **Product Category表**: $O(C)$ where C is number of categories
- **Product Feature表**: $O(F)$ where F is number of features
- **Price Component表**: $O(P \times V)$ where V is average pricing variations per product

**查询复杂度**:

- **产品查询**: $O(\log P)$ with index
- **分类查询**: $O(\log C)$ with index
- **定价查询**: $O(\log V)$ with index
- **特征查询**: $O(\log F)$ with index or $O(1)$ with GIN index for JSONB

### 1.4 与Volume 1的对应关系

本模型基于Silverston《数据模型资源手册》卷1 Chapter 3: Products，完整整合了以下内容：

| Volume 1章节 | 本模型对应部分 | 整合度 |
|-------------|--------------|--------|
| Product Definition | 2. 产品定义与分类 | 100% |
| Product Category | 2.3 Product Category | 100% |
| Product Identification | 2.4 Product Identification | 100% |
| Product Features | 3. 产品特征管理 | 100% |
| Product Pricing | 4. 产品定价模型 | 100% |
| Product Costing | 5. 产品成本模型 | 100% |
| Product Associations | 6. 产品关联关系 | 100% |
| Suppliers and Manufacturers | 7. 供应商与制造商 | 100% |
| Inventory Management | 8. 库存管理 | 100% |

---

## 2. 产品定义与分类

### 2.1 Product（产品）实体

**核心实体设计**:

```sql
-- 产品表（使用继承，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            CREATE TABLE product (
                product_id SERIAL PRIMARY KEY,
                product_type CHAR(1) NOT NULL CHECK (product_type IN ('G', 'S')),
                product_code VARCHAR(50) UNIQUE NOT NULL,
                name VARCHAR(200) NOT NULL,
                description TEXT,
                introduction_date DATE,
                sales_discontinuation_date DATE,
                support_discontinuation_date DATE,
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                updated_at TIMESTAMPTZ DEFAULT NOW()
            ) PARTITION BY LIST (product_type);
            RAISE NOTICE '表 product 创建成功';
        ELSE
            RAISE NOTICE '表 product 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product 失败: %', SQLERRM;
    END;
END $$;

-- 商品子表（Goods，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'goods') THEN
            CREATE TABLE goods PARTITION OF product
                FOR VALUES IN ('G');
            RAISE NOTICE '分区 goods 创建成功';
        ELSE
            RAISE NOTICE '分区 goods 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '分区 goods 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '父表 product 不存在，无法创建分区';
        WHEN OTHERS THEN
            RAISE WARNING '创建分区 goods 失败: %', SQLERRM;
    END;
END $$;

-- 服务子表（Services，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'services') THEN
            CREATE TABLE services PARTITION OF product
                FOR VALUES IN ('S');
            RAISE NOTICE '分区 services 创建成功';
        ELSE
            RAISE NOTICE '分区 services 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '分区 services 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '父表 product 不存在，无法创建分区';
        WHEN OTHERS THEN
            RAISE WARNING '创建分区 services 失败: %', SQLERRM;
    END;
END $$;
```

**设计要点**:

- **继承表**: 使用PostgreSQL继承表实现产品类型的分离
- **产品标识**: `product_code`用于业务标识，`product_id`用于技术主键
- **生命周期**: `introduction_date`、`sales_discontinuation_date`、`support_discontinuation_date`管理产品生命周期
- **类型约束**: 通过CHECK约束确保产品类型只能是Goods或Services

### 2.2 Goods（商品）和Services（服务）

**Goods（商品）**: 有形的物理商品，可以物理存储。例如：笔、家具、设备等。

**Services（服务）**: 无形的服务产品，涉及出售人员的时间和专业知识。例如：咨询服务、法律服务等。

**PostgreSQL实现**:

```sql
-- 商品扩展属性（可选）
ALTER TABLE goods ADD COLUMN weight NUMERIC(10,2);
ALTER TABLE goods ADD COLUMN dimensions JSONB; -- {"width": 10, "height": 20, "depth": 5}

-- 服务扩展属性（可选）
ALTER TABLE services ADD COLUMN service_duration INTERVAL;
ALTER TABLE services ADD COLUMN service_terms TEXT;

-- 示例数据（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'goods') THEN
            RAISE WARNING '表 goods 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO goods (product_type, product_code, name, introduction_date)
        VALUES ('G', 'PEN202', 'Goldstein Elite Pen', '2020-01-01')
        ON CONFLICT (product_code) DO NOTHING;
        RAISE NOTICE '商品数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '商品数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入商品数据失败: %', SQLERRM;
    END;
END $$;

DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'services') THEN
            RAISE WARNING '表 services 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO services (product_type, product_code, name, introduction_date)
        VALUES ('S', 'CNS109', 'Office Supply Inventory Management Consulting Service', '2020-01-01')
        ON CONFLICT (product_code) DO NOTHING;
        RAISE NOTICE '服务数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '服务数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入服务数据失败: %', SQLERRM;
    END;
END $$;
```

### 2.3 Product Category（产品分类）

**核心实体设计**:

```sql
-- 产品分类表（支持递归层次结构，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category') THEN
            CREATE TABLE product_category (
                category_id SERIAL PRIMARY KEY,
                parent_category_id INT REFERENCES product_category(category_id),
                category_code VARCHAR(50) UNIQUE NOT NULL,
                category_name VARCHAR(200) NOT NULL,
                description TEXT,
                display_order INT DEFAULT 0,
                is_active BOOLEAN DEFAULT TRUE,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 product_category 创建成功';
        ELSE
            RAISE NOTICE '表 product_category 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_category 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_category 失败: %', SQLERRM;
    END;
END $$;

-- 产品分类关联表（支持多分类和主分类，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category_classification') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
               NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category') THEN
                RAISE WARNING '必需的表不存在（product或product_category），无法创建关联表';
                RETURN;
            END IF;
            CREATE TABLE product_category_classification (
                classification_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                category_id INT NOT NULL REFERENCES product_category(category_id),
                is_primary BOOLEAN DEFAULT FALSE,
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                UNIQUE(product_id, category_id, from_date),
                -- 业务规则：每个产品在每个时间点只能有一个主分类
                EXCLUDE USING gist (product_id WITH =, from_date WITH &&)
                WHERE (is_primary = TRUE)
            );
            RAISE NOTICE '表 product_category_classification 创建成功';
        ELSE
            RAISE NOTICE '表 product_category_classification 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_category_classification 已存在';
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，请先创建product和product_category表';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_category_classification 失败: %', SQLERRM;
    END;
END $$;
```

**分类层次查询**:

```sql
-- 查询分类的所有子分类（递归CTE）
WITH RECURSIVE category_tree AS (
    -- 基础查询：起始分类
    SELECT category_id, category_name, parent_category_id, 1 AS level
    FROM product_category
    WHERE category_id = :root_category_id

    UNION ALL

    -- 递归查询：子分类
    SELECT c.category_id, c.category_name, c.parent_category_id, ct.level + 1
    FROM product_category c
    INNER JOIN category_tree ct ON c.parent_category_id = ct.category_id
)
-- 查询分类树（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category') THEN
            RAISE WARNING '表 product_category 不存在，无法执行分类树查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行分类树查询';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT * FROM category_tree;

-- 查询产品的主分类和所有分类（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category_classification') THEN
            RAISE WARNING '必需的表不存在，无法执行分类查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行查询：产品分类信息';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    p.product_id,
    p.name AS product_name,
    pc.category_name AS primary_category,
    ARRAY_AGG(pc2.category_name) FILTER (WHERE pcc2.is_primary = FALSE) AS other_categories
FROM product p
LEFT JOIN product_category_classification pcc ON p.product_id = pcc.product_id AND pcc.is_primary = TRUE
LEFT JOIN product_category pc ON pcc.category_id = pc.category_id
LEFT JOIN product_category_classification pcc2 ON p.product_id = pcc2.product_id
LEFT JOIN product_category pc2 ON pcc2.category_id = pc2.category_id
GROUP BY p.product_id, p.name, pc.category_name;
```

**市场兴趣模型**:

```sql
-- 市场兴趣表（链接Party Type和Product Category，用于销售预测和潜在客户识别，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'market_interest') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category') THEN
                RAISE WARNING '表 product_category 不存在，无法创建market_interest表';
                RETURN;
            END IF;
            CREATE TABLE market_interest (
                interest_id SERIAL PRIMARY KEY,
                party_type_id INT NOT NULL, -- 引用Party Type（假设已有Party模型）
                category_id INT NOT NULL REFERENCES product_category(category_id),
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                description TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 market_interest 创建成功';
        ELSE
            RAISE NOTICE '表 market_interest 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 market_interest 已存在';
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，请先创建product_category表';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 market_interest 失败: %', SQLERRM;
    END;
END $$;

-- 示例：识别对特定产品分类感兴趣的组织类型（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'market_interest') THEN
            RAISE WARNING '表 market_interest 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO market_interest (party_type_id, category_id, description)
        VALUES
            (1, 5, 'Insurance companies interested in insurance products'),
            (2, 5, 'Insurance brokers interested in insurance products'),
            (3, 5, 'Health care provider organizations interested in insurance products')
        ON CONFLICT DO NOTHING;
        RAISE NOTICE '市场兴趣数据插入成功';
    EXCEPTION
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，category_id可能不存在';
        WHEN OTHERS THEN
            RAISE WARNING '插入市场兴趣数据失败: %', SQLERRM;
    END;
END $$;
```

### 2.4 Product Identification（产品标识）

**核心实体设计**:

```sql
-- 产品标识类型表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'good_identification_type') THEN
            CREATE TABLE good_identification_type (
                id_type_id SERIAL PRIMARY KEY,
                type_code VARCHAR(50) UNIQUE NOT NULL,
                type_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 good_identification_type 创建成功';
        ELSE
            RAISE NOTICE '表 good_identification_type 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 good_identification_type 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 good_identification_type 失败: %', SQLERRM;
    END;
END $$;

-- 初始化常用标识类型（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'good_identification_type') THEN
            RAISE WARNING '表 good_identification_type 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO good_identification_type (type_code, type_name, description) VALUES
            ('MANUFACTURER_ID', 'Manufacturer ID Number', '制造商指定的产品ID'),
            ('SKU', 'Stock-Keeping Unit', '库存单位标准产品ID'),
            ('UPCA', 'Universal Product Code - American', '美国通用产品代码'),
            ('UPCE', 'Universal Product Code - European', '欧洲通用产品代码'),
            ('ISBN', 'International Standard Book Number', '国际标准书号'),
            ('EAN', 'European Article Number', '欧洲商品编号'),
            ('GTIN', 'Global Trade Item Number', '全球贸易项目代码')
        ON CONFLICT (type_code) DO NOTHING;
        RAISE NOTICE '产品标识类型数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '产品标识类型数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入产品标识类型数据失败: %', SQLERRM;
    END;
END $$;

-- 产品标识表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'good_identification') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
               NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'good_identification_type') THEN
                RAISE WARNING '必需的表不存在（product或good_identification_type），无法创建good_identification表';
                RETURN;
            END IF;
            CREATE TABLE good_identification (
                identification_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                id_type_id INT NOT NULL REFERENCES good_identification_type(id_type_id),
                id_value VARCHAR(100) NOT NULL,
                is_primary BOOLEAN DEFAULT FALSE,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                UNIQUE(product_id, id_type_id, id_value)
            );
            RAISE NOTICE '表 good_identification 创建成功';
        ELSE
            RAISE NOTICE '表 good_identification 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 good_identification 已存在';
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，请先创建product和good_identification_type表';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 good_identification 失败: %', SQLERRM;
    END;
END $$;

-- 索引优化（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'good_identification') THEN
            RAISE WARNING '表 good_identification 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_good_identification_value') THEN
            CREATE INDEX idx_good_identification_value ON good_identification(id_value);
            RAISE NOTICE '索引 idx_good_identification_value 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_good_identification_product') THEN
            CREATE INDEX idx_good_identification_product ON good_identification(product_id);
            RAISE NOTICE '索引 idx_good_identification_product 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_good_identification_primary') THEN
            CREATE INDEX idx_good_identification_primary ON good_identification(product_id) WHERE is_primary = TRUE;
            RAISE NOTICE '索引 idx_good_identification_primary 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 good_identification 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
    END;
END $$;
```

**查询示例**:

```sql
-- 通过SKU查找产品（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'good_identification') THEN
            RAISE WARNING '必需的表不存在，无法执行SKU查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行查询：通过SKU查找产品';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT p.*, gi.id_value AS sku
FROM product p
INNER JOIN good_identification gi ON p.product_id = gi.product_id
INNER JOIN good_identification_type git ON gi.id_type_id = git.id_type_id
WHERE git.type_code = 'SKU' AND gi.id_value = :sku_value;

-- 获取产品的所有标识（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'good_identification') THEN
            RAISE WARNING '必需的表不存在，无法执行产品标识查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行查询：获取产品的所有标识';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    p.name AS product_name,
    git.type_name,
    gi.id_value,
    gi.is_primary
FROM product p
INNER JOIN good_identification gi ON p.product_id = gi.product_id
INNER JOIN good_identification_type git ON gi.id_type_id = git.id_type_id
WHERE p.product_id = :product_id
ORDER BY gi.is_primary DESC, git.type_name;
```

---

## 3. 产品特征管理

### 3.1 Product Feature（产品特征）

产品特征用于描述产品的特性、选项、变体或修饰符。同一特征可能在一个产品中是必需部分，在另一个产品中是可选组件。

**核心实体设计**:

```sql
-- 产品特征分类表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_category') THEN
            CREATE TABLE product_feature_category (
                category_id SERIAL PRIMARY KEY,
                category_code VARCHAR(50) UNIQUE NOT NULL,
                category_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 product_feature_category 创建成功';
        ELSE
            RAISE NOTICE '表 product_feature_category 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_feature_category 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_feature_category 失败: %', SQLERRM;
    END;
END $$;

-- 初始化常用特征分类（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_category') THEN
            RAISE WARNING '表 product_feature_category 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO product_feature_category (category_code, category_name) VALUES
            ('QUALITY', 'Product Quality'),
            ('COLOR', 'Color'),
            ('SIZE', 'Size'),
            ('DIMENSION', 'Dimension'),
            ('BRAND', 'Brand Name'),
            ('SOFTWARE', 'Software Feature'),
            ('HARDWARE', 'Hardware Feature'),
            ('BILLING', 'Billing Feature')
        ON CONFLICT (category_code) DO NOTHING;
        RAISE NOTICE '产品特征分类数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '产品特征分类数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入产品特征分类数据失败: %', SQLERRM;
    END;
END $$;

-- 产品特征表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_category') THEN
                RAISE WARNING '表 product_feature_category 不存在，无法创建product_feature表';
                RETURN;
            END IF;
            CREATE TABLE product_feature (
                feature_id SERIAL PRIMARY KEY,
                category_id INT NOT NULL REFERENCES product_feature_category(category_id),
                feature_code VARCHAR(50) UNIQUE NOT NULL,
                feature_name VARCHAR(200) NOT NULL,
                description TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 product_feature 创建成功';
        ELSE
            RAISE NOTICE '表 product_feature 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_feature 已存在';
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，请先创建product_feature_category表';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_feature 失败: %', SQLERRM;
    END;
END $$;

-- 产品特征适用性表（产品与特征的关联，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_applicability') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
               NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature') THEN
                RAISE WARNING '必需的表不存在（product或product_feature），无法创建product_feature_applicability表';
                RETURN;
            END IF;
            CREATE TABLE product_feature_applicability (
                applicability_id SERIAL PRIMARY KEY,
                product_id INT REFERENCES product(product_id) ON DELETE CASCADE,
                feature_id INT NOT NULL REFERENCES product_feature(feature_id),
                applicability_type VARCHAR(20) NOT NULL CHECK (applicability_type IN ('REQUIRED', 'STANDARD', 'SELECTABLE', 'OPTIONAL')),
                display_order INT DEFAULT 0,
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                UNIQUE(product_id, feature_id, from_date)
            );
            RAISE NOTICE '表 product_feature_applicability 创建成功';
        ELSE
            RAISE NOTICE '表 product_feature_applicability 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_feature_applicability 已存在';
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，请先创建product和product_feature表';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_feature_applicability 失败: %', SQLERRM;
    END;
END $$;
```

**PostgreSQL 18优化**: 使用虚拟生成列优化特征查询

```sql
-- PostgreSQL 18: 虚拟生成列用于特征搜索
ALTER TABLE product_feature_applicability
ADD COLUMN feature_search_text TEXT
GENERATED ALWAYS AS (
    pf.feature_name || ' ' || COALESCE(pf.description, '')
) STORED;

-- 全文搜索索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_applicability') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_feature_applicability_search') THEN
                CREATE INDEX idx_feature_applicability_search ON product_feature_applicability
                USING GIN(to_tsvector('english', feature_search_text));
                RAISE NOTICE '全文搜索索引 idx_feature_applicability_search 创建成功';
            ELSE
                RAISE NOTICE '索引 idx_feature_applicability_search 已存在';
            END IF;
        ELSE
            RAISE WARNING '表 product_feature_applicability 不存在，跳过索引创建';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建全文搜索索引失败: %', SQLERRM;
    END;
END $$;
```

### 3.2 产品特征子类型

**常见特征子类型**:

- **Product Quality（产品质量）**: "grade A"、"grade B"、"expert"、"junior"
- **Color（颜色）**: 产品的颜色选项
- **Dimension（尺寸）**: 数值描述（如"8½英寸宽"、"11英寸长"）
- **Size（大小）**: 通用尺寸（"extra large"、"large"、"medium"、"small"）
- **Brand Name（品牌名称）**: 营销名称（可能与制造商名称不同）
- **Software Feature（软件特征）**: 软件设置或限制
- **Hardware Feature（硬件特征）**: 硬件组件
- **Billing Feature（计费特征）**: 计费选项（如月度或季度计费）

### 3.3 产品特征交互

某些特征的适用性可能依赖于其他特征的选择。

**核心实体设计**:

```sql
-- 产品特征交互类型表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_interaction_type') THEN
            CREATE TABLE product_feature_interaction_type (
                interaction_type_id SERIAL PRIMARY KEY,
                type_code VARCHAR(50) UNIQUE NOT NULL,
                type_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 product_feature_interaction_type 创建成功';
        ELSE
            RAISE NOTICE '表 product_feature_interaction_type 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_feature_interaction_type 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_feature_interaction_type 失败: %', SQLERRM;
    END;
END $$;

-- 初始化交互类型（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_interaction_type') THEN
            RAISE WARNING '表 product_feature_interaction_type 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO product_feature_interaction_type (type_code, type_name) VALUES
            ('INCOMPATIBILITY', 'Feature Incompatibility'),
            ('DEPENDENCY', 'Feature Dependency')
        ON CONFLICT (type_code) DO NOTHING;
        RAISE NOTICE '产品特征交互类型数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '产品特征交互类型数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入产品特征交互类型数据失败: %', SQLERRM;
    END;
END $$;

-- 产品特征交互表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_interaction') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature') OR
               NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_interaction_type') THEN
                RAISE WARNING '必需的表不存在（product_feature或product_feature_interaction_type），无法创建product_feature_interaction表';
                RETURN;
            END IF;
            CREATE TABLE product_feature_interaction (
                interaction_id SERIAL PRIMARY KEY,
                feature_id INT NOT NULL REFERENCES product_feature(feature_id),
                related_feature_id INT NOT NULL REFERENCES product_feature(feature_id),
                interaction_type_id INT NOT NULL REFERENCES product_feature_interaction_type(interaction_type_id),
                description TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                CHECK (feature_id != related_feature_id),
                UNIQUE(feature_id, related_feature_id, interaction_type_id)
            );
            RAISE NOTICE '表 product_feature_interaction 创建成功';
        ELSE
            RAISE NOTICE '表 product_feature_interaction 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_feature_interaction 已存在';
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，请先创建product_feature和product_feature_interaction_type表';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_feature_interaction 失败: %', SQLERRM;
    END;
END $$;

-- 示例：DVD和CD-ROM不兼容（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_interaction') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_interaction_type') THEN
            RAISE WARNING '必需的表不存在，无法插入特征交互数据';
            RETURN;
        END IF;
        INSERT INTO product_feature_interaction (feature_id, related_feature_id, interaction_type_id, description)
        SELECT
            f1.feature_id,
            f2.feature_id,
            (SELECT interaction_type_id FROM product_feature_interaction_type WHERE type_code = 'INCOMPATIBILITY'),
            'Internal DVD cannot be selected if internal rewritable CD-ROM is selected'
        FROM product_feature f1, product_feature f2
        WHERE f1.feature_code = 'DVD_INTERNAL' AND f2.feature_code = 'CDROM_REWRITABLE'
        ON CONFLICT (feature_id, related_feature_id, interaction_type_id) DO NOTHING;
        RAISE NOTICE '产品特征交互数据插入成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '插入产品特征交互数据失败: %', SQLERRM;
    END;
END $$;
```

### 3.4 Unit of Measure（计量单位）

**核心实体设计**:

```sql
-- 计量单位表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'unit_of_measure') THEN
            CREATE TABLE unit_of_measure (
                unit_id SERIAL PRIMARY KEY,
                unit_code VARCHAR(50) UNIQUE NOT NULL,
                unit_name VARCHAR(100) NOT NULL,
                unit_type VARCHAR(50), -- 'WEIGHT', 'LENGTH', 'VOLUME', 'TIME', 'QUANTITY'
                description TEXT,
                is_active BOOLEAN DEFAULT TRUE
            );
            RAISE NOTICE '表 unit_of_measure 创建成功';
        ELSE
            RAISE NOTICE '表 unit_of_measure 已存在，跳过创建';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 unit_of_measure 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 unit_of_measure 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 初始化常用计量单位（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'unit_of_measure') THEN
            RAISE WARNING '表 unit_of_measure 不存在，无法插入数据';
            RETURN;
        END IF;
        IF (SELECT COUNT(*) FROM unit_of_measure) = 0 THEN
            INSERT INTO unit_of_measure (unit_code, unit_name, unit_type) VALUES
                ('EACH', 'Each', 'QUANTITY'),
                ('BOX', 'Box', 'QUANTITY'),
                ('REAM', 'Ream', 'QUANTITY'),
                ('KG', 'Kilogram', 'WEIGHT'),
                ('LB', 'Pound', 'WEIGHT'),
                ('L', 'Liter', 'VOLUME'),
                ('GAL', 'Gallon', 'VOLUME'),
                ('IN', 'Inch', 'LENGTH'),
                ('CM', 'Centimeter', 'LENGTH'),
                ('HOUR', 'Hour', 'TIME'),
                ('DAY', 'Day', 'TIME')
            ON CONFLICT (unit_code) DO NOTHING;
            RAISE NOTICE '已初始化常用计量单位';
        ELSE
            RAISE NOTICE '表 unit_of_measure 已有数据，跳过插入';
        END IF;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '插入计量单位数据失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 产品计量单位关联（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE WARNING '表 product 不存在，无法添加列';
            RETURN;
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'unit_of_measure') THEN
            RAISE EXCEPTION '引用表 unit_of_measure 不存在，无法创建外键约束';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'product' AND column_name = 'unit_of_measure_id') THEN
            ALTER TABLE product ADD COLUMN unit_of_measure_id INT REFERENCES unit_of_measure(unit_id);
            RAISE NOTICE '列 unit_of_measure_id 添加成功';
        ELSE
            RAISE NOTICE '列 unit_of_measure_id 已存在';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE EXCEPTION '引用表 unit_of_measure 不存在';
        WHEN duplicate_column THEN
            RAISE NOTICE '列 unit_of_measure_id 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '添加列失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 计量单位转换表（用于库存计算，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'unit_of_measure') THEN
            RAISE EXCEPTION '引用表 unit_of_measure 不存在，无法创建外键约束表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'unit_of_measure_conversion') THEN
            CREATE TABLE unit_of_measure_conversion (
                conversion_id SERIAL PRIMARY KEY,
                from_unit_id INT NOT NULL REFERENCES unit_of_measure(unit_id),
                to_unit_id INT NOT NULL REFERENCES unit_of_measure(unit_id),
                conversion_factor NUMERIC(10,4) NOT NULL CHECK (conversion_factor > 0),
                created_at TIMESTAMPTZ DEFAULT NOW(),
                UNIQUE(from_unit_id, to_unit_id),
                CHECK (from_unit_id != to_unit_id)
            );
            RAISE NOTICE '表 unit_of_measure_conversion 创建成功';
        ELSE
            RAISE NOTICE '表 unit_of_measure_conversion 已存在，跳过创建';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE EXCEPTION '引用表 unit_of_measure 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '表 unit_of_measure_conversion 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 unit_of_measure_conversion 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 示例：盒装铅笔转换为单个（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'unit_of_measure_conversion') THEN
            RAISE WARNING '表 unit_of_measure_conversion 不存在，无法插入数据';
            RETURN;
        END IF;
        IF NOT EXISTS (SELECT 1 FROM unit_of_measure WHERE unit_code = 'BOX_SMALL') OR
           NOT EXISTS (SELECT 1 FROM unit_of_measure WHERE unit_code = 'EACH') THEN
            RAISE WARNING '必需的计量单位不存在，无法插入转换数据';
            RETURN;
        END IF;
        INSERT INTO unit_of_measure_conversion (from_unit_id, to_unit_id, conversion_factor)
        SELECT
            (SELECT unit_id FROM unit_of_measure WHERE unit_code = 'BOX_SMALL'),
            (SELECT unit_id FROM unit_of_measure WHERE unit_code = 'EACH'),
            12.0  -- 1小盒 = 12个
        ON CONFLICT (from_unit_id, to_unit_id) DO NOTHING;
        RAISE NOTICE '盒装铅笔转换数据插入成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '插入转换数据失败: %', SQLERRM;
    END;
END $$;

-- 示例：夸脱转换为加仑（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'unit_of_measure_conversion') THEN
            RAISE WARNING '表 unit_of_measure_conversion 不存在，无法插入数据';
            RETURN;
        END IF;
        IF NOT EXISTS (SELECT 1 FROM unit_of_measure WHERE unit_code = 'QUART') OR
           NOT EXISTS (SELECT 1 FROM unit_of_measure WHERE unit_code = 'GAL') THEN
            RAISE WARNING '必需的计量单位不存在，无法插入转换数据';
            RETURN;
        END IF;
        INSERT INTO unit_of_measure_conversion (from_unit_id, to_unit_id, conversion_factor)
        SELECT
    (SELECT unit_id FROM unit_of_measure WHERE unit_code = 'QUART'),
    (SELECT unit_id FROM unit_of_measure WHERE unit_code = 'GAL'),
    4.0; -- 4夸脱 = 1加仑
```

---

## 4. 产品定价模型

### 4.1 Price Component（价格组件）

价格组件存储产品的各种价格要素：基础价格、折扣、附加费等。

**核心实体设计**:

```sql
-- 价格组件类型表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'price_component_type') THEN
            CREATE TABLE price_component_type (
                type_id SERIAL PRIMARY KEY,
                type_code VARCHAR(50) UNIQUE NOT NULL,
                type_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 price_component_type 创建成功';
        ELSE
            RAISE NOTICE '表 price_component_type 已存在，跳过创建';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 price_component_type 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 price_component_type 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 初始化价格组件类型（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'price_component_type') THEN
            RAISE WARNING '表 price_component_type 不存在，无法插入数据';
            RETURN;
        END IF;
        IF (SELECT COUNT(*) FROM price_component_type) = 0 THEN
            INSERT INTO price_component_type (type_code, type_name) VALUES
                ('BASE_PRICE', 'Base Price'),
                ('DISCOUNT', 'Discount Component'),
                ('SURCHARGE', 'Surcharge Component'),
                ('MSRP', 'Manufacturer Suggested Retail Price')
            ON CONFLICT (type_code) DO NOTHING;
            RAISE NOTICE '已初始化价格组件类型';
        ELSE
            RAISE NOTICE '表 price_component_type 已有数据，跳过插入';
        END IF;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '插入价格组件类型数据失败: %', SQLERRM;
            RAISE;
    END;
END $$;
    ('ONE_TIME', 'One Time Charge'),
    ('RECURRING', 'Recurring Charge'),
    ('UTILIZATION', 'Utilization Charge');

-- 价格组件表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'price_component') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
                RAISE EXCEPTION '表 product 不存在，无法创建price_component表';
            END IF;
            CREATE TABLE price_component (
                price_component_id SERIAL PRIMARY KEY,
                product_id INT REFERENCES product(product_id) ON DELETE CASCADE,
                feature_id INT REFERENCES product_feature(feature_id) ON DELETE CASCADE,
    organization_id INT, -- 引用Organization（假设已有Party模型）
    type_id INT NOT NULL REFERENCES price_component_type(type_id),
    price_amount NUMERIC(10,2),
    percent_value NUMERIC(5,2), -- 百分比折扣
    currency_code VARCHAR(3) DEFAULT 'USD',
    from_date DATE DEFAULT CURRENT_DATE,
    thru_date DATE,
    comment TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    -- 业务规则：价格或百分比只能有一个
    CHECK ((price_amount IS NOT NULL AND percent_value IS NULL) OR
           (price_amount IS NULL AND percent_value IS NOT NULL)),
    -- 至少关联产品或特征之一
    CHECK (product_id IS NOT NULL OR feature_id IS NOT NULL)
);

-- PostgreSQL 18优化：虚拟生成列用于价格计算
ALTER TABLE price_component
ADD COLUMN effective_price NUMERIC(10,2)
GENERATED ALWAYS AS (
    CASE
        WHEN price_amount IS NOT NULL THEN price_amount
        WHEN percent_value IS NOT NULL THEN NULL -- 百分比需要结合基础价格计算
        ELSE NULL
    END
) STORED;
```

### 4.2 定价子类型

定价组件分为两个非互斥的子类型集合：

1. **按定价角色分类**: Base Price、Discount Component、Surcharge Component、Manufacturer's Suggested Price
2. **按定价频率分类**: One Time Charge、Recurring Charge、Utilization Charge

### 4.3 定价因子

定价可能基于多个变量的组合：

- **Geographic Boundary（地理边界）**: 国家、州、城市等
- **Party Type（客户类型）**: 客户分类（如少数族裔企业、政府机构）
- **Product Category（产品分类）**: 产品家族、型号、类型等
- **Quantity Break（数量分段）**: 不同数量级别的定价
- **Order Value（订单价值）**: 基于订单总金额的定价
- **Sale Type（销售类型）**: 互联网销售、零售、目录销售等

**定价因子关联表设计**:

```sql
-- 数量分段表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'quantity_break') THEN
            CREATE TABLE quantity_break (
                break_id SERIAL PRIMARY KEY,
                from_quantity INT NOT NULL CHECK (from_quantity >= 0),
                thru_quantity INT CHECK (thru_quantity IS NULL OR thru_quantity > from_quantity),
                description TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 quantity_break 创建成功';
        ELSE
            RAISE NOTICE '表 quantity_break 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 quantity_break 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 quantity_break 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 销售类型表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'sale_type') THEN
            CREATE TABLE sale_type (
                sale_type_id SERIAL PRIMARY KEY,
                type_code VARCHAR(50) UNIQUE NOT NULL,
                type_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 sale_type 创建成功';
        ELSE
            RAISE NOTICE '表 sale_type 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 sale_type 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 sale_type 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 初始化销售类型数据（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'sale_type') THEN
            RAISE WARNING '表 sale_type 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO sale_type (type_code, type_name) VALUES
            ('INTERNET', 'Internet Sales'),
            ('RETAIL', 'Retail Sales'),
            ('CATALOG', 'Catalog Sales'),
            ('WHOLESALE', 'Wholesale')
        ON CONFLICT (type_code) DO NOTHING;
        RAISE NOTICE '销售类型数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '销售类型数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入销售类型数据失败: %', SQLERRM;
    END;
END $$;

-- 价格组件与定价因子的关联（使用JSONB存储灵活的关联）
ALTER TABLE price_component ADD COLUMN pricing_factors JSONB;

-- 示例：存储定价因子（带错误处理）
DO $$
DECLARE
    updated_count INT;
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'price_component') THEN
            RAISE WARNING '表 price_component 不存在，无法更新定价因子';
            RETURN;
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'price_component' AND column_name = 'pricing_factors') THEN
            RAISE WARNING '列 pricing_factors 不存在，请先添加该列';
            RETURN;
        END IF;
        UPDATE price_component
        SET pricing_factors = jsonb_build_object(
            'geographic_boundary_id', 1,
            'party_type_id', 2,
            'quantity_break_id', 3,
            'product_category_id', 5,
            'sale_type_id', 1
        )
        WHERE price_component_id = :price_component_id;
        GET DIAGNOSTICS updated_count = ROW_COUNT;
        IF updated_count > 0 THEN
            RAISE NOTICE '定价因子更新成功，更新了 % 行', updated_count;
        ELSE
            RAISE NOTICE '未找到匹配的price_component_id，未更新任何行';
        END IF;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '更新定价因子失败: %', SQLERRM;
    END;
END $$;

-- GIN索引优化JSONB查询（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'price_component') THEN
            RAISE WARNING '表 price_component 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_price_component_factors') THEN
            CREATE INDEX idx_price_component_factors ON price_component USING GIN (pricing_factors);
            RAISE NOTICE 'GIN索引 idx_price_component_factors 创建成功';
        ELSE
            RAISE NOTICE '索引 idx_price_component_factors 已存在';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 price_component 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建GIN索引失败: %', SQLERRM;
    END;
END $$;
```

### 4.4 国际定价

支持多货币定价：

```sql
-- 货币表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'currency') THEN
            CREATE TABLE currency (
                currency_id SERIAL PRIMARY KEY,
                currency_code CHAR(3) UNIQUE NOT NULL,
                currency_name VARCHAR(100) NOT NULL,
                symbol VARCHAR(10),
                is_active BOOLEAN DEFAULT TRUE
            );
            RAISE NOTICE '表 currency 创建成功';
        ELSE
            RAISE NOTICE '表 currency 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 currency 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 currency 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 初始化货币数据（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'currency') THEN
            RAISE WARNING '表 currency 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO currency (currency_code, currency_name, symbol) VALUES
            ('USD', 'US Dollar', '$'),
            ('EUR', 'Euro', '€'),
            ('GBP', 'British Pound', '£'),
            ('CNY', 'Chinese Yuan', '¥'),
            ('JPY', 'Japanese Yen', '¥')
        ON CONFLICT (currency_code) DO NOTHING;
        RAISE NOTICE '货币数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '货币数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入货币数据失败: %', SQLERRM;
    END;
END $$;

-- 货币汇率表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'currency') THEN
            RAISE EXCEPTION '表 currency 不存在，无法创建currency_exchange_rate表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'currency_exchange_rate') THEN
            CREATE TABLE currency_exchange_rate (
                rate_id SERIAL PRIMARY KEY,
                from_currency_id INT NOT NULL REFERENCES currency(currency_id),
                to_currency_id INT NOT NULL REFERENCES currency(currency_id),
                exchange_rate NUMERIC(10,6) NOT NULL,
                rate_date DATE NOT NULL,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                UNIQUE(from_currency_id, to_currency_id, rate_date),
                CHECK (from_currency_id != to_currency_id)
            );
            RAISE NOTICE '表 currency_exchange_rate 创建成功';
        ELSE
            RAISE NOTICE '表 currency_exchange_rate 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 currency_exchange_rate 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 currency 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 currency_exchange_rate 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 价格组件已包含currency_code字段，可直接使用
```

---

## 5. 产品成本模型

### 5.1 Estimated Product Cost（预估产品成本）

**核心实体设计**:

```sql
-- 成本组件类型表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'cost_component_type') THEN
            CREATE TABLE cost_component_type (
                cost_type_id SERIAL PRIMARY KEY,
                type_code VARCHAR(50) UNIQUE NOT NULL,
                type_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 cost_component_type 创建成功';
        ELSE
            RAISE NOTICE '表 cost_component_type 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 cost_component_type 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 cost_component_type 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 初始化成本类型（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'cost_component_type') THEN
            RAISE WARNING '表 cost_component_type 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO cost_component_type (type_code, type_name) VALUES
            ('MATERIALS', 'Estimated Materials Cost'),
            ('LABOR', 'Estimated Labor Cost'),
            ('MANUFACTURING', 'Manufacturing Cost'),
            ('SHRINKAGE', 'Shrinkage Cost'),
            ('SHIPPING', 'Shipping Cost'),
            ('SELLING', 'Selling Cost'),
            ('ADMINISTRATIVE', 'Administrative Overhead')
        ON CONFLICT (type_code) DO NOTHING;
        RAISE NOTICE '成本类型数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '成本类型数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入成本类型数据失败: %', SQLERRM;
    END;
END $$;

-- 预估产品成本表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'cost_component_type') THEN
            RAISE EXCEPTION '表 cost_component_type 不存在，无法创建estimated_product_cost表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'estimated_product_cost') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
                RAISE WARNING '表 product 不存在，estimated_product_cost表的外键约束可能失败';
            END IF;
            CREATE TABLE estimated_product_cost (
                cost_id SERIAL PRIMARY KEY,
                product_id INT REFERENCES product(product_id) ON DELETE CASCADE,
                feature_id INT REFERENCES product_feature(feature_id) ON DELETE CASCADE,
                cost_type_id INT NOT NULL REFERENCES cost_component_type(cost_type_id),
                organization_id INT, -- 引用Organization（假设已有Party模型）
                geographic_boundary_id INT, -- 引用地理边界（可选）
                cost_amount NUMERIC(10,2) NOT NULL CHECK (cost_amount >= 0),
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                CHECK (product_id IS NOT NULL OR feature_id IS NOT NULL)
            );
            RAISE NOTICE '表 estimated_product_cost 创建成功';
        ELSE
            RAISE NOTICE '表 estimated_product_cost 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 estimated_product_cost 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 estimated_product_cost 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- PostgreSQL 18优化：虚拟生成列用于成本汇总
CREATE VIEW product_cost_summary AS
SELECT
    p.product_id,
    p.name AS product_name,
    SUM(epc.cost_amount) AS total_cost,
    jsonb_object_agg(ct.type_name, epc.cost_amount) AS cost_breakdown
FROM product p
LEFT JOIN estimated_product_cost epc ON p.product_id = epc.product_id
LEFT JOIN cost_component_type ct ON epc.cost_type_id = ct.cost_type_id
WHERE epc.thru_date IS NULL OR epc.thru_date >= CURRENT_DATE
GROUP BY p.product_id, p.name;
```

### 5.2 Cost Component Type（成本组件类型）

成本类型包括：材料成本、人工成本、制造成本、损耗成本、运输成本、销售成本、管理成本等。

---

## 6. 产品关联关系

### 6.1 Product Component（产品组件）

产品可以由其他产品组成，支持产品套件和产品包。

**核心实体设计**:

```sql
-- 产品组件表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE EXCEPTION '表 product 不存在，无法创建product_component表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_component') THEN
            CREATE TABLE product_component (
                component_id SERIAL PRIMARY KEY,
                parent_product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                child_product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                quantity_used NUMERIC(10,4) NOT NULL DEFAULT 1.0 CHECK (quantity_used > 0),
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                instruction TEXT,
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                CHECK (parent_product_id != child_product_id),
                UNIQUE(parent_product_id, child_product_id, from_date)
            );
            RAISE NOTICE '表 product_component 创建成功';
        ELSE
            RAISE NOTICE '表 product_component 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_component 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 product 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_component 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 递归查询产品组件结构
WITH RECURSIVE product_bom AS (
    -- 基础查询：起始产品
    SELECT
        p.product_id,
        p.name,
        pc.child_product_id,
        pc.quantity_used,
        1 AS level
    FROM product p
    INNER JOIN product_component pc ON p.product_id = pc.parent_product_id
    WHERE p.product_id = :root_product_id

    UNION ALL

    -- 递归查询：子产品组件
    SELECT
        p.product_id,
        p.name,
        pc.child_product_id,
        pc.quantity_used * pbom.quantity_used,
        pbom.level + 1
    FROM product p
    INNER JOIN product_component pc ON p.product_id = pc.parent_product_id
    INNER JOIN product_bom pbom ON pc.parent_product_id = pbom.child_product_id
    WHERE pbom.level < 10 -- 防止无限递归
)
-- 查询产品BOM结构（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_component') THEN
            RAISE WARNING '必需的表不存在，无法执行BOM查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行查询：产品BOM结构';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT * FROM product_bom;
```

### 6.2 Product Substitute（产品替代）

**核心实体设计**:

```sql
-- 产品替代表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE EXCEPTION '表 product 不存在，无法创建product_substitute表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_substitute') THEN
            CREATE TABLE product_substitute (
                substitute_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                substitute_product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                quantity_ratio NUMERIC(10,4) DEFAULT 1.0 CHECK (quantity_ratio > 0),
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                CHECK (product_id != substitute_product_id),
                UNIQUE(product_id, substitute_product_id, from_date)
            );
            RAISE NOTICE '表 product_substitute 创建成功';
        ELSE
            RAISE NOTICE '表 product_substitute 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_substitute 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 product 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_substitute 失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 6.3 Product Obsolescence（产品淘汰）

**核心实体设计**:

```sql
-- 产品淘汰表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE EXCEPTION '表 product 不存在，无法创建product_obsolescence表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_obsolescence') THEN
            CREATE TABLE product_obsolescence (
                obsolescence_id SERIAL PRIMARY KEY,
                obsolete_product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                replacement_product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                CHECK (obsolete_product_id != replacement_product_id)
            );
            RAISE NOTICE '表 product_obsolescence 创建成功';
        ELSE
            RAISE NOTICE '表 product_obsolescence 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_obsolescence 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 product 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_obsolescence 失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 6.4 Product Complement（产品互补）

**核心实体设计**:

```sql
-- 产品互补表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE EXCEPTION '表 product 不存在，无法创建product_complement表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_complement') THEN
            CREATE TABLE product_complement (
                complement_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                complement_product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                CHECK (product_id != complement_product_id),
                UNIQUE(product_id, complement_product_id, from_date)
            );
            RAISE NOTICE '表 product_complement 创建成功';
        ELSE
            RAISE NOTICE '表 product_complement 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_complement 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 product 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_complement 失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 6.5 Product Incompatibility（产品不兼容）

**核心实体设计**:

```sql
-- 产品不兼容表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE EXCEPTION '表 product 不存在，无法创建product_incompatibility表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_incompatibility') THEN
            CREATE TABLE product_incompatibility (
                incompatibility_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                incompatible_product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                reason TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                CHECK (product_id != incompatible_product_id),
                UNIQUE(product_id, incompatible_product_id, from_date)
            );
            RAISE NOTICE '表 product_incompatibility 创建成功';
        ELSE
            RAISE NOTICE '表 product_incompatibility 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 product_incompatibility 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 product 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 product_incompatibility 失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

---

## 7. 供应商与制造商

### 7.1 Supplier Product（供应商产品）

**核心实体设计**:

```sql
-- 供应商产品表（需要Party模型支持，带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE EXCEPTION '表 product 不存在，无法创建supplier_product表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'supplier_product') THEN
            CREATE TABLE supplier_product (
                supplier_product_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES product(product_id) ON DELETE CASCADE,
                supplier_party_id INT NOT NULL, -- 引用Party（假设已有Party模型）
                preference_type_id INT, -- 引用偏好类型
                rating_type_id INT, -- 引用评级类型
                standard_lead_time INTERVAL,
                available_from_date DATE DEFAULT CURRENT_DATE,
                available_thru_date DATE,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                UNIQUE(product_id, supplier_party_id, available_from_date)
            );
            RAISE NOTICE '表 supplier_product 创建成功';
        ELSE
            RAISE NOTICE '表 supplier_product 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 supplier_product 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 product 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 supplier_product 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 索引优化（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'supplier_product') THEN
            RAISE WARNING '表 supplier_product 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_supplier_product_supplier') THEN
            CREATE INDEX idx_supplier_product_supplier ON supplier_product(supplier_party_id);
            RAISE NOTICE '索引 idx_supplier_product_supplier 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_supplier_product_product') THEN
            CREATE INDEX idx_supplier_product_product ON supplier_product(product_id);
            RAISE NOTICE '索引 idx_supplier_product_product 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_supplier_product_active') THEN
            CREATE INDEX idx_supplier_product_active ON supplier_product(product_id, supplier_party_id)
            WHERE available_thru_date IS NULL OR available_thru_date >= CURRENT_DATE;
            RAISE NOTICE '部分索引 idx_supplier_product_active 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 supplier_product 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建供应商产品索引失败: %', SQLERRM;
    END;
END $$;
```

### 7.2 Manufacturer（制造商）

产品只能由一个组织制造。如果组织分包给其他组织，仍视为原组织是制造商。

### 7.3 Reorder Guideline（补货指南）

**核心实体设计**:

```sql
-- 补货指南表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'goods') THEN
            RAISE EXCEPTION '表 goods 不存在，无法创建reorder_guideline表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'reorder_guideline') THEN
            CREATE TABLE reorder_guideline (
                guideline_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES goods(product_id) ON DELETE CASCADE, -- 仅适用于商品
                geographic_boundary_id INT, -- 引用地理边界（可选）
                facility_id INT, -- 引用设施（可选）
                organization_id INT, -- 引用内部组织（可选）
                reorder_level NUMERIC(10,2) NOT NULL CHECK (reorder_level >= 0),
                reorder_quantity NUMERIC(10,2) NOT NULL CHECK (reorder_quantity > 0),
                from_date DATE DEFAULT CURRENT_DATE,
                thru_date DATE,
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 reorder_guideline 创建成功';
        ELSE
            RAISE NOTICE '表 reorder_guideline 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 reorder_guideline 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 goods 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 reorder_guideline 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 查询需要补货的产品（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'goods') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'reorder_guideline') THEN
            RAISE WARNING '必需的表不存在，无法执行补货查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行查询：需要补货的产品';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    g.product_id,
    g.name,
    rg.reorder_level,
    COALESCE(SUM(ii.quantity_on_hand), 0) AS current_stock,
    rg.reorder_quantity AS suggested_order_quantity
FROM goods g
INNER JOIN reorder_guideline rg ON g.product_id = rg.product_id
LEFT JOIN inventory_item ii ON g.product_id = ii.product_id
WHERE (rg.thru_date IS NULL OR rg.thru_date >= CURRENT_DATE)
GROUP BY g.product_id, g.name, rg.reorder_level, rg.reorder_quantity
HAVING COALESCE(SUM(ii.quantity_on_hand), 0) <= rg.reorder_level;
```

---

## 8. 库存管理

### 8.1 Inventory Item（库存项）

**核心实体设计**:

```sql
-- 库存项状态类型表（带错误处理，先创建状态类型表，因为inventory_item需要引用它）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item_status_type') THEN
            CREATE TABLE inventory_item_status_type (
                status_type_id SERIAL PRIMARY KEY,
                status_code VARCHAR(50) UNIQUE NOT NULL,
                status_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 inventory_item_status_type 创建成功';
        ELSE
            RAISE NOTICE '表 inventory_item_status_type 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 inventory_item_status_type 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 inventory_item_status_type 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 初始化库存项状态类型数据（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item_status_type') THEN
            RAISE WARNING '表 inventory_item_status_type 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO inventory_item_status_type (status_code, status_name) VALUES
            ('GOOD', 'Good'),
            ('REPAIRING', 'Being Repaired'),
            ('DAMAGED', 'Slightly Damaged'),
            ('DEFECTIVE', 'Defective'),
            ('SCRAP', 'Scrap')
        ON CONFLICT (status_code) DO NOTHING;
        RAISE NOTICE '库存项状态类型数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '库存项状态类型数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入库存项状态类型数据失败: %', SQLERRM;
    END;
END $$;

-- 库存项表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'goods') THEN
            RAISE EXCEPTION '表 goods 不存在，无法创建inventory_item表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item') THEN
            CREATE TABLE inventory_item (
                inventory_item_id SERIAL PRIMARY KEY,
                product_id INT NOT NULL REFERENCES goods(product_id) ON DELETE CASCADE,
                lot_id INT REFERENCES lot(lot_id),
                facility_id INT, -- 引用设施
                container_id INT, -- 引用容器（可选）
                inventory_type CHAR(1) NOT NULL CHECK (inventory_type IN ('S', 'N')), -- S=Serialized, N=Non-serialized
                serial_number VARCHAR(100),
                quantity_on_hand NUMERIC(10,2) NOT NULL DEFAULT 0 CHECK (quantity_on_hand >= 0),
                status_type_id INT REFERENCES inventory_item_status_type(status_type_id),
                created_at TIMESTAMPTZ DEFAULT NOW(),
                updated_at TIMESTAMPTZ DEFAULT NOW()
            ) PARTITION BY LIST (inventory_type);
            RAISE NOTICE '分区表 inventory_item 创建成功';
        ELSE
            RAISE NOTICE '表 inventory_item 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 inventory_item 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 goods 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 inventory_item 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 序列化库存项子表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item') THEN
            RAISE WARNING '主表 inventory_item 不存在，无法创建分区';
            RETURN;
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'serialized_inventory_item') THEN
            CREATE TABLE serialized_inventory_item PARTITION OF inventory_item
                FOR VALUES IN ('S');
            RAISE NOTICE '分区 serialized_inventory_item 创建成功';
        ELSE
            RAISE NOTICE '分区 serialized_inventory_item 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '分区 serialized_inventory_item 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建分区 serialized_inventory_item 失败: %', SQLERRM;
    END;
END $$;

-- 非序列化库存项子表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item') THEN
            RAISE WARNING '主表 inventory_item 不存在，无法创建分区';
            RETURN;
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'non_serialized_inventory_item') THEN
            CREATE TABLE non_serialized_inventory_item PARTITION OF inventory_item
                FOR VALUES IN ('N');
            RAISE NOTICE '分区 non_serialized_inventory_item 创建成功';
        ELSE
            RAISE NOTICE '分区 non_serialized_inventory_item 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '分区 non_serialized_inventory_item 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建分区 non_serialized_inventory_item 失败: %', SQLERRM;
    END;
END $$;
```

### 8.2 Lot（批次）管理

**核心实体设计**:

```sql
-- 批次表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'goods') THEN
            RAISE EXCEPTION '表 goods 不存在，无法创建lot表';
        END IF;
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'lot') THEN
            CREATE TABLE lot (
                lot_id SERIAL PRIMARY KEY,
                lot_number VARCHAR(100) UNIQUE NOT NULL,
                product_id INT NOT NULL REFERENCES goods(product_id),
                production_date DATE,
                expiration_date DATE,
                source_type VARCHAR(50), -- 'PRODUCTION', 'SUPPLIER_SHIPMENT'
                source_reference TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 lot 创建成功';
        ELSE
            RAISE NOTICE '表 lot 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 lot 已存在';
        WHEN undefined_table THEN
            RAISE WARNING '引用表 goods 不存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 lot 失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 索引优化（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'lot') THEN
            RAISE WARNING '表 lot 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_lot_product') THEN
            CREATE INDEX idx_lot_product ON lot(product_id);
            RAISE NOTICE '索引 idx_lot_product 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_lot_number') THEN
            CREATE INDEX idx_lot_number ON lot(lot_number);
            RAISE NOTICE '索引 idx_lot_number 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 lot 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建批次索引失败: %', SQLERRM;
    END;
END $$;
```

### 8.3 Inventory Item Variance（库存差异）

**核心实体设计**:

```sql
-- 库存差异原因表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'variance_reason') THEN
            CREATE TABLE variance_reason (
                reason_id SERIAL PRIMARY KEY,
                reason_code VARCHAR(50) UNIQUE NOT NULL,
                reason_name VARCHAR(100) NOT NULL,
                description TEXT
            );
            RAISE NOTICE '表 variance_reason 创建成功';
        ELSE
            RAISE NOTICE '表 variance_reason 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 variance_reason 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 variance_reason 失败: %', SQLERRM;
    END;
END $$;

-- 初始化差异原因数据（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'variance_reason') THEN
            RAISE WARNING '表 variance_reason 不存在，无法插入数据';
            RETURN;
        END IF;
        INSERT INTO variance_reason (reason_code, reason_name) VALUES
            ('THEFT', 'Theft'),
            ('SHRINKAGE', 'Shrinkage'),
            ('UNKNOWN', 'Unknown Variance'),
            ('RUINED', 'Ruined Goods')
        ON CONFLICT (reason_code) DO NOTHING;
        RAISE NOTICE '差异原因数据插入成功';
    EXCEPTION
        WHEN unique_violation THEN
            RAISE NOTICE '差异原因数据已存在，跳过插入';
        WHEN OTHERS THEN
            RAISE WARNING '插入差异原因数据失败: %', SQLERRM;
    END;
END $$;

-- 库存差异表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item_variance') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item') OR
               NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'variance_reason') THEN
                RAISE WARNING '必需的表不存在（inventory_item或variance_reason），无法创建inventory_item_variance表';
                RETURN;
            END IF;
            CREATE TABLE inventory_item_variance (
                variance_id SERIAL PRIMARY KEY,
                inventory_item_id INT NOT NULL REFERENCES inventory_item(inventory_item_id) ON DELETE CASCADE,
                reason_id INT NOT NULL REFERENCES variance_reason(reason_id),
                physical_inventory_date DATE NOT NULL,
                quantity_variance NUMERIC(10,2) NOT NULL, -- 正数表示盘盈，负数表示盘亏
                comment TEXT,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 inventory_item_variance 创建成功';
        ELSE
            RAISE NOTICE '表 inventory_item_variance 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 inventory_item_variance 已存在';
        WHEN foreign_key_violation THEN
            RAISE WARNING '外键约束违反，请先创建inventory_item和variance_reason表';
        WHEN OTHERS THEN
            RAISE WARNING '创建表 inventory_item_variance 失败: %', SQLERRM;
    END;
END $$;

-- 触发器：自动调整库存数量
CREATE OR REPLACE FUNCTION adjust_inventory_on_variance()
RETURNS TRIGGER AS $$
BEGIN
    UPDATE inventory_item
    SET quantity_on_hand = quantity_on_hand + NEW.quantity_variance,
        updated_at = NOW()
    WHERE inventory_item_id = NEW.inventory_item_id;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_adjust_inventory_variance
    AFTER INSERT ON inventory_item_variance
    FOR EACH ROW
    EXECUTE FUNCTION adjust_inventory_on_variance();
```

---

## 9. PostgreSQL完整实现

### 9.1 完整DDL脚本

完整的产品管理模型DDL脚本已在前述章节中提供。所有表都包含：

- ✅ 完整的约束（CHECK、UNIQUE、FOREIGN KEY）
- ✅ 索引优化
- ✅ PostgreSQL 18特性支持（虚拟生成列、分区表等）
- ✅ 错误处理机制

### 9.2 索引设计

**关键索引策略**:

```sql
-- 产品表索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE WARNING '表 product 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_code') THEN
            CREATE INDEX idx_product_code ON product(product_code);
            RAISE NOTICE '索引 idx_product_code 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_type') THEN
            CREATE INDEX idx_product_type ON product(product_type);
            RAISE NOTICE '索引 idx_product_type 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_name') THEN
            CREATE INDEX idx_product_name ON product USING gin(to_tsvector('english', name));
            RAISE NOTICE '索引 idx_product_name 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 product 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建产品表索引失败: %', SQLERRM;
    END;
END $$;

-- 产品分类索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category') THEN
            RAISE WARNING '表 product_category 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_category_parent') THEN
            CREATE INDEX idx_product_category_parent ON product_category(parent_category_id);
            RAISE NOTICE '索引 idx_product_category_parent 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_category_code') THEN
            CREATE INDEX idx_product_category_code ON product_category(category_code);
            RAISE NOTICE '索引 idx_product_category_code 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 product_category 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建产品分类索引失败: %', SQLERRM;
    END;
END $$;

-- 产品分类关联表索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_category_classification') THEN
            RAISE WARNING '表 product_category_classification 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_category_classification_product') THEN
            CREATE INDEX idx_product_category_classification_product ON product_category_classification(product_id);
            RAISE NOTICE '索引 idx_product_category_classification_product 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_category_classification_category') THEN
            CREATE INDEX idx_product_category_classification_category ON product_category_classification(category_id);
            RAISE NOTICE '索引 idx_product_category_classification_category 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 product_category_classification 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建产品分类关联表索引失败: %', SQLERRM;
    END;
END $$;

-- 产品特征索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature') THEN
            RAISE WARNING '表 product_feature 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_feature_category') THEN
            CREATE INDEX idx_product_feature_category ON product_feature(category_id);
            RAISE NOTICE '索引 idx_product_feature_category 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 product_feature 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建产品特征索引失败: %', SQLERRM;
    END;
END $$;
-- 产品特征适用性索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_feature_applicability') THEN
            RAISE WARNING '表 product_feature_applicability 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_feature_applicability_product') THEN
            CREATE INDEX idx_product_feature_applicability_product ON product_feature_applicability(product_id);
            RAISE NOTICE '索引 idx_product_feature_applicability_product 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_product_feature_applicability_feature') THEN
            CREATE INDEX idx_product_feature_applicability_feature ON product_feature_applicability(feature_id);
            RAISE NOTICE '索引 idx_product_feature_applicability_feature 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 product_feature_applicability 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建产品特征适用性索引失败: %', SQLERRM;
    END;
END $$;

-- 定价索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'price_component') THEN
            RAISE WARNING '表 price_component 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_price_component_product') THEN
            CREATE INDEX idx_price_component_product ON price_component(product_id);
            RAISE NOTICE '索引 idx_price_component_product 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_price_component_feature') THEN
            CREATE INDEX idx_price_component_feature ON price_component(feature_id);
            RAISE NOTICE '索引 idx_price_component_feature 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_price_component_dates') THEN
            CREATE INDEX idx_price_component_dates ON price_component USING gist (daterange(from_date, COALESCE(thru_date, '9999-12-31'::date)));
            RAISE NOTICE '索引 idx_price_component_dates 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 price_component 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建定价索引失败: %', SQLERRM;
    END;
END $$;

-- 库存索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'inventory_item') THEN
            RAISE WARNING '表 inventory_item 不存在，无法创建索引';
            RETURN;
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_inventory_item_product') THEN
            CREATE INDEX idx_inventory_item_product ON inventory_item(product_id);
            RAISE NOTICE '索引 idx_inventory_item_product 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_inventory_item_facility') THEN
            CREATE INDEX idx_inventory_item_facility ON inventory_item(facility_id);
            RAISE NOTICE '索引 idx_inventory_item_facility 创建成功';
        END IF;

        IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_inventory_item_status') THEN
            CREATE INDEX idx_inventory_item_status ON inventory_item(status_type_id);
            RAISE NOTICE '索引 idx_inventory_item_status 创建成功';
        END IF;
    EXCEPTION
        WHEN undefined_table THEN
            RAISE WARNING '表 inventory_item 不存在';
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建库存索引失败: %', SQLERRM;
    END;
END $$;
```

### 9.3 约束设计

**关键约束**:

- ✅ 产品类型约束：只能是Goods或Services
- ✅ 价格组件约束：价格或百分比只能有一个
- ✅ 产品组件约束：不能自己包含自己
- ✅ 计量单位转换约束：转换因子必须大于0
- ✅ 库存数量约束：数量不能为负

### 9.4 PostgreSQL 18优化

**PostgreSQL 18新特性应用**:

1. **虚拟生成列**: 用于价格计算、成本汇总
2. **分区表**: 产品按类型分区（Goods/Services），库存按类型分区（Serialized/Non-serialized）
3. **JSONB和GIN索引**: 存储灵活的定价因子
4. **范围索引**: 使用GIST索引优化日期范围查询

---

## 10. 查询示例

### 查询1: 获取产品的完整信息

```sql
-- 查询产品完整信息（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') THEN
            RAISE WARNING '表 product 不存在，无法执行查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行查询：产品完整信息';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    p.product_id,
    p.name,
    p.product_type,
    pc.category_name AS primary_category,
    uom.unit_name AS unit_of_measure,
    jsonb_agg(DISTINCT jsonb_build_object(
        'feature', pf.feature_name,
        'type', pfa.applicability_type
    )) AS features
FROM product p
LEFT JOIN product_category_classification pcc ON p.product_id = pcc.product_id AND pcc.is_primary = TRUE
LEFT JOIN product_category pc ON pcc.category_id = pc.category_id
LEFT JOIN unit_of_measure uom ON p.unit_of_measure_id = uom.unit_id
LEFT JOIN product_feature_applicability pfa ON p.product_id = pfa.product_id
LEFT JOIN product_feature pf ON pfa.feature_id = pf.feature_id
WHERE p.product_id = :product_id
GROUP BY p.product_id, p.name, p.product_type, pc.category_name, uom.unit_name;
```

### 查询2: 计算产品当前价格（考虑所有定价因子）

```sql
-- 计算产品价格函数
CREATE OR REPLACE FUNCTION calculate_product_price(
    p_product_id INT,
    p_party_type_id INT DEFAULT NULL,
    p_geographic_boundary_id INT DEFAULT NULL,
    p_quantity INT DEFAULT 1,
    p_sale_type_id INT DEFAULT NULL
)
RETURNS NUMERIC(10,2) AS $$
DECLARE
    v_base_price NUMERIC(10,2) := 0;
    v_discount_pct NUMERIC(5,2) := 0;
    v_surcharge NUMERIC(10,2) := 0;
    v_final_price NUMERIC(10,2);
BEGIN
    -- 获取基础价格（考虑所有定价因子）
    SELECT COALESCE(MAX(pc.price_amount), 0)
    INTO v_base_price
    FROM price_component pc
    INNER JOIN price_component_type pct ON pc.type_id = pct.type_id
    WHERE pc.product_id = p_product_id
        AND pct.type_code = 'BASE_PRICE'
        AND (pc.from_date <= CURRENT_DATE)
        AND (pc.thru_date IS NULL OR pc.thru_date >= CURRENT_DATE)
        AND (
            pc.pricing_factors IS NULL OR
            (p_party_type_id IS NULL OR (pc.pricing_factors->>'party_type_id')::INT = p_party_type_id) AND
            (p_geographic_boundary_id IS NULL OR (pc.pricing_factors->>'geographic_boundary_id')::INT = p_geographic_boundary_id) AND
            (p_sale_type_id IS NULL OR (pc.pricing_factors->>'sale_type_id')::INT = p_sale_type_id)
        );

    -- 计算折扣
    SELECT COALESCE(MAX(pc.percent_value), 0)
    INTO v_discount_pct
    FROM price_component pc
    INNER JOIN price_component_type pct ON pc.type_id = pct.type_id
    WHERE pc.product_id = p_product_id
        AND pct.type_code = 'DISCOUNT'
        AND (pc.from_date <= CURRENT_DATE)
        AND (pc.thru_date IS NULL OR pc.thru_date >= CURRENT_DATE);

    -- 计算附加费
    SELECT COALESCE(SUM(pc.price_amount), 0)
    INTO v_surcharge
    FROM price_component pc
    INNER JOIN price_component_type pct ON pc.type_id = pct.type_id
    WHERE pc.product_id = p_product_id
        AND pct.type_code = 'SURCHARGE'
        AND (pc.from_date <= CURRENT_DATE)
        AND (pc.thru_date IS NULL OR pc.thru_date >= CURRENT_DATE);

    -- 计算最终价格
    v_final_price := (v_base_price * (1 - v_discount_pct / 100) + v_surcharge) * p_quantity;

    RETURN v_final_price;
EXCEPTION
    WHEN OTHERS THEN
        RAISE WARNING '计算产品价格失败: %', SQLERRM;
        RETURN NULL;
END;
$$ LANGUAGE plpgsql;
```

### 查询3: 获取产品BOM（物料清单）

```sql
-- 递归查询产品BOM（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product') OR
           NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_component') THEN
            RAISE WARNING '必需的表不存在，无法执行BOM查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始执行查询：产品BOM结构';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
WITH RECURSIVE product_bom AS (
    SELECT
        p.product_id AS root_product_id,
        p.name AS root_product_name,
        p.product_id,
        p.name,
        pc.quantity_used,
        1 AS level,
        CAST(p.name AS TEXT) AS path
    FROM product p
    LEFT JOIN product_component pc ON p.product_id = pc.parent_product_id
    WHERE p.product_id = :root_product_id

    UNION ALL

    SELECT
        pbom.root_product_id,
        pbom.root_product_name,
        p.product_id,
        p.name,
        pc.quantity_used * pbom.quantity_used,
        pbom.level + 1,
        pbom.path || ' -> ' || p.name
    FROM product_bom pbom
    INNER JOIN product_component pc ON pbom.product_id = pc.parent_product_id
    INNER JOIN product p ON pc.child_product_id = p.product_id
    WHERE pbom.level < 10
)
SELECT
    root_product_name,
    name AS component_name,
    quantity_used,
    level,
    path
FROM product_bom
ORDER BY level, name;
```

---

## 11. 性能优化

### 11.1 索引优化策略

1. **产品查询**: 使用product_code和name的索引
2. **分类查询**: 使用递归CTE + 索引优化层次查询
3. **特征查询**: 使用GIN索引优化JSONB和全文搜索
4. **定价查询**: 使用日期范围索引和部分索引
5. **库存查询**: 使用复合索引（product_id + facility_id）

### 11.2 分区策略

- **产品表**: 按product_type分区（Goods/Services）
- **库存表**: 按inventory_type分区（Serialized/Non-serialized）
- **价格组件表**: 可按from_date分区（时间范围分区）

### 11.3 查询优化建议

1. 使用物化视图缓存复杂查询结果
2. 使用部分索引过滤活跃数据
3. 使用JSONB存储灵活的定价因子，避免过多关联表

---

## 12. 实际应用案例

### 案例1: 电商产品管理

**场景**: 电商平台需要管理商品、SKU、价格、库存、分类等。

**实现要点**:

- 使用product表存储商品基础信息
- 使用product_feature管理SKU（颜色、尺寸等）
- 使用price_component实现动态定价
- 使用inventory_item管理多仓库库存

### 案例2: B2B产品目录管理

**场景**: B2B平台需要管理产品目录、供应商、多维度定价。

**实现要点**:

- 使用supplier_product管理供应商产品
- 使用price_component实现基于客户类型的差异化定价
- 使用product_category实现产品目录层次结构

### 案例3: 制造业产品管理

**场景**: 制造企业需要管理BOM、批次、成本等。

**实现要点**:

- 使用product_component管理BOM
- 使用lot管理批次
- 使用estimated_product_cost管理成本
- 使用inventory_item管理序列化库存

---

## 13. 常见问题解答 / FAQ

### Q1: 产品模型相比传统设计有什么优势？

**A**: 产品模型的优势包括：

- 统一管理：统一存储企业自有产品、供应商产品和竞争对手产品
- 灵活分类：支持多维度产品分类和层次结构
- 特征管理：灵活的产品特征和变体管理，避免为每个变体创建独立产品
- 定价灵活：支持复杂的多维度定价策略

### Q2: 如何处理产品的大量变体（SKU）？

**A**: 使用产品特征模型：

- 基础产品：存储在product表
- 变体特征：使用product_feature_applicability关联特征
- 查询时：通过特征组合动态生成SKU
- 优势：避免为每个SKU创建独立产品记录

### Q3: 如何实现复杂的产品定价规则？

**A**: 使用价格组件模型：

- 基础价格：存储在price_component表中
- 定价因子：使用JSONB存储灵活的定价因子组合
- 计算逻辑：使用函数计算最终价格，考虑所有定价因子
- 业务规则：在应用层实现定价规则优先级

### Q4: 产品模型如何处理产品版本和淘汰？

**A**: 使用产品关联模型：

- 产品淘汰：使用product_obsolescence记录产品替代关系
- 产品版本：可以使用product_component表示版本升级
- 历史追踪：通过from_date/thru_date管理时间有效性

### Q5: 如何优化产品模型的查询性能？

**A**: 性能优化策略：

- 索引：为常用查询字段创建索引
- 分区：对大数据量表进行分区
- 物化视图：缓存复杂查询结果
- JSONB索引：使用GIN索引优化JSONB查询

---

**最后更新**: 2025年1月
**维护者**: PostgreSQL Modern Team
**状态**: ✅ 文档完成，基于Volume 1 Chapter 3完整整合

---

## 完整实现脚本补充

### 完整DDL脚本（带错误处理）

```sql
-- 完整产品管理模型DDL脚本
-- 基于Silverston Volume 1 Chapter 3

-- 启用必要的扩展
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "btree_gin";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";

-- 产品表（已在前面定义）
-- ... (前面已定义的表结构)

-- 添加PostgreSQL 18优化的虚拟生成列
ALTER TABLE price_component
ADD COLUMN IF NOT EXISTS calculated_price NUMERIC(10,2)
GENERATED ALWAYS AS (
    CASE
        WHEN price_amount IS NOT NULL THEN price_amount
        ELSE NULL
    END
) STORED;

-- 添加触发器自动更新时间戳
CREATE OR REPLACE FUNCTION update_product_timestamp()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_product_updated_at
    BEFORE UPDATE ON product
    FOR EACH ROW
    EXECUTE FUNCTION update_product_timestamp();
```

### 完整查询函数库

```sql
-- 产品价格计算函数（完整版）
CREATE OR REPLACE FUNCTION get_product_price(
    p_product_id INT,
    p_customer_party_id INT DEFAULT NULL,
    p_geographic_boundary_id INT DEFAULT NULL,
    p_quantity NUMERIC DEFAULT 1,
    p_sale_date DATE DEFAULT CURRENT_DATE
)
RETURNS TABLE (
    base_price NUMERIC(10,2),
    discount_amount NUMERIC(10,2),
    surcharge_amount NUMERIC(10,2),
    final_price NUMERIC(10,2)
) AS $$
BEGIN
    RETURN QUERY
    WITH base_price_calc AS (
        SELECT COALESCE(MAX(pc.price_amount), 0) AS price
        FROM price_component pc
        INNER JOIN price_component_type pct ON pc.type_id = pct.type_id
        WHERE pc.product_id = p_product_id
            AND pct.type_code = 'BASE_PRICE'
            AND pc.from_date <= p_sale_date
            AND (pc.thru_date IS NULL OR pc.thru_date >= p_sale_date)
    ),
    discount_calc AS (
        SELECT COALESCE(MAX(pc.percent_value), 0) AS discount_pct
        FROM price_component pc
        INNER JOIN price_component_type pct ON pc.type_id = pct.type_id
        WHERE pc.product_id = p_product_id
            AND pct.type_code = 'DISCOUNT'
            AND pc.from_date <= p_sale_date
            AND (pc.thru_date IS NULL OR pc.thru_date >= p_sale_date)
    ),
    surcharge_calc AS (
        SELECT COALESCE(SUM(pc.price_amount), 0) AS surcharge
        FROM price_component pc
        INNER JOIN price_component_type pct ON pc.type_id = pct.type_id
        WHERE pc.product_id = p_product_id
            AND pct.type_code = 'SURCHARGE'
            AND pc.from_date <= p_sale_date
            AND (pc.thru_date IS NULL OR pc.thru_date >= p_sale_date)
    )
    SELECT
        bp.price,
        bp.price * (dc.discount_pct / 100) AS discount,
        sc.surcharge,
        (bp.price * (1 - dc.discount_pct / 100) + sc.surcharge) * p_quantity AS final
    FROM base_price_calc bp, discount_calc dc, surcharge_calc sc;
END;
$$ LANGUAGE plpgsql;
```

---

**文档状态**: ✅ 完整整合Volume 1 Chapter 3所有核心内容

---

## 14. 相关资源

### 14.1 核心相关文档

- [Party模型](./Party模型.md) - 人员与组织模型（产品供应商关联）
- [订单管理模型](./订单管理模型.md) - 订单中使用产品
- [Silverston数据模型资源手册](../02-权威资源与标准/Silverston数据模型资源手册.md) - 三卷本详解

### 14.2 归档资源

- [Volume 1完整内容](../../99-归档/参考资源/books/The Data Model Resource Book 1/full.md) - Chapter 3: Products

### 14.3 参考资源

- [PostgreSQL 18文档](https://www.postgresql.org/docs/18/) - PostgreSQL 18官方文档
- [Silverston官网](https://www.silverston.com/) - 数据模型资源手册官方资源
