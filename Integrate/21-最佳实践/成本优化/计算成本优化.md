# PostgreSQL计算成本优化指南

> **创建日期**: 2025年1月
> **技术版本**: PostgreSQL 17+/18+
> **难度等级**: ⭐⭐⭐ 中级

---

## 📋 目录

- [PostgreSQL计算成本优化指南](#postgresql计算成本优化指南)
  - [📋 目录](#-目录)
  - [1. 概述](#1-概述)
  - [2. 查询优化](#2-查询优化)
    - [2.1 慢查询优化](#21-慢查询优化)
    - [2.2 查询重写](#22-查询重写)
  - [3. 索引优化](#3-索引优化)
    - [3.1 索引分析](#31-索引分析)
    - [3.2 索引维护](#32-索引维护)
  - [4. 连接优化](#4-连接优化)
    - [4.1 连接池](#41-连接池)
    - [4.2 连接管理](#42-连接管理)
  - [5. 配置优化](#5-配置优化)
    - [5.1 性能参数](#51-性能参数)
    - [5.2 并行查询](#52-并行查询)
  - [📚 相关文档](#-相关文档)

---

## 1. 概述

计算成本优化通过减少CPU使用降低计算成本。

**优化策略**:

- 查询优化
- 索引优化
- 连接优化
- 配置优化

---

## 2. 查询优化

### 2.1 慢查询优化

```sql
-- 查找慢查询
SELECT
    query,
    calls,
    total_exec_time,
    mean_exec_time
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 10;
```

### 2.2 查询重写

**查询优化示例**：

```sql
-- 优化前
SELECT * FROM users WHERE email LIKE '%@example.com';

-- 优化后
SELECT * FROM users WHERE email LIKE 'user%@example.com';
-- 使用前缀索引
CREATE INDEX idx_users_email_prefix ON users(email text_pattern_ops);
```

**查询成本分析**：

```sql
-- 创建查询成本分析表
CREATE TABLE IF NOT EXISTS query_cost_analysis (
    query_id SERIAL PRIMARY KEY,
    query_hash BIGINT,
    query_text TEXT,
    execution_count BIGINT,
    total_exec_time NUMERIC,
    mean_exec_time NUMERIC,
    max_exec_time NUMERIC,
    cost_per_execution NUMERIC,
    total_cost NUMERIC,
    optimization_potential NUMERIC,
    analyzed_date TIMESTAMPTZ DEFAULT NOW()
);

-- 从pg_stat_statements导入查询统计
INSERT INTO query_cost_analysis
(query_hash, query_text, execution_count, total_exec_time, mean_exec_time, max_exec_time)
SELECT
    queryid,
    LEFT(query, 200) AS query_text,
    calls,
    total_exec_time,
    mean_exec_time,
    max_exec_time
FROM pg_stat_statements
WHERE calls > 100
ORDER BY total_exec_time DESC
LIMIT 50;

-- 计算查询成本（假设CPU成本与执行时间成正比）
UPDATE query_cost_analysis
SET
    cost_per_execution = mean_exec_time * 0.001, -- 假设每毫秒0.001成本单位
    total_cost = mean_exec_time * execution_count * 0.001,
    optimization_potential = CASE
        WHEN mean_exec_time > 1000 THEN 50.0 -- 慢查询优化潜力50%
        WHEN mean_exec_time > 100 THEN 30.0 -- 中等查询优化潜力30%
        ELSE 10.0 -- 快速查询优化潜力10%
    END;

-- 查询高成本查询
SELECT
    query_text,
    execution_count,
    ROUND(mean_exec_time, 2) AS mean_ms,
    ROUND(total_cost, 2) AS total_cost,
    optimization_potential || '%' AS optimization_potential,
    ROUND(total_cost * optimization_potential / 100, 2) AS potential_savings
FROM query_cost_analysis
ORDER BY total_cost DESC
LIMIT 20;
```

**查询优化建议生成**：

```sql
-- 创建查询优化建议函数
CREATE OR REPLACE FUNCTION generate_query_optimization_suggestions()
RETURNS TABLE (
    query_text TEXT,
    current_cost NUMERIC,
    optimization_suggestion TEXT,
    expected_savings NUMERIC
) AS $$
BEGIN
    RETURN QUERY
    SELECT
        qca.query_text,
        qca.total_cost,
        CASE
            WHEN qca.query_text LIKE '%LIKE ''%%%' THEN '使用全文搜索索引替代LIKE查询'
            WHEN qca.query_text LIKE '%SELECT *%' THEN '只选择需要的列，减少数据传输'
            WHEN qca.query_text LIKE '%ORDER BY%' AND qca.query_text NOT LIKE '%LIMIT%' THEN '添加LIMIT限制结果集大小'
            WHEN qca.mean_exec_time > 1000 THEN '考虑添加索引或优化查询计划'
            ELSE '分析执行计划，优化JOIN和WHERE条件'
        END AS optimization_suggestion,
        ROUND(qca.total_cost * qca.optimization_potential / 100, 2) AS expected_savings
    FROM query_cost_analysis qca
    WHERE qca.total_cost > 10
    ORDER BY qca.total_cost DESC
    LIMIT 10;
END;
$$ LANGUAGE plpgsql;

-- 查询优化建议
SELECT * FROM generate_query_optimization_suggestions();
```

---

## 3. 索引优化

### 3.1 索引分析

```sql
-- 分析索引使用
SELECT
    schemaname,
    tablename,
    indexname,
    idx_scan,
    pg_size_pretty(pg_relation_size(indexrelid)) AS index_size
FROM pg_stat_user_indexes
ORDER BY idx_scan;
```

### 3.2 索引维护

**索引维护策略**：

```sql
-- 删除未使用的索引
DROP INDEX IF EXISTS unused_index;

-- 重建索引
REINDEX INDEX idx_users_email;
```

**索引成本分析**：

```sql
-- 创建索引成本分析表
CREATE TABLE IF NOT EXISTS index_cost_analysis (
    index_id SERIAL PRIMARY KEY,
    schemaname VARCHAR(100),
    tablename VARCHAR(200),
    indexname VARCHAR(200),
    index_size_bytes BIGINT,
    index_size_gb NUMERIC,
    index_scans BIGINT,
    index_tuples_read BIGINT,
    index_tuples_fetched BIGINT,
    storage_cost_per_month NUMERIC,
    usage_efficiency NUMERIC,
    cost_per_scan NUMERIC,
    analyzed_date TIMESTAMPTZ DEFAULT NOW()
);

-- 从pg_stat_user_indexes导入索引统计
INSERT INTO index_cost_analysis
(schemaname, tablename, indexname, index_size_bytes, index_scans, index_tuples_read, index_tuples_fetched)
SELECT
    schemaname,
    tablename,
    indexname,
    pg_relation_size(indexrelid) AS index_size_bytes,
    idx_scan,
    idx_tup_read,
    idx_tup_fetch
FROM pg_stat_user_indexes
WHERE pg_relation_size(indexrelid) > 0;

-- 计算索引成本和效率
UPDATE index_cost_analysis
SET
    index_size_gb = index_size_bytes::NUMERIC / 1024 / 1024 / 1024,
    storage_cost_per_month = (index_size_bytes::NUMERIC / 1024 / 1024 / 1024) * 0.1, -- 假设存储成本0.1/GB/月
    usage_efficiency = CASE
        WHEN index_scans > 0 THEN
            ROUND(index_tuples_fetched::NUMERIC / NULLIF(index_tuples_read, 0) * 100, 2)
        ELSE 0
    END,
    cost_per_scan = CASE
        WHEN index_scans > 0 THEN
            (index_size_bytes::NUMERIC / 1024 / 1024 / 1024) * 0.1 / index_scans
        ELSE NULL
    END;

-- 查询索引成本分析
SELECT
    schemaname || '.' || tablename AS table_name,
    indexname,
    pg_size_pretty(index_size_bytes) AS index_size,
    index_scans,
    ROUND(storage_cost_per_month, 2) AS monthly_storage_cost,
    ROUND(usage_efficiency, 2) || '%' AS usage_efficiency,
    CASE
        WHEN index_scans = 0 THEN '未使用'
        WHEN index_scans < 10 THEN '使用率低'
        WHEN usage_efficiency < 50 THEN '效率低'
        ELSE '正常'
    END AS index_status
FROM index_cost_analysis
ORDER BY storage_cost_per_month DESC
LIMIT 20;
```

**索引优化建议**：

```sql
-- 创建索引优化建议函数
CREATE OR REPLACE FUNCTION generate_index_optimization_suggestions()
RETURNS TABLE (
    index_name VARCHAR(200),
    table_name VARCHAR(200),
    index_status TEXT,
    optimization_suggestion TEXT,
    potential_savings NUMERIC
) AS $$
BEGIN
    RETURN QUERY
    SELECT
        ica.indexname,
        ica.schemaname || '.' || ica.tablename,
        CASE
            WHEN ica.index_scans = 0 THEN '未使用索引'
            WHEN ica.index_scans < 10 THEN '低使用率索引'
            WHEN ica.usage_efficiency < 50 THEN '低效率索引'
            ELSE '正常索引'
        END AS index_status,
        CASE
            WHEN ica.index_scans = 0 THEN '考虑删除此索引'
            WHEN ica.index_scans < 10 THEN '评估是否可以合并到其他索引'
            WHEN ica.usage_efficiency < 50 THEN '优化索引列顺序或重建索引'
            ELSE '索引使用正常'
        END AS optimization_suggestion,
        CASE
            WHEN ica.index_scans = 0 THEN ica.storage_cost_per_month
            WHEN ica.index_scans < 10 THEN ica.storage_cost_per_month * 0.5
            ELSE 0
        END AS potential_savings
    FROM index_cost_analysis ica
    WHERE ica.index_scans = 0 OR ica.index_scans < 10 OR ica.usage_efficiency < 50
    ORDER BY potential_savings DESC
    LIMIT 20;
END;
$$ LANGUAGE plpgsql;

-- 查询索引优化建议
SELECT * FROM generate_index_optimization_suggestions();
```

---

## 4. 连接优化

### 4.1 连接池

```text
使用PgBouncer:
- 减少连接数
- 提高资源利用率
- 降低内存使用
```

### 4.2 连接管理

**连接管理优化**：

```sql
-- 限制连接数
ALTER DATABASE mydb CONNECTION LIMIT 100;

-- 查看连接使用
SELECT COUNT(*) FROM pg_stat_activity;
```

**连接成本分析**：

```sql
-- 创建连接成本分析表
CREATE TABLE IF NOT EXISTS connection_cost_analysis (
    analysis_id SERIAL PRIMARY KEY,
    analysis_date TIMESTAMPTZ DEFAULT NOW(),
    total_connections INTEGER,
    active_connections INTEGER,
    idle_connections INTEGER,
    connection_cost_per_month NUMERIC,
    idle_cost_per_month NUMERIC,
    active_cost_per_month NUMERIC,
    cost_efficiency NUMERIC,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 插入连接成本数据
INSERT INTO connection_cost_analysis
(total_connections, active_connections, idle_connections)
SELECT
    COUNT(*) AS total_connections,
    COUNT(*) FILTER (WHERE state = 'active') AS active_connections,
    COUNT(*) FILTER (WHERE state = 'idle') AS idle_connections
FROM pg_stat_activity
WHERE datname NOT IN ('template0', 'template1');

-- 计算连接成本（假设每个连接成本0.1/月）
UPDATE connection_cost_analysis
SET
    connection_cost_per_month = total_connections * 0.1,
    idle_cost_per_month = idle_connections * 0.1,
    active_cost_per_month = active_connections * 0.1,
    cost_efficiency = CASE
        WHEN total_connections > 0 THEN
            ROUND(active_connections::NUMERIC / total_connections * 100, 2)
        ELSE 0
    END
WHERE analysis_date >= CURRENT_DATE - INTERVAL '1 day';

-- 查询连接成本分析
SELECT
    analysis_date,
    total_connections,
    active_connections,
    idle_connections,
    ROUND(connection_cost_per_month, 2) AS total_cost,
    ROUND(idle_cost_per_month, 2) AS idle_cost,
    ROUND(active_cost_per_month, 2) AS active_cost,
    cost_efficiency || '%' AS efficiency
FROM connection_cost_analysis
ORDER BY analysis_date DESC
LIMIT 30;
```

**连接池优化效果**：

```sql
-- 创建连接池优化跟踪表
CREATE TABLE IF NOT EXISTS connection_pool_optimization (
    optimization_id SERIAL PRIMARY KEY,
    optimization_date DATE DEFAULT CURRENT_DATE,
    before_connections INTEGER,
    after_connections INTEGER,
    connection_reduction INTEGER,
    cost_savings NUMERIC,
    pool_type VARCHAR(50),
    pool_config JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 插入连接池优化数据
INSERT INTO connection_pool_optimization
(before_connections, after_connections, pool_type, pool_config)
VALUES
(200, 50, 'PgBouncer', '{"pool_mode": "transaction", "max_client_conn": 1000, "default_pool_size": 25}'::JSONB),
(150, 40, 'pgpool-II', '{"num_init_children": 32, "max_pool": 4}'::JSONB);

-- 计算成本节省
UPDATE connection_pool_optimization
SET
    connection_reduction = before_connections - after_connections,
    cost_savings = (before_connections - after_connections) * 0.1; -- 假设每个连接成本0.1/月

-- 查询连接池优化效果
SELECT
    optimization_date,
    pool_type,
    before_connections,
    after_connections,
    connection_reduction,
    ROUND(cost_savings, 2) AS monthly_savings,
    ROUND(cost_savings * 12, 2) AS annual_savings,
    ROUND((connection_reduction::NUMERIC / before_connections) * 100, 2) || '%' AS reduction_percentage
FROM connection_pool_optimization
ORDER BY optimization_date DESC;
```

---

## 5. 配置优化

### 5.1 性能参数

```text
shared_buffers = 25% of RAM
work_mem = (RAM - shared_buffers) / max_connections
effective_cache_size = 50-75% of RAM
max_connections = 根据实际需求
```

### 5.2 并行查询

**并行查询配置**：

```sql
-- 启用并行查询
SET max_parallel_workers_per_gather = 4;
SET max_parallel_workers = 8;
```

**并行查询成本效益分析**：

```sql
-- 创建并行查询分析表
CREATE TABLE IF NOT EXISTS parallel_query_analysis (
    query_id SERIAL PRIMARY KEY,
    query_text TEXT,
    sequential_time_ms NUMERIC,
    parallel_time_ms NUMERIC,
    parallel_workers INTEGER,
    speedup_ratio NUMERIC,
    cost_reduction NUMERIC,
    analyzed_date TIMESTAMPTZ DEFAULT NOW()
);

-- 插入并行查询分析数据
INSERT INTO parallel_query_analysis
(query_text, sequential_time_ms, parallel_time_ms, parallel_workers)
VALUES
('SELECT COUNT(*) FROM large_table WHERE condition', 5000, 1250, 4),
('SELECT SUM(amount) FROM orders GROUP BY region', 3000, 750, 4);

-- 计算加速比和成本降低
UPDATE parallel_query_analysis
SET
    speedup_ratio = sequential_time_ms / NULLIF(parallel_time_ms, 0),
    cost_reduction = CASE
        WHEN sequential_time_ms > 0 THEN
            (sequential_time_ms - parallel_time_ms) / sequential_time_ms * 100
        ELSE 0
    END;

-- 查询并行查询效果
SELECT
    LEFT(query_text, 100) AS query_preview,
    ROUND(sequential_time_ms, 2) AS sequential_ms,
    ROUND(parallel_time_ms, 2) AS parallel_ms,
    parallel_workers,
    ROUND(speedup_ratio, 2) || 'x' AS speedup,
    ROUND(cost_reduction, 2) || '%' AS cost_reduction
FROM parallel_query_analysis
ORDER BY speedup_ratio DESC;
```

**PostgreSQL 18并行查询优化**：

```sql
-- PostgreSQL 18并行查询增强
-- 检查并行查询设置
SELECT
    name,
    setting,
    unit,
    context
FROM pg_settings
WHERE name LIKE '%parallel%'
ORDER BY name;

-- 优化并行查询配置
ALTER SYSTEM SET max_parallel_workers_per_gather = 4;
ALTER SYSTEM SET max_parallel_workers = 8;
ALTER SYSTEM SET max_parallel_maintenance_workers = 4;
ALTER SYSTEM SET parallel_setup_cost = 100;
ALTER SYSTEM SET parallel_tuple_cost = 0.01;
SELECT pg_reload_conf();

-- 验证并行查询效果
EXPLAIN (ANALYZE, BUFFERS, VERBOSE)
SELECT
    region,
    SUM(amount) AS total_amount,
    COUNT(*) AS order_count
FROM orders
WHERE order_date >= CURRENT_DATE - INTERVAL '1 year'
GROUP BY region;
-- 应该看到Parallel HashAggregate计划节点
```

---

## 📚 相关文档

- [成本优化完整指南.md](./成本优化完整指南.md) - 成本优化完整指南
- [资源使用优化.md](./资源使用优化.md) - 资源使用优化
- [存储成本优化.md](./存储成本优化.md) - 存储成本优化
- [21-最佳实践/README.md](../README.md) - 最佳实践主题

---

**最后更新**: 2025年1月
