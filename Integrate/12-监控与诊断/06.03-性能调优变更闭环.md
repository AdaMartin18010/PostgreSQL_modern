---

> **📋 文档来源**: `PostgreSQL\06-运维实践\监控与诊断\06.03-性能调优变更闭环.md`
> **📅 复制日期**: 2025-12-22
> **⚠️ 注意**: 本文档为复制版本，原文件保持不变

---

# 性能调优-变更闭环（Runbook）

> **文档版本**: v1.0
> **最后更新**: 2025-11-22
> **PostgreSQL版本**: 18.x (推荐) ⭐ | 17.x (推荐) | 16.x (兼容)
> 对齐文档：`../../05-部署架构/单机部署/05.02-性能调优实践.md`、`./06.01-监控与诊断.md`

---

## 📋 目录

- [性能调优-变更闭环（Runbook）](#性能调优-变更闭环runbook)
  - [📋 目录](#-目录)
  - [1. 目标](#1-目标)
  - [2. 前置检查](#2-前置检查)
    - [2.1 业务窗口确认](#21-业务窗口确认)
    - [2.2 备份准备](#22-备份准备)
    - [2.3 指标面板准备](#23-指标面板准备)
  - [3. 诊断入口](#3-诊断入口)
    - [3.1 诊断SQL脚本](#31-诊断sql脚本)
    - [3.2 执行计划分析](#32-执行计划分析)
  - [4. 变更票据模板](#4-变更票据模板)
    - [4.1 变更记录表](#41-变更记录表)
  - [5. 执行步骤](#5-执行步骤)
    - [5.1 变更前审计脚本](#51-变更前审计脚本)
    - [5.2 参数变更执行脚本](#52-参数变更执行脚本)
    - [5.3 索引变更执行脚本](#53-索引变更执行脚本)
    - [5.4 观察期监控脚本](#54-观察期监控脚本)
  - [6. 常用参数建议（摘）](#6-常用参数建议摘)
    - [6.1 参数推荐配置](#61-参数推荐配置)
  - [7. 验证清单](#7-验证清单)
    - [7.1 性能对比脚本](#71-性能对比脚本)
    - [7.2 验证标准](#72-验证标准)
  - [8. 回滚与复盘](#8-回滚与复盘)
    - [8.1 回滚脚本](#81-回滚脚本)
    - [8.2 复盘记录](#82-复盘记录)

---

## 1. 目标

- 在生产环境中以最小风险完成参数/SQL/结构优化，形成"监测→诊断→定位→变更→验证→回滚/固化"的闭环。
- 对齐文档：`../../05-部署架构/单机部署/05.02-性能调优实践.md`、`./06.01-监控与诊断.md`。

## 2. 前置检查

- 业务窗口：确认灰度范围与低峰时段；
- 备份/回滚：配置备份点/快照；准备回滚脚本（参数旧值、DDL 回滚方案）。
- 指标面板：P95/P99、TPS/QPS、命中率、WAL 速率、检查点、锁等待、Autovacuum 滞后。

### 2.1 业务窗口确认

```bash
# 检查当前业务负载
psql -c "
SELECT
    date_trunc('hour', now()) as hour,
    COUNT(*) as active_connections,
    COUNT(*) FILTER (WHERE state = 'active') as active_queries
FROM pg_stat_activity
WHERE datname = current_database()
GROUP BY 1
ORDER BY 1 DESC
LIMIT 24;
"

# 确认低峰时段（示例：凌晨2-6点）
LOW_TRAFFIC_WINDOW="02:00-06:00"
```

### 2.2 备份准备

```bash
# 1. 创建参数备份
psql -c "
SELECT name, setting, source
FROM pg_settings
WHERE name IN ('shared_buffers', 'work_mem', 'max_connections')
ORDER BY name;
" > /tmp/pg_params_backup_$(date +%Y%m%d_%H%M%S).txt

# 2. 创建配置备份
cp $PGDATA/postgresql.conf $PGDATA/postgresql.conf.backup_$(date +%Y%m%d_%H%M%S)

# 3. 创建执行计划基线
psql -c "
SELECT queryid, query, plan
FROM pg_stat_statements
ORDER BY total_exec_time DESC
LIMIT 10;
" > /tmp/query_plans_baseline_$(date +%Y%m%d_%H%M%S).txt
```

### 2.3 指标面板准备

```sql
-- 创建性能基线视图（带错误处理）
DO $$
BEGIN
    BEGIN
        CREATE OR REPLACE VIEW performance_baseline AS
        SELECT
            NOW() as baseline_time,
            (SELECT COUNT(*) FROM pg_stat_activity WHERE state = 'active') as active_queries,
            (SELECT SUM(total_exec_time) FROM pg_stat_statements) as total_exec_time,
            (SELECT SUM(blks_hit)::float / NULLIF(SUM(blks_hit) + SUM(blks_read), 0) * 100
             FROM pg_stat_database WHERE datname = current_database()) as cache_hit_ratio,
            (SELECT COUNT(*) FROM pg_stat_activity WHERE wait_event_type IS NOT NULL) as waiting_queries;
        RAISE NOTICE '性能基线视图创建成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '创建性能基线视图失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 记录基线
INSERT INTO performance_baseline SELECT * FROM performance_baseline;
```

## 3. 诊断入口

1) 慢查询/吞吐下降/延迟升高告警触发；
2) 执行 `sql/diagnostics.sql` 中 TopN + 等待事件；
3) 对疑似语句 `EXPLAIN (ANALYZE, BUFFERS, TIMING)`；
4) 评估策略：索引/统计/SQL/参数/架构。

### 3.1 诊断SQL脚本

```sql
-- diagnostics.sql

-- 1. Top 10 慢查询（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'pg_stat_statements') THEN
            RAISE WARNING '扩展 pg_stat_statements 不存在，无法查询慢查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始查看Top 10慢查询';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    queryid,
    LEFT(query, 100) as query_preview,
    calls,
    total_exec_time,
    mean_exec_time,
    max_exec_time,
    rows,
    100.0 * shared_blks_hit / NULLIF(shared_blks_hit + shared_blks_read, 0) AS hit_percent
FROM pg_stat_statements
ORDER BY total_exec_time DESC
LIMIT 10;

-- 2. 等待事件分布
SELECT
    wait_event_type,
    wait_event,
    COUNT(*) as count,
    COUNT(*) * 100.0 / SUM(COUNT(*)) OVER () as percentage
FROM pg_stat_activity
WHERE wait_event_type IS NOT NULL
GROUP BY wait_event_type, wait_event
ORDER BY count DESC;

-- 3. 锁等待情况
SELECT
    blocked_locks.pid AS blocked_pid,
    blocked_activity.usename AS blocked_user,
    blocking_locks.pid AS blocking_pid,
    blocking_activity.usename AS blocking_user,
    blocked_activity.query AS blocked_statement,
    blocking_activity.query AS blocking_statement
FROM pg_catalog.pg_locks blocked_locks
JOIN pg_catalog.pg_stat_activity blocked_activity ON blocked_activity.pid = blocked_locks.pid
JOIN pg_catalog.pg_locks blocking_locks
    ON blocking_locks.locktype = blocked_locks.locktype
    AND blocking_locks.database IS NOT DISTINCT FROM blocked_locks.database
    AND blocking_locks.relation IS NOT DISTINCT FROM blocked_locks.relation
    AND blocking_locks.pid != blocked_locks.pid
JOIN pg_catalog.pg_stat_activity blocking_activity ON blocking_activity.pid = blocking_locks.pid
WHERE NOT blocked_locks.granted;

-- 4. 表膨胀情况
SELECT
    schemaname,
    tablename,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS total_size,
    n_live_tup,
    n_dead_tup,
    CASE
        WHEN n_live_tup > 0 THEN n_dead_tup::float / n_live_tup * 100
        ELSE 0
    END AS dead_tuple_percent
FROM pg_stat_user_tables
WHERE n_dead_tup > 1000
ORDER BY n_dead_tup DESC
LIMIT 20;

-- 5. 索引使用情况
SELECT
    schemaname,
    tablename,
    indexname,
    idx_scan,
    idx_tup_read,
    idx_tup_fetch,
    pg_size_pretty(pg_relation_size(indexrelid)) AS index_size
FROM pg_stat_user_indexes
WHERE schemaname = 'public'
ORDER BY idx_scan ASC
LIMIT 20;
```

### 3.2 执行计划分析

```sql
-- 对慢查询执行详细分析（带错误处理和性能测试）
EXPLAIN (ANALYZE, BUFFERS, VERBOSE, COSTS, TIMING)
SELECT /* 慢查询SQL */;

-- 检查计划缓存（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'pg_stat_statements') THEN
            RAISE WARNING '扩展 pg_stat_statements 不存在，无法检查计划缓存';
            RETURN;
        END IF;
        RAISE NOTICE '开始检查计划缓存';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    queryid,
    LEFT(query, 100) as query_preview,
    plans,
    calls,
    total_exec_time,
    mean_exec_time
FROM pg_stat_statements
WHERE plans > 1
ORDER BY plans DESC
LIMIT 10;
```

## 4. 变更票据模板

```text
变更项：<参数/SQL/索引/结构>
对象/范围：<库/表/索引/服务>
旧值→新值：<从> → <到>
预期影响：<延迟/吞吐/资源>
验证窗口：<起止时间/灰度比例>
回滚条件：<P95 恶化>10% / 错误率升 / 锁冲突升>
负责人/审批：<Owner/Reviewer>
```

### 4.1 变更记录表

```sql
-- 创建变更记录表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'performance_change_log') THEN
            CREATE TABLE performance_change_log (
                change_id SERIAL PRIMARY KEY,
                change_time TIMESTAMP DEFAULT NOW(),
                change_type VARCHAR(50),  -- 'parameter', 'index', 'sql', 'structure'
                change_target TEXT,
                old_value TEXT,
                new_value TEXT,
                change_by VARCHAR(100),
                expected_impact TEXT,
                verification_window TEXT,
                rollback_condition TEXT,
                change_status VARCHAR(20),  -- 'pending', 'in_progress', 'completed', 'rolled_back'
                actual_impact TEXT,
                notes TEXT
            );
            RAISE NOTICE '变更记录表创建成功';
        ELSE
            RAISE NOTICE '变更记录表已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '变更记录表已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建变更记录表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 记录变更
INSERT INTO performance_change_log (
    change_type,
    change_target,
    old_value,
    new_value,
    change_by,
    expected_impact,
    verification_window,
    rollback_condition
)
VALUES (
    'parameter',
    'shared_buffers',
    '256MB',
    '512MB',
    'dba_user',
    '提高缓存命中率5-10%',
    '2025-11-22 02:00 - 06:00',
    'P95延迟增加>10%或错误率上升'
);
```

## 5. 执行步骤

1) 建立审计：记录参数旧值（`SHOW`/`pg_file_settings`）与计划基线；
2) 单变量小步变更；
3) 观察 15–30 分钟稳定窗口；
4) 达标则固化与文档化；不达标则回滚。

### 5.1 变更前审计脚本

```sql
-- 记录当前参数值（带错误处理和性能测试）
EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    name,
    setting,
    unit,
    source,
    sourcefile,
    sourceline
FROM pg_settings
WHERE name IN ('shared_buffers', 'work_mem', 'max_connections')
ORDER BY name;

-- 记录性能基线（带错误处理）
DO $$
BEGIN
    BEGIN
        INSERT INTO performance_baseline
        SELECT * FROM performance_baseline;
        RAISE NOTICE '性能基线记录成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '记录性能基线失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 记录查询计划基线（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'pg_stat_statements') THEN
            RAISE WARNING '扩展 pg_stat_statements 不存在，无法记录查询计划基线';
            RETURN;
        END IF;
        RAISE NOTICE '开始记录查询计划基线';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    queryid,
    query,
    calls,
    mean_exec_time,
    total_exec_time
FROM pg_stat_statements
WHERE mean_exec_time > 100
ORDER BY total_exec_time DESC
LIMIT 20;
```

### 5.2 参数变更执行脚本

```sql
-- 方法1: ALTER SYSTEM SET（需要重启或reload）（带错误处理）
DO $$
BEGIN
    BEGIN
        ALTER SYSTEM SET shared_buffers = '512MB';
        PERFORM pg_reload_conf();  -- 仅对部分参数生效
        RAISE NOTICE '参数设置成功，已重新加载配置';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '设置参数失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 方法2: 直接修改配置文件（需要重启）
-- 编辑 postgresql.conf
-- shared_buffers = 512MB

-- 方法3: 会话级设置（仅当前会话）（带错误处理）
DO $$
BEGIN
    BEGIN
        SET work_mem = '64MB';
        RAISE NOTICE '会话级参数设置成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '设置会话级参数失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 验证变更（带错误处理和性能测试）
EXPLAIN (ANALYZE, BUFFERS, TIMING)
SHOW shared_buffers;
```

### 5.3 索引变更执行脚本

```sql
-- 在线创建索引（不阻塞写操作）（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (
            SELECT 1 FROM pg_indexes
            WHERE indexname = 'idx_orders_created_at'
        ) THEN
            CREATE INDEX CONCURRENTLY idx_orders_created_at ON orders(created_at);
            RAISE NOTICE '索引创建成功';
        ELSE
            RAISE NOTICE '索引已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 在线删除索引（带错误处理）
DO $$
BEGIN
    BEGIN
        DROP INDEX CONCURRENTLY IF EXISTS idx_orders_old;
        RAISE NOTICE '索引删除成功（如果存在）';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '删除索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 重建索引（在线）（带错误处理）
DO $$
BEGIN
    BEGIN
        REINDEX INDEX CONCURRENTLY idx_orders_created_at;
        RAISE NOTICE '索引重建成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '重建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 5.4 观察期监控脚本

```sql
-- 实时监控关键指标（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        RAISE NOTICE '开始实时监控关键指标';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    NOW() as check_time,
    (SELECT COUNT(*) FROM pg_stat_activity WHERE state = 'active') as active_queries,
    (SELECT AVG(mean_exec_time) FROM pg_stat_statements WHERE calls > 100) as avg_query_time,
    (SELECT SUM(blks_hit)::float / NULLIF(SUM(blks_hit) + SUM(blks_read), 0) * 100
     FROM pg_stat_database WHERE datname = current_database()) as cache_hit_ratio;

-- 监控慢查询变化（带错误处理和性能测试）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'pg_stat_statements') THEN
            RAISE WARNING '扩展 pg_stat_statements 不存在，无法监控慢查询';
            RETURN;
        END IF;
        RAISE NOTICE '开始监控慢查询变化';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '查询准备失败: %', SQLERRM;
            RAISE;
    END;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    queryid,
    LEFT(query, 100) as query_preview,
    calls,
    mean_exec_time,
    mean_exec_time - LAG(mean_exec_time) OVER (ORDER BY queryid) as time_delta
FROM pg_stat_statements
WHERE mean_exec_time > 100
ORDER BY total_exec_time DESC
LIMIT 10;
```

## 6. 常用参数建议（摘）

- 连接与并发：`max_connections`、`work_mem`；
- 缓冲：`shared_buffers`、`effective_cache_size`；
- 并行/JIT：`max_parallel_workers_per_gather`、`jit`；
- I/O：`random_page_cost`、`effective_io_concurrency`；
- 检查点/WAL：`checkpoint_timeout`、`max_wal_size`、`checkpoint_completion_target`、`wal_compression`；
- 维护：`autovacuum_*`、`maintenance_work_mem`。

### 6.1 参数推荐配置

```sql
-- 连接与并发
max_connections = 200  -- 根据实际需求调整
work_mem = 64MB  -- 每个操作可用内存，总内存 = work_mem * max_connections

-- 缓冲
shared_buffers = 25% of RAM  -- 生产环境推荐
effective_cache_size = 75% of RAM  -- 查询规划器使用

-- 并行
max_parallel_workers_per_gather = 4
max_parallel_workers = 8
max_worker_processes = 8

-- I/O（SSD环境）
random_page_cost = 1.1  -- SSD推荐值
effective_io_concurrency = 200  -- SSD推荐值

-- 检查点/WAL
checkpoint_timeout = 15min
max_wal_size = 4GB
checkpoint_completion_target = 0.9
wal_compression = on

-- 维护
maintenance_work_mem = 1GB
autovacuum_max_workers = 3
autovacuum_naptime = 10s
```

## 7. 验证清单

- 指标对比：调优前/后 P95、TPS/QPS、命中率、WAL、检查点；
- SQL 对比：TopN 是否下降、估算偏差是否缓解；
- 资源：CPU/IO/内存是否在阈值内；
- 业务：错误率/超时是否正常。

### 7.1 性能对比脚本

```sql
-- 对比变更前后的性能指标
WITH baseline AS (
    SELECT * FROM performance_baseline
    WHERE baseline_time = (SELECT MAX(baseline_time) FROM performance_baseline WHERE baseline_time < NOW() - INTERVAL '1 hour')
),
current AS (
    SELECT * FROM performance_baseline
    WHERE baseline_time = (SELECT MAX(baseline_time) FROM performance_baseline)
)
SELECT
    'Active Queries' as metric,
    baseline.active_queries as before_value,
    current.active_queries as after_value,
    current.active_queries - baseline.active_queries as delta,
    ROUND((current.active_queries - baseline.active_queries)::numeric / NULLIF(baseline.active_queries, 0) * 100, 2) as percent_change
FROM baseline, current
UNION ALL
SELECT
    'Cache Hit Ratio',
    baseline.cache_hit_ratio,
    current.cache_hit_ratio,
    current.cache_hit_ratio - baseline.cache_hit_ratio,
    ROUND((current.cache_hit_ratio - baseline.cache_hit_ratio)::numeric / NULLIF(baseline.cache_hit_ratio, 0) * 100, 2)
FROM baseline, current;
```

### 7.2 验证标准

```sql
-- 创建验证检查清单
CREATE OR REPLACE FUNCTION verify_performance_change()
RETURNS TABLE (
    check_item TEXT,
    status TEXT,
    details TEXT
) AS $$
BEGIN
    -- 检查1: P95延迟
    RETURN QUERY
    SELECT
        'P95 Query Time'::TEXT,
        CASE
            WHEN (SELECT percentile_cont(0.95) WITHIN GROUP (ORDER BY mean_exec_time) FROM pg_stat_statements) < 1000
            THEN 'PASS'::TEXT
            ELSE 'FAIL'::TEXT
        END,
        'P95 query time check'::TEXT;

    -- 检查2: 缓存命中率
    RETURN QUERY
    SELECT
        'Cache Hit Ratio'::TEXT,
        CASE
            WHEN (SELECT SUM(blks_hit)::float / NULLIF(SUM(blks_hit) + SUM(blks_read), 0) * 100
                  FROM pg_stat_database WHERE datname = current_database()) > 95
            THEN 'PASS'::TEXT
            ELSE 'FAIL'::TEXT
        END,
        'Cache hit ratio check'::TEXT;

    -- 检查3: 锁等待
    RETURN QUERY
    SELECT
        'Lock Waits'::TEXT,
        CASE
            WHEN (SELECT COUNT(*) FROM pg_stat_activity WHERE wait_event_type = 'Lock') < 5
            THEN 'PASS'::TEXT
            ELSE 'FAIL'::TEXT
        END,
        'Lock wait check'::TEXT;
END;
$$ LANGUAGE plpgsql;

-- 执行验证
SELECT * FROM verify_performance_change();
```

## 8. 回滚与复盘

- 立即恢复参数旧值/撤销索引或重写；
- 附加证据：日志、慢SQL、EXPLAIN、面板截图；
- 复盘条目：问题根因、无效尝试、有效手段、可复用脚本。

### 8.1 回滚脚本

```sql
-- 参数回滚
ALTER SYSTEM SET shared_buffers = '256MB';  -- 恢复到旧值
SELECT pg_reload_conf();

-- 索引回滚
DROP INDEX CONCURRENTLY IF EXISTS idx_orders_created_at;

-- 更新变更记录
UPDATE performance_change_log
SET
    change_status = 'rolled_back',
    notes = '回滚原因：P95延迟增加15%，超过阈值10%'
WHERE change_id = <change_id>;
```

### 8.2 复盘记录

```sql
-- 创建复盘记录表
CREATE TABLE IF NOT EXISTS performance_tuning_review (
    review_id SERIAL PRIMARY KEY,
    review_date TIMESTAMP DEFAULT NOW(),
    change_id INTEGER REFERENCES performance_change_log(change_id),
    root_cause TEXT,
    failed_attempts TEXT[],
    successful_approaches TEXT[],
    reusable_scripts TEXT,
    lessons_learned TEXT
);

-- 记录复盘
INSERT INTO performance_tuning_review (
    change_id,
    root_cause,
    failed_attempts,
    successful_approaches,
    reusable_scripts
)
VALUES (
    1,
    'shared_buffers设置过大导致操作系统缓存减少',
    ARRAY['直接设置为1GB', '未考虑操作系统缓存需求'],
    ARRAY['逐步增加并观察', '使用25% RAM规则'],
    '性能对比脚本、验证函数'
);
```
