---

> **📋 文档来源**: `PostgreSQL_AI\03-核心能力\数据注入与治理.md`
> **📅 复制日期**: 2025-12-22
> **⚠️ 注意**: 本文档为复制版本，原文件保持不变

---

# 数据注入与治理

> **文档编号**: AI-03-06
> **最后更新**: 2025年1月
> **主题**: 03-核心能力
> **子主题**: 06-数据注入与治理

## 📑 目录

- [数据注入与治理](#数据注入与治理)
  - [📑 目录](#-目录)
  - [一、概述](#一概述)
  - [二、数据注入方式](#二数据注入方式)
    - [2.1 CDC变更数据捕获](#21-cdc变更数据捕获)
    - [2.2 批量ETL处理](#22-批量etl处理)
    - [2.3 实时流式处理](#23-实时流式处理)
  - [三、数据质量保证](#三数据质量保证)
    - [3.1 数据验证](#31-数据验证)
    - [3.2 数据清洗](#32-数据清洗)
    - [3.3 数据标准化](#33-数据标准化)
  - [四、数据版本管理](#四数据版本管理)
    - [4.1 版本控制策略](#41-版本控制策略)
    - [4.2 数据快照](#42-数据快照)
    - [4.3 数据回滚](#43-数据回滚)
  - [五、自动化向量化管道](#五自动化向量化管道)
    - [5.1 pgai Vectorizer](#51-pgai-vectorizer)
    - [5.2 批量处理](#52-批量处理)
    - [5.3 增量更新](#53-增量更新)
  - [六、监控与告警](#六监控与告警)
    - [6.1 数据质量监控](#61-数据质量监控)
    - [6.2 处理进度监控](#62-处理进度监控)
    - [6.3 异常告警](#63-异常告警)
  - [七、最佳实践](#七最佳实践)
  - [八、关联主题](#八关联主题)
  - [九、对标资源](#九对标资源)
    - [技术文档](#技术文档)
    - [企业案例](#企业案例)

## 一、概述

PostgreSQL AI应用的数据注入与治理能力，包括CDC变更捕获、批量ETL、实时流处理、数据质量保证、版本管理和自动化向量化管道，确保数据从源头到AI应用的完整治理链路。

## 二、数据注入方式

### 2.1 CDC变更数据捕获

**使用Debezium实现CDC**:

```sql
-- 配置逻辑复制
ALTER SYSTEM SET wal_level = logical;
SELECT pg_reload_conf();

-- 创建逻辑复制槽
SELECT pg_create_logical_replication_slot('debezium_slot', 'pgoutput');

-- 使用Debezium捕获变更
-- Debezium会自动创建变更表
CREATE TABLE products_cdc (
    id INTEGER PRIMARY KEY,
    title TEXT,
    description TEXT,
    created_at TIMESTAMPTZ
);

-- 变更数据自动同步到向量表
-- CDC同步到向量触发器函数（带完整错误处理）
CREATE OR REPLACE FUNCTION sync_cdc_to_vector()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
DECLARE
    v_embedding vector;
    v_description TEXT;
BEGIN
    -- 检查NEW记录是否存在
    IF NEW IS NULL THEN
        RAISE WARNING 'NEW记录为空，无法同步向量';
        RETURN NULL;
    END IF;

    -- 检查必需字段
    IF NEW.id IS NULL THEN
        RAISE WARNING 'product_id为空，无法同步向量';
        RETURN NEW;
    END IF;

    v_description := NEW.description;

    -- 检查描述是否为空
    IF v_description IS NULL OR TRIM(v_description) = '' THEN
        RAISE WARNING '产品描述为空，无法生成向量: product_id=%', NEW.id;
        RETURN NEW;
    END IF;

    -- 检查表是否存在
    IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'product_embeddings') THEN
        RAISE WARNING 'product_embeddings表不存在，无法同步向量';
        RETURN NEW;
    END IF;

    -- 检查ai扩展是否可用（假设ai.embedding函数存在）
    BEGIN
        -- 生成向量
        BEGIN
            v_embedding := ai.embedding('text-embedding-3-small', v_description);
        EXCEPTION
            WHEN undefined_function THEN
                RAISE WARNING 'ai.embedding函数不存在，可能需要安装相关扩展';
                RETURN NEW;
            WHEN OTHERS THEN
                RAISE WARNING '生成向量失败: product_id=%, 错误: %', NEW.id, SQLERRM;
                RETURN NEW;
        END;

        -- 插入或更新向量
        BEGIN
            INSERT INTO product_embeddings (product_id, embedding)
            VALUES (NEW.id, v_embedding)
            ON CONFLICT (product_id) DO UPDATE
            SET embedding = v_embedding;

            RAISE NOTICE '向量同步成功: product_id=%', NEW.id;
        EXCEPTION
            WHEN unique_violation THEN
                -- 使用ON CONFLICT处理，不应该到这里
                RAISE WARNING '向量记录冲突: product_id=%', NEW.id;
            WHEN OTHERS THEN
                RAISE WARNING '插入或更新向量失败: product_id=%, 错误: %', NEW.id, SQLERRM;
        END;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING 'sync_cdc_to_vector执行失败: %', SQLERRM;
    END;

    RETURN NEW;
EXCEPTION
    WHEN OTHERS THEN
        RAISE WARNING 'sync_cdc_to_vector触发器函数执行失败: %', SQLERRM;
        RETURN NEW;  -- 即使出错也返回NEW，避免影响主操作
END;
$$;

CREATE TRIGGER cdc_vector_sync
AFTER INSERT OR UPDATE ON products_cdc
FOR EACH ROW
EXECUTE FUNCTION sync_cdc_to_vector();
```

### 2.2 批量ETL处理

**使用Airflow调度ETL任务**:

```python
# Airflow DAG示例
from airflow.providers.postgres.operators.postgres import PostgresOperator
from airflow.operators.python import PythonOperator

def generate_embeddings(**context):
    """批量生成Embedding"""
    import psycopg2
    import openai

    conn = psycopg2.connect("postgresql://...")
    cur = conn.cursor()

    # 获取未处理的文档
    cur.execute("""
        SELECT id, content
        FROM documents
        WHERE embedding IS NULL
        LIMIT 1000
    """)

    docs = cur.fetchall()

    # 批量生成向量
    for doc_id, content in docs:
        embedding = openai.Embedding.create(
            input=content,
            model="text-embedding-3-small"
        )

        # 更新向量
        cur.execute("""
            UPDATE documents
            SET embedding = %s
            WHERE id = %s
        """, (embedding['data'][0]['embedding'], doc_id))

    conn.commit()
    conn.close()

with DAG('ai_data_pipeline', schedule_interval='@hourly') as dag:
    extract = PostgresOperator(
        task_id='extract',
        sql="SELECT * FROM raw_documents WHERE processed=false"
    )

    transform = PythonOperator(
        task_id='generate_embeddings',
        python_callable=generate_embeddings
    )

    load = PostgresOperator(
        task_id='load',
        sql="UPDATE raw_documents SET processed=true"
    )

    extract >> transform >> load
```

### 2.3 实时流式处理

**使用Kafka + Flink实现实时处理**:

```sql
-- 使用kafka_fdw连接Kafka
CREATE EXTENSION kafka_fdw;

CREATE SERVER kafka_server
FOREIGN DATA WRAPPER kafka_fdw
OPTIONS (
    broker 'localhost:9092',
    topic 'documents'
);

-- 创建外部表
CREATE FOREIGN TABLE kafka_documents (
    id INTEGER,
    content TEXT,
    timestamp TIMESTAMPTZ
) SERVER kafka_server;

-- 实时处理流数据函数（带完整错误处理）
CREATE OR REPLACE FUNCTION process_stream_data()
RETURNS void
LANGUAGE plpgsql
AS $$
DECLARE
    v_record RECORD;
    v_processed_count INTEGER := 0;
    v_error_count INTEGER := 0;
    v_embedding vector;
    v_content TEXT;
BEGIN
    -- 检查表是否存在
    IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'kafka_documents') THEN
        RAISE EXCEPTION 'kafka_documents表不存在';
    END IF;

    IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'document_embeddings') THEN
        RAISE EXCEPTION 'document_embeddings表不存在';
    END IF;

    -- 处理流数据
    BEGIN
        FOR v_record IN
            SELECT * FROM kafka_documents
            WHERE processed = false
              AND id IS NOT NULL
              AND content IS NOT NULL
            LIMIT 1000  -- 限制每次处理的数量，防止超时
        LOOP
            BEGIN
                v_content := v_record.content;

                -- 检查内容长度
                IF LENGTH(v_content) = 0 THEN
                    RAISE WARNING '文档内容为空，跳过: id=%', v_record.id;
                    v_error_count := v_error_count + 1;
                    CONTINUE;
                END IF;

                -- 生成向量
                BEGIN
                    v_embedding := ai.embedding('text-embedding-3-small', v_content);
                EXCEPTION
                    WHEN undefined_function THEN
                        RAISE WARNING 'ai.embedding函数不存在: id=%', v_record.id;
                        v_error_count := v_error_count + 1;
                        CONTINUE;
                    WHEN OTHERS THEN
                        RAISE WARNING '生成向量失败: id=%, 错误: %', v_record.id, SQLERRM;
                        v_error_count := v_error_count + 1;
                        CONTINUE;
                END;

                -- 插入向量
                BEGIN
                    INSERT INTO document_embeddings (document_id, embedding)
                    VALUES (v_record.id, v_embedding)
                    ON CONFLICT (document_id) DO UPDATE
                    SET embedding = v_embedding;
                EXCEPTION
                    WHEN OTHERS THEN
                        RAISE WARNING '插入向量失败: id=%, 错误: %', v_record.id, SQLERRM;
                        v_error_count := v_error_count + 1;
                        CONTINUE;
                END;

                -- 标记已处理
                BEGIN
                    UPDATE kafka_documents
                    SET processed = true
                    WHERE id = v_record.id;

                    v_processed_count := v_processed_count + 1;
                EXCEPTION
                    WHEN OTHERS THEN
                        RAISE WARNING '标记已处理失败: id=%, 错误: %', v_record.id, SQLERRM;
                        v_error_count := v_error_count + 1;
                END;
            EXCEPTION
                WHEN OTHERS THEN
                    RAISE WARNING '处理记录失败: id=%, 错误: %', v_record.id, SQLERRM;
                    v_error_count := v_error_count + 1;
            END;
        END LOOP;

        RAISE NOTICE '流数据处理完成。处理: % 条，错误: % 条', v_processed_count, v_error_count;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE EXCEPTION '处理流数据失败: %', SQLERRM;
    END;
EXCEPTION
    WHEN OTHERS THEN
        RAISE EXCEPTION 'process_stream_data执行失败: %', SQLERRM;
END;
$$;
```

## 三、数据质量保证

### 3.1 数据验证

```sql
-- 数据验证函数（带完整错误处理）
CREATE OR REPLACE FUNCTION validate_document(
    p_content TEXT,
    p_metadata JSONB
)
RETURNS BOOLEAN
LANGUAGE plpgsql
AS $$
BEGIN
    -- 检查内容非空
    IF p_content IS NULL OR LENGTH(TRIM(p_content)) = 0 THEN
        RETURN false;
    END IF;

    -- 检查内容长度（最小长度要求）
    IF LENGTH(p_content) < 10 THEN
        RETURN false;
    END IF;

    -- 检查内容长度（最大长度限制，防止过长内容）
    IF LENGTH(p_content) > 1000000 THEN
        RETURN false;
    END IF;

    -- 检查元数据格式
    IF p_metadata IS NOT NULL THEN
        -- 验证JSONB格式
        BEGIN
            IF NOT (p_metadata ? 'source') THEN
                RETURN false;
            END IF;

            -- 检查source字段不为空
            IF p_metadata->>'source' IS NULL OR TRIM(p_metadata->>'source') = '' THEN
                RETURN false;
            END IF;
        EXCEPTION
            WHEN OTHERS THEN
                -- JSONB操作失败，返回false
                RETURN false;
        END;
    END IF;

    RETURN true;
EXCEPTION
    WHEN OTHERS THEN
        -- 验证过程中发生错误，返回false
        RETURN false;
END;
$$;

-- 插入验证文档函数（带完整错误处理）
CREATE OR REPLACE FUNCTION insert_validated_document(
    p_content TEXT,
    p_metadata JSONB
)
RETURNS INTEGER
LANGUAGE plpgsql
AS $$
DECLARE
    v_doc_id INTEGER;
BEGIN
    -- 参数验证
    IF p_content IS NULL THEN
        RAISE EXCEPTION '文档内容不能为空';
    END IF;

    -- 验证数据
    IF NOT validate_document(p_content, p_metadata) THEN
        RAISE EXCEPTION '文档数据验证失败：内容长度不足、格式错误或元数据缺失'
            USING HINT = '请确保内容长度>=10且<=1000000，元数据包含source字段';
    END IF;

    -- 检查表是否存在
    IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'documents') THEN
        RAISE EXCEPTION 'documents表不存在';
    END IF;

    -- 插入数据
    BEGIN
        INSERT INTO documents (content, metadata)
        VALUES (TRIM(p_content), p_metadata)
        RETURNING id INTO v_doc_id;

        IF v_doc_id IS NULL THEN
            RAISE EXCEPTION '插入文档失败：未返回文档ID';
        END IF;

        RETURN v_doc_id;
    EXCEPTION
        WHEN unique_violation THEN
            RAISE EXCEPTION '文档已存在';
        WHEN foreign_key_violation THEN
            RAISE EXCEPTION '违反外键约束';
        WHEN check_violation THEN
            RAISE EXCEPTION '违反检查约束';
        WHEN OTHERS THEN
            RAISE EXCEPTION '插入文档失败: %', SQLERRM;
    END;
EXCEPTION
    WHEN OTHERS THEN
        RAISE EXCEPTION 'insert_validated_document执行失败: %', SQLERRM;
END;
$$;
```

### 3.2 数据清洗

```sql
-- 数据清洗函数（带完整错误处理）
CREATE OR REPLACE FUNCTION clean_document_content(
    p_content TEXT
)
RETURNS TEXT
LANGUAGE plpgsql
AS $$
DECLARE
    v_cleaned_content TEXT;
BEGIN
    -- 参数验证
    IF p_content IS NULL THEN
        RETURN NULL;
    END IF;

    -- 检查内容长度（防止过长内容导致性能问题）
    IF LENGTH(p_content) > 1000000 THEN
        RAISE WARNING '文档内容过长，可能影响清洗性能: % 字符', LENGTH(p_content);
        -- 截断到合理长度
        p_content := LEFT(p_content, 1000000);
    END IF;

    -- 数据清洗
    BEGIN
        v_cleaned_content := p_content;

        -- 移除HTML标签
        BEGIN
            v_cleaned_content := regexp_replace(v_cleaned_content, '<[^>]+>', '', 'g');
        EXCEPTION
            WHEN OTHERS THEN
                RAISE WARNING '移除HTML标签失败: %', SQLERRM;
        END;

        -- 移除多余空白
        BEGIN
            v_cleaned_content := regexp_replace(v_cleaned_content, '\s+', ' ', 'g');
        EXCEPTION
            WHEN OTHERS THEN
                RAISE WARNING '移除多余空白失败: %', SQLERRM;
        END;

        -- 移除特殊字符（保留字母、数字、空格）
        BEGIN
            v_cleaned_content := regexp_replace(v_cleaned_content, '[^\w\s]', '', 'g');
        EXCEPTION
            WHEN OTHERS THEN
                RAISE WARNING '移除特殊字符失败: %', SQLERRM;
        END;

        -- 转换为小写
        BEGIN
            v_cleaned_content := LOWER(v_cleaned_content);
        EXCEPTION
            WHEN OTHERS THEN
                RAISE WARNING '转换为小写失败: %', SQLERRM;
        END;

        -- 去除首尾空格
        v_cleaned_content := TRIM(v_cleaned_content);

        -- 检查清洗后内容是否为空
        IF v_cleaned_content IS NULL OR LENGTH(v_cleaned_content) = 0 THEN
            RAISE WARNING '清洗后内容为空';
            RETURN NULL;
        END IF;

        RETURN v_cleaned_content;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '清洗文档内容失败: %', SQLERRM;
            RETURN TRIM(p_content);  -- 返回原始内容（去除首尾空格）
    END;
END;
$$;

-- 使用清洗函数
UPDATE documents
SET content = clean_document_content(content)
WHERE content != clean_document_content(content);
```

### 3.3 数据标准化

```sql
-- 数据标准化表
CREATE TABLE data_quality_rules (
    id SERIAL PRIMARY KEY,
    rule_name TEXT UNIQUE,
    rule_type TEXT,  -- 'validation', 'transformation', 'normalization'
    rule_definition JSONB,
    enabled BOOLEAN DEFAULT true
);

-- 应用标准化规则函数（带完整错误处理）
CREATE OR REPLACE FUNCTION apply_quality_rules(
    p_data JSONB
)
RETURNS JSONB
LANGUAGE plpgsql
AS $$
DECLARE
    v_rule RECORD;
    v_result JSONB := p_data;
    v_rule_count INTEGER := 0;
BEGIN
    -- 参数验证
    IF p_data IS NULL THEN
        RAISE EXCEPTION '数据不能为空';
    END IF;

    -- 检查表是否存在
    IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'data_quality_rules') THEN
        RAISE WARNING 'data_quality_rules表不存在，返回原始数据';
        RETURN p_data;
    END IF;

    -- 应用规则
    BEGIN
        FOR v_rule IN
            SELECT * FROM data_quality_rules
            WHERE enabled = true
              AND rule_type IS NOT NULL
              AND rule_definition IS NOT NULL
            ORDER BY id
        LOOP
            BEGIN
                -- 应用规则
                CASE v_rule.rule_type
                    WHEN 'normalization' THEN
                        -- 标准化处理（假设存在jsonb_normalize函数）
                        IF EXISTS (SELECT 1 FROM pg_proc WHERE proname = 'jsonb_normalize') THEN
                            v_result := jsonb_normalize(v_result, v_rule.rule_definition);
                        ELSE
                            RAISE WARNING 'jsonb_normalize函数不存在，跳过标准化规则: %', v_rule.rule_name;
                        END IF;
                    WHEN 'transformation' THEN
                        -- 转换处理（假设存在jsonb_transform函数）
                        IF EXISTS (SELECT 1 FROM pg_proc WHERE proname = 'jsonb_transform') THEN
                            v_result := jsonb_transform(v_result, v_rule.rule_definition);
                        ELSE
                            RAISE WARNING 'jsonb_transform函数不存在，跳过转换规则: %', v_rule.rule_name;
                        END IF;
                    WHEN 'validation' THEN
                        -- 验证规则（仅记录，不修改数据）
                        RAISE NOTICE '应用验证规则: %', v_rule.rule_name;
                    ELSE
                        RAISE WARNING '未知规则类型: %, 跳过', v_rule.rule_type;
                END CASE;

                v_rule_count := v_rule_count + 1;
            EXCEPTION
                WHEN OTHERS THEN
                    RAISE WARNING '应用规则失败: rule_name=%, 错误: %', v_rule.rule_name, SQLERRM;
                    -- 继续处理下一个规则
            END;
        END LOOP;

        RAISE NOTICE '成功应用 % 条质量规则', v_rule_count;
        RETURN COALESCE(v_result, p_data);
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '应用质量规则失败: %', SQLERRM;
            RETURN p_data;  -- 返回原始数据
    END;
EXCEPTION
    WHEN OTHERS THEN
        RAISE EXCEPTION 'apply_quality_rules执行失败: %', SQLERRM;
END;
$$;
```

## 四、数据版本管理

### 4.1 版本控制策略

```sql
-- 版本控制表
CREATE TABLE document_versions (
    id SERIAL PRIMARY KEY,
    document_id INTEGER REFERENCES documents(id),
    version INTEGER,
    content TEXT,
    embedding vector(1536),
    metadata JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    created_by TEXT,
    UNIQUE(document_id, version)
);

-- 版本控制函数
CREATE OR REPLACE FUNCTION create_document_version(
    p_document_id INTEGER,
    p_content TEXT,
    p_metadata JSONB
) RETURNS INTEGER AS $$
DECLARE
    v_version INTEGER;
    v_embedding vector(1536);
BEGIN
    -- 获取下一个版本号
    SELECT COALESCE(MAX(version), 0) + 1
    INTO v_version
    FROM document_versions
    WHERE document_id = p_document_id;

    -- 生成向量
    v_embedding := ai.embedding('text-embedding-3-small', p_content);

    -- 创建版本
    INSERT INTO document_versions (
        document_id,
        version,
        content,
        embedding,
        metadata
    ) VALUES (
        p_document_id,
        v_version,
        p_content,
        v_embedding,
        p_metadata
    );

    RETURN v_version;
END;
$$ LANGUAGE plpgsql;
```

### 4.2 数据快照

```sql
-- 使用表继承实现快照
CREATE TABLE documents_snapshot_20240101 (
    LIKE documents INCLUDING ALL
) INHERITS (documents);

-- 创建快照
CREATE OR REPLACE FUNCTION create_snapshot(
    p_table_name TEXT,
    p_snapshot_name TEXT
) RETURNS void AS $$
BEGIN
    EXECUTE format('
        CREATE TABLE %I (
            LIKE %I INCLUDING ALL
        ) INHERITS (%I)
    ', p_snapshot_name, p_table_name, p_table_name);

    EXECUTE format('
        INSERT INTO %I SELECT * FROM %I
    ', p_snapshot_name, p_table_name);
END;
$$ LANGUAGE plpgsql;
```

### 4.3 数据回滚

```sql
-- 回滚到指定版本
CREATE OR REPLACE FUNCTION rollback_document(
    p_document_id INTEGER,
    p_version INTEGER
) RETURNS void AS $$
DECLARE
    v_version_data RECORD;
BEGIN
    -- 获取版本数据
    SELECT * INTO v_version_data
    FROM document_versions
    WHERE document_id = p_document_id
      AND version = p_version;

    IF NOT FOUND THEN
        RAISE EXCEPTION 'Version % not found', p_version;
    END IF;

    -- 回滚数据
    UPDATE documents
    SET
        content = v_version_data.content,
        embedding = v_version_data.embedding,
        metadata = v_version_data.metadata
    WHERE id = p_document_id;
END;
$$ LANGUAGE plpgsql;
```

## 五、自动化向量化管道

### 5.1 pgai Vectorizer

```sql
-- 创建自动向量化器
SELECT ai.create_vectorizer(
    'news_articles'::regclass,
    destination => 'news_embeddings',
    embedding => ai.embedding_openai('text-embedding-3-small', 'content'),
    chunking => ai.chunking_recursive_character_text_splitter(
        'content',
        chunk_size => 500,
        chunk_overlap => 50
    )
);

-- 后续INSERT自动触发向量化
INSERT INTO news_articles (title, content)
VALUES ('Fed Raises Rates', 'The Federal Reserve...');
-- 自动同步生成向量到news_embeddings表
```

### 5.2 批量处理

```sql
-- 批量处理历史数据
SELECT ai.vectorize_table(
    source_table => 'news_articles',
    destination_table => 'news_embeddings',
    batch_size => 1000
);

-- 监控处理进度
SELECT
    COUNT(*) FILTER (WHERE processed = true) AS processed,
    COUNT(*) AS total,
    ROUND(100.0 * COUNT(*) FILTER (WHERE processed = true) / COUNT(*), 2) AS progress_pct
FROM news_articles;
```

### 5.3 增量更新

```sql
-- 增量更新向量
CREATE OR REPLACE FUNCTION incremental_vectorize()
RETURNS void AS $$
BEGIN
    -- 处理新增数据
    INSERT INTO news_embeddings (article_id, embedding)
    SELECT
        id,
        ai.embedding('text-embedding-3-small', content)
    FROM news_articles
    WHERE id NOT IN (SELECT article_id FROM news_embeddings)
      AND content IS NOT NULL;

    -- 处理更新数据
    UPDATE news_embeddings ne
    SET embedding = ai.embedding('text-embedding-3-small', na.content)
    FROM news_articles na
    WHERE ne.article_id = na.id
      AND na.updated_at > ne.updated_at;
END;
$$ LANGUAGE plpgsql;

-- 定时执行增量更新
SELECT cron.schedule(
    'incremental-vectorize',
    '*/30 * * * *',  -- 每30分钟
    'SELECT incremental_vectorize()'
);
```

## 六、监控与告警

### 6.1 数据质量监控

```sql
-- 数据质量监控视图
CREATE VIEW data_quality_metrics AS
SELECT
    'documents' AS table_name,
    COUNT(*) AS total_records,
    COUNT(*) FILTER (WHERE content IS NOT NULL) AS valid_records,
    COUNT(*) FILTER (WHERE embedding IS NULL) AS missing_embeddings,
    AVG(LENGTH(content)) AS avg_content_length
FROM documents;

-- 质量告警
CREATE OR REPLACE FUNCTION check_data_quality()
RETURNS void AS $$
DECLARE
    v_quality_metrics RECORD;
BEGIN
    SELECT * INTO v_quality_metrics FROM data_quality_metrics;

    -- 检查缺失向量比例
    IF v_quality_metrics.missing_embeddings::DECIMAL /
       NULLIF(v_quality_metrics.total_records, 0) > 0.1 THEN
        PERFORM pg_notify('data_quality_alert',
            format('High missing embedding rate: %s%%',
                ROUND(100.0 * v_quality_metrics.missing_embeddings /
                      NULLIF(v_quality_metrics.total_records, 0), 2)));
    END IF;
END;
$$ LANGUAGE plpgsql;
```

### 6.2 处理进度监控

```sql
-- 处理进度监控
CREATE OR REPLACE FUNCTION monitor_processing_progress()
RETURNS TABLE (
    table_name TEXT,
    total_count BIGINT,
    processed_count BIGINT,
    progress_pct DECIMAL
) AS $$
BEGIN
    RETURN QUERY
    SELECT
        'news_articles'::TEXT,
        COUNT(*)::BIGINT,
        COUNT(*) FILTER (WHERE id IN (
            SELECT article_id FROM news_embeddings
        ))::BIGINT,
        ROUND(100.0 * COUNT(*) FILTER (WHERE id IN (
            SELECT article_id FROM news_embeddings
        )) / NULLIF(COUNT(*), 0), 2)::DECIMAL
    FROM news_articles;
END;
$$ LANGUAGE plpgsql;
```

### 6.3 异常告警

```sql
-- 异常告警函数
CREATE OR REPLACE FUNCTION check_processing_errors()
RETURNS void AS $$
DECLARE
    v_error_count INTEGER;
BEGIN
    -- 检查处理失败的数据
    SELECT COUNT(*) INTO v_error_count
    FROM news_articles
    WHERE content IS NOT NULL
      AND id NOT IN (SELECT article_id FROM news_embeddings)
      AND created_at < NOW() - INTERVAL '1 hour';

    IF v_error_count > 10 THEN
        PERFORM pg_notify('processing_error_alert',
            format('Processing errors detected: %s records', v_error_count));
    END IF;
END;
$$ LANGUAGE plpgsql;

-- 定时检查
SELECT cron.schedule(
    'check-processing-errors',
    '*/15 * * * *',
    'SELECT check_processing_errors()'
);
```

## 七、最佳实践

1. **数据注入**:
   - 使用CDC实现实时同步
   - 批量ETL处理历史数据
   - 流式处理实时数据

2. **数据质量**:
   - 在入口处验证数据
   - 自动清洗和标准化
   - 监控数据质量指标

3. **版本管理**:
   - 保留重要数据版本
   - 支持快速回滚
   - 记录变更历史

4. **自动化**:
   - 使用pgai Vectorizer自动向量化
   - 定时增量更新
   - 监控和告警自动化

## 八、关联主题

- [AI原生调用 (pgai)](./AI原生调用-pgai.md) - Vectorizer功能
- [性能优化技术](./性能优化技术.md) - 批量处理优化
- [Timescale MarketReader案例](../05-实践案例/Timescale-MarketReader案例.md) - 自动化应用

## 九、对标资源

### 技术文档

- [Debezium文档](https://debezium.io/documentation/)
- [Airflow文档](https://airflow.apache.org/docs/)
- [pgai Vectorizer文档](https://github.com/pgai/pgai)

### 企业案例

- **Timescale MarketReader**: 自动化Embedding生成
- **开发周期**: 3个月 → 2周 (缩短85%)

---

**最后更新**: 2025年1月
**维护者**: PostgreSQL Modern Team
**文档编号**: AI-03-06
