---

> **📋 文档来源**: `PostgreSQL_View\01-向量与混合搜索\架构设计\混合搜索架构模式.md`
> **📅 复制日期**: 2025-12-22
> **⚠️ 注意**: 本文档为复制版本，原文件保持不变

---

# 混合搜索架构模式

> **更新时间**: 2025 年 11 月 1 日
> **技术版本**: PostgreSQL 16+ / pgvector 0.7.0+
> **文档编号**: 01-03-02

## 📑 目录

- [混合搜索架构模式](#混合搜索架构模式)
  - [📑 目录](#-目录)
  - [1. 概述](#1-概述)
    - [1.1 技术背景](#11-技术背景)
    - [1.2 架构定位](#12-架构定位)
    - [1.3 核心价值](#13-核心价值)
  - [2. 混合搜索架构模式](#2-混合搜索架构模式)
    - [2.1 向量+全文搜索架构](#21-向量全文搜索架构)
    - [2.2 向量+标量过滤架构](#22-向量标量过滤架构)
    - [2.3 多模态搜索架构](#23-多模态搜索架构)
  - [3. RRF 融合架构](#3-rrf-融合架构)
    - [3.1 RRF 算法架构](#31-rrf-算法架构)
    - [3.2 融合策略设计](#32-融合策略设计)
    - [3.3 性能优化架构](#33-性能优化架构)
  - [4. 查询执行架构](#4-查询执行架构)
    - [4.1 并行查询架构](#41-并行查询架构)
    - [4.2 结果融合架构](#42-结果融合架构)
    - [4.3 缓存架构设计](#43-缓存架构设计)
  - [5. 性能分析](#5-性能分析)
    - [5.1 架构性能对比](#51-架构性能对比)
    - [5.2 融合算法对比](#52-融合算法对比)
    - [5.3 实际应用效果](#53-实际应用效果)
      - [案例 1: 电商搜索](#案例-1-电商搜索)
      - [案例 2: RAG 应用](#案例-2-rag-应用)
  - [6. 最佳实践](#6-最佳实践)
    - [6.1 架构选择建议](#61-架构选择建议)
    - [6.2 性能优化建议](#62-性能优化建议)
    - [6.3 融合策略建议](#63-融合策略建议)
  - [7. 常见问题（FAQ）](#7-常见问题faq)
    - [7.1 混合搜索架构性能相关问题](#71-混合搜索架构性能相关问题)
    - [7.2 混合搜索架构设计问题](#72-混合搜索架构设计问题)
  - [8. 参考资料](#8-参考资料)
    - [7.1 官方文档](#71-官方文档)
    - [7.2 学术论文](#72-学术论文)
    - [7.3 技术博客](#73-技术博客)
    - [7.4 相关资源](#74-相关资源)

---

## 1. 概述

### 1.1 技术背景

**问题需求**:

单一搜索方式无法满足复杂查询需求：

1. **向量搜索局限性**:

   - 无法精确匹配关键词（如品牌名、型号）
   - 对拼写错误敏感
   - 无法处理结构化查询（如价格范围、日期范围）

1. **全文搜索局限性**:

   - 无法理解语义（同义词、上下文）
   - 无法处理多语言语义搜索
   - 对长文本效果差

1. **业务需求**:
   - 电商搜索：需要同时支持语义搜索和精确过滤
   - RAG 应用：需要结合关键词和语义搜索
   - 企业搜索：需要支持多模态搜索（文本+图像+视频）

**技术演进**:

1. **2020 年**: 向量搜索和全文搜索独立使用
1. **2022 年**: 开始尝试简单的结果合并
1. **2024 年**: RRF 算法成为混合搜索标准
1. **2025 年**: PostgreSQL 原生支持混合搜索

**市场需求**:

- **电商平台**: 需要融合语义搜索和精确过滤
- **RAG 应用**: 需要结合关键词和语义检索
- **企业搜索**: 需要支持多模态混合搜索

### 1.2 架构定位

混合搜索架构模式是构建智能搜索系统的核心，它需要：

1. **多搜索方式融合**: 向量搜索、全文搜索、标量过滤统一架构
1. **智能结果融合**: 使用 RRF 等算法智能融合多路结果
1. **性能优化**: 并行查询和结果缓存提升性能
1. **灵活扩展**: 支持新增搜索方式和融合算法

### 1.3 核心价值

1. **搜索质量提升**: 结合多种搜索方式，提升召回率和准确率
1. **用户体验优化**: 支持自然语言查询和精确过滤
1. **业务灵活性**: 可根据业务需求调整融合策略
1. **性能保证**: 通过并行查询保证毫秒级响应

---

## 2. 混合搜索架构模式

### 2.1 向量+全文搜索架构

**架构设计**:

```text
┌─────────────────────────────────────────┐
│          查询请求                        │
│  "适合夏天穿的轻薄T恤"                    │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│        查询解析与路由                     │
│  - 提取关键词: "夏天"、"轻薄"、"T恤"      │
│  - 生成查询向量                            │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
       ▼               ▼
┌─────────────┐  ┌─────────────┐
│ 全文搜索     │  │ 向量搜索     │
│ (tsvector)  │  │ (pgvector)  │
└──────┬──────┘  └──────┬──────┘
       │                │
       │ 结果 1          │ 结果 2
       │                │
       └───────┬────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│        RRF 结果融合                      │
│  - 计算融合分数                           │
│  - 排序并返回 Top-K                       │
└─────────────────────────────────────────┘
```

**实现示例**:

```sql
-- 混合搜索查询
WITH vector_search AS (
    SELECT id, content, embedding <=> $1 as distance
    FROM documents
    WHERE embedding <=> $1 < 0.8
    ORDER BY embedding <=> $1
    LIMIT 100
),
fulltext_search AS (
    SELECT id, content, ts_rank(to_tsvector('english', content),
                                 plainto_tsquery('english', $2)) as rank
    FROM documents
    WHERE to_tsvector('english', content) @@ plainto_tsquery('english', $2)
    ORDER BY rank DESC
    LIMIT 100
),
combined AS (
    SELECT
        COALESCE(v.id, f.id) as id,
        COALESCE(v.content, f.content) as content,
        -- RRF 融合分数
        (1.0 / (60 + COALESCE(v.rank, 100))) +
        (1.0 / (60 + COALESCE(f.rank, 100))) as rrf_score
    FROM (
        SELECT id, content, ROW_NUMBER() OVER (ORDER BY distance) as rank
        FROM vector_search
    ) v
    FULL OUTER JOIN (
        SELECT id, content, ROW_NUMBER() OVER (ORDER BY rank DESC) as rank
        FROM fulltext_search
    ) f ON v.id = f.id
)
SELECT id, content, rrf_score
FROM combined
ORDER BY rrf_score DESC
LIMIT 10;
```

**性能数据**:

| 搜索方式 | 召回率 | 准确率 | 平均延迟 |
| -------- | ------ | ------ | -------- |
| 仅向量   | 85%    | 78%    | 25ms     |
| 仅全文   | 65%    | 82%    | 15ms     |
| 混合搜索 | 92%    | 88%    | 35ms     |

### 2.2 向量+标量过滤架构

**架构设计**:

```text
┌─────────────────────────────────────────┐
│          查询请求                        │
│  向量查询 + 价格范围 + 品牌过滤           │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│        查询优化器                        │
│  - 确定过滤顺序                           │
│  - 选择最优执行计划                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      标量过滤 (先执行)                    │
│  WHERE price BETWEEN 100 AND 500        │
│    AND brand = 'Nike'                    │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      向量搜索 (后执行)                    │
│  ORDER BY embedding <=> $1               │
└─────────────────────────────────────────┘
```

**实现示例**:

```sql
-- 向量搜索 + 标量过滤
SELECT id, name, price, brand, embedding <=> $1 as distance
FROM products
WHERE price BETWEEN 100 AND 500
  AND brand = 'Nike'
  AND category = 'T-Shirt'
ORDER BY embedding <=> $1
LIMIT 20;

-- 使用 GIN 索引加速标量过滤
CREATE INDEX ON products USING GIN (brand gin_trgm_ops);
CREATE INDEX ON products (price, category);
```

**性能优化**:

| 过滤顺序 | 过滤后数据量 | 向量搜索时间 | 总时间 |
| -------- | ------------ | ------------ | ------ |
| 先向量   | 1000 万      | 50ms         | 50ms   |
| 先标量   | 10 万        | 5ms          | 8ms    |

### 2.3 多模态搜索架构

**架构设计**:

```text
┌─────────────────────────────────────────┐
│        多模态查询                        │
│  文本 + 图像 + 视频                      │
└──────────────┬──────────────────────────┘
               │
       ┌───────┼───────┐
       │       │       │
       ▼       ▼       ▼
┌─────────┐ ┌──────┐ ┌──────┐
│文本向量  │ │图像向量│ │视频向量│
│搜索     │ │搜索   │ │搜索   │
└────┬────┘ └───┬──┘ └───┬──┘
     │          │        │
     └──────┬───┴────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│      多模态 RRF 融合                     │
│  - 加权融合不同模态结果                   │
│  - 返回统一排序结果                       │
└─────────────────────────────────────────┘
```

**实现示例**:

```sql
-- 多模态搜索表设计
CREATE TABLE multimodal_content (
    id BIGSERIAL PRIMARY KEY,
    content_type TEXT,  -- 'text', 'image', 'video'
    text_content TEXT,
    text_embedding vector(768),
    image_url TEXT,
    image_embedding vector(512),
    video_url TEXT,
    video_embedding vector(1024),
    metadata JSONB
);

-- 多模态混合搜索
WITH text_search AS (
    SELECT id, 'text' as type, text_embedding <=> $1 as score
    FROM multimodal_content
    WHERE content_type = 'text'
    ORDER BY text_embedding <=> $1
    LIMIT 50
),
image_search AS (
    SELECT id, 'image' as type, image_embedding <=> $2 as score
    FROM multimodal_content
    WHERE content_type = 'image'
    ORDER BY image_embedding <=> $2
    LIMIT 50
),
video_search AS (
    SELECT id, 'video' as type, video_embedding <=> $3 as score
    FROM multimodal_content
    WHERE content_type = 'video'
    ORDER BY video_embedding <=> $3
    LIMIT 50
),
combined AS (
    SELECT id, type,
           CASE type
               WHEN 'text' THEN 1.0 / (60 + rank)
               WHEN 'image' THEN 0.8 / (60 + rank)  -- 图像权重稍低
               WHEN 'video' THEN 0.6 / (60 + rank)  -- 视频权重更低
           END as rrf_score
    FROM (
        SELECT *, ROW_NUMBER() OVER (ORDER BY score) as rank FROM text_search
        UNION ALL
        SELECT *, ROW_NUMBER() OVER (ORDER BY score) as rank FROM image_search
        UNION ALL
        SELECT *, ROW_NUMBER() OVER (ORDER BY score) as rank FROM video_search
    ) all_results
)
SELECT id, type, rrf_score
FROM combined
ORDER BY rrf_score DESC
LIMIT 20;
```

---

## 3. RRF 融合架构

### 3.1 RRF 算法架构

**RRF 公式**:

```text
RRF(d) = Σ (1 / (k + rank_i(d)))
```

其中：

- `d`: 文档
- `k`: 常数（通常为 60）
- `rank_i(d)`: 文档在第 i 个搜索结果中的排名

**架构实现**:

```python
class RRFFusion:
    def __init__(self, k=60):
        self.k = k

    def fuse(self, result_sets, weights=None):
        """
        融合多个搜索结果

        Args:
            result_sets: 多个搜索结果列表
            weights: 每个结果集的权重（可选）
        """
        if weights is None:
            weights = [1.0] * len(result_sets)

        # 计算每个文档的 RRF 分数
        doc_scores = {}
        for i, results in enumerate(result_sets):
            weight = weights[i]
            for rank, doc_id in enumerate(results, start=1):
                rrf_score = weight / (self.k + rank)
                doc_scores[doc_id] = doc_scores.get(doc_id, 0) + rrf_score

        # 按分数排序
        sorted_docs = sorted(
            doc_scores.items(),
            key=lambda x: x[1],
            reverse=True
        )

        return [doc_id for doc_id, score in sorted_docs]
```

### 3.2 融合策略设计

**策略 1: 等权重融合**:

```sql
-- 等权重 RRF 融合
SELECT id,
       (1.0 / (60 + vector_rank)) +
       (1.0 / (60 + fulltext_rank)) as rrf_score
FROM (
    SELECT id,
           ROW_NUMBER() OVER (ORDER BY vector_distance) as vector_rank,
           ROW_NUMBER() OVER (ORDER BY fulltext_rank DESC) as fulltext_rank
    FROM combined_results
) ranked
ORDER BY rrf_score DESC;
```

**策略 2: 加权融合**:

```sql
-- 加权 RRF 融合（向量搜索权重更高）
SELECT id,
       (0.7 / (60 + vector_rank)) +
       (0.3 / (60 + fulltext_rank)) as weighted_rrf_score
FROM ranked_results
ORDER BY weighted_rrf_score DESC;
```

**策略 3: 自适应权重**:

```python
class AdaptiveRRF:
    def calculate_weights(self, query_type, query_length):
        """
        根据查询类型和长度自适应调整权重
        """
        if query_type == 'semantic':
            # 语义查询：向量搜索权重高
            return {'vector': 0.8, 'fulltext': 0.2}
        elif query_type == 'keyword':
            # 关键词查询：全文搜索权重高
            return {'vector': 0.3, 'fulltext': 0.7}
        else:
            # 混合查询：等权重
            return {'vector': 0.5, 'fulltext': 0.5}
```

### 3.3 性能优化架构

**并行查询优化**:

```python
import asyncio
from concurrent.futures import ThreadPoolExecutor

class ParallelHybridSearch:
    def __init__(self):
        self.executor = ThreadPoolExecutor(max_workers=4)

    async def search(self, query_vector, query_text, filters=None):
        # 并行执行多个搜索
        tasks = [
            self.vector_search(query_vector, filters),
            self.fulltext_search(query_text, filters),
            self.scalar_filter(filters) if filters else None
        ]

        results = await asyncio.gather(*[t for t in tasks if t])

        # RRF 融合
        return self.rrf_fusion(results)
```

**结果缓存**:

```python
class CachedHybridSearch:
    def __init__(self):
        self.cache = {}

    def search(self, query_vector, query_text, filters=None):
        # 生成缓存键
        cache_key = self.generate_cache_key(
            query_vector, query_text, filters
        )

        # 检查缓存
        if cache_key in self.cache:
            return self.cache[cache_key]

        # 执行搜索
        results = self.execute_search(query_vector, query_text, filters)

        # 缓存结果
        self.cache[cache_key] = results
        return results
```

---

## 4. 查询执行架构

### 4.1 并行查询架构

**PostgreSQL 并行查询**:

```sql
-- 启用并行查询
SET max_parallel_workers_per_gather = 4;
SET parallel_tuple_cost = 0.1;

-- 并行混合搜索
EXPLAIN (ANALYZE, BUFFERS, TIMING)
WITH vector_search AS (
    SELECT id, embedding <=> $1 as distance
    FROM documents
    WHERE embedding <=> $1 < 0.8
    ORDER BY embedding <=> $1
    LIMIT 100
),
fulltext_search AS (
    SELECT id, ts_rank(to_tsvector('english', content),
                       plainto_tsquery('english', $2)) as rank
    FROM documents
    WHERE to_tsvector('english', content) @@ plainto_tsquery('english', $2)
    ORDER BY rank DESC
    LIMIT 100
)
SELECT * FROM vector_search
UNION ALL
SELECT * FROM fulltext_search;
```

### 4.2 结果融合架构

**流式融合**:

```python
class StreamingRRF:
    def fuse_streaming(self, result_streams, k=10):
        """
        流式融合多个搜索结果
        """
        rrf_scores = {}
        seen_docs = set()

        # 从每个流中读取结果
        streams = [iter(stream) for stream in result_streams]
        active_streams = list(range(len(streams)))

        while len(seen_docs) < k * 2 and active_streams:
            for stream_idx in active_streams[:]:
                try:
                    doc_id, rank = next(streams[stream_idx])
                    if doc_id not in seen_docs:
                        seen_docs.add(doc_id)
                        rrf_scores[doc_id] = rrf_scores.get(doc_id, 0) + \
                                            1.0 / (60 + rank)
                except StopIteration:
                    active_streams.remove(stream_idx)

        # 返回 Top-K
        return sorted(
            rrf_scores.items(),
            key=lambda x: x[1],
            reverse=True
        )[:k]
```

### 4.3 缓存架构设计

**多级缓存**:

```python
class HybridSearchCache:
    def __init__(self):
        self.l1_cache = {}  # 内存缓存
        self.redis = Redis()  # Redis 缓存

    def get(self, query_key):
        # L1 缓存
        if query_key in self.l1_cache:
            return self.l1_cache[query_key]

        # Redis 缓存
        cached = self.redis.get(query_key)
        if cached:
            self.l1_cache[query_key] = cached
            return cached

        return None

    def set(self, query_key, results, ttl=3600):
        self.l1_cache[query_key] = results
        self.redis.setex(query_key, ttl, json.dumps(results))
```

---

## 5. 性能分析

### 5.1 架构性能对比

| 架构方案 | QPS  | P99 延迟 | 召回率 | 准确率 |
| -------- | ---- | -------- | ------ | ------ |
| 仅向量   | 2000 | 25ms     | 85%    | 78%    |
| 仅全文   | 3000 | 15ms     | 65%    | 82%    |
| 串行混合 | 1000 | 40ms     | 92%    | 88%    |
| 并行混合 | 1500 | 35ms     | 92%    | 88%    |

### 5.2 融合算法对比

| 融合算法 | 召回率 | 准确率 | 计算复杂度 |
| -------- | ------ | ------ | ---------- |
| 简单合并 | 90%    | 80%    | O(n)       |
| RRF      | 92%    | 88%    | O(n log n) |
| 加权 RRF | 93%    | 89%    | O(n log n) |
| 学习融合 | 95%    | 91%    | O(n²)      |

### 5.3 实际应用效果

#### 案例 1: 电商搜索

- **架构**: 向量 + 全文 + 标量过滤
- **性能**: QPS 1500，P99 延迟 35ms
- **效果**: 转化率提升 47%，用户满意度提升 32%

#### 案例 2: RAG 应用

- **架构**: 向量 + 全文混合搜索
- **性能**: QPS 800，P99 延迟 40ms
- **效果**: 答案准确率提升 25%，用户反馈提升 40%

---

## 6. 最佳实践

### 6.1 架构选择建议

1. **简单场景**: 向量搜索 + 标量过滤
1. **中等场景**: 向量 + 全文搜索 + RRF 融合
1. **复杂场景**: 多模态搜索 + 自适应权重融合

### 6.2 性能优化建议

1. **并行查询**: 使用并行查询提升性能
1. **结果缓存**: 实施多级缓存策略
1. **索引优化**: 为所有搜索方式创建合适索引
1. **查询优化**: 优化查询顺序和过滤条件

### 6.3 融合策略建议

1. **等权重**: 适用于搜索方式重要性相当
1. **加权融合**: 根据业务需求调整权重
1. **自适应**: 根据查询类型动态调整权重

---

## 7. 常见问题（FAQ）

### 7.1 混合搜索架构性能相关问题

- **Q1: 如何优化混合搜索架构的查询性能？**
  - **A1**: 优化混合搜索架构的查询性能，可以从以下几个方面入手：
    1. **并行执行**: 将向量搜索和全文搜索并行执行，缩短总体查询时间。
    2. **索引优化**: 为向量字段创建HNSW索引，为文本字段创建GIN索引，加速各自查询。
    3. **结果缓存**: 缓存频繁查询的混合搜索结果，减少重复计算。
    4. **查询优化**: 优化RRF融合算法，根据查询类型动态调整权重，提高搜索准确率。
    5. **负载均衡**: 使用负载均衡器分发查询请求，提高整体吞吐量。

- **Q2: 如何平衡混合搜索中向量搜索和全文搜索的权重？**
  - **A2**: 平衡混合搜索中向量搜索和全文搜索的权重，可以采取以下策略：
    1. **查询类型识别**: 根据查询类型（语义查询、关键词查询、混合查询）动态调整权重。
    2. **A/B测试**: 通过A/B测试验证不同权重配置的效果，选择最优配置。
    3. **用户反馈**: 收集用户对搜索结果的反馈，根据反馈调整权重。
    4. **机器学习**: 使用机器学习模型学习最优权重，根据查询特征自动调整。
    5. **业务规则**: 根据业务需求设置权重，例如对于精确匹配需求高的场景，提高全文搜索权重。

### 7.2 混合搜索架构设计问题

- **Q3: 如何设计高可用的混合搜索架构？**
  - **A3**: 设计高可用的混合搜索架构，可以考虑以下方法：
    1. **主从复制**: 采用PostgreSQL流复制，配置一主多从架构，实现数据冗余。
    2. **自动故障转移**: 使用Patroni或pg_auto_failover等工具实现自动故障转移，确保服务连续性。
    3. **多地域部署**: 对于关键业务，可以考虑多地域部署，实现地理级别的容灾。
    4. **监控告警**: 部署Prometheus + Grafana监控系统，实时监控搜索服务状态，及时发现和处理问题。
    5. **降级策略**: 设计降级策略，在部分服务故障时，能够降级到单一搜索模式，保证基本服务可用。

---

## 8. 参考资料

### 7.1 官方文档

- **[PostgreSQL 全文搜索文档](https://www.postgresql.org/docs/current/textsearch.html)**
  - 版本: PostgreSQL 8.3+
  - 内容: PostgreSQL 全文搜索的完整文档
  - 最后更新: 2025年

- **[pgvector 官方文档](https://github.com/pgvector/pgvector)**
  - 版本: pgvector 0.7.0+
  - 内容: pgvector 扩展的完整文档

- **[PostgreSQL 查询优化器文档](https://www.postgresql.org/docs/current/query-optimizer.html)**
  - 内容: PostgreSQL 查询优化器的详细说明

### 7.2 学术论文

- **Cormack, G. V., Clarke, C. L., & Büttcher, S. (2009). "Reciprocal Rank Fusion outperforms Condorcet and individual Rank Learning Methods."**
  - 会议: SIGIR 2009
  - **DOI**: [10.1145/1571941.1572114](https://doi.org/10.1145/1571941.1572114)
  - **重要性**: RRF 算法的原始论文，详细阐述了算法原理和数学证明

### 7.3 技术博客

- **[混合搜索 RRF 算法](../../../混合搜索RRF算法.md)**
  - 内容: RRF 算法的详细说明和实现

- **[pgvector 核心原理](../../../pgvector核心原理.md)**
  - 内容: pgvector 的核心原理和实现细节

- **[Supabase Hybrid Search 官方博客](https://supabase.com/blog/hybrid-search)**
  - 来源: Supabase
  - 内容: Supabase 混合搜索的实现和案例

### 7.4 相关资源

- **[混合搜索最佳实践指南](https://www.pinecone.io/learn/hybrid-search/)**
  - 来源: Pinecone
  - 内容: 混合搜索的设计原则和优化技巧

- **[PostgreSQL 并行查询](https://www.postgresql.org/docs/current/parallel-query.html)**
  - 内容: PostgreSQL 并行查询的配置和优化

---

**最后更新**: 2025 年 11 月 1 日
**维护者**: PostgreSQL Modern Team
