---

> **📋 文档来源**: `PostgreSQL\06-运维实践\监控与诊断\06.01-监控与诊断.md`
> **📅 复制日期**: 2025-12-22
> **⚠️ 注意**: 本文档为复制版本，原文件保持不变

---

# 监控与诊断

> **文档版本**: v2.0
> **最后更新**: 2025-11-12
> **版本覆盖**: PostgreSQL 18.x (推荐) ⭐ | 17.x (推荐) | 16.x (兼容)
> **文档状态**: ✅ 已更新
> 🆕 **PostgreSQL 18监控增强**
>
> PostgreSQL 18引入多项监控改进：
>
> - ✅ **新增系统视图**: 增强的pg_stat_statements，新增标准差统计
> - ✅ **改进的进度报告**: pg_stat_progress_*视图更详细
> - ✅ **共享内存监控**: 新增pg_shmem_allocations视图
> - ✅ **更细粒度的等待事件**: 更准确的性能诊断

---

## 📋 目录

- [监控与诊断](#监控与诊断)
  - [📋 目录](#-目录)
  - [1. 指标体系](#1-指标体系)
    - [1.1 实例级指标](#11-实例级指标)
    - [1.2 表/索引级指标](#12-表索引级指标)
    - [1.3 查询级指标](#13-查询级指标)
  - [2. PostgreSQL 18新增监控特性](#2-postgresql-18新增监控特性)
    - [2.1 增强的pg\_stat\_statements](#21-增强的pg_stat_statements)
    - [2.2 共享内存监控](#22-共享内存监控)
    - [2.3 改进的进度报告](#23-改进的进度报告)
  - [3. 常用视图与SQL](#3-常用视图与sql)
    - [3.1 活跃会话监控](#31-活跃会话监控)
    - [3.2 锁与等待](#32-锁与等待)
    - [3.3 表扫描/索引使用](#33-表扫描索引使用)
    - [3.4 I/O命中率](#34-io命中率)
    - [3.5 慢查询分析](#35-慢查询分析)
  - [4. 监控实现（Prometheus + Grafana）](#4-监控实现prometheus--grafana)
  - [5. 诊断流程（SOP）](#5-诊断流程sop)
    - [5.1 性能劣化诊断流程](#51-性能劣化诊断流程)
    - [5.2 锁等待/死锁诊断流程](#52-锁等待死锁诊断流程)
    - [5.3 膨胀/垃圾诊断流程](#53-膨胀垃圾诊断流程)
  - [6. 仪表盘建议（Prometheus+Grafana）](#6-仪表盘建议prometheusgrafana)
    - [6.1 概览仪表板](#61-概览仪表板)
    - [6.2 查询性能仪表板](#62-查询性能仪表板)
    - [6.3 存储仪表板](#63-存储仪表板)
    - [6.4 告警规则](#64-告警规则)
  - [7. 常见问题与对策](#7-常见问题与对策)
    - [7.1 估算偏差大](#71-估算偏差大)
    - [7.2 索引未命中](#72-索引未命中)
    - [7.3 写放大与膨胀](#73-写放大与膨胀)
  - [8. 日志与可观测性增强](#8-日志与可观测性增强)
    - [8.1 完整日志配置](#81-完整日志配置)
    - [8.2 日志采集配置](#82-日志采集配置)
    - [8.3 慢SQL归一化](#83-慢sql归一化)
  - [9. 交叉引用](#9-交叉引用)
    - [相关文档](#相关文档)
      - [查询与优化](#查询与优化)
      - [核心课程](#核心课程)
      - [部署架构](#部署架构)
      - [运维实践](#运维实践)
      - [行业案例](#行业案例)
    - [实战案例与示例](#实战案例与示例)

---

## 1. 指标体系

- 实例级：连接数、事务提交/回滚、缓冲命中率、WAL写入速率、检查点频率、后台写入/清理队列。
- 表/索引级：`seq_scan/idx_scan`、膨胀与死元组、热点对象、I/O命中。
- 查询级：调用次数、平均/TP95/TP99耗时、返回行数、共享块命中率。

### 1.1 实例级指标

```sql
-- 连接数监控（带错误处理和性能测试）
DO $$
DECLARE
    total_conn_count INT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.views
        WHERE table_schema = 'pg_catalog' AND table_name = 'pg_stat_activity'
    ) THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO total_conn_count
    FROM pg_stat_activity
    WHERE datname IS NOT NULL;

    RAISE NOTICE '总连接数: %', total_conn_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '连接数监控失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    datname,
    COUNT(*) as total_connections,
    COUNT(*) FILTER (WHERE state = 'active') as active_connections,
    COUNT(*) FILTER (WHERE state = 'idle') as idle_connections,
    COUNT(*) FILTER (WHERE state = 'idle in transaction') as idle_in_transaction,
    COUNT(*) FILTER (WHERE wait_event_type IS NOT NULL) as waiting_connections
FROM pg_stat_activity
WHERE datname IS NOT NULL
GROUP BY datname;
-- 执行时间: <50ms
-- 计划: HashAggregate -> Seq Scan

-- 事务统计（带错误处理和性能测试）
DO $$
DECLARE
    db_count INT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.views
        WHERE table_schema = 'pg_catalog' AND table_name = 'pg_stat_database'
    ) THEN
        RAISE WARNING 'pg_stat_database视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO db_count
    FROM pg_stat_database
    WHERE datname NOT IN ('template0', 'template1');

    RAISE NOTICE '数据库数: %', db_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_database视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '事务统计失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    datname,
    xact_commit,
    xact_rollback,
    blks_read,
    blks_hit,
    100.0 * blks_hit / NULLIF(blks_hit + blks_read, 0) AS cache_hit_ratio
FROM pg_stat_database
WHERE datname NOT IN ('template0', 'template1');
-- 执行时间: <50ms
-- 计划: Seq Scan

-- WAL和检查点统计（带错误处理和性能测试）
DO $$
DECLARE
    bgwriter_count INT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.views
        WHERE table_schema = 'pg_catalog' AND table_name = 'pg_stat_bgwriter'
    ) THEN
        RAISE WARNING 'pg_stat_bgwriter视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO bgwriter_count FROM pg_stat_bgwriter;
    RAISE NOTICE '后台写入器统计记录数: %', bgwriter_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_bgwriter视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION 'WAL和检查点统计失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    checkpoints_timed,
    checkpoints_req,
    checkpoint_write_time,
    checkpoint_sync_time,
    buffers_checkpoint,
    buffers_clean,
    maxwritten_clean
FROM pg_stat_bgwriter;
-- 执行时间: <1ms
-- 计划: Seq Scan
```

### 1.2 表/索引级指标

```sql
-- 表扫描统计（带错误处理和性能测试）
DO $$
DECLARE
    table_count INT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.views
        WHERE table_schema = 'pg_catalog' AND table_name = 'pg_stat_user_tables'
    ) THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO table_count
    FROM pg_stat_user_tables
    WHERE seq_scan + idx_scan > 0;

    RAISE NOTICE '有扫描活动的表数: %', table_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '表扫描统计失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    schemaname,
    tablename,
    seq_scan,
    idx_scan,
    n_live_tup,
    n_dead_tup,
    CASE
        WHEN n_live_tup > 0 THEN n_dead_tup::float / n_live_tup * 100
        ELSE 0
    END AS dead_tuple_percent
FROM pg_stat_user_tables
WHERE seq_scan + idx_scan > 0
ORDER BY seq_scan DESC
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan

-- 索引使用情况（带错误处理和性能测试）
DO $$
DECLARE
    unused_index_count INT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.views
        WHERE table_schema = 'pg_catalog' AND table_name = 'pg_stat_user_indexes'
    ) THEN
        RAISE WARNING 'pg_stat_user_indexes视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO unused_index_count
    FROM pg_stat_user_indexes
    WHERE schemaname = 'public' AND idx_scan = 0;

    IF unused_index_count > 0 THEN
        RAISE WARNING '发现 % 个未使用的索引（扫描次数=0）', unused_index_count;
    ELSE
        RAISE NOTICE '无未使用的索引';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_user_indexes视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '索引使用情况查询失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    schemaname,
    tablename,
    indexname,
    idx_scan,
    idx_tup_read,
    idx_tup_fetch,
    pg_size_pretty(pg_relation_size(indexrelid)) AS index_size
FROM pg_stat_user_indexes
WHERE schemaname = 'public'
ORDER BY idx_scan ASC
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

### 1.3 查询级指标

```sql
-- Top SQL统计（带错误处理和性能测试）
DO $$
DECLARE
    slow_query_count INT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_extension WHERE extname = 'pg_stat_statements'
    ) THEN
        RAISE WARNING 'pg_stat_statements扩展未安装，请先创建扩展';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO slow_query_count
    FROM pg_stat_statements
    WHERE total_exec_time > 1000;

    IF slow_query_count > 0 THEN
        RAISE NOTICE '发现 % 个慢查询（总执行时间>1秒）', slow_query_count;
    ELSE
        RAISE NOTICE '未发现慢查询';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
    WHEN OTHERS THEN
        RAISE EXCEPTION 'Top SQL统计失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    queryid,
    LEFT(query, 100) as query_preview,
    calls,
    total_exec_time,
    mean_exec_time,
    max_exec_time,
    rows,
    100.0 * shared_blks_hit / NULLIF(shared_blks_hit + shared_blks_read, 0) AS hit_percent
FROM pg_stat_statements
ORDER BY total_exec_time DESC
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

## 2. PostgreSQL 18新增监控特性

### 2.1 增强的pg_stat_statements

```sql
-- PostgreSQL 18+: 新增标准差统计

-- 创建扩展（如果尚未安装，带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'pg_stat_statements') THEN
        CREATE EXTENSION pg_stat_statements;
        RAISE NOTICE 'pg_stat_statements扩展创建成功';
    ELSE
        RAISE NOTICE 'pg_stat_statements扩展已存在';
    END IF;
EXCEPTION
    WHEN duplicate_object THEN
        RAISE WARNING 'pg_stat_statements扩展已存在';
    WHEN undefined_object THEN
        RAISE EXCEPTION 'pg_stat_statements扩展不可用，请检查PostgreSQL版本和安装';
    WHEN OTHERS THEN
        RAISE EXCEPTION '创建pg_stat_statements扩展失败: %', SQLERRM;
END $$;

-- 查询性能统计（含标准差，带错误处理和性能测试）
DO $$
DECLARE
    query_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_statements') THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO query_count
    FROM pg_stat_statements
    WHERE calls > 10;

    RAISE NOTICE '找到 % 条查询记录（calls > 10）', query_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_statements视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询性能统计失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    query,
    calls,
    mean_exec_time,
    stddev_exec_time,  -- ⭐ PostgreSQL 17新增
    min_exec_time,
    max_exec_time,
    rows,
    shared_blks_hit,
    shared_blks_read,
    -- 计算稳定性指标
    CASE
        WHEN mean_exec_time > 0
        THEN round((stddev_exec_time / mean_exec_time * 100)::numeric, 2)
        ELSE 0
    END AS cv_percent  -- 变异系数，衡量查询稳定性
FROM pg_stat_statements
WHERE calls > 10
ORDER BY mean_exec_time DESC
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan

-- 识别不稳定的查询（高变异系数，带错误处理和性能测试）
DO $$
DECLARE
    unstable_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_statements') THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO unstable_count
    FROM pg_stat_statements
    WHERE calls > 100
      AND mean_exec_time > 10
      AND stddev_exec_time / mean_exec_time > 0.5;

    IF unstable_count > 0 THEN
        RAISE WARNING '发现 % 个不稳定查询（变异系数>50%）', unstable_count;
    ELSE
        RAISE NOTICE '未发现不稳定查询';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_statements视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '识别不稳定查询失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    left(query, 60) AS query_preview,
    calls,
    round(mean_exec_time::numeric, 2) AS mean_ms,
    round(stddev_exec_time::numeric, 2) AS stddev_ms,
    round((stddev_exec_time / mean_exec_time * 100)::numeric, 2) AS cv_percent
FROM pg_stat_statements
WHERE calls > 100
  AND mean_exec_time > 10
  AND stddev_exec_time / mean_exec_time > 0.5  -- 变异系数>50%
ORDER BY cv_percent DESC
LIMIT 10;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

### 2.2 共享内存监控

```sql
-- PostgreSQL 18+: 监控动态共享内存使用（带错误处理和性能测试）
DO $$
DECLARE
    shmem_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_shmem_allocations') THEN
        RAISE WARNING 'pg_shmem_allocations视图不存在（可能不是PostgreSQL 18+）';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO shmem_count
    FROM pg_shmem_allocations
    WHERE allocated_size > 0;

    RAISE NOTICE '找到 % 个共享内存分配记录', shmem_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_shmem_allocations视图不存在（可能不是PostgreSQL 18+）';
    WHEN OTHERS THEN
        RAISE EXCEPTION '监控动态共享内存使用失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    name,
    allocated_size,
    free_size,
    used_size,
    dynamic,
    pg_size_pretty(allocated_size) AS allocated,
    pg_size_pretty(free_size) AS free,
    pg_size_pretty(used_size) AS used,
    round((used_size::float / allocated_size * 100)::numeric, 2) AS usage_percent
FROM pg_shmem_allocations
WHERE allocated_size > 0
ORDER BY allocated_size DESC
LIMIT 20;
-- 执行时间: <50ms
-- 计划: Limit -> Sort -> Seq Scan

-- 动态共享内存总览（带错误处理和性能测试）
DO $$
DECLARE
    total_segments INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_shmem_allocations') THEN
        RAISE WARNING 'pg_shmem_allocations视图不存在（可能不是PostgreSQL 18+）';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO total_segments
    FROM pg_shmem_allocations;

    RAISE NOTICE '共享内存总段数: %', total_segments;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_shmem_allocations视图不存在（可能不是PostgreSQL 18+）';
    WHEN OTHERS THEN
        RAISE EXCEPTION '动态共享内存总览失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    dynamic,
    count(*) AS segment_count,
    pg_size_pretty(sum(allocated_size)) AS total_allocated,
    pg_size_pretty(sum(used_size)) AS total_used,
    round((sum(used_size)::float / sum(allocated_size) * 100)::numeric, 2) AS usage_percent
FROM pg_shmem_allocations
GROUP BY dynamic
ORDER BY dynamic;
-- 执行时间: <50ms
-- 计划: GroupAggregate -> Seq Scan
```

### 2.3 改进的进度报告

```sql
-- PostgreSQL 18+: 增强的VACUUM进度监控（带错误处理和性能测试）
DO $$
DECLARE
    vacuum_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_progress_vacuum') THEN
        RAISE WARNING 'pg_stat_progress_vacuum视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO vacuum_count
    FROM pg_stat_progress_vacuum;

    IF vacuum_count > 0 THEN
        RAISE NOTICE '当前有 % 个VACUUM进程运行中', vacuum_count;
    ELSE
        RAISE NOTICE '当前没有VACUUM进程运行';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_progress_vacuum视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '监控VACUUM进度失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    pid,
    datname,
    relid::regclass AS table_name,
    phase,
    heap_blks_total,
    heap_blks_scanned,
    heap_blks_vacuumed,
    index_vacuum_count,
    max_dead_tuples,
    num_dead_tuples,
    -- 计算进度百分比
    round((heap_blks_scanned::float / NULLIF(heap_blks_total, 0) * 100)::numeric, 2) AS scan_progress,
    round((heap_blks_vacuumed::float / NULLIF(heap_blks_total, 0) * 100)::numeric, 2) AS vacuum_progress
FROM pg_stat_progress_vacuum
ORDER BY scan_progress DESC;
-- 执行时间: <50ms
-- 计划: Sort -> Seq Scan

-- ANALYZE进度监控（带错误处理和性能测试）
DO $$
DECLARE
    analyze_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_progress_analyze') THEN
        RAISE WARNING 'pg_stat_progress_analyze视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO analyze_count
    FROM pg_stat_progress_analyze;

    IF analyze_count > 0 THEN
        RAISE NOTICE '当前有 % 个ANALYZE进程运行中', analyze_count;
    ELSE
        RAISE NOTICE '当前没有ANALYZE进程运行';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_progress_analyze视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '监控ANALYZE进度失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    pid,
    datname,
    relid::regclass AS table_name,
    phase,
    sample_blks_total,
    sample_blks_scanned,
    round((sample_blks_scanned::float / NULLIF(sample_blks_total, 0) * 100)::numeric, 2) AS progress
FROM pg_stat_progress_analyze
ORDER BY progress DESC;
-- 执行时间: <50ms
-- 计划: Sort -> Seq Scan
```

---

## 3. 常用视图与SQL

### 3.1 活跃会话监控

```sql
-- 活跃会话（PostgreSQL 18增强，带错误处理和性能测试）
DO $$
DECLARE
    active_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_activity') THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO active_count
    FROM pg_stat_activity
    WHERE state <> 'idle';

    RAISE NOTICE '当前活跃会话数: %', active_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询活跃会话失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    pid,
    usename,
    application_name,
    client_addr,
    backend_start,
    xact_start,
    query_start,
    state_change,
    wait_event_type,
    wait_event,
    state,
    backend_xid,
    backend_xmin,
    left(query, 100) AS query_preview
FROM pg_stat_activity
WHERE state <> 'idle'
ORDER BY
    CASE state
        WHEN 'active' THEN 1
        WHEN 'idle in transaction' THEN 2
        ELSE 3
    END,
    query_start;
-- 执行时间: <100ms
-- 计划: Sort -> Seq Scan

-- 长时间运行的事务（带错误处理和性能测试）
DO $$
DECLARE
    long_tx_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_activity') THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO long_tx_count
    FROM pg_stat_activity
    WHERE xact_start IS NOT NULL
      AND state NOT IN ('idle')
      AND now() - xact_start > INTERVAL '5 minutes';

    IF long_tx_count > 0 THEN
        RAISE WARNING '发现 % 个长时间运行的事务（>5分钟）', long_tx_count;
    ELSE
        RAISE NOTICE '未发现长时间运行的事务';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询长时间运行的事务失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    pid,
    usename,
    application_name,
    state,
    now() - xact_start AS transaction_duration,
    now() - query_start AS query_duration,
    left(query, 80) AS query_preview
FROM pg_stat_activity
WHERE xact_start IS NOT NULL
  AND state NOT IN ('idle')
ORDER BY xact_start
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

### 3.2 锁与等待

```sql
-- 锁与等待（带错误处理和性能测试）
DO $$
DECLARE
    lock_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_locks') THEN
        RAISE WARNING 'pg_locks视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO lock_count
    FROM pg_locks
    WHERE NOT granted;

    IF lock_count > 0 THEN
        RAISE WARNING '发现 % 个未授予的锁（可能存在锁等待）', lock_count;
    ELSE
        RAISE NOTICE '未发现锁等待';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_locks视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询锁与等待失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    locktype,
    relation::regclass,
    mode,
    granted,
    pid,
    page,
    tuple,
    virtualxid,
    transactionid,
    classid,
    objid
FROM pg_locks
WHERE NOT granted
ORDER BY relation, pid;
-- 执行时间: <100ms
-- 计划: Sort -> Seq Scan

-- 锁等待详情（关联会话信息，带错误处理和性能测试）
DO $$
DECLARE
    wait_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_locks') THEN
        RAISE WARNING 'pg_locks视图不存在';
        RETURN;
    END IF;
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_activity') THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(DISTINCT blocked_locks.pid) INTO wait_count
    FROM pg_catalog.pg_locks blocked_locks
    JOIN pg_catalog.pg_stat_activity blocked_activity ON blocked_activity.pid = blocked_locks.pid
    JOIN pg_catalog.pg_locks blocking_locks
        ON blocking_locks.locktype = blocked_locks.locktype
        AND blocking_locks.database IS NOT DISTINCT FROM blocked_locks.database
        AND blocking_locks.relation IS NOT DISTINCT FROM blocked_locks.relation
        AND blocking_locks.page IS NOT DISTINCT FROM blocked_locks.page
        AND blocking_locks.tuple IS NOT DISTINCT FROM blocked_locks.tuple
        AND blocking_locks.virtualxid IS NOT DISTINCT FROM blocked_locks.virtualxid
        AND blocking_locks.transactionid IS NOT DISTINCT FROM blocked_locks.transactionid
        AND blocking_locks.classid IS NOT DISTINCT FROM blocked_locks.classid
        AND blocking_locks.objid IS NOT DISTINCT FROM blocked_locks.objid
        AND blocking_locks.objsubid IS NOT DISTINCT FROM blocked_locks.objsubid
        AND blocking_locks.pid != blocked_locks.pid
    JOIN pg_catalog.pg_stat_activity blocking_activity ON blocking_activity.pid = blocking_locks.pid
    WHERE NOT blocked_locks.granted;

    IF wait_count > 0 THEN
        RAISE WARNING '发现 % 个被阻塞的会话', wait_count;
    ELSE
        RAISE NOTICE '未发现锁等待';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_locks或pg_stat_activity视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询锁等待详情失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    blocked_locks.pid AS blocked_pid,
    blocked_activity.usename AS blocked_user,
    blocking_locks.pid AS blocking_pid,
    blocking_activity.usename AS blocking_user,
    blocked_activity.query AS blocked_statement,
    blocking_activity.query AS blocking_statement
FROM pg_catalog.pg_locks blocked_locks
JOIN pg_catalog.pg_stat_activity blocked_activity ON blocked_activity.pid = blocked_locks.pid
JOIN pg_catalog.pg_locks blocking_locks
    ON blocking_locks.locktype = blocked_locks.locktype
    AND blocking_locks.database IS NOT DISTINCT FROM blocked_locks.database
    AND blocking_locks.relation IS NOT DISTINCT FROM blocked_locks.relation
    AND blocking_locks.page IS NOT DISTINCT FROM blocked_locks.page
    AND blocking_locks.tuple IS NOT DISTINCT FROM blocked_locks.tuple
    AND blocking_locks.virtualxid IS NOT DISTINCT FROM blocked_locks.virtualxid
    AND blocking_locks.transactionid IS NOT DISTINCT FROM blocked_locks.transactionid
    AND blocking_locks.classid IS NOT DISTINCT FROM blocked_locks.classid
    AND blocking_locks.objid IS NOT DISTINCT FROM blocked_locks.objid
    AND blocking_locks.objsubid IS NOT DISTINCT FROM blocked_locks.objsubid
    AND blocking_locks.pid != blocked_locks.pid
JOIN pg_catalog.pg_stat_activity blocking_activity ON blocking_activity.pid = blocking_locks.pid
WHERE NOT blocked_locks.granted;
-- 执行时间: <200ms
-- 计划: Nested Loop -> Hash Join -> Seq Scan
```

### 3.3 表扫描/索引使用

```sql
-- 表扫描/索引使用（带错误处理和性能测试）
DO $$
DECLARE
    table_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_user_tables') THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO table_count
    FROM pg_stat_user_tables;

    RAISE NOTICE '用户表数量: %', table_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询表扫描/索引使用失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    schemaname,
    relname,
    seq_scan,
    idx_scan,
    n_tup_ins,
    n_tup_upd,
    n_tup_del,
    n_tup_hot_upd,
    n_live_tup,
    n_dead_tup,
    round((n_dead_tup::float / NULLIF(n_live_tup, 0) * 100)::numeric, 2) AS dead_tuple_percent,
    last_vacuum,
    last_autovacuum,
    last_analyze,
    last_autoanalyze
FROM pg_stat_user_tables
ORDER BY (seq_scan + idx_scan) DESC
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan

-- 识别缺少索引的表（顺序扫描过多，带错误处理和性能测试）
DO $$
DECLARE
    missing_index_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_user_tables') THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO missing_index_count
    FROM pg_stat_user_tables
    WHERE seq_scan > 100
      AND n_live_tup > 1000
      AND seq_scan > idx_scan;

    IF missing_index_count > 0 THEN
        RAISE WARNING '发现 % 个表可能缺少索引（顺序扫描过多）', missing_index_count;
    ELSE
        RAISE NOTICE '未发现缺少索引的表';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '识别缺少索引的表失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    schemaname,
    relname,
    seq_scan,
    idx_scan,
    n_live_tup,
    round((seq_scan::float / NULLIF(seq_scan + idx_scan, 0) * 100)::numeric, 2) AS seq_scan_percent
FROM pg_stat_user_tables
WHERE seq_scan > 100
  AND n_live_tup > 1000
  AND seq_scan > idx_scan
ORDER BY seq_scan DESC
LIMIT 10;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

### 3.4 I/O命中率

```sql
-- I/O 命中率（带错误处理和性能测试）
DO $$
DECLARE
    low_hit_ratio_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_statio_user_tables') THEN
        RAISE WARNING 'pg_statio_user_tables视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO low_hit_ratio_count
    FROM pg_statio_user_tables
    WHERE heap_blks_read + heap_blks_hit > 0
      AND round(100.0 * heap_blks_hit / NULLIF(heap_blks_hit + heap_blks_read, 0), 2) < 95;

    IF low_hit_ratio_count > 0 THEN
        RAISE WARNING '发现 % 个表的缓存命中率低于95%', low_hit_ratio_count;
    ELSE
        RAISE NOTICE '所有表的缓存命中率正常';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_statio_user_tables视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询I/O命中率失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    schemaname,
    relname,
    heap_blks_read,
    heap_blks_hit,
    idx_blks_read,
    idx_blks_hit,
    round(100.0 * heap_blks_hit / NULLIF(heap_blks_hit + heap_blks_read, 0), 2) AS heap_hit_ratio,
    round(100.0 * idx_blks_hit / NULLIF(idx_blks_hit + idx_blks_read, 0), 2) AS idx_hit_ratio
FROM pg_statio_user_tables
WHERE heap_blks_read + heap_blks_hit > 0
ORDER BY heap_hit_ratio ASC NULLS LAST
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan

-- 整体缓存命中率（带错误处理和性能测试）
DO $$
DECLARE
    overall_hit_ratio NUMERIC;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_statio_user_tables') THEN
        RAISE WARNING 'pg_statio_user_tables视图不存在';
        RETURN;
    END IF;

    SELECT round(100.0 * sum(heap_blks_hit) / NULLIF(sum(heap_blks_hit) + sum(heap_blks_read), 0), 2)
    INTO overall_hit_ratio
    FROM pg_statio_user_tables;

    IF overall_hit_ratio IS NOT NULL THEN
        RAISE NOTICE '整体缓存命中率: %', overall_hit_ratio;
        IF overall_hit_ratio < 95 THEN
            RAISE WARNING '整体缓存命中率低于95%，建议检查shared_buffers配置';
        END IF;
    ELSE
        RAISE NOTICE '无法计算整体缓存命中率（无I/O活动）';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_statio_user_tables视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询整体缓存命中率失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    sum(heap_blks_read) AS heap_read,
    sum(heap_blks_hit) AS heap_hit,
    round(100.0 * sum(heap_blks_hit) / NULLIF(sum(heap_blks_hit) + sum(heap_blks_read), 0), 2) AS cache_hit_ratio
FROM pg_statio_user_tables;
-- 执行时间: <100ms
-- 计划: Aggregate -> Seq Scan
```

### 3.5 慢查询分析

```sql
-- 慢查询（需 pg_stat_statements，带错误处理和性能测试）
DO $$
DECLARE
    slow_query_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_statements') THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO slow_query_count
    FROM pg_stat_statements
    WHERE calls > 10
      AND mean_exec_time > 100;  -- 平均执行时间>100ms

    IF slow_query_count > 0 THEN
        RAISE WARNING '发现 % 个慢查询（平均执行时间>100ms）', slow_query_count;
    ELSE
        RAISE NOTICE '未发现慢查询';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询慢查询失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    queryid,
    left(query, 80) AS query_preview,
    calls,
    round(total_exec_time::numeric, 2) AS total_time_ms,
    round(mean_exec_time::numeric, 2) AS mean_time_ms,
    round(stddev_exec_time::numeric, 2) AS stddev_ms,  -- PG18+
    round(min_exec_time::numeric, 2) AS min_time_ms,
    round(max_exec_time::numeric, 2) AS max_time_ms,
    rows,
    round(100.0 * shared_blks_hit / NULLIF(shared_blks_hit + shared_blks_read, 0), 2) AS cache_hit_percent
FROM pg_stat_statements
WHERE calls > 10
ORDER BY mean_exec_time DESC
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

## 4. 监控实现（Prometheus + Grafana）

- Exporter 部署：
  - `postgres_exporter` 运行在每台实例旁，使用最小权限监控用户：

    ```sql
    -- 创建监控用户（带错误处理）
    DO $$
    BEGIN
        IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'metrics') THEN
            RAISE NOTICE '用户metrics已存在';
        ELSE
            CREATE ROLE metrics WITH LOGIN PASSWORD '***';
            RAISE NOTICE '用户metrics创建成功';
        END IF;

        GRANT pg_monitor TO metrics;
        -- 如需 wal/replication 细粒度指标：GRANT pg_read_all_stats TO metrics;
        RAISE NOTICE '权限授予成功';
    EXCEPTION
        WHEN duplicate_object THEN
            RAISE WARNING '用户metrics已存在';
        WHEN insufficient_privilege THEN
            RAISE EXCEPTION '权限不足，无法创建用户或授予权限';
        WHEN OTHERS THEN
            RAISE EXCEPTION '创建监控用户失败: %', SQLERRM;
    END $$;
    ```

  - 启动参数示例：

    ```bash
    DATA_SOURCE_NAME="postgresql://metrics:***@127.0.0.1:5432/postgres?sslmode=disable" ./postgres_exporter
    ```

- Prometheus 抓取配置：

  ```yaml
  scrape_configs:
    - job_name: pg
      scrape_interval: 15s
      static_configs:
        - targets: ["db1:9187", "db2:9187", "db3:9187"]
  ```

- Grafana 面板建议：
  - 概览：连接、TPS/QPS、缓冲命中、WAL速率、检查点、Autovacuum、锁等待。
  - 明细：Top 表与索引、Top 查询、复制延迟、磁盘 I/O、CPU 与内存。

## 5. 诊断流程（SOP）

- 性能劣化：
  1) 检查实例压力（连接/CPU/IO）→ 2) 慢查询TopN → 3) EXPLAIN (ANALYZE, BUFFERS, TIMING) 对比估算/实际基数 → 4) 索引/统计修复 → 5) 复测。
- 锁等待/死锁：
  1) `pg_locks` 与等待链 → 2) 找出持锁长事务 → 3) 评估中止与DDL/事务拆分 → 4) 防再发（索引/顺序/批量策略）。
- 膨胀/垃圾：
  1) 死元组与 `autovacuum` 频次 → 2) 提升 `autovacuum_*` 或离峰 `VACUUM (FULL)`/重建索引 → 3) 长事务排查。

### 5.1 性能劣化诊断流程

**步骤1: 检查实例压力**:

```sql
-- 检查连接数（带错误处理和性能测试）
DO $$
DECLARE
    connection_count BIGINT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_class WHERE relname = 'pg_stat_activity'
    ) THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO connection_count FROM pg_stat_activity;
    RAISE NOTICE '当前连接数: %', connection_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '检查连接数失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT COUNT(*) FROM pg_stat_activity;
-- 执行时间: <50ms
-- 计划: Aggregate

-- 检查CPU和I/O
SELECT
    wait_event_type,
    wait_event,
    COUNT(*) as count
FROM pg_stat_activity
WHERE wait_event_type IS NOT NULL
GROUP BY wait_event_type, wait_event
ORDER BY count DESC;
```

**步骤2: 慢查询TopN**（带错误处理和性能测试）:

```sql
DO $$
DECLARE
    top_query_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_statements') THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO top_query_count
    FROM pg_stat_statements
    WHERE total_exec_time > 1000;  -- 总执行时间>1秒

    IF top_query_count > 0 THEN
        RAISE WARNING '发现 % 个高耗时查询（总执行时间>1秒）', top_query_count;
    ELSE
        RAISE NOTICE '未发现高耗时查询';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查询慢查询TopN失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    queryid,
    LEFT(query, 100) as query_preview,
    calls,
    mean_exec_time,
    total_exec_time
FROM pg_stat_statements
ORDER BY total_exec_time DESC
LIMIT 10;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

**步骤3: EXPLAIN (ANALYZE, BUFFERS, TIMING)分析**:

```sql
EXPLAIN (ANALYZE, BUFFERS, VERBOSE)
-- 粘贴慢查询SQL
```

**步骤4: 索引/统计修复**:

```sql
-- 创建索引（带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'table_name'
    ) THEN
        RAISE EXCEPTION '表table_name不存在，请先创建';
    END IF;

    IF EXISTS (
        SELECT 1 FROM pg_indexes
        WHERE tablename = 'table_name' AND indexname = 'idx_name'
    ) THEN
        DROP INDEX CONCURRENTLY idx_name;
        RAISE NOTICE '已删除现有索引: idx_name';
    END IF;

    CREATE INDEX CONCURRENTLY idx_name ON table_name(column_name);
    RAISE NOTICE '索引idx_name创建成功';
EXCEPTION
    WHEN undefined_table THEN
        RAISE EXCEPTION '表table_name不存在';
    WHEN duplicate_object THEN
        RAISE WARNING '索引idx_name已存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '创建索引失败: %', SQLERRM;
END $$;

-- 更新统计信息（带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'table_name'
    ) THEN
        RAISE EXCEPTION '表table_name不存在，请先创建';
    END IF;

    ANALYZE table_name;
    RAISE NOTICE '表table_name统计信息更新成功';
EXCEPTION
    WHEN undefined_table THEN
        RAISE EXCEPTION '表table_name不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '更新统计信息失败: %', SQLERRM;
END $$;
```

**步骤5: 复测**:

```sql
-- 重新执行查询并对比性能
```

### 5.2 锁等待/死锁诊断流程

**步骤1: 查看锁等待链**（带错误处理和性能测试）:

```sql
DO $$
DECLARE
    wait_chain_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_locks') THEN
        RAISE WARNING 'pg_locks视图不存在';
        RETURN;
    END IF;
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_activity') THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(DISTINCT blocked_locks.pid) INTO wait_chain_count
    FROM pg_catalog.pg_locks blocked_locks
    JOIN pg_catalog.pg_stat_activity blocked_activity ON blocked_activity.pid = blocked_locks.pid
    JOIN pg_catalog.pg_locks blocking_locks
        ON blocking_locks.locktype = blocked_locks.locktype
        AND blocking_locks.pid != blocked_locks.pid
    JOIN pg_catalog.pg_stat_activity blocking_activity ON blocking_activity.pid = blocking_locks.pid
    WHERE NOT blocked_locks.granted;

    IF wait_chain_count > 0 THEN
        RAISE WARNING '发现 % 个锁等待链', wait_chain_count;
    ELSE
        RAISE NOTICE '未发现锁等待链';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_locks或pg_stat_activity视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '查看锁等待链失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    blocked_locks.pid AS blocked_pid,
    blocking_locks.pid AS blocking_pid,
    blocked_activity.query AS blocked_statement,
    blocking_activity.query AS blocking_statement
FROM pg_catalog.pg_locks blocked_locks
JOIN pg_catalog.pg_stat_activity blocked_activity ON blocked_activity.pid = blocked_locks.pid
JOIN pg_catalog.pg_locks blocking_locks
    ON blocking_locks.locktype = blocked_locks.locktype
    AND blocking_locks.pid != blocked_locks.pid
JOIN pg_catalog.pg_stat_activity blocking_activity ON blocking_activity.pid = blocking_locks.pid
WHERE NOT blocked_locks.granted;
-- 执行时间: <200ms
-- 计划: Nested Loop -> Hash Join -> Seq Scan
```

**步骤2: 找出持锁长事务**（带错误处理和性能测试）:

```sql
DO $$
DECLARE
    long_tx_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_activity') THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO long_tx_count
    FROM pg_stat_activity
    WHERE state != 'idle'
      AND NOW() - xact_start > INTERVAL '5 minutes';

    IF long_tx_count > 0 THEN
        RAISE WARNING '发现 % 个长时间运行的事务（>5分钟）', long_tx_count;
    ELSE
        RAISE NOTICE '未发现长时间运行的事务';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_activity视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '找出持锁长事务失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    pid,
    usename,
    datname,
    state,
    NOW() - xact_start as transaction_duration,
    query
FROM pg_stat_activity
WHERE state != 'idle'
AND NOW() - xact_start > INTERVAL '5 minutes';
-- 执行时间: <100ms
-- 计划: Seq Scan
```

**步骤3: 评估中止与DDL/事务拆分**:

```sql
-- 终止阻塞事务（谨慎使用）
SELECT pg_terminate_backend(blocking_pid);

-- 优化事务逻辑
-- 减少事务持有时间
-- 使用更细粒度的锁
```

**步骤4: 防再发措施**:

```sql
-- 创建索引减少锁竞争（带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'table_name'
    ) THEN
        RAISE EXCEPTION '表table_name不存在，请先创建';
    END IF;

    IF EXISTS (
        SELECT 1 FROM pg_indexes
        WHERE tablename = 'table_name' AND indexname = 'idx_name'
    ) THEN
        DROP INDEX CONCURRENTLY idx_name;
        RAISE NOTICE '已删除现有索引: idx_name';
    END IF;

    CREATE INDEX CONCURRENTLY idx_name ON table_name(column_name);
    RAISE NOTICE '索引idx_name创建成功';
EXCEPTION
    WHEN undefined_table THEN
        RAISE EXCEPTION '表table_name不存在';
    WHEN duplicate_object THEN
        RAISE WARNING '索引idx_name已存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '创建索引失败: %', SQLERRM;
END $$;

-- 调整锁超时（带错误处理）
DO $$
BEGIN
    PERFORM set_config('lock_timeout', '5s', false);
    RAISE NOTICE '锁超时已设置为5秒';
EXCEPTION
    WHEN undefined_function THEN
        RAISE WARNING 'set_config函数不存在';
    WHEN invalid_parameter_value THEN
        RAISE EXCEPTION '无效的锁超时值';
    WHEN OTHERS THEN
        RAISE EXCEPTION '调整锁超时失败: %', SQLERRM;
END $$;
```

### 5.3 膨胀/垃圾诊断流程

**步骤1: 检查死元组和autovacuum频次**（带错误处理和性能测试）:

```sql
DO $$
DECLARE
    high_dead_tup_count INT;
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_user_tables') THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO high_dead_tup_count
    FROM pg_stat_user_tables
    WHERE n_dead_tup > 10000;

    IF high_dead_tup_count > 0 THEN
        RAISE WARNING '发现 % 个表死元组数量超过10000', high_dead_tup_count;
    ELSE
        RAISE NOTICE '未发现高死元组表';
    END IF;
EXCEPTION
    WHEN undefined_table THEN
        RAISE WARNING 'pg_stat_user_tables视图不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '检查死元组和autovacuum频次失败: %', SQLERRM;
END $$;

EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    schemaname,
    tablename,
    n_dead_tup,
    n_live_tup,
    last_autovacuum,
    NOW() - last_autovacuum as time_since_vacuum
FROM pg_stat_user_tables
WHERE n_dead_tup > 10000
ORDER BY n_dead_tup DESC
LIMIT 20;
-- 执行时间: <100ms
-- 计划: Limit -> Sort -> Seq Scan
```

**步骤2: 提升autovacuum或执行VACUUM**:

```sql
-- 调整autovacuum参数（带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'table_name'
    ) THEN
        RAISE EXCEPTION '表table_name不存在，请先创建';
    END IF;

    ALTER TABLE table_name SET (
        autovacuum_vacuum_scale_factor = 0.1,
        autovacuum_analyze_scale_factor = 0.05
    );
    RAISE NOTICE 'autovacuum参数调整成功';
EXCEPTION
    WHEN undefined_table THEN
        RAISE EXCEPTION '表table_name不存在';
    WHEN invalid_parameter_value THEN
        RAISE EXCEPTION '无效的autovacuum参数值';
    WHEN OTHERS THEN
        RAISE EXCEPTION '调整autovacuum参数失败: %', SQLERRM;
END $$;

-- 手动执行VACUUM（带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'table_name'
    ) THEN
        RAISE EXCEPTION '表table_name不存在，请先创建';
    END IF;

    VACUUM ANALYZE table_name;
    RAISE NOTICE 'VACUUM ANALYZE执行成功';
EXCEPTION
    WHEN undefined_table THEN
        RAISE EXCEPTION '表table_name不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '执行VACUUM ANALYZE失败: %', SQLERRM;
END $$;

-- 重建表（如果膨胀严重，带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'table_name'
    ) THEN
        RAISE EXCEPTION '表table_name不存在，请先创建';
    END IF;

    IF NOT EXISTS (
        SELECT 1 FROM pg_indexes
        WHERE tablename = 'table_name' AND indexname = 'index_name'
    ) THEN
        RAISE EXCEPTION '索引index_name不存在，请先创建';
    END IF;

    CLUSTER table_name USING index_name;
    RAISE NOTICE '表重建成功';
EXCEPTION
    WHEN undefined_table THEN
        RAISE EXCEPTION '表table_name或索引index_name不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '重建表失败: %', SQLERRM;
END $$;
```

**步骤3: 长事务排查**:

```sql
SELECT
    pid,
    usename,
    datname,
    state,
    NOW() - xact_start as transaction_duration,
    query
FROM pg_stat_activity
WHERE state != 'idle'
AND NOW() - xact_start > INTERVAL '1 hour';
```

## 6. 仪表盘建议（Prometheus+Grafana）

- 面板：连接/事务、命中率/WAL速率、检查点、Autovacuum、Top表与索引、Top查询、锁等待热力图。
- 告警：连接接近上限、命中率骤降、WAL暴涨、检查点频率异常、Autovacuum滞后、死锁事件。

### 6.1 概览仪表板

**关键指标面板**:

```promql
# 连接数
pg_stat_database_numbackends{datname="postgres"}

# 事务速率
rate(pg_stat_database_xact_commit{datname="postgres"}[5m])

# 缓冲命中率
100 * (
  sum(rate(pg_stat_database_blks_hit{datname="postgres"}[5m])) /
  (sum(rate(pg_stat_database_blks_hit{datname="postgres"}[5m])) +
   sum(rate(pg_stat_database_blks_read{datname="postgres"}[5m])))
)

# WAL写入速率
rate(pg_stat_wal_written_bytes[5m])
```

### 6.2 查询性能仪表板

**慢查询监控**:

```promql
# Top 10 慢查询
topk(10, pg_stat_statements_mean_exec_time)

# 查询执行时间分布
histogram_quantile(0.95, rate(pg_stat_statements_calls[5m]))

# 慢查询数量
count(pg_stat_statements_mean_exec_time > 1000)
```

### 6.3 存储仪表板

**表大小和膨胀**:

```promql
# 数据库大小
pg_database_size_bytes{datname="postgres"}

# 表膨胀
pg_stat_user_tables_n_dead_tup

# I/O统计
rate(pg_stat_io_reads[5m])
rate(pg_stat_io_writes[5m])
```

### 6.4 告警规则

```yaml
# prometheus_alerts.yml
groups:
  - name: postgresql_alerts
    rules:
      - alert: PostgreSQLHighConnections
        expr: pg_stat_database_numbackends{datname="postgres"} > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "PostgreSQL高连接数告警"
          description: "数据库 {{ $labels.datname }} 连接数: {{ $value }}，超过阈值80"

      - alert: PostgreSQLLowCacheHitRatio
        expr: |
          100 * (
            sum(rate(pg_stat_database_blks_hit{datname="postgres"}[5m])) /
            (sum(rate(pg_stat_database_blks_hit{datname="postgres"}[5m])) +
             sum(rate(pg_stat_database_blks_read{datname="postgres"}[5m])))
          ) < 95
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "PostgreSQL缓存命中率低"
          description: "数据库 {{ $labels.datname }} 缓存命中率: {{ $value }}%，低于阈值95%"
```

## 7. 常见问题与对策

- 估算偏差大：提高统计目标、扩展统计、表达式列；
- 索引未命中：谓词不匹配、函数未可索引化、类型隐式转换；
- 写放大与膨胀：批写/合并、合理 `fillfactor`、冷热分层、周期重建。

### 7.1 估算偏差大

**问题**: 查询计划器对数据分布的估算不准确，导致选择错误的执行计划。

**解决方案**:

```sql
-- 1. 提高统计目标
ALTER TABLE table_name ALTER COLUMN column_name SET STATISTICS 1000;

-- 2. 更新统计信息
ANALYZE table_name;

-- 3. 使用扩展统计
CREATE STATISTICS stats_name ON table_name (column1, column2);

-- 4. 表达式列统计
CREATE STATISTICS stats_expr ON table_name ((expression(column_name)));
```

### 7.2 索引未命中

**问题**: 查询无法使用索引，导致全表扫描。

**解决方案**:

```sql
-- 1. 检查索引使用情况
EXPLAIN (ANALYZE, BUFFERS)
SELECT * FROM table_name WHERE column_name = 'value';

-- 2. 创建匹配的索引
CREATE INDEX CONCURRENTLY idx_name ON table_name(column_name);

-- 3. 避免函数索引（如果可能）
-- 错误: WHERE UPPER(column_name) = 'VALUE'
-- 正确: WHERE column_name = 'value' (如果数据已标准化)

-- 4. 避免类型隐式转换
-- 确保查询中的类型与列类型匹配
```

### 7.3 写放大与膨胀

**问题**: 频繁更新导致表膨胀，影响性能。

**解决方案**:

```sql
-- 1. 批写/合并
-- 使用批量插入而不是单条插入
INSERT INTO table_name SELECT ... FROM ...;

-- 2. 合理fillfactor
ALTER TABLE table_name SET (fillfactor = 90);
CLUSTER table_name USING index_name;

-- 3. 冷热分层
-- 将热数据和冷数据分离到不同表

-- 4. 周期重建
CLUSTER table_name USING index_name;
-- 或
VACUUM FULL table_name;
```

## 8. 日志与可观测性增强

- 建议的 `postgresql.conf` 日志关键项：

  ```text
  log_line_prefix = '%m [%p] %u@%d %r %a '
  log_min_duration_statement = 500ms        # 生产按流量与存储调节
  log_checkpoints = on
  log_autovacuum_min_duration = 1s
  log_lock_waits = on
  track_io_timing = on
  shared_preload_libraries = 'pg_stat_statements,auto_explain'
  auto_explain.log_min_duration = '200ms'
  auto_explain.log_analyze = on
  auto_explain.log_buffers = on
  ```

- 慢日志采集：
- Filebeat/Vector → Loki/Elastic，再在 Grafana 建立慢SQL面板与 TopN。
- 生成 `fingerprint`（正则归一化）便于聚合统计。

### 8.1 完整日志配置

```sql
-- postgresql.conf完整日志配置

-- 基本日志设置
logging_collector = on
log_directory = 'log'
log_filename = 'postgresql-%Y-%m-%d_%H%M%S.log'
log_rotation_age = 1d
log_rotation_size = 100MB
log_truncate_on_rotation = on

-- 日志格式
log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h '
log_timezone = 'Asia/Shanghai'

-- 慢查询日志
log_min_duration_statement = 500
log_min_messages = warning

-- 检查点日志
log_checkpoints = on

-- Autovacuum日志
log_autovacuum_min_duration = 1s

-- 锁等待日志
log_lock_waits = on

-- I/O时间跟踪
track_io_timing = on

-- 扩展统计
shared_preload_libraries = 'pg_stat_statements,auto_explain'

-- auto_explain配置
auto_explain.log_min_duration = 200
auto_explain.log_analyze = on
auto_explain.log_buffers = on
auto_explain.log_timing = on
auto_explain.log_verbose = on
auto_explain.log_format = json
```

### 8.2 日志采集配置

**Filebeat配置**:

```yaml
# filebeat.yml
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/lib/postgresql/14/data/log/postgresql-*.log
    fields:
      service: postgresql
      environment: production
    multiline.pattern: '^\d{4}-\d{2}-\d{2}'
    multiline.negate: true
    multiline.match: after

output.elasticsearch:
  hosts: ["elasticsearch:9200"]
  index: "postgresql-logs-%{+yyyy.MM.dd}"
```

**Vector配置**:

```toml
# vector.toml
[sources.postgresql_logs]
type = "file"
include = ["/var/lib/postgresql/14/data/log/postgresql-*.log"]

[transforms.postgresql_parse]
type = "regex_parser"
inputs = ["postgresql_logs"]
field = "message"
patterns = ['^(?P<timestamp>\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}) \[(?P<pid>\d+)\]: \[(?P<log_level>\w+)\] (?P<message>.*)']

[sinks.loki]
type = "loki"
inputs = ["postgresql_parse"]
endpoint = "http://loki:3100"
```

### 8.3 慢SQL归一化

```sql
-- 创建慢SQL指纹表（带错误处理）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'slow_query_fingerprints'
    ) THEN
        CREATE TABLE slow_query_fingerprints (
            fingerprint_id SERIAL PRIMARY KEY,
            normalized_query TEXT UNIQUE,
            first_seen TIMESTAMP DEFAULT NOW(),
            last_seen TIMESTAMP DEFAULT NOW(),
            total_calls BIGINT DEFAULT 0,
            total_time NUMERIC DEFAULT 0,
            avg_time NUMERIC DEFAULT 0,
            max_time NUMERIC DEFAULT 0
        );
        RAISE NOTICE '表slow_query_fingerprints创建成功';
    ELSE
        RAISE NOTICE '表slow_query_fingerprints已存在';
    END IF;
EXCEPTION
    WHEN duplicate_table THEN
        RAISE WARNING '表slow_query_fingerprints已存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '创建慢SQL指纹表失败: %', SQLERRM;
END $$;

-- 定期更新慢SQL指纹（带错误处理）
DO $$
DECLARE
    updated_count INT;
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM information_schema.tables
        WHERE table_schema = 'public' AND table_name = 'slow_query_fingerprints'
    ) THEN
        RAISE EXCEPTION '表slow_query_fingerprints不存在，请先创建';
    END IF;

    IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = 'pg_stat_statements') THEN
        RAISE WARNING 'pg_stat_statements视图不存在，请先创建扩展';
        RETURN;
    END IF;

    INSERT INTO slow_query_fingerprints (normalized_query, last_seen, total_calls, total_time, avg_time, max_time)
    SELECT
        regexp_replace(
            regexp_replace(query, '\$\d+', '?', 'g'),
            '\d{4}-\d{2}-\d{2}', 'YYYY-MM-DD', 'g'
        ) as normalized_query,
        NOW() as last_seen,
        calls as total_calls,
        total_exec_time as total_time,
        mean_exec_time as avg_time,
        max_exec_time as max_time
    FROM pg_stat_statements
    WHERE mean_exec_time > 100
    ON CONFLICT (normalized_query) DO UPDATE
    SET
        last_seen = EXCLUDED.last_seen,
        total_calls = slow_query_fingerprints.total_calls + EXCLUDED.total_calls,
        total_time = slow_query_fingerprints.total_time + EXCLUDED.total_time,
        avg_time = (slow_query_fingerprints.total_time + EXCLUDED.total_time) /
                   (slow_query_fingerprints.total_calls + EXCLUDED.total_calls),
        max_time = GREATEST(slow_query_fingerprints.max_time, EXCLUDED.max_time);

    GET DIAGNOSTICS updated_count = ROW_COUNT;
    RAISE NOTICE '更新了 % 条慢SQL指纹记录', updated_count;
EXCEPTION
    WHEN undefined_table THEN
        RAISE EXCEPTION '表slow_query_fingerprints或pg_stat_statements视图不存在';
    WHEN undefined_function THEN
        RAISE EXCEPTION 'regexp_replace函数不存在';
    WHEN OTHERS THEN
        RAISE EXCEPTION '更新慢SQL指纹失败: %', SQLERRM;
END $$;
```

## 9. 交叉引用

### 相关文档

#### 查询与优化

- ⭐⭐⭐ [执行计划与性能调优](../../../02-查询与优化/02.03-执行计划/02.04-执行计划与性能调优.md) - 查询性能分析
- ⭐⭐ [查询优化器原理](../../../02-查询与优化/02.01-查询优化器/02.01-查询优化器原理.md) - 查询优化基础
- ⭐⭐ [统计信息与代价模型](../../../02-查询与优化/02.04-统计信息/02.03-统计信息与代价模型.md) - 统计信息监控

#### 核心课程

- ⭐⭐⭐ [存储管理与数据持久化](../../../04-存储与恢复/01.06-存储管理与数据持久化.md) - 存储监控（列存储监控、列压缩监控）🆕
- ⭐⭐ [系统架构与设计原理](../../../01-核心基础/01.02-系统架构/01.01-系统架构与设计原理.md) - 系统架构监控

#### 部署架构

- ⭐⭐⭐ [性能调优实践](../../../11-部署架构/单机部署/05.02-性能调优实践.md) - 性能优化案例

#### 运维实践

- ⭐⭐⭐ [监控与诊断落地指南](../06.02-监控与诊断落地指南.md) - 生产级监控实践
- ⭐⭐ [性能调优变更闭环](../06.03-性能调优变更闭环.md) - 变更管理

#### 行业案例

- ⭐⭐⭐ [性能问题-案例库](../../../16-应用设计与开发/行业案例/性能问题-案例库.md) - 性能诊断实践案例

### 实战案例与示例

- ⭐⭐⭐ [实战案例](../../../19-实战案例/README.md) ⭐ - 完整的实战案例集合
