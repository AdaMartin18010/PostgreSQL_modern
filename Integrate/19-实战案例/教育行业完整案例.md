# PostgreSQL教育行业完整案例

> **PostgreSQL版本**: 18.x
> **行业**: 教育
> **特点**: 个性化学习、推荐系统、学习分析
> **参考案例**: [教育场景/智能学习推荐系统](./教育场景/智能学习推荐系统.md)

---

## 📋 目录

- [PostgreSQL教育行业完整案例](#postgresql教育行业完整案例)
  - [📋 目录](#-目录)
  - [1. 概述](#1-概述)
    - [1.1 教育行业特点](#11-教育行业特点)
    - [1.2 核心场景](#12-核心场景)
  - [2. 教育行业核心场景](#2-教育行业核心场景)
    - [2.1 在线学习平台](#21-在线学习平台)
    - [2.2 智能学习推荐](#22-智能学习推荐)
    - [2.3 学习行为分析](#23-学习行为分析)
  - [3. 技术架构](#3-技术架构)
    - [3.1 推荐系统架构](#31-推荐系统架构)
    - [3.2 数据流](#32-数据流)
  - [4. 数据库设计](#4-数据库设计)
    - [4.1 课程表设计](#41-课程表设计)
    - [4.2 学习记录表设计](#42-学习记录表设计)
  - [5. 性能优化](#5-性能优化)
    - [5.1 推荐优化](#51-推荐优化)
    - [5.2 查询优化](#52-查询优化)
  - [6. 最佳实践](#6-最佳实践)
    - [6.1 推荐系统](#61-推荐系统)
    - [6.2 学习分析](#62-学习分析)
    - [6.3 性能优化](#63-性能优化)
  - [📚 相关文档](#-相关文档)

---

## 1. 概述

### 1.1 教育行业特点

教育行业对数据库的要求：

- ✅ **个性化学习**: 个性化推荐
- ✅ **学习分析**: 学习行为分析
- ✅ **内容管理**: 课程内容管理
- ✅ **推荐系统**: 智能推荐
- ✅ **数据分析**: 学习数据分析

### 1.2 核心场景

- **在线学习平台**: 课程学习
- **学习推荐**: 个性化推荐
- **学习分析**: 学习行为分析
- **成绩管理**: 成绩统计
- **内容管理**: 课程内容管理

---

## 2. 教育行业核心场景

### 2.1 在线学习平台

**核心需求**:

- 课程管理
- 学习进度跟踪
- 作业管理
- 成绩统计

**技术方案**:

```sql
-- 课程表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'courses') THEN
            CREATE TABLE courses (
                course_id BIGSERIAL PRIMARY KEY,
                title TEXT NOT NULL,
                description TEXT,
                instructor_id BIGINT,
                category_id INT,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 courses 创建成功';
        ELSE
            RAISE NOTICE '表 courses 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 courses 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 学生表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'students') THEN
            CREATE TABLE students (
                student_id BIGSERIAL PRIMARY KEY,
                username TEXT UNIQUE NOT NULL,
                email TEXT,
                level INT DEFAULT 1,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 students 创建成功';
        ELSE
            RAISE NOTICE '表 students 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 students 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 学习进度表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_progress') THEN
            CREATE TABLE learning_progress (
                progress_id BIGSERIAL PRIMARY KEY,
                student_id BIGINT NOT NULL REFERENCES students(student_id),
                course_id BIGINT NOT NULL REFERENCES courses(course_id),
                lesson_id BIGINT,
                progress_percent NUMERIC(5,2) DEFAULT 0,
                completed_at TIMESTAMPTZ,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                UNIQUE(student_id, course_id, lesson_id)
            );
            RAISE NOTICE '表 learning_progress 创建成功';
        ELSE
            RAISE NOTICE '表 learning_progress 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 learning_progress 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_progress') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_progress_student') THEN
                CREATE INDEX idx_progress_student ON learning_progress(student_id);
            END IF;
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_progress_course') THEN
                CREATE INDEX idx_progress_course ON learning_progress(course_id);
            END IF;
            RAISE NOTICE '索引创建完成';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 2.2 智能学习推荐

**参考**: [教育场景/智能学习推荐系统](./教育场景/智能学习推荐系统.md)

**核心需求**:

- 个性化推荐
- 向量搜索
- 协同过滤
- 内容推荐

**技术方案**:

```sql
-- 学生向量表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'student_vectors') THEN
            CREATE TABLE student_vectors (
                student_id BIGINT PRIMARY KEY REFERENCES students(student_id),
                preference_vector vector(128),
                learning_style TEXT,
                updated_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 student_vectors 创建成功';
        ELSE
            RAISE NOTICE '表 student_vectors 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 student_vectors 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 课程向量表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'course_vectors') THEN
            CREATE TABLE course_vectors (
                course_id BIGINT PRIMARY KEY REFERENCES courses(course_id),
                embedding vector(128),
                difficulty_level INT,
                updated_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 course_vectors 创建成功';
        ELSE
            RAISE NOTICE '表 course_vectors 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 course_vectors 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 向量索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'student_vectors') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_student_vectors') THEN
                CREATE INDEX idx_student_vectors ON student_vectors USING hnsw (preference_vector vector_l2_ops);
            END IF;
        END IF;

        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'course_vectors') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_course_vectors') THEN
                CREATE INDEX idx_course_vectors ON course_vectors USING hnsw (embedding vector_l2_ops);
            END IF;
        END IF;
        RAISE NOTICE '向量索引创建完成';
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 推荐查询（带性能测试）
EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    c.course_id,
    c.title,
    1 - (s.preference_vector <=> c.embedding) as similarity
FROM student_vectors s
CROSS JOIN course_vectors c
WHERE s.student_id = 12345
AND c.difficulty_level <= (
    SELECT level FROM students WHERE student_id = 12345
) + 1
ORDER BY similarity DESC
LIMIT 10;
```

### 2.3 学习行为分析

**核心需求**:

- 行为数据采集
- 实时分析
- 学习路径分析
- 效果评估

**技术方案**:

```sql
-- 学习行为表（分区）（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_events') THEN
            CREATE TABLE learning_events (
                event_id BIGSERIAL,
                student_id BIGINT NOT NULL,
                event_type TEXT NOT NULL,
                course_id BIGINT,
                lesson_id BIGINT,
                event_data JSONB,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                PRIMARY KEY (event_id, created_at)
            ) PARTITION BY RANGE (created_at);
            RAISE NOTICE '表 learning_events 创建成功';
        ELSE
            RAISE NOTICE '表 learning_events 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 learning_events 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 按月分区（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_events') THEN
            IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_events_2025_01') THEN
                CREATE TABLE learning_events_2025_01 PARTITION OF learning_events
                FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');
                RAISE NOTICE '分区 learning_events_2025_01 创建成功';
            ELSE
                RAISE NOTICE '分区 learning_events_2025_01 已存在';
            END IF;
        ELSE
            RAISE NOTICE '表 learning_events 不存在，跳过分区创建';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '分区已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建分区失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 学习分析查询（带性能测试）
EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT
    student_id,
    course_id,
    COUNT(*) as event_count,
    SUM((event_data->>'duration')::NUMERIC) as total_duration,
    AVG((event_data->>'score')::NUMERIC) as avg_score
FROM learning_events
WHERE created_at >= NOW() - INTERVAL '30 days'
GROUP BY student_id, course_id
ORDER BY total_duration DESC
LIMIT 100;
```

---

## 3. 技术架构

### 3.1 推荐系统架构

```text
学习平台
    ↓
推荐服务
    ↓
PostgreSQL集群
├── 主库 (读写 - 学习数据)
├── 从库1 (只读 - 推荐计算)
└── 从库2 (只读 - 分析)
    ↓
向量数据库 (pgvector)
```

### 3.2 数据流

```text
学习行为
    ↓
事件采集
    ↓
实时处理
    ↓
向量更新
    ↓
推荐计算
```

---

## 4. 数据库设计

### 4.1 课程表设计

```sql
-- 课程表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'courses') THEN
            CREATE TABLE courses (
                course_id BIGSERIAL PRIMARY KEY,
                title TEXT NOT NULL,
                description TEXT,
                instructor_id BIGINT,
                category_id INT,
                difficulty_level INT,
                duration_minutes INT,
                embedding vector(128),
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 courses 创建成功';
        ELSE
            RAISE NOTICE '表 courses 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 courses 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'courses') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_courses_category') THEN
                CREATE INDEX idx_courses_category ON courses(category_id);
            END IF;
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_courses_embedding') THEN
                CREATE INDEX idx_courses_embedding ON courses USING hnsw (embedding vector_l2_ops);
            END IF;
            RAISE NOTICE '索引创建完成';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 4.2 学习记录表设计

```sql
-- 学习记录表（带错误处理）
DO $$
BEGIN
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_records') THEN
            CREATE TABLE learning_records (
                record_id BIGSERIAL PRIMARY KEY,
                student_id BIGINT NOT NULL REFERENCES students(student_id),
                course_id BIGINT NOT NULL REFERENCES courses(course_id),
                lesson_id BIGINT,
                start_time TIMESTAMPTZ,
                end_time TIMESTAMPTZ,
                duration_minutes INT,
                score NUMERIC(5,2),
                completed BOOLEAN DEFAULT false,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
            RAISE NOTICE '表 learning_records 创建成功';
        ELSE
            RAISE NOTICE '表 learning_records 已存在';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '表 learning_records 已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建表失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_records') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_records_student') THEN
                CREATE INDEX idx_records_student ON learning_records(student_id, created_at DESC);
            END IF;
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_records_course') THEN
                CREATE INDEX idx_records_course ON learning_records(course_id);
            END IF;
            RAISE NOTICE '索引创建完成';
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

---

## 5. 性能优化

### 5.1 推荐优化

```sql
-- 使用物化视图预计算（带错误处理）
DO $$
BEGIN
    BEGIN
        DROP MATERIALIZED VIEW IF EXISTS mv_student_course_similarity;
        CREATE MATERIALIZED VIEW mv_student_course_similarity AS
        SELECT
            s.student_id,
            c.course_id,
            1 - (s.preference_vector <=> c.embedding) as similarity
        FROM student_vectors s
        CROSS JOIN course_vectors c;
        RAISE NOTICE '物化视图 mv_student_course_similarity 创建成功';
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '创建物化视图失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 创建唯一索引（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM pg_matviews WHERE matviewname = 'mv_student_course_similarity') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'mv_student_course_similarity_student_id_course_id_idx') THEN
                CREATE UNIQUE INDEX ON mv_student_course_similarity(student_id, course_id);
                RAISE NOTICE '唯一索引创建成功';
            ELSE
                RAISE NOTICE '唯一索引已存在';
            END IF;
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 定期刷新（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM pg_matviews WHERE matviewname = 'mv_student_course_similarity') THEN
            REFRESH MATERIALIZED VIEW CONCURRENTLY mv_student_course_similarity;
            RAISE NOTICE '物化视图刷新成功';
        ELSE
            RAISE NOTICE '物化视图不存在，跳过刷新';
        END IF;
    EXCEPTION
        WHEN OTHERS THEN
            RAISE WARNING '刷新物化视图失败: %', SQLERRM;
            RAISE;
    END;
END $$;
```

### 5.2 查询优化

```sql
-- 优化学习进度查询（带错误处理）
DO $$
BEGIN
    BEGIN
        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_progress') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_progress_student_course') THEN
                CREATE INDEX idx_progress_student_course ON learning_progress(student_id, course_id);
                RAISE NOTICE '索引 idx_progress_student_course 创建成功';
            ELSE
                RAISE NOTICE '索引 idx_progress_student_course 已存在';
            END IF;
        END IF;

        IF EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'learning_events') THEN
            IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_events_student_time') THEN
                CREATE INDEX idx_events_student_time ON learning_events(student_id, created_at DESC);
                RAISE NOTICE '索引 idx_events_student_time 创建成功';
            ELSE
                RAISE NOTICE '索引 idx_events_student_time 已存在';
            END IF;
        END IF;
    EXCEPTION
        WHEN duplicate_table THEN
            RAISE NOTICE '索引已存在';
        WHEN OTHERS THEN
            RAISE WARNING '创建索引失败: %', SQLERRM;
            RAISE;
    END;
END $$;

-- 性能测试
EXPLAIN (ANALYZE, BUFFERS, TIMING)
SELECT * FROM learning_progress WHERE student_id = 1 AND course_id = 1 LIMIT 10;
```

---

## 6. 最佳实践

### 6.1 推荐系统

- ✅ **向量搜索**: 使用pgvector进行相似度计算
- ✅ **协同过滤**: 结合用户行为数据
- ✅ **内容推荐**: 基于课程内容推荐
- ✅ **实时更新**: 实时更新用户向量

### 6.2 学习分析

- ✅ **数据采集**: 完整采集学习行为
- ✅ **实时分析**: 实时分析学习效果
- ✅ **路径分析**: 分析学习路径
- ✅ **效果评估**: 评估学习效果

### 6.3 性能优化

- ✅ **索引优化**: 创建合适索引
- ✅ **物化视图**: 预聚合数据
- ✅ **分区表**: 按时间分区
- ✅ **缓存策略**: 缓存热点数据

---

## 📚 相关文档

- [教育场景/智能学习推荐系统](./教育场景/智能学习推荐系统.md) - 学习推荐系统案例
- [07-实时推荐系统](./07-实时推荐系统/README.md) - 推荐系统
- [10-AI与机器学习](../10-AI与机器学习/README.md) - AI与机器学习

---

**最后更新**: 2025年1月
**状态**: ✅ 完成
