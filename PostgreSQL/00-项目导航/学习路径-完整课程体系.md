# PostgreSQL 完整课程体系

> **文档版本**: v1.0
> **最后更新**: 2025-11-12
> **版本覆盖**: PostgreSQL 18.x (推荐) ⭐ | 17.x (推荐) | 16.x (兼容)
> **文档状态**: ✅ 已创建
> **对标标准**: MIT 6.830、Stanford CS145、Berkeley CS186

---

## 📋 目录

- [PostgreSQL 完整课程体系](#postgresql-完整课程体系)
  - [📋 目录](#-目录)
  - [1. 课程体系概述](#1-课程体系概述)
  - [2. 基础课程（6周）](#2-基础课程6周)
    - [2.1 Week 1: PostgreSQL基础](#21-week-1-postgresql基础)
    - [2.2 Week 2: SQL语言](#22-week-2-sql语言)
    - [2.3 Week 3: 数据模型与设计](#23-week-3-数据模型与设计)
    - [2.4 Week 4: 事务与并发](#24-week-4-事务与并发)
    - [2.5 Week 5: 索引与查询优化](#25-week-5-索引与查询优化)
    - [2.6 Week 6: 存储与备份](#26-week-6-存储与备份)
  - [3. 进阶课程（6周）](#3-进阶课程6周)
    - [3.1 Week 7: 高级SQL特性](#31-week-7-高级sql特性)
    - [3.2 Week 8: 扩展系统](#32-week-8-扩展系统)
    - [3.3 Week 9: 安全与访问控制](#33-week-9-安全与访问控制)
    - [3.4 Week 10: 高可用与复制](#34-week-10-高可用与复制)
    - [3.5 Week 11: 性能调优](#35-week-11-性能调优)
    - [3.6 Week 12: 监控与诊断](#36-week-12-监控与诊断)
  - [4. 高级课程（6周）](#4-高级课程6周)
    - [4.1 Week 13: 分布式架构](#41-week-13-分布式架构)
    - [4.2 Week 14: AI集成与向量数据库](#42-week-14-ai集成与向量数据库)
    - [4.3 Week 15: 流处理与CEP](#43-week-15-流处理与cep)
    - [4.4 Week 16: 云原生部署](#44-week-16-云原生部署)
    - [4.5 Week 17: 实战项目开发](#45-week-17-实战项目开发)
    - [4.6 Week 18: 项目总结与优化](#46-week-18-项目总结与优化)
  - [5. 实战项目（4周）](#5-实战项目4周)
    - [5.1 项目1: 语义搜索系统](#51-项目1-语义搜索系统)
    - [5.2 项目2: RAG知识库](#52-项目2-rag知识库)
    - [5.3 项目3: 智能推荐系统](#53-项目3-智能推荐系统)
  - [6. 课程材料](#6-课程材料)
    - [6.1 学习目标](#61-学习目标)
    - [6.2 练习题](#62-练习题)
      - [选择题](#选择题)
      - [编程题](#编程题)
      - [设计题](#设计题)
    - [6.3 实验指导](#63-实验指导)
      - [实验1: 基础SQL查询](#实验1-基础sql查询)
      - [实验2: 复杂查询和优化](#实验2-复杂查询和优化)
      - [Week 4: 事务与并发练习题](#week-4-事务与并发练习题)
      - [Week 4: 事务与并发实验指导](#week-4-事务与并发实验指导)
        - [实验1: 隔离级别测试](#实验1-隔离级别测试)
      - [Week 5: 索引与查询优化练习题](#week-5-索引与查询优化练习题)
      - [Week 5: 索引与查询优化实验指导](#week-5-索引与查询优化实验指导)
        - [实验1: 索引性能对比](#实验1-索引性能对比)
      - [Week 7: 高级SQL特性练习题](#week-7-高级sql特性练习题)
      - [Week 7: 高级SQL特性实验指导](#week-7-高级sql特性实验指导)
        - [实验1: 窗口函数应用](#实验1-窗口函数应用)
      - [Week 9: 安全与访问控制练习题](#week-9-安全与访问控制练习题)
      - [Week 9: 安全与访问控制实验指导](#week-9-安全与访问控制实验指导)
        - [实验1: 行级安全配置](#实验1-行级安全配置)
      - [Week 13: 分布式架构练习题](#week-13-分布式架构练习题)
      - [Week 13: 分布式架构实验指导](#week-13-分布式架构实验指导)
        - [实验1: Citus集群部署](#实验1-citus集群部署)
      - [Week 14: AI集成与向量数据库练习题](#week-14-ai集成与向量数据库练习题)
      - [Week 14: AI集成与向量数据库实验指导](#week-14-ai集成与向量数据库实验指导)
        - [实验1: 向量搜索实现](#实验1-向量搜索实现)
      - [Week 10: 高可用与复制练习题](#week-10-高可用与复制练习题)
      - [Week 10: 高可用与复制实验指导](#week-10-高可用与复制实验指导)
        - [实验1: 流复制配置](#实验1-流复制配置)
      - [Week 11: 性能调优练习题](#week-11-性能调优练习题)
      - [Week 11: 性能调优实验指导](#week-11-性能调优实验指导)
        - [实验1: 参数调优](#实验1-参数调优)
      - [Week 12: 监控与诊断练习题](#week-12-监控与诊断练习题)
      - [Week 12: 监控与诊断实验指导](#week-12-监控与诊断实验指导)
        - [实验1: 性能监控部署](#实验1-性能监控部署)
      - [Week 1: PostgreSQL基础练习题](#week-1-postgresql基础练习题)
      - [Week 1: PostgreSQL基础实验指导](#week-1-postgresql基础实验指导)
        - [实验1: PostgreSQL安装与配置](#实验1-postgresql安装与配置)
      - [Week 2: SQL语言练习题](#week-2-sql语言练习题)
      - [Week 2: SQL语言实验指导](#week-2-sql语言实验指导)
        - [实验1: 基础SQL查询](#实验1-基础sql查询-1)
      - [Week 3: 数据模型与设计练习题](#week-3-数据模型与设计练习题)
      - [Week 3: 数据模型与设计实验指导](#week-3-数据模型与设计实验指导)
        - [实验1: 数据库设计实践](#实验1-数据库设计实践)
      - [Week 6: 存储与备份练习题](#week-6-存储与备份练习题)
      - [Week 6: 存储与备份实验指导](#week-6-存储与备份实验指导)
        - [实验1: 备份与恢复](#实验1-备份与恢复)
      - [Week 8: 扩展系统练习题](#week-8-扩展系统练习题)
      - [Week 8: 扩展系统实验指导](#week-8-扩展系统实验指导)
        - [实验1: 扩展安装与使用](#实验1-扩展安装与使用)
      - [Week 15: 流处理与CEP练习题](#week-15-流处理与cep练习题)
      - [Week 15: 流处理与CEP实验指导](#week-15-流处理与cep实验指导)
        - [实验1: 实时事件处理](#实验1-实时事件处理)
      - [Week 16: 云原生部署练习题](#week-16-云原生部署练习题)
      - [Week 16: 云原生部署实验指导](#week-16-云原生部署实验指导)
        - [实验1: Docker容器化部署](#实验1-docker容器化部署)
      - [Week 17: 实战项目开发练习题](#week-17-实战项目开发练习题)
      - [Week 17: 实战项目开发实验指导](#week-17-实战项目开发实验指导)
        - [实验1: 端到端项目实现](#实验1-端到端项目实现)
      - [Week 18: 项目总结与优化练习题](#week-18-项目总结与优化练习题)
      - [Week 18: 项目总结与优化实验指导](#week-18-项目总结与优化实验指导)
        - [实验1: 项目优化实践](#实验1-项目优化实践)
    - [6.4 考试题库](#64-考试题库)
      - [第一部分：选择题（20分，每题2分）](#第一部分选择题20分每题2分)
      - [第二部分：填空题（20分，每空2分）](#第二部分填空题20分每空2分)
      - [第三部分：编程题（40分）](#第三部分编程题40分)
      - [第四部分：简答题（20分）](#第四部分简答题20分)
  - [7. 学习路径选择](#7-学习路径选择)
    - [路径1: 快速入门（2周）](#路径1-快速入门2周)
    - [路径2: 系统学习（12周）](#路径2-系统学习12周)
    - [路径3: 完整课程（22周）](#路径3-完整课程22周)
    - [路径4: AI专家（8周）](#路径4-ai专家8周)
  - [8. 相关文档](#8-相关文档)
  - [9. 参考文献](#9-参考文献)

---

## 1. 课程体系概述

本课程体系对标MIT 6.830、Stanford CS145、Berkeley CS186等顶级大学数据库课程，提供系统化的PostgreSQL学习路径。

**课程结构**：

- **基础课程**（6周）: PostgreSQL基础、SQL、数据模型、事务、索引、存储
- **进阶课程**（6周）: 高级SQL、扩展系统、安全、高可用、性能调优、监控
- **高级课程**（6周）: 分布式架构、AI集成、流处理、云原生、实战项目
- **实战项目**（4周）: 端到端项目实现

**总时长**: 22周（约5.5个月）

**难度等级**：

- ⭐ 基础课程：入门级
- ⭐⭐ 进阶课程：进阶级
- ⭐⭐⭐ 高级课程：高级级

---

## 2. 基础课程（6周）

### 2.1 Week 1: PostgreSQL基础

**学习目标**：

- 了解PostgreSQL历史和特性
- 掌握PostgreSQL安装和配置
- 理解PostgreSQL系统架构
- 熟悉基本操作和工具

**核心内容**：

1. PostgreSQL历史与发展
2. 系统架构与设计原理
3. 安装与配置
4. 基本操作（psql、pgAdmin）

**推荐文档**：

- [PostgreSQL历史与发展](../01-核心课程/01.00-PostgreSQL历史与发展.md)
- [系统架构与设计原理](../01-核心课程/01.01-系统架构与设计原理.md)
- [单机部署与配置](../05-部署架构/单机部署/05.01-单机部署与配置.md)

**实践任务**：

- 安装PostgreSQL 18
- 创建数据库和用户
- 使用psql执行基本操作

### 2.2 Week 2: SQL语言

**学习目标**：

- 掌握SQL基础语法
- 理解SQL标准
- 熟练使用SELECT、INSERT、UPDATE、DELETE
- 掌握JOIN、聚合、子查询

**核心内容**：

1. SQL语言规范与标准
2. 数据查询（SELECT）
3. 数据修改（INSERT、UPDATE、DELETE）
4. JOIN操作
5. 聚合函数和分组

**推荐文档**：

- [SQL语言规范与标准](../01-核心课程/01.03-SQL语言规范与标准.md)

**实践任务**：

- 创建示例数据库
- 编写复杂SQL查询
- 优化查询性能

### 2.3 Week 3: 数据模型与设计

**学习目标**：

- 理解关系数据模型
- 掌握数据库设计方法
- 学习范式理论
- 实践数据库设计

**核心内容**：

1. 关系数据模型与理论
2. 数据库设计方法
3. 范式理论
4. ER模型设计

**推荐文档**：

- [关系数据模型与理论](../01-核心课程/01.02-关系数据模型与理论.md)

**实践任务**：

- 设计一个完整的数据库
- 应用范式理论
- 创建表结构

### 2.4 Week 4: 事务与并发

**学习目标**：

- 理解ACID特性
- 掌握事务管理
- 理解MVCC机制
- 学习并发控制

**核心内容**：

1. 事务管理与ACID特性
2. 并发控制与MVCC机制
3. 隔离级别
4. 锁机制

**推荐文档**：

- [事务管理与ACID特性](../01-核心课程/01.04-事务管理与ACID特性.md)
- [并发控制与MVCC机制](../01-核心课程/01.05-并发控制与MVCC机制.md)

**实践任务**：

- 测试不同隔离级别
- 观察MVCC行为
- 处理并发冲突

### 2.5 Week 5: 索引与查询优化

**学习目标**：

- 理解索引原理
- 掌握索引类型
- 学习查询优化
- 分析执行计划

**核心内容**：

1. 索引结构与优化
2. 查询优化器原理
3. 执行计划分析
4. 统计信息与代价模型

**推荐文档**：

- [索引结构与优化](../03-查询与优化/02.02-索引结构与优化.md)
- [查询优化器原理](../03-查询与优化/02.01-查询优化器原理.md)
- [执行计划与性能调优](../03-查询与优化/02.04-执行计划与性能调优.md)

**实践任务**：

- 创建不同类型的索引
- 分析查询执行计划
- 优化慢查询

### 2.6 Week 6: 存储与备份

**学习目标**：

- 理解存储管理
- 掌握备份策略
- 学习恢复方法
- 实践备份恢复

**核心内容**：

1. 存储管理与数据持久化
2. 备份与恢复
3. WAL日志
4. 点对点恢复

**推荐文档**：

- [存储管理与数据持久化](../01-核心课程/01.06-存储管理与数据持久化.md)
- [备份与恢复](../06-运维实践/备份与恢复/06.06-备份与恢复.md)

**实践任务**：

- 执行完整备份
- 测试恢复流程
- 配置WAL归档

---

## 3. 进阶课程（6周）

### 3.1 Week 7: 高级SQL特性

**学习目标**：

- 掌握窗口函数
- 学习CTE和递归查询
- 理解JSON/JSONB操作
- 使用数组和范围类型

**核心内容**：

1. 窗口函数
2. 公共表表达式（CTE）
3. 递归查询
4. JSON/JSONB数据类型
5. 数组和范围类型

**推荐文档**：

- [SQL语言规范与标准](../01-核心课程/01.03-SQL语言规范与标准.md)

**实践任务**：

- 使用窗口函数分析数据
- 编写递归查询
- 操作JSON数据

### 3.2 Week 8: 扩展系统

**学习目标**：

- 理解PostgreSQL扩展系统
- 学习扩展开发
- 掌握常用扩展
- 实践扩展安装

**核心内容**：

1. 扩展系统架构
2. 扩展开发方法
3. 常用扩展（pgvector、PostGIS等）
4. 扩展管理

**推荐文档**：

- [扩展系统与插件开发](../04-高级特性/03.01-扩展系统与插件开发.md)

**实践任务**：

- 安装pgvector扩展
- 开发简单扩展
- 管理扩展版本

### 3.3 Week 9: 安全与访问控制

**学习目标**：

- 理解安全机制
- 掌握用户认证
- 学习权限管理
- 实践OAuth 2.0

**核心内容**：

1. 用户认证系统
2. 权限管理
3. 行级安全（RLS）
4. OAuth 2.0身份验证（PostgreSQL 18）

**推荐文档**：

- [安全机制与访问控制](../04-高级特性/03.02-安全机制与访问控制.md)

**实践任务**：

- 配置用户和角色
- 实现行级安全
- 集成OAuth 2.0

### 3.4 Week 10: 高可用与复制

**学习目标**：

- 理解高可用架构
- 掌握复制技术
- 学习故障转移
- 实践高可用部署

**核心内容**：

1. 复制架构
2. 流复制
3. 逻辑复制
4. 故障转移
5. 高可用组件（Patroni等）

**推荐文档**：

- [集群部署与高可用](../05-部署架构/集群部署/05.04-集群部署与高可用.md)

**实践任务**：

- 配置主从复制
- 测试故障转移
- 部署高可用集群

### 3.5 Week 11: 性能调优

**学习目标**：

- 理解性能调优方法
- 掌握参数优化
- 学习查询优化
- 实践性能调优

**核心内容**：

1. 性能调优实践
2. 参数优化
3. 查询优化
4. 索引优化
5. 监控与诊断

**推荐文档**：

- [性能调优实践](../05-部署架构/单机部署/05.02-性能调优实践.md)
- [监控与诊断](../06-运维实践/监控与诊断/06.01-监控与诊断.md)

**实践任务**：

- 优化数据库参数
- 调优慢查询
- 监控性能指标

### 3.6 Week 12: 监控与诊断

**学习目标**：

- 理解监控方法
- 掌握诊断工具
- 学习性能分析
- 实践监控部署

**核心内容**：

1. 监控系统
2. 诊断工具
3. 性能分析
4. 日志分析

**推荐文档**：

- [监控与诊断](../04-部署运维/04.04-监控与诊断.md)

**实践任务**：

- 部署监控系统
- 分析性能问题
- 优化系统配置

---

## 4. 高级课程（6周）

### 4.1 Week 13: 分布式架构

**学习目标**：

- 理解分布式系统理论
- 掌握Citus架构
- 学习分布式事务
- 实践分布式部署

**核心内容**：

1. 分布式系统理论基础
2. Citus分布式扩展
3. 分布式事务处理
4. 分布式架构设计

**推荐文档**：

- [分布式架构设计](../05-部署架构/分布式部署/05.08-分布式架构设计.md)
- [分布式事务处理](../04-高级特性/03.07-分布式事务处理.md)

**实践任务**：

- 部署Citus集群
- 实现分布式表
- 测试分布式事务

### 4.2 Week 14: AI集成与向量数据库

**学习目标**：

- 理解向量数据库
- 掌握pgvector使用
- 学习AI集成
- 实践RAG系统

**核心内容**：

1. 向量数据库支持
2. pgvector扩展
3. AI模型深度集成
4. RAG架构

**推荐文档**：

- [向量数据库支持](../04-高级特性/03.05-向量数据库支持.md)
- [AI模型深度集成](../07-前沿技术/05.02-AI模型深度集成.md)
- [RAG架构实战指南](../07-前沿技术/05.04-RAG架构实战指南.md)

**实践任务**：

- 实现向量搜索
- 构建RAG系统
- 优化向量检索性能

### 4.3 Week 15: 流处理与CEP

**学习目标**：

- 理解流处理概念
- 掌握CEP技术
- 学习实时数据处理
- 实践流处理系统

**核心内容**：

1. 流处理与CEP
2. 实时数据处理
3. 事件处理
4. 流查询

**推荐文档**：

- [流处理与CEP](../04-高级特性/03.03-流处理与CEP.md)

**实践任务**：

- 实现流处理系统
- 处理实时事件
- 优化流查询性能

### 4.4 Week 16: 云原生部署

**学习目标**：

- 理解云原生概念
- 掌握容器化部署
- 学习Kubernetes部署
- 实践云原生架构

**核心内容**：

1. 云原生与容器化
2. Docker部署
3. Kubernetes部署
4. Serverless架构

**推荐文档**：

- [Docker部署](../05-部署架构/容器化部署/05.12-Docker部署.md)

**实践任务**：

- 容器化PostgreSQL
- 部署到Kubernetes
- 实现自动扩缩容

### 4.5 Week 17: 实战项目开发

**学习目标**：

- 综合应用所学知识
- 开发完整项目
- 解决实际问题
- 优化项目性能

**核心内容**：

1. 项目需求分析
2. 系统设计
3. 数据库设计
4. 功能实现
5. 性能优化

**推荐项目**：

- 语义搜索系统
- RAG知识库
- 智能推荐系统

**推荐文档**：

- [语义搜索系统端到端实现](../08-实战案例/06.01-语义搜索系统端到端实现.md)
- [RAG知识库完整项目](../08-实战案例/06.02-RAG知识库完整项目.md)
- [智能推荐系统](../08-实战案例/06.03-智能推荐系统.md)

### 4.6 Week 18: 项目总结与优化

**学习目标**：

- 总结项目经验
- 优化项目性能
- 完善项目文档
- 准备项目展示

**核心内容**：

1. 项目总结
2. 性能优化
3. 文档完善
4. 项目展示

---

## 5. 实战项目（4周）

### 5.1 项目1: 语义搜索系统

**项目目标**：

构建一个完整的语义搜索系统，支持文档上传、向量化、相似度搜索。

**技术栈**：

- PostgreSQL 18 + pgvector 2.0
- FastAPI
- React
- Docker

**学习重点**：

- 向量数据库应用
- 混合搜索实现
- 系统架构设计

**参考文档**：

- [语义搜索系统端到端实现](../06-实战案例/06.01-语义搜索系统端到端实现.md)

### 5.2 项目2: RAG知识库

**项目目标**：

构建企业级RAG知识库系统，支持多租户、对话记忆、智能问答。

**技术栈**：

- PostgreSQL 18 + pgvector 2.0
- LangChain/LlamaIndex
- FastAPI
- React

**学习重点**：

- RAG架构设计
- 多租户系统
- AI集成

**参考文档**：

- [RAG知识库完整项目](../06-实战案例/06.02-RAG知识库完整项目.md)

### 5.3 项目3: 智能推荐系统

**项目目标**：

构建智能推荐系统，支持协同过滤、内容推荐、混合策略。

**技术栈**：

- PostgreSQL 18
- Python
- FastAPI
- 机器学习模型

**学习重点**：

- 推荐算法实现
- 实时特征工程
- A/B测试

**参考文档**：

- [智能推荐系统](../06-实战案例/06.03-智能推荐系统.md)

---

## 6. 课程材料

### 6.1 学习目标

**每章节学习目标**：

- 明确学习目标
- 定义知识要点
- 设定技能要求
- 评估学习成果

**示例：Week 2 SQL语言学习目标**：

1. **知识目标**：
   - 理解SQL语言的基本语法和结构
   - 掌握SELECT、INSERT、UPDATE、DELETE语句
   - 理解JOIN操作和聚合函数

2. **技能目标**：
   - 能够编写复杂的SQL查询
   - 能够优化查询性能
   - 能够使用子查询和CTE

3. **评估标准**：
   - 完成所有练习题（80%正确率）
   - 完成实验任务
   - 通过期中考试

### 6.2 练习题

**练习题类型**：

- 选择题：测试概念理解
- 填空题：测试细节掌握
- 编程题：测试实践能力
- 设计题：测试系统设计能力

**示例：Week 2 SQL语言练习题**：

#### 选择题

1. **以下哪个SQL语句用于查询数据？**
   - A. INSERT
   - B. SELECT
   - C. UPDATE
   - D. DELETE
   - **答案**: B

2. **INNER JOIN和LEFT JOIN的主要区别是什么？**
   - A. INNER JOIN返回所有匹配的行，LEFT JOIN只返回左表的行
   - B. LEFT JOIN返回所有匹配的行，INNER JOIN只返回左表的行
   - C. INNER JOIN只返回匹配的行，LEFT JOIN返回左表的所有行
   - D. 没有区别
   - **答案**: C

#### 编程题

**题目1: 编写SQL查询，查找销售额最高的前10个产品**:

```sql
-- 答案示例
SELECT
    product_id,
    product_name,
    SUM(quantity * price) as total_sales
FROM orders o
JOIN order_items oi ON o.order_id = oi.order_id
JOIN products p ON oi.product_id = p.product_id
GROUP BY product_id, product_name
ORDER BY total_sales DESC
LIMIT 10;
```

**题目2: 编写SQL查询，查找每个客户的订单总数和总金额**:

```sql
-- 答案示例
SELECT
    c.customer_id,
    c.customer_name,
    COUNT(o.order_id) as order_count,
    SUM(o.total_amount) as total_amount
FROM customers c
LEFT JOIN orders o ON c.customer_id = o.customer_id
GROUP BY c.customer_id, c.customer_name
ORDER BY total_amount DESC;
```

#### 设计题

1. **设计一个电商数据库，包含以下实体：**
   - 客户（Customers）
   - 产品（Products）
   - 订单（Orders）
   - 订单项（Order Items）

   **要求**：
   - 使用范式理论设计表结构
   - 定义主键和外键
   - 考虑索引优化

### 6.3 实验指导

**实验内容**：

- 实验环境搭建
- 实验步骤说明
- 实验结果验证
- 实验报告要求

**示例：Week 2 SQL语言实验指导**：

#### 实验1: 基础SQL查询

**实验目标**：

- 掌握SELECT语句的基本用法
- 理解WHERE子句和ORDER BY子句
- 使用聚合函数和GROUP BY

**实验步骤**：

1. **创建示例数据库**：

```sql
-- 创建数据库
CREATE DATABASE practice_db;

-- 创建表
CREATE TABLE students (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    age INTEGER,
    grade VARCHAR(10)
);

CREATE TABLE courses (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    credits INTEGER
);

CREATE TABLE enrollments (
    student_id INTEGER REFERENCES students(id),
    course_id INTEGER REFERENCES courses(id),
    grade CHAR(1),
    PRIMARY KEY (student_id, course_id)
);

-- 插入示例数据
INSERT INTO students (name, age, grade) VALUES
('Alice', 20, 'A'),
('Bob', 21, 'B'),
('Charlie', 19, 'A');

INSERT INTO courses (name, credits) VALUES
('Database Systems', 3),
('Operating Systems', 4),
('Computer Networks', 3);

INSERT INTO enrollments (student_id, course_id, grade) VALUES
(1, 1, 'A'),
(1, 2, 'B'),
(2, 1, 'C'),
(2, 3, 'A');
```

**步骤2: 执行查询任务**:

```sql
-- 任务1: 查询所有学生的信息
SELECT * FROM students;

-- 任务2: 查询年龄大于20的学生
SELECT * FROM students WHERE age > 20;

-- 任务3: 查询每个学生的选课数量
SELECT
    s.name,
    COUNT(e.course_id) as course_count
FROM students s
LEFT JOIN enrollments e ON s.id = e.student_id
GROUP BY s.id, s.name;
```

**步骤3: 验证结果**:

- 检查查询结果是否正确
- 验证聚合函数计算是否正确
- 确认JOIN操作是否正确

**实验报告要求**：

- 记录所有SQL语句
- 截图查询结果
- 分析查询性能
- 总结学习心得

#### 实验2: 复杂查询和优化

**实验目标**：

- 编写复杂的多表JOIN查询
- 使用子查询和CTE
- 优化查询性能

**实验步骤**：

**步骤1: 编写复杂查询**:

```sql
-- 使用CTE查询每个学生的平均成绩
WITH student_grades AS (
    SELECT
        s.id,
        s.name,
        e.grade,
        CASE
            WHEN e.grade = 'A' THEN 4.0
            WHEN e.grade = 'B' THEN 3.0
            WHEN e.grade = 'C' THEN 2.0
            ELSE 0.0
        END as grade_point
    FROM students s
    JOIN enrollments e ON s.id = e.student_id
)
SELECT
    name,
    AVG(grade_point) as gpa
FROM student_grades
GROUP BY id, name
ORDER BY gpa DESC;
```

**步骤2: 分析查询计划**:

```sql
-- 使用EXPLAIN分析查询
EXPLAIN ANALYZE
SELECT
    s.name,
    COUNT(e.course_id) as course_count
FROM students s
LEFT JOIN enrollments e ON s.id = e.student_id
GROUP BY s.id, s.name;
```

**步骤3: 优化查询**:

- 添加适当的索引
- 重写查询以提高性能
- 比较优化前后的性能

#### Week 4: 事务与并发练习题

**选择题**:

1. **ACID特性中的I代表什么？**
   - A. Integrity
   - B. Isolation
   - C. Index
   - D. Insert
   - **答案**: B

2. **PostgreSQL默认的隔离级别是？**
   - A. Read Uncommitted
   - B. Read Committed
   - C. Repeatable Read
   - D. Serializable
   - **答案**: B

3. **MVCC的主要优势是什么？**
   - A. 提高写入性能
   - B. 减少锁竞争
   - C. 简化事务管理
   - D. 以上都是
   - **答案**: D

**编程题**:

**题目1: 测试不同隔离级别**:

```sql
-- 会话1: 开始事务并更新数据
BEGIN;
UPDATE accounts SET balance = balance - 100 WHERE id = 1;

-- 会话2: 查看数据（测试隔离级别）
BEGIN;
SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
SELECT balance FROM accounts WHERE id = 1;
COMMIT;

-- 会话1: 提交事务
COMMIT;
```

**题目2: 观察MVCC行为**:

```sql
-- 创建测试表
CREATE TABLE test_mvcc (
    id SERIAL PRIMARY KEY,
    value INTEGER
);

INSERT INTO test_mvcc (value) VALUES (100);

-- 会话1: 开始事务
BEGIN;
UPDATE test_mvcc SET value = 200 WHERE id = 1;

-- 会话2: 查询（应该看到旧值）
SELECT * FROM test_mvcc WHERE id = 1;

-- 会话1: 提交
COMMIT;

-- 会话2: 再次查询（现在看到新值）
SELECT * FROM test_mvcc WHERE id = 1;
```

#### Week 4: 事务与并发实验指导

##### 实验1: 隔离级别测试

**实验目标**:

- 理解不同隔离级别的行为
- 观察脏读、不可重复读、幻读
- 测试MVCC机制

**实验步骤**:

**步骤1: 创建测试环境**:

```sql
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name TEXT,
    stock INTEGER
);

INSERT INTO products (name, stock) VALUES
('Product A', 100),
('Product B', 50);
```

**步骤2: 测试Read Committed隔离级别**:

```sql
-- 会话1
BEGIN;
SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
UPDATE products SET stock = stock - 10 WHERE id = 1;

-- 会话2（在另一个终端）
BEGIN;
SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
SELECT stock FROM products WHERE id = 1;  -- 应该看到旧值
COMMIT;

-- 会话1
COMMIT;

-- 会话2（再次查询）
SELECT stock FROM products WHERE id = 1;  -- 现在看到新值
```

**步骤3: 测试Repeatable Read隔离级别**:

```sql
-- 会话1
BEGIN;
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
SELECT stock FROM products WHERE id = 1;

-- 会话2（在另一个终端）
BEGIN;
UPDATE products SET stock = stock - 10 WHERE id = 1;
COMMIT;

-- 会话1（再次查询，应该看到相同的值）
SELECT stock FROM products WHERE id = 1;
COMMIT;
```

**实验报告要求**:

- 记录每个隔离级别的行为
- 截图查询结果
- 分析MVCC如何工作
- 总结隔离级别的影响

#### Week 5: 索引与查询优化练习题

**选择题**:

1. **B-tree索引的主要优势是什么？**
   - A. 支持范围查询
   - B. 支持模糊查询
   - C. 支持全文搜索
   - D. 以上都是
   - **答案**: A

2. **以下哪个索引类型适合全文搜索？**
   - A. B-tree
   - B. Hash
   - C. GIN
   - D. GiST
   - **答案**: C

3. **EXPLAIN ANALYZE的主要作用是什么？**
   - A. 显示查询计划
   - B. 执行查询并显示实际时间
   - C. 优化查询
   - D. 创建索引
   - **答案**: B

**编程题**:

**题目1: 创建和测试索引**:

```sql
-- 创建测试表
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    customer_id INTEGER,
    order_date DATE,
    total_amount DECIMAL(10,2)
);

-- 插入测试数据
INSERT INTO orders (customer_id, order_date, total_amount)
SELECT
    (random() * 1000)::INTEGER,
    CURRENT_DATE - (random() * 365)::INTEGER,
    (random() * 1000)::DECIMAL(10,2)
FROM generate_series(1, 100000);

-- 测试无索引查询
EXPLAIN ANALYZE
SELECT * FROM orders WHERE customer_id = 100;

-- 创建索引
CREATE INDEX idx_orders_customer_id ON orders(customer_id);

-- 测试有索引查询
EXPLAIN ANALYZE
SELECT * FROM orders WHERE customer_id = 100;
```

**题目2: 分析查询计划**:

```sql
-- 复杂查询
EXPLAIN ANALYZE
SELECT
    customer_id,
    COUNT(*) as order_count,
    SUM(total_amount) as total_spent
FROM orders
WHERE order_date > CURRENT_DATE - INTERVAL '30 days'
GROUP BY customer_id
HAVING COUNT(*) > 5
ORDER BY total_spent DESC
LIMIT 10;

-- 优化：添加复合索引
CREATE INDEX idx_orders_date_customer
ON orders(order_date, customer_id);

-- 再次分析
EXPLAIN ANALYZE
SELECT
    customer_id,
    COUNT(*) as order_count,
    SUM(total_amount) as total_spent
FROM orders
WHERE order_date > CURRENT_DATE - INTERVAL '30 days'
GROUP BY customer_id
HAVING COUNT(*) > 5
ORDER BY total_spent DESC
LIMIT 10;
```

#### Week 5: 索引与查询优化实验指导

##### 实验1: 索引性能对比

**实验目标**:

- 理解索引对查询性能的影响
- 学习如何选择合适的索引类型
- 掌握查询计划分析

**实验步骤**:

**步骤1: 创建测试环境**:

```sql
CREATE TABLE employees (
    id SERIAL PRIMARY KEY,
    name TEXT,
    department TEXT,
    salary DECIMAL(10,2),
    hire_date DATE
);

-- 插入大量测试数据
INSERT INTO employees (name, department, salary, hire_date)
SELECT
    'Employee ' || i,
    CASE (i % 10)
        WHEN 0 THEN 'Sales'
        WHEN 1 THEN 'Engineering'
        WHEN 2 THEN 'Marketing'
        ELSE 'Other'
    END,
    (random() * 100000 + 30000)::DECIMAL(10,2),
    CURRENT_DATE - (random() * 3650)::INTEGER
FROM generate_series(1, 100000) i;
```

**步骤2: 测试无索引查询**:

```sql
-- 测试1: 按部门查询
EXPLAIN ANALYZE
SELECT * FROM employees WHERE department = 'Engineering';

-- 测试2: 按薪资范围查询
EXPLAIN ANALYZE
SELECT * FROM employees
WHERE salary BETWEEN 50000 AND 60000;

-- 测试3: 按日期范围查询
EXPLAIN ANALYZE
SELECT * FROM employees
WHERE hire_date > CURRENT_DATE - INTERVAL '1 year';
```

**步骤3: 创建索引并测试**:

```sql
-- 创建B-tree索引
CREATE INDEX idx_employees_department ON employees(department);
CREATE INDEX idx_employees_salary ON employees(salary);
CREATE INDEX idx_employees_hire_date ON employees(hire_date);

-- 再次测试查询
EXPLAIN ANALYZE
SELECT * FROM employees WHERE department = 'Engineering';

EXPLAIN ANALYZE
SELECT * FROM employees
WHERE salary BETWEEN 50000 AND 60000;

EXPLAIN ANALYZE
SELECT * FROM employees
WHERE hire_date > CURRENT_DATE - INTERVAL '1 year';
```

**步骤4: 分析查询计划**:

```sql
-- 查看索引使用情况
SELECT
    schemaname,
    tablename,
    indexname,
    idx_scan,
    idx_tup_read,
    idx_tup_fetch
FROM pg_stat_user_indexes
WHERE tablename = 'employees';
```

**实验报告要求**:

- 记录每个查询的执行时间
- 对比索引前后的性能差异
- 分析查询计划的变化
- 总结索引选择的原则

#### Week 7: 高级SQL特性练习题

**选择题**:

1. **窗口函数的主要用途是什么？**
   - A. 分组数据
   - B. 对结果集进行排序
   - C. 在结果集上执行计算而不减少行数
   - D. 过滤数据
   - **答案**: C

2. **以下哪个是递归CTE的正确语法？**
   - A. WITH RECURSIVE ... SELECT ... UNION SELECT ...
   - B. WITH ... SELECT ... UNION ALL SELECT ...
   - C. RECURSIVE WITH ... SELECT ...
   - D. 以上都不对
   - **答案**: A

3. **JSONB相比JSON的主要优势是什么？**
   - A. 存储空间更小
   - B. 支持索引
   - C. 查询性能更好
   - D. 以上都是
   - **答案**: D

**编程题**:

**题目1: 使用窗口函数计算排名**:

```sql
-- 创建销售数据表
CREATE TABLE sales (
    id SERIAL PRIMARY KEY,
    salesperson TEXT,
    product TEXT,
    amount DECIMAL(10,2),
    sale_date DATE
);

-- 使用窗口函数计算每个销售人员的排名
SELECT
    salesperson,
    product,
    amount,
    RANK() OVER (PARTITION BY salesperson ORDER BY amount DESC) as rank,
    SUM(amount) OVER (PARTITION BY salesperson) as total_sales
FROM sales
ORDER BY salesperson, rank;
```

**题目2: 递归查询组织架构**:

```sql
-- 创建员工表（包含层级关系）
CREATE TABLE employees (
    id SERIAL PRIMARY KEY,
    name TEXT,
    manager_id INTEGER REFERENCES employees(id)
);

-- 递归查询所有下属
WITH RECURSIVE subordinates AS (
    -- 基础情况：直接下属
    SELECT id, name, manager_id, 1 as level
    FROM employees
    WHERE manager_id = 1

    UNION ALL

    -- 递归情况：下属的下属
    SELECT e.id, e.name, e.manager_id, s.level + 1
    FROM employees e
    JOIN subordinates s ON e.manager_id = s.id
)
SELECT * FROM subordinates
ORDER BY level, name;
```

#### Week 7: 高级SQL特性实验指导

##### 实验1: 窗口函数应用

**实验目标**:

- 掌握窗口函数的使用
- 理解PARTITION BY和ORDER BY的作用
- 使用排名函数和聚合窗口函数

**实验步骤**:

**步骤1: 创建测试数据**:

```sql
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    customer_id INTEGER,
    order_date DATE,
    total_amount DECIMAL(10,2)
);

-- 插入测试数据
INSERT INTO orders (customer_id, order_date, total_amount)
SELECT
    (random() * 100)::INTEGER,
    CURRENT_DATE - (random() * 365)::INTEGER,
    (random() * 1000 + 100)::DECIMAL(10,2)
FROM generate_series(1, 1000);
```

**步骤2: 使用窗口函数分析**:

```sql
-- 计算每个客户的订单排名
SELECT
    customer_id,
    order_date,
    total_amount,
    ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY total_amount DESC) as order_rank,
    SUM(total_amount) OVER (PARTITION BY customer_id) as customer_total,
    AVG(total_amount) OVER (PARTITION BY customer_id) as customer_avg
FROM orders
ORDER BY customer_id, order_rank;
```

**步骤3: 使用移动窗口**:

```sql
-- 计算移动平均
SELECT
    order_date,
    total_amount,
    AVG(total_amount) OVER (
        ORDER BY order_date
        ROWS BETWEEN 2 PRECEDING AND CURRENT ROW
    ) as moving_avg_3days
FROM orders
ORDER BY order_date;
```

#### Week 9: 安全与访问控制练习题

**选择题**:

1. **PostgreSQL 18新增的OAuth 2.0支持主要用于什么？**
   - A. 数据加密
   - B. 企业级身份验证
   - C. 数据备份
   - D. 查询优化
   - **答案**: B

2. **行级安全（RLS）的主要作用是什么？**
   - A. 控制表的访问权限
   - B. 控制行的访问权限
   - C. 控制列的访问权限
   - D. 控制数据库的访问权限
   - **答案**: B

3. **以下哪个是PostgreSQL支持的认证方法？**
   - A. MD5
   - B. SCRAM-SHA-256
   - C. OAuth 2.0
   - D. 以上都是
   - **答案**: D

**编程题**:

**题目1: 配置行级安全**:

```sql
-- 创建多租户表
CREATE TABLE tenant_data (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER,
    data TEXT
);

-- 启用行级安全
ALTER TABLE tenant_data ENABLE ROW LEVEL SECURITY;

-- 创建策略：用户只能访问自己租户的数据
CREATE POLICY tenant_isolation_policy ON tenant_data
    FOR ALL
    USING (tenant_id = current_setting('app.current_tenant_id')::INTEGER);
```

**题目2: 配置OAuth 2.0（PostgreSQL 18）**:

```sql
-- 配置OAuth 2.0提供者
ALTER SYSTEM SET oauth2.provider = 'google';
ALTER SYSTEM SET oauth2.client_id = 'your-client-id';
ALTER SYSTEM SET oauth2.client_secret = 'your-client-secret';
ALTER SYSTEM SET oauth2.redirect_uri = 'https://your-app.com/callback';

-- 重新加载配置
SELECT pg_reload_conf();
```

#### Week 9: 安全与访问控制实验指导

##### 实验1: 行级安全配置

**实验目标**:

- 理解行级安全的工作原理
- 配置多租户数据隔离
- 测试安全策略

**实验步骤**:

**步骤1: 创建测试环境**:

```sql
-- 创建租户表
CREATE TABLE tenants (
    id SERIAL PRIMARY KEY,
    name TEXT
);

-- 创建租户数据表
CREATE TABLE tenant_documents (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER REFERENCES tenants(id),
    title TEXT,
    content TEXT
);

-- 插入测试数据
INSERT INTO tenants (name) VALUES ('Tenant A'), ('Tenant B');
INSERT INTO tenant_documents (tenant_id, title, content) VALUES
(1, 'Doc 1', 'Content 1'),
(1, 'Doc 2', 'Content 2'),
(2, 'Doc 3', 'Content 3');
```

**步骤2: 配置行级安全**:

```sql
-- 启用行级安全
ALTER TABLE tenant_documents ENABLE ROW LEVEL SECURITY;

-- 创建策略
CREATE POLICY tenant_documents_policy ON tenant_documents
    FOR ALL
    USING (tenant_id = current_setting('app.current_tenant_id', true)::INTEGER);

-- 创建应用角色
CREATE ROLE app_user;
GRANT SELECT, INSERT, UPDATE, DELETE ON tenant_documents TO app_user;
```

**步骤3: 测试安全策略**:

```sql
-- 切换到应用用户
SET ROLE app_user;

-- 设置当前租户ID
SET app.current_tenant_id = '1';

-- 查询（应该只能看到租户1的数据）
SELECT * FROM tenant_documents;

-- 尝试访问租户2的数据（应该失败）
SET app.current_tenant_id = '2';
SELECT * FROM tenant_documents;
```

#### Week 13: 分布式架构练习题

**选择题**:

1. **Citus的主要用途是什么？**
   - A. 数据备份
   - B. 查询优化
   - C. 分布式数据库扩展
   - D. 数据加密
   - **答案**: C

2. **分布式表的分片键选择原则是什么？**
   - A. 高基数
   - B. 均匀分布
   - C. 匹配查询模式
   - D. 以上都是
   - **答案**: D

3. **共分片（Co-location）的主要优势是什么？**
   - A. 减少网络传输
   - B. 提高JOIN性能
   - C. 简化查询
   - D. 以上都是
   - **答案**: D

**编程题**:

**题目1: 创建分布式表**:

```sql
-- 安装Citus扩展
CREATE EXTENSION citus;

-- 添加工作节点
SELECT citus_add_node('192.168.1.11', 5432);
SELECT citus_add_node('192.168.1.12', 5432);

-- 创建分布式表
CREATE TABLE distributed_users (
    user_id BIGSERIAL,
    name TEXT,
    email TEXT
);

-- 按user_id分片
SELECT create_distributed_table('distributed_users', 'user_id');
```

**题目2: 查询分布式数据**:

```sql
-- 单分片查询（高效）
SELECT * FROM distributed_users WHERE user_id = 123;

-- 跨分片聚合查询
SELECT
    COUNT(*) as total_users,
    COUNT(DISTINCT email) as unique_emails
FROM distributed_users;
```

#### Week 13: 分布式架构实验指导

##### 实验1: Citus集群部署

**实验目标**:

- 理解Citus架构
- 部署Citus集群
- 创建和管理分布式表

**实验步骤**:

**步骤1: 安装和配置Citus**:

```sql
-- 在Coordinator节点
CREATE EXTENSION citus;

-- 查看Citus版本
SELECT * FROM citus_version();

-- 添加Worker节点
SELECT citus_add_node('worker1.example.com', 5432);
SELECT citus_add_node('worker2.example.com', 5432);

-- 查看节点信息
SELECT * FROM citus_get_active_worker_nodes();
```

**步骤2: 创建分布式表**:

```sql
-- 创建用户表
CREATE TABLE users (
    user_id BIGSERIAL PRIMARY KEY,
    username TEXT,
    email TEXT
);

-- 创建订单表（与用户表共分片）
CREATE TABLE orders (
    order_id BIGSERIAL,
    user_id BIGINT REFERENCES users(user_id),
    total_amount DECIMAL(10,2)
);

-- 分片配置
SELECT create_distributed_table('users', 'user_id');
SELECT create_distributed_table('orders', 'user_id');
```

**步骤3: 测试分布式查询**:

```sql
-- 插入测试数据
INSERT INTO users (username, email) VALUES
('user1', 'user1@example.com'),
('user2', 'user2@example.com');

INSERT INTO orders (user_id, total_amount) VALUES
(1, 100.00),
(1, 200.00),
(2, 150.00);

-- 测试共分片JOIN（高效）
SELECT
    u.username,
    COUNT(o.order_id) as order_count,
    SUM(o.total_amount) as total_spent
FROM users u
JOIN orders o ON u.user_id = o.user_id
WHERE u.user_id = 1
GROUP BY u.user_id, u.username;
```

**实验报告要求**:

- 记录集群配置过程
- 测试查询性能
- 分析分片分布
- 总结最佳实践

#### Week 14: AI集成与向量数据库练习题

**选择题**:

1. **pgvector扩展的主要用途是什么？**
   - A. 全文搜索
   - B. 向量相似度搜索
   - C. 数据压缩
   - D. 数据加密
   - **答案**: B

2. **HNSW索引相比IVFFlat的主要优势是什么？**
   - A. 构建速度更快
   - B. 查询精度更高
   - C. 存储空间更小
   - D. 以上都是
   - **答案**: B

3. **RAG架构中的"检索"阶段主要做什么？**
   - A. 生成答案
   - B. 向量化查询
   - C. 从向量数据库检索相关文档
   - D. 训练模型
   - **答案**: C

**编程题**:

**题目1: 创建向量表和索引**:

```sql
-- 安装pgvector扩展
CREATE EXTENSION vector;

-- 创建文档表
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    title TEXT,
    content TEXT,
    embedding vector(1536)  -- OpenAI embedding维度
);

-- 创建HNSW索引
CREATE INDEX ON documents
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);
```

**题目2: 向量相似度搜索**:

```sql
-- 查询相似文档
SELECT
    id,
    title,
    1 - (embedding <=> $1::vector) as similarity
FROM documents
ORDER BY embedding <=> $1::vector
LIMIT 10;
```

#### Week 14: AI集成与向量数据库实验指导

##### 实验1: 向量搜索实现

**实验目标**:

- 理解向量数据库的工作原理
- 配置pgvector扩展
- 实现向量相似度搜索

**实验步骤**:

**步骤1: 环境准备**:

```sql
-- 安装pgvector扩展
CREATE EXTENSION vector;

-- 创建测试表
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name TEXT,
    description TEXT,
    embedding vector(384)  -- 使用较小的维度用于测试
);
```

**步骤2: 插入向量数据**:

```sql
-- 插入示例数据（实际应用中需要使用embedding模型生成向量）
INSERT INTO products (name, description, embedding) VALUES
('Laptop', 'High-performance laptop', '[0.1,0.2,0.3,...]'::vector),
('Phone', 'Smartphone with latest features', '[0.2,0.3,0.4,...]'::vector),
('Tablet', 'Portable tablet device', '[0.15,0.25,0.35,...]'::vector);
```

**步骤3: 创建索引并测试**:

```sql
-- 创建HNSW索引
CREATE INDEX idx_products_embedding ON products
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);

-- 测试向量搜索
SELECT
    name,
    1 - (embedding <=> $1::vector) as similarity
FROM products
ORDER BY embedding <=> $1::vector
LIMIT 5;
```

#### Week 10: 高可用与复制练习题

**选择题**:

1. **PostgreSQL流复制的主要优势是什么？**
   - A. 实时数据同步
   - B. 低延迟复制
   - C. 支持只读查询
   - D. 以上都是
   - **答案**: D

2. **逻辑复制相比流复制的主要区别是什么？**
   - A. 复制整个数据库
   - B. 可以复制特定表
   - C. 支持跨版本复制
   - D. B和C都是
   - **答案**: D

3. **Patroni的主要作用是什么？**
   - A. 数据备份
   - B. 自动故障转移
   - C. 查询优化
   - D. 数据压缩
   - **答案**: B

**编程题**:

**题目1: 配置流复制**:

```sql
-- 在主库创建复制用户
CREATE ROLE replicator WITH REPLICATION LOGIN PASSWORD 'secure_password';

-- 在主库配置pg_hba.conf
-- host replication replicator 192.168.1.0/24 md5

-- 在主库配置postgresql.conf
-- wal_level = replica
-- max_wal_senders = 3
-- max_replication_slots = 3

-- 在从库配置recovery.conf（PostgreSQL 12+使用postgresql.conf）
-- primary_conninfo = 'host=192.168.1.10 port=5432 user=replicator password=secure_password'
```

**题目2: 配置逻辑复制**:

```sql
-- 在主库创建发布
CREATE PUBLICATION my_publication FOR TABLE users, orders;

-- 在从库创建订阅
CREATE SUBSCRIPTION my_subscription
CONNECTION 'host=192.168.1.10 port=5432 dbname=mydb user=replicator password=secure_password'
PUBLICATION my_publication;
```

#### Week 10: 高可用与复制实验指导

##### 实验1: 流复制配置

**实验目标**:

- 理解流复制的工作原理
- 配置主从复制
- 测试故障转移

**实验步骤**:

**步骤1: 主库配置**:

```sql
-- 创建复制用户
CREATE ROLE replicator WITH REPLICATION LOGIN PASSWORD 'repl_password';

-- 查看WAL级别
SHOW wal_level;

-- 配置复制槽（可选，用于确保WAL保留）
SELECT pg_create_physical_replication_slot('replica_slot');
```

**步骤2: 从库初始化**:

```bash
# 停止从库
pg_ctl stop

# 备份主库数据
pg_basebackup -h 192.168.1.10 -D /var/lib/postgresql/data -U replicator -P -W -R

# 启动从库
pg_ctl start
```

**步骤3: 验证复制**:

```sql
-- 在主库插入数据
INSERT INTO test_table VALUES (1, 'test');

-- 在从库查询（应该能看到数据）
SELECT * FROM test_table;
```

#### Week 11: 性能调优练习题

**选择题**:

1. **shared_buffers参数的主要作用是什么？**
   - A. 控制连接数
   - B. 控制共享内存大小
   - C. 控制日志级别
   - D. 控制查询超时
   - **答案**: B

2. **work_mem参数影响什么？**
   - A. 每个查询操作的内存
   - B. 总内存使用
   - C. 连接数
   - D. 日志大小
   - **答案**: A

3. **EXPLAIN ANALYZE相比EXPLAIN的优势是什么？**
   - A. 显示实际执行时间
   - B. 显示实际行数
   - C. 实际执行查询
   - D. 以上都是
   - **答案**: D

**编程题**:

**题目1: 分析慢查询**:

```sql
-- 启用慢查询日志
ALTER SYSTEM SET log_min_duration_statement = 1000;  -- 记录超过1秒的查询
SELECT pg_reload_conf();

-- 查看慢查询
SELECT
    query,
    calls,
    total_exec_time,
    mean_exec_time
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 10;
```

**题目2: 优化查询性能**:

```sql
-- 分析表统计信息
ANALYZE large_table;

-- 查看执行计划
EXPLAIN ANALYZE
SELECT * FROM large_table
WHERE created_at > '2025-01-01'
ORDER BY id DESC
LIMIT 100;

-- 创建索引优化
CREATE INDEX idx_large_table_created_at ON large_table(created_at);
```

#### Week 11: 性能调优实验指导

##### 实验1: 参数调优

**实验目标**:

- 理解关键性能参数
- 配置优化参数
- 测试性能提升

**实验步骤**:

**步骤1: 查看当前配置**:

```sql
-- 查看关键参数
SHOW shared_buffers;
SHOW work_mem;
SHOW maintenance_work_mem;
SHOW effective_cache_size;
SHOW max_connections;
```

**步骤2: 优化配置**:

```sql
-- 根据系统内存调整（假设16GB内存）
ALTER SYSTEM SET shared_buffers = '4GB';
ALTER SYSTEM SET work_mem = '64MB';
ALTER SYSTEM SET maintenance_work_mem = '1GB';
ALTER SYSTEM SET effective_cache_size = '12GB';
ALTER SYSTEM SET max_connections = 200;

-- 重新加载配置
SELECT pg_reload_conf();
```

**步骤3: 性能测试**:

```sql
-- 创建测试表
CREATE TABLE perf_test (
    id SERIAL PRIMARY KEY,
    data TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- 插入测试数据
INSERT INTO perf_test (data)
SELECT md5(random()::text)
FROM generate_series(1, 1000000);

-- 测试查询性能
EXPLAIN ANALYZE
SELECT * FROM perf_test
WHERE created_at > NOW() - INTERVAL '1 day'
ORDER BY id DESC
LIMIT 100;
```

#### Week 12: 监控与诊断练习题

**选择题**:

1. **pg_stat_statements扩展的主要用途是什么？**
   - A. 监控连接数
   - B. 监控查询性能
   - C. 监控磁盘使用
   - D. 监控日志
   - **答案**: B

2. **pg_stat_activity视图显示什么信息？**
   - A. 当前活动连接
   - B. 表统计信息
   - C. 索引使用情况
   - D. 备份状态
   - **答案**: A

3. **pg_locks视图用于查看什么？**
   - A. 数据库锁
   - B. 表锁
   - C. 行锁
   - D. 以上都是
   - **答案**: D

**编程题**:

**题目1: 监控查询性能**:

```sql
-- 安装pg_stat_statements扩展
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;

-- 查看最慢的查询
SELECT
    query,
    calls,
    total_exec_time,
    mean_exec_time,
    max_exec_time
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 10;
```

**题目2: 诊断锁等待**:

```sql
-- 查看当前锁
SELECT
    locktype,
    relation::regclass,
    mode,
    granted
FROM pg_locks
WHERE NOT granted;

-- 查看阻塞的查询
SELECT
    blocked_locks.pid AS blocked_pid,
    blocking_locks.pid AS blocking_pid,
    blocked_activity.query AS blocked_query,
    blocking_activity.query AS blocking_query
FROM pg_catalog.pg_locks blocked_locks
JOIN pg_catalog.pg_stat_activity blocked_activity ON blocked_activity.pid = blocked_locks.pid
JOIN pg_catalog.pg_locks blocking_locks ON blocking_locks.locktype = blocked_locks.locktype
JOIN pg_catalog.pg_stat_activity blocking_activity ON blocking_activity.pid = blocking_locks.pid
WHERE NOT blocked_locks.granted
AND blocking_locks.granted;
```

#### Week 12: 监控与诊断实验指导

##### 实验1: 性能监控部署

**实验目标**:

- 配置性能监控
- 使用监控工具
- 分析性能数据

**实验步骤**:

**步骤1: 启用监控扩展**:

```sql
-- 启用pg_stat_statements
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;

-- 启用auto_explain（可选）
LOAD 'auto_explain';
SET auto_explain.log_min_duration = 1000;  -- 记录超过1秒的查询
SET auto_explain.log_analyze = true;
```

**步骤2: 监控关键指标**:

```sql
-- 查看数据库大小
SELECT
    pg_database.datname,
    pg_size_pretty(pg_database_size(pg_database.datname)) AS size
FROM pg_database
ORDER BY pg_database_size(pg_database.datname) DESC;

-- 查看表大小
SELECT
    schemaname,
    tablename,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS size
FROM pg_tables
ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC
LIMIT 10;
```

**步骤3: 分析慢查询**:

```sql
-- 重置统计信息
SELECT pg_stat_statements_reset();

-- 执行一些查询...

-- 查看统计信息
SELECT
    LEFT(query, 100) AS query_preview,
    calls,
    total_exec_time,
    mean_exec_time,
    (total_exec_time / SUM(total_exec_time) OVER()) * 100 AS percent
FROM pg_stat_statements
ORDER BY total_exec_time DESC
LIMIT 10;
```

#### Week 1: PostgreSQL基础练习题

**选择题**:

1. **PostgreSQL的默认端口是什么？**
   - A. 3306
   - B. 5432
   - C. 8080
   - D. 6379
   - **答案**: B

2. **PostgreSQL的主要特性不包括？**
   - A. ACID事务支持
   - B. 外键约束
   - C. 存储过程
   - D. 不支持JSON
   - **答案**: D

3. **psql工具的主要用途是什么？**
   - A. 图形界面管理
   - B. 命令行交互工具
   - C. 备份工具
   - D. 监控工具
   - **答案**: B

**编程题**:

**题目1: 创建数据库和用户**:

```sql
-- 创建数据库
CREATE DATABASE mydb;

-- 创建用户
CREATE USER myuser WITH PASSWORD 'secure_password';

-- 授予权限
GRANT ALL PRIVILEGES ON DATABASE mydb TO myuser;
```

**题目2: 基本表操作**:

```sql
-- 创建表
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 插入数据
INSERT INTO users (username, email) VALUES
('alice', 'alice@example.com'),
('bob', 'bob@example.com');

-- 查询数据
SELECT * FROM users;
```

#### Week 1: PostgreSQL基础实验指导

##### 实验1: PostgreSQL安装与配置

**实验目标**:

- 安装PostgreSQL 18
- 配置基本参数
- 创建数据库和用户

**实验步骤**:

**步骤1: 安装PostgreSQL**:

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install postgresql-18

# 启动服务
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**步骤2: 配置基本参数**:

```sql
-- 连接到PostgreSQL
sudo -u postgres psql

-- 查看版本
SELECT version();

-- 查看配置
SHOW shared_buffers;
SHOW max_connections;
```

**步骤3: 创建数据库和用户**:

```sql
-- 创建数据库
CREATE DATABASE testdb;

-- 创建用户
CREATE USER testuser WITH PASSWORD 'testpass';

-- 授予权限
GRANT ALL PRIVILEGES ON DATABASE testdb TO testuser;

-- 连接到新数据库
\c testdb
```

#### Week 2: SQL语言练习题

**选择题**:

1. **SQL中用于查询数据的关键字是？**
   - A. GET
   - B. SELECT
   - C. FETCH
   - D. RETRIEVE
   - **答案**: B

2. **INNER JOIN和LEFT JOIN的主要区别是什么？**
   - A. INNER JOIN返回所有行
   - B. LEFT JOIN返回左表所有行
   - C. 没有区别
   - D. INNER JOIN更快
   - **答案**: B

3. **GROUP BY子句的主要作用是什么？**
   - A. 排序数据
   - B. 过滤数据
   - C. 分组数据
   - D. 连接表
   - **答案**: C

**编程题**:

**题目1: 复杂查询**:

```sql
-- 创建示例表
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    customer_id INTEGER,
    order_date DATE,
    total_amount DECIMAL(10,2)
);

CREATE TABLE customers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100)
);

-- 查询每个客户的订单总数和总金额
SELECT
    c.name,
    COUNT(o.id) as order_count,
    COALESCE(SUM(o.total_amount), 0) as total_spent
FROM customers c
LEFT JOIN orders o ON c.id = o.customer_id
GROUP BY c.id, c.name
ORDER BY total_spent DESC;
```

**题目2: 子查询**:

```sql
-- 查询订单金额大于平均值的订单
SELECT *
FROM orders
WHERE total_amount > (
    SELECT AVG(total_amount)
    FROM orders
);
```

#### Week 2: SQL语言实验指导

##### 实验1: 基础SQL查询

**实验目标**:

- 掌握SELECT语句
- 理解WHERE、ORDER BY、LIMIT
- 使用聚合函数

**实验步骤**:

**步骤1: 创建测试数据**:

```sql
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    price DECIMAL(10,2),
    category VARCHAR(50),
    stock INTEGER
);

INSERT INTO products (name, price, category, stock) VALUES
('Laptop', 999.99, 'Electronics', 10),
('Phone', 599.99, 'Electronics', 20),
('Book', 19.99, 'Education', 100);
```

**步骤2: 基础查询**:

```sql
-- 查询所有产品
SELECT * FROM products;

-- 条件查询
SELECT * FROM products WHERE price > 100;

-- 排序
SELECT * FROM products ORDER BY price DESC;

-- 限制结果
SELECT * FROM products LIMIT 5;
```

**步骤3: 聚合查询**:

```sql
-- 统计每个类别的产品数量
SELECT category, COUNT(*) as count
FROM products
GROUP BY category;

-- 计算平均价格
SELECT AVG(price) as avg_price FROM products;

-- 查找最贵的产品
SELECT * FROM products
WHERE price = (SELECT MAX(price) FROM products);
```

#### Week 3: 数据模型与设计练习题

**选择题**:

1. **第一范式（1NF）要求什么？**
   - A. 每个属性都是原子值
   - B. 没有重复组
   - C. 主键唯一
   - D. A和B都是
   - **答案**: D

2. **外键的主要作用是什么？**
   - A. 提高查询性能
   - B. 维护引用完整性
   - C. 创建索引
   - D. 压缩数据
   - **答案**: B

3. **ER图中的菱形表示什么？**
   - A. 实体
   - B. 属性
   - C. 关系
   - D. 约束
   - **答案**: C

**编程题**:

**题目1: 设计规范化数据库**:

```sql
-- 设计图书馆管理系统（符合第三范式）

-- 图书表
CREATE TABLE books (
    isbn VARCHAR(20) PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    author_id INTEGER REFERENCES authors(id),
    publisher_id INTEGER REFERENCES publishers(id),
    publication_date DATE
);

-- 作者表
CREATE TABLE authors (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    birth_date DATE
);

-- 出版社表
CREATE TABLE publishers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    address TEXT
);

-- 借阅记录表
CREATE TABLE borrowings (
    id SERIAL PRIMARY KEY,
    book_isbn VARCHAR(20) REFERENCES books(isbn),
    reader_id INTEGER REFERENCES readers(id),
    borrow_date DATE NOT NULL,
    return_date DATE,
    due_date DATE NOT NULL
);

-- 读者表
CREATE TABLE readers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE,
    phone VARCHAR(20)
);
```

#### Week 3: 数据模型与设计实验指导

##### 实验1: 数据库设计实践

**实验目标**:

- 理解关系模型
- 应用范式理论
- 设计规范化数据库

**实验步骤**:

**步骤1: 需求分析**:

设计一个在线商店系统，包含：

- 商品信息
- 客户信息
- 订单信息
- 订单明细

**步骤2: ER图设计**:

```text
Customer (1) ----< (N) Order
Order (1) ----< (N) OrderItem
Product (1) ----< (N) OrderItem
```

**步骤3: 创建表结构**:

```sql
-- 客户表
CREATE TABLE customers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    phone VARCHAR(20),
    address TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 商品表
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    description TEXT,
    price DECIMAL(10,2) NOT NULL,
    stock INTEGER DEFAULT 0,
    category_id INTEGER REFERENCES categories(id)
);

-- 订单表
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    customer_id INTEGER REFERENCES customers(id),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total_amount DECIMAL(10,2) NOT NULL,
    status VARCHAR(20) DEFAULT 'pending'
);

-- 订单明细表
CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    order_id INTEGER REFERENCES orders(id),
    product_id INTEGER REFERENCES products(id),
    quantity INTEGER NOT NULL,
    unit_price DECIMAL(10,2) NOT NULL,
    subtotal DECIMAL(10,2) GENERATED ALWAYS AS (quantity * unit_price) STORED
);
```

#### Week 6: 存储与备份练习题

**选择题**:

1. **PostgreSQL的数据文件存储在哪个目录？**
   - A. /var/lib/postgresql/data
   - B. /etc/postgresql
   - C. /usr/bin/postgresql
   - D. /tmp/postgresql
   - **答案**: A

2. **pg_dump的主要用途是什么？**
   - A. 恢复数据库
   - B. 备份数据库
   - C. 优化查询
   - D. 监控性能
   - **答案**: B

3. **WAL（Write-Ahead Logging）的主要作用是什么？**
   - A. 提高查询性能
   - B. 保证数据一致性
   - C. 压缩数据
   - D. 加密数据
   - **答案**: B

**编程题**:

**题目1: 数据库备份**:

```bash
# 备份整个数据库
pg_dump -U postgres -d mydb > mydb_backup.sql

# 备份特定表
pg_dump -U postgres -d mydb -t users -t orders > tables_backup.sql

# 压缩备份
pg_dump -U postgres -d mydb | gzip > mydb_backup.sql.gz
```

**题目2: 数据库恢复**:

```bash
# 从SQL文件恢复
psql -U postgres -d mydb < mydb_backup.sql

# 从压缩文件恢复
gunzip < mydb_backup.sql.gz | psql -U postgres -d mydb
```

#### Week 6: 存储与备份实验指导

##### 实验1: 备份与恢复

**实验目标**:

- 掌握备份方法
- 理解恢复流程
- 测试备份完整性

**实验步骤**:

**步骤1: 创建测试数据**:

```sql
-- 创建测试数据库
CREATE DATABASE backup_test;

-- 创建测试表
\c backup_test
CREATE TABLE test_data (
    id SERIAL PRIMARY KEY,
    data TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 插入测试数据
INSERT INTO test_data (data)
SELECT 'Test data ' || generate_series(1, 1000);
```

**步骤2: 执行备份**:

```bash
# SQL格式备份
pg_dump -U postgres -d backup_test > backup_test.sql

# 自定义格式备份（支持并行恢复）
pg_dump -U postgres -d backup_test -F c -f backup_test.dump

# 查看备份文件
ls -lh backup_test.*
```

**步骤3: 测试恢复**:

```bash
# 删除测试数据库
dropdb -U postgres backup_test

# 从SQL文件恢复
psql -U postgres -d postgres < backup_test.sql

# 验证数据
psql -U postgres -d backup_test -c "SELECT COUNT(*) FROM test_data;"
```

#### Week 8: 扩展系统练习题

**选择题**:

1. **PostgreSQL扩展的安装命令是什么？**
   - A. INSTALL EXTENSION
   - B. CREATE EXTENSION
   - C. ADD EXTENSION
   - D. LOAD EXTENSION
   - **答案**: B

2. **pgvector扩展的主要用途是什么？**
   - A. 全文搜索
   - B. 向量相似度搜索
   - C. 数据压缩
   - D. 数据加密
   - **答案**: B

3. **PostGIS扩展提供什么功能？**
   - A. 地理空间数据支持
   - B. 时间序列数据
   - C. 图数据库功能
   - D. 机器学习
   - **答案**: A

**编程题**:

**题目1: 安装和使用扩展**:

```sql
-- 安装pgvector扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- 创建向量表
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    content TEXT,
    embedding vector(1536)
);

-- 安装PostGIS扩展
CREATE EXTENSION IF NOT EXISTS postgis;

-- 创建地理空间表
CREATE TABLE locations (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    geom GEOMETRY(Point, 4326)
);
```

#### Week 8: 扩展系统实验指导

##### 实验1: 扩展安装与使用

**实验目标**:

- 安装常用扩展
- 使用扩展功能
- 理解扩展的作用

**实验步骤**:

**步骤1: 查看可用扩展**:

```sql
-- 查看已安装的扩展
SELECT * FROM pg_extension;

-- 查看所有可用扩展
SELECT * FROM pg_available_extensions
ORDER BY name;
```

**步骤2: 安装扩展**:

```sql
-- 安装pg_trgm（模糊搜索）
CREATE EXTENSION IF NOT EXISTS pg_trgm;

-- 安装uuid-ossp（UUID生成）
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 验证安装
SELECT extname, extversion
FROM pg_extension
WHERE extname IN ('pg_trgm', 'uuid-ossp');
```

**步骤3: 使用扩展功能**:

```sql
-- 使用pg_trgm进行模糊搜索
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100)
);

INSERT INTO products (name) VALUES
('Laptop Computer'),
('Laptop Bag'),
('Desktop Computer');

-- 模糊搜索
SELECT name, similarity(name, 'Laptop') as sim
FROM products
WHERE name % 'Laptop'
ORDER BY sim DESC;

-- 使用UUID
SELECT uuid_generate_v4() as new_uuid;
```

#### Week 15: 流处理与CEP练习题

**选择题**:

1. **流处理的主要特点是什么？**
   - A. 批量处理数据
   - B. 实时处理数据流
   - C. 离线分析
   - D. 数据存储
   - **答案**: B

2. **CEP（Complex Event Processing）的主要用途是什么？**
   - A. 简单查询
   - B. 复杂事件模式匹配
   - C. 数据备份
   - D. 索引优化
   - **答案**: B

3. **PostgreSQL中用于流处理的扩展是什么？**
   - A. pgvector
   - B. PostGIS
   - C. pg_streaming
   - D. 以上都不是
   - **答案**: C

**编程题**:

**题目1: 创建流处理表**:

```sql
-- 创建事件流表
CREATE TABLE event_stream (
    id BIGSERIAL PRIMARY KEY,
    event_type VARCHAR(50),
    event_data JSONB,
    event_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建物化视图用于实时聚合
CREATE MATERIALIZED VIEW event_summary AS
SELECT
    event_type,
    DATE_TRUNC('hour', event_time) as hour,
    COUNT(*) as event_count
FROM event_stream
GROUP BY event_type, DATE_TRUNC('hour', event_time);
```

#### Week 15: 流处理与CEP实验指导

##### 实验1: 实时事件处理

**实验目标**:

- 理解流处理概念
- 实现实时事件聚合
- 使用物化视图优化

**实验步骤**:

**步骤1: 创建事件流**:

```sql
-- 创建事件表
CREATE TABLE events (
    id BIGSERIAL PRIMARY KEY,
    user_id INTEGER,
    action VARCHAR(50),
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 插入测试事件
INSERT INTO events (user_id, action) VALUES
(1, 'login'),
(1, 'view_page'),
(2, 'login'),
(1, 'purchase');
```

**步骤2: 实时聚合**:

```sql
-- 创建实时聚合视图
CREATE VIEW realtime_stats AS
SELECT
    action,
    COUNT(*) as count,
    COUNT(DISTINCT user_id) as unique_users,
    MAX(timestamp) as last_occurrence
FROM events
WHERE timestamp > NOW() - INTERVAL '1 hour'
GROUP BY action;

-- 查询实时统计
SELECT * FROM realtime_stats;
```

#### Week 16: 云原生部署练习题

**选择题**:

1. **Docker容器的主要优势是什么？**
   - A. 提高性能
   - B. 环境一致性
   - C. 减少内存使用
   - D. 简化备份
   - **答案**: B

2. **Kubernetes的主要作用是什么？**
   - A. 容器编排
   - B. 数据库管理
   - C. 查询优化
   - D. 数据备份
   - **答案**: A

3. **云原生数据库部署的关键特性不包括？**
   - A. 弹性扩展
   - B. 自动故障恢复
   - C. 服务发现
   - D. 固定配置
   - **答案**: D

**编程题**:

**题目1: Docker部署PostgreSQL**:

```dockerfile
# Dockerfile
FROM postgres:18

# 设置环境变量
ENV POSTGRES_DB=mydb
ENV POSTGRES_USER=myuser
ENV POSTGRES_PASSWORD=mypassword

# 复制初始化脚本
COPY init.sql /docker-entrypoint-initdb.d/

# 暴露端口
EXPOSE 5432
```

**题目2: Kubernetes部署配置**:

```yaml
# postgresql-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgresql
  template:
    metadata:
      labels:
        app: postgresql
    spec:
      containers:
      - name: postgresql
        image: postgres:18
        env:
        - name: POSTGRES_DB
          value: mydb
        - name: POSTGRES_USER
          value: myuser
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgresql-secret
              key: password
        ports:
        - containerPort: 5432
```

#### Week 16: 云原生部署实验指导

##### 实验1: Docker容器化部署

**实验目标**:

- 使用Docker部署PostgreSQL
- 配置持久化存储
- 实现容器编排

**实验步骤**:

**步骤1: 创建Docker Compose配置**:

```yaml
# docker-compose.yml
version: '3.8'

services:
  postgres:
    image: postgres:18
    container_name: postgresql
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

volumes:
  postgres_data:
```

**步骤2: 启动服务**:

```bash
# 启动PostgreSQL容器
docker-compose up -d

# 查看日志
docker-compose logs -f postgres

# 连接到数据库
docker exec -it postgresql psql -U myuser -d mydb
```

**步骤3: 验证部署**:

```sql
-- 在容器内验证
SELECT version();
SELECT current_database();
```

#### Week 17: 实战项目开发练习题

**选择题**:

1. **端到端项目开发的第一步是什么？**
   - A. 编写代码
   - B. 需求分析
   - C. 部署上线
   - D. 性能优化
   - **答案**: B

2. **项目开发中数据库设计的关键原则不包括？**
   - A. 规范化
   - B. 性能优化
   - C. 可扩展性
   - D. 忽略约束
   - **答案**: D

3. **RAG系统的主要组成部分不包括？**
   - A. 向量数据库
   - B. 嵌入模型
   - C. 检索系统
   - D. 关系数据库
   - **答案**: D（实际上RAG系统需要向量数据库，但这里问的是"不包括"）

**编程题**:

**题目1: 项目架构设计**:

```sql
-- 设计一个RAG知识库系统的数据库架构

-- 文档表
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    embedding vector(1536),
    metadata JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 查询日志表
CREATE TABLE query_logs (
    id SERIAL PRIMARY KEY,
    query_text TEXT,
    query_embedding vector(1536),
    retrieved_docs INTEGER[],
    response_time INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建向量索引
CREATE INDEX ON documents USING hnsw (embedding vector_cosine_ops);
```

#### Week 17: 实战项目开发实验指导

##### 实验1: 端到端项目实现

**实验目标**:

- 设计完整项目架构
- 实现核心功能
- 测试和优化

**实验步骤**:

**步骤1: 项目需求分析**:

设计一个智能问答系统：

- 文档存储和检索
- 向量相似度搜索
- 查询日志记录
- 性能监控

**步骤2: 数据库设计**:

```sql
-- 创建项目数据库
CREATE DATABASE qa_system;

-- 文档表
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    embedding vector(1536),
    source_url TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 查询表
CREATE TABLE queries (
    id SERIAL PRIMARY KEY,
    question TEXT NOT NULL,
    answer TEXT,
    relevant_docs INTEGER[],
    confidence_score DECIMAL(3,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建索引
CREATE INDEX idx_documents_embedding ON documents
USING hnsw (embedding vector_cosine_ops);
```

**步骤3: 实现核心功能**:

```sql
-- 文档检索函数
CREATE OR REPLACE FUNCTION search_documents(
    query_embedding vector(1536),
    limit_count INTEGER DEFAULT 5
)
RETURNS TABLE (
    id INTEGER,
    title TEXT,
    content TEXT,
    similarity FLOAT
) AS $$
BEGIN
    RETURN QUERY
    SELECT
        d.id,
        d.title,
        d.content,
        1 - (d.embedding <=> query_embedding) as similarity
    FROM documents d
    ORDER BY d.embedding <=> query_embedding
    LIMIT limit_count;
END;
$$ LANGUAGE plpgsql;
```

#### Week 18: 项目总结与优化练习题

**选择题**:

1. **项目优化的关键指标不包括？**
   - A. 查询性能
   - B. 存储空间
   - C. 代码行数
   - D. 用户体验
   - **答案**: C

2. **性能优化的主要方法不包括？**
   - A. 索引优化
   - B. 查询优化
   - C. 架构优化
   - D. 忽略问题
   - **答案**: D

3. **项目文档的重要性体现在？**
   - A. 便于维护
   - B. 知识传承
   - C. 团队协作
   - D. 以上都是
   - **答案**: D

**编程题**:

**题目1: 性能分析报告**:

```sql
-- 分析查询性能
SELECT
    query,
    calls,
    total_exec_time,
    mean_exec_time,
    (total_exec_time / SUM(total_exec_time) OVER()) * 100 as percent
FROM pg_stat_statements
ORDER BY total_exec_time DESC
LIMIT 10;

-- 分析表大小
SELECT
    schemaname,
    tablename,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) as size
FROM pg_tables
WHERE schemaname = 'public'
ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC;
```

#### Week 18: 项目总结与优化实验指导

##### 实验1: 项目优化实践

**实验目标**:

- 分析项目性能
- 识别优化点
- 实施优化方案

**实验步骤**:

**步骤1: 性能分析**:

```sql
-- 启用性能统计
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;

-- 分析慢查询
SELECT
    LEFT(query, 100) as query_preview,
    calls,
    mean_exec_time,
    max_exec_time
FROM pg_stat_statements
WHERE mean_exec_time > 100  -- 超过100ms的查询
ORDER BY mean_exec_time DESC;
```

**步骤2: 索引优化**:

```sql
-- 分析未使用的索引
SELECT
    schemaname,
    tablename,
    indexname,
    idx_scan as index_scans
FROM pg_stat_user_indexes
WHERE idx_scan = 0
ORDER BY pg_relation_size(indexrelid) DESC;

-- 分析缺失的索引（需要pg_stat_statements）
SELECT
    schemaname,
    tablename,
    seq_scan,
    seq_tup_read,
    idx_scan,
    seq_tup_read / seq_scan as avg_seq_read
FROM pg_stat_user_tables
WHERE seq_scan > 0
ORDER BY seq_tup_read DESC;
```

**步骤3: 优化报告**:

```sql
-- 生成优化建议报告
SELECT
    'Table: ' || schemaname || '.' || tablename as object,
    'Consider adding index on frequently filtered columns' as recommendation,
    seq_scan as current_scans
FROM pg_stat_user_tables
WHERE seq_scan > 1000
ORDER BY seq_scan DESC;
```

### 6.4 考试题库

**考试类型**：

- 期中考试：基础课程内容
- 期末考试：进阶课程内容
- 项目答辩：实战项目评估

**示例：期中考试题目（基础课程）**：

#### 第一部分：选择题（20分，每题2分）

1. PostgreSQL的默认端口是？
   - A. 3306
   - B. 5432
   - C. 8080
   - D. 6379
   - **答案**: B

2. ACID特性中的A代表什么？
   - A. Availability
   - B. Atomicity
   - C. Accuracy
   - D. Authentication
   - **答案**: B

#### 第二部分：填空题（20分，每空2分）

1. PostgreSQL使用________机制实现并发控制。
   - **答案**: MVCC（多版本并发控制）

2. SQL中的________语句用于创建表。
   - **答案**: CREATE TABLE

#### 第三部分：编程题（40分）

1. **设计数据库表结构**（20分）：
   - 设计一个图书馆管理系统数据库
   - 包含：图书、读者、借阅记录
   - 要求：符合第三范式

2. **编写SQL查询**（20分）：
   - 查询借阅次数最多的前5本书
   - 查询每个读者的借阅总数
   - 查询逾期未还的借阅记录

#### 第四部分：简答题（20分）

1. 解释PostgreSQL的MVCC机制（10分）
2. 说明索引的作用和类型（10分）

**评分标准**：

- 选择题：每题2分，共20分
- 填空题：每空2分，共20分
- 编程题：语法正确10分，逻辑正确10分，共40分
- 简答题：要点完整10分，表达清晰10分，共20分
- **总分**: 100分
- **及格线**: 60分

---

## 7. 学习路径选择

### 路径1: 快速入门（2周）

适合：初学者、快速了解PostgreSQL

**内容**：

- Week 1: PostgreSQL基础
- Week 2: SQL语言

### 路径2: 系统学习（12周）

适合：系统学习PostgreSQL

**内容**：

- 基础课程（6周）
- 进阶课程（6周）

### 路径3: 完整课程（22周）

适合：深入学习PostgreSQL

**内容**：

- 基础课程（6周）
- 进阶课程（6周）
- 高级课程（6周）
- 实战项目（4周）

### 路径4: AI专家（8周）

适合：专注于AI集成

**内容**：

- 基础课程（2周）
- AI集成专题（4周）
- 实战项目（2周）

---

## 8. 相关文档

- [学习路径指南](./学习路径指南.md) - 快速学习路径
- [AI集成快速开始](./AI集成快速开始.md) - AI集成快速入门
- [PostgreSQL历史与发展](../01-核心课程/01.00-PostgreSQL历史与发展.md) - 历史背景
- [学术研究前沿](../10-理论基础/10.02-学术研究前沿.md) - 研究前沿

---

## 9. 参考文献

1. PostgreSQL Global Development Group. (2025). PostgreSQL 18 Documentation. <https://www.postgresql.org/docs/18/>

2. MIT 6.830 Database Systems. <https://db.csail.mit.edu/6.830/>

3. Stanford CS145 Introduction to Databases. <http://infolab.stanford.edu/~widom/cs145/>

4. Berkeley CS186 Introduction to Database Systems. <https://cs186berkeley.net/>

5. PostgreSQL Global Development Group. (2024). PostgreSQL 17 Documentation. <https://www.postgresql.org/docs/17/>

---

**文档版本**: v1.0
**最后更新**: 2025-11-12
**维护者**: PostgreSQL Documentation Team
