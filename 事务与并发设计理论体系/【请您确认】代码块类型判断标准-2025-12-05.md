# 请您确认：代码块类型判断标准

> **日期**: 2025-12-05
> **目的**: 和您确认代码块类型的判断标准，避免错误修复

---

## 📋 当前情况

在LSEM文档中发现了44个代码块位置，其中一些缺少语言标识。

---

## 🔍 需要您确认的判断标准

### 问题1: 树状结构文本

**示例** (第162-166行):

```
为什么需要LSEM理论?
├─ 无理论: 概念重复定义，设计模式无法复用
├─ 经验方法: 不完整，理论孤岛难以融合
└─ LSEM理论: 统一框架、跨层复用、理论融合
```

**问题**: 这种树状结构应该用 `text` 吗？

---

### 问题2: ASCII框线图

**示例** (第486-510行):

```
┌────────────────────────────────────────────┐
│  L1 Cache (CPU缓存)                        │
│  ├─ 延迟: ~1ns                             │
│  ├─ 容量: ~32KB/core                       │
└────────────────────────────────────────────┘
```

**问题**: 这种ASCII框线图应该用 `text` 吗？

---

### 问题3: 文本描述/状态转换

**示例** (第595-604行，已经有text标识):

```
初始状态: Tuple(xmin=50, xmax=0, data='A')
    ↓ UPDATE (TxID=100)
旧版本:   Tuple(xmin=50, xmax=100, data='A')
```

**问题**: 这种文本描述应该用 `text` 吗？

---

### 问题4: 代码块（已确认的）

**Python代码** → 用 `python` ✅
**Rust代码** → 用 `rust` ✅
**SQL代码** → 用 `sql` ✅
**C代码** → 用 `c` ✅

---

## ❓ 请您确认

1. **树状结构文本** (使用 `├─`, `└─`) → 应该用 `text` 吗？
2. **ASCII框线图** (使用 `┌─┐`, `│`, `└─┘`) → 应该用 `text` 吗？
3. **文本描述/状态转换** → 应该用 `text` 吗？
4. **还有其他需要特别注意的类型吗？**

---

## 📝 我的修复策略（待您确认）

确认后，我会：

1. **树状结构文本** → 如果是 `text`，则添加 `text` 标识
2. **ASCII框线图** → 如果是 `text`，则添加 `text` 标识
3. **文本描述** → 如果是 `text`，则添加 `text` 标识
4. **代码块** → 根据内容判断（python/rust/sql/c等）

---

**请告诉我您的判断标准，我会严格按照标准修复！** ✅
